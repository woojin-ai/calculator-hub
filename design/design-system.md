# 계산기 허브 — 디자인 시스템 스펙 (v1.2, 2026-08-02)

> 이 문서는 디자인팀이 작성한 스타일 가이드이다. 실제 코드(Tailwind config, 컴포넌트 등)는
> 개발팀이 프로젝트 스캐폴딩 완료 후 이 스펙을 참고해 구현한다. 본 문서 자체는 코드가 아니다.

> **[v1.2 갱신 — 데이터 표 정본화]** 블로그 본문 데이터 모델(`BlogSection`)에 표 타입이 없어
> **블로그 36장 전부 `<table>` 0개**였는데, 본문 산문은 "표를 세로로 훑어보면"·"위 표에서" 같은
> 표현을 **22곳**에서 써 존재하지 않는 표를 가리키고 있었다(2026-08-02 라이브 실측). 이번 판에서
> **§3-6 데이터 표(Table)** 를 신설해 마크업·치수·접근성·근거를 정본으로 기록하고, §2 스케일 표에
> 표 헤더/셀 행을 추가했으며, §4 설계 원칙에 **원칙 5(문서 레벨 가로 스크롤 금지)** 를 추가했다.
> 2026-08-01 사고(코드만 고치고 문서를 안 고쳐 결함이 규범으로 역류)의 교훈을 적용해,
> **개발팀 구현과 동시에 이 문서를 정본화**한다. 색 토큰·광고 규범은 변경 없음(신규 색 토큰 0개).

> **[v1.1 갱신]** 브레드크럼이 전 상세 페이지에 라이브인데 이 문서에 컴포넌트 정의가 없었고,
> 그 공백을 페이지 스펙이 **코드를 인용해** 메우면서 12px·11×16px 터치 타깃 결함이 52개 라우트로
> 퍼졌다(2026-08-01 코드 수정 완료). 재발 방지로 **§3-5 브레드크럼**을 신설하고, §4 원칙 3에
> 44px 선언과 내비류 실채택값 36px의 **충돌을 그대로 기록**했다. 색/폰트/광고 규범은 변경 없음.

---

## 1. 색상 팔레트 (라이트 모드 기준)

애드센스 사이트 특성상 "신뢰감 + 가독성 + 눈 피로도 낮음"을 최우선으로 한다.
과도한 원색·비비드 컬러는 지양하고, 블루 계열 메인 + 뉴트럴 그레이 조합을 사용한다.

| 용도 | 색상명 | HEX | 비고 |
|---|---|---|---|
| Primary (브랜드/버튼/링크) | Blue 600 | `#2563EB` | 액션 버튼, 강조 링크 |
| Primary Hover | Blue 700 | `#1D4ED8` | 버튼 hover/active |
| Accent (결과 강조) | Emerald 500 | `#10B981` | 계산 결과값, 성공 상태 |
| Warning (주의/경고) | Amber 500 | `#F59E0B` | 입력 오류, 유의사항 |
| Danger (위험/비만 등 강조) | Red 500 | `#EF4444` | 결과 상태 중 위험 수준 표시 |
| Background (기본) | Gray 50 | `#F9FAFB` | 페이지 배경 |
| Surface (카드/입력영역) | White | `#FFFFFF` | 카드, 입력 필드 배경 |
| Border | Gray 200 | `#E5E7EB` | 카드/입력 테두리 |
| Text Primary | Gray 900 | `#111827` | 본문/제목 |
| Text Secondary | Gray 500 | `#6B7280` | 설명, 캡션, 보조 텍스트 |
| Text Disabled | Gray 300 | `#D1D5DB` | 비활성 텍스트 |

- 다크모드는 이번 단계에서 정의하지 않음 (향후 별도 스펙 필요 시 마스터 팀장 확인 후 진행).
- 색상 대비는 WCAG AA 기준(본문 텍스트 대 배경 4.5:1 이상) 충족하도록 위 조합 선정.

---

## 2. 타이포그래피

### 추천 폰트
- **본문/전체 기본**: `Pretendard` (한글 가독성 우수, 웹폰트 무료, 다양한 굵기 지원)
- **대체(폴백)**: `Noto Sans KR` → 시스템 폰트(`-apple-system, Malgun Gothic, sans-serif`)
- 숫자(계산 결과)는 가독성을 위해 `tabular-nums` 속성 적용 권장 (자릿수 정렬).

### 스케일

| 레벨 | 크기 (px/rem) | 굵기 | 용도 |
|---|---|---|---|
| Display | 32px / 2rem | 700 (Bold) | 페이지 대제목 (H1) |
| Heading 1 | 24px / 1.5rem | 700 | 섹션 제목 (H2) |
| Heading 2 | 20px / 1.25rem | 600 (SemiBold) | 카드/블록 제목 (H3) |
| Body | 16px / 1rem | 400 (Regular) | 본문 텍스트 |
| Body Small | 14px / 0.875rem | 400 | 보조 설명, FAQ 답변 |
| Caption | 12px / 0.75rem | 400 | 출처, 저작권, 광고 라벨 |
| Result (계산 결과 전용) | 36~40px / 2.25~2.5rem | 700 | 계산기 결과값 — 모바일에서도 28px 이하로 축소 금지 |
| Table Header (표 헤더 셀) | 14px / 0.875rem | 600 (SemiBold) | 데이터 표의 `<th>` — §3-6 |
| Table Cell (표 데이터 셀) | 14px / 0.875rem | 400 | 데이터 표의 `<td>`, 숫자열은 `tabular-nums` — §3-6 |
| Table Caption / Note | 14px(캡션) / 12px(하단 note) | 400 | 표 제목은 Body Small, 표 아래 가정·출처는 Caption — §3-6 |

- 줄 간격(line-height): 본문 1.6, 제목 1.3 기준.
- 모바일에서는 Display/H1을 24px로 한 단계 축소 (섹션 5 반응형 원칙 참고).

> **표 셀 14px는 "본문 16px 축소 금지" 규칙의 명시적 예외다.**
> 본문 문단은 모바일에서도 16px를 유지한다(`design/blog-layout-spec.md` §2-3·§3). 데이터 표의
> 셀은 이 규칙의 **적용 대상이 아니다** — 표는 왼쪽에서 오른쪽으로 읽는 산문이 아니라 열을 위아래로
> **스캔하는 데이터**이고, 좁은 폭(모바일 375px)에서 열을 한 화면에 담아 비교 가능하게 만드는 것이
> 가독성의 실질 조건이기 때문이다. 이 예외는 **표 내부(`<caption>`/`<th>`/`<td>`)에만** 적용되며,
> 표 앞뒤 문단으로 전이되지 않는다. 자세한 근거는 §3-6.

---

## 3. 계산기 UI 컴포넌트 스타일 가이드

### 3-1. 입력 필드 (Input)
- 배경 `#FFFFFF`, 테두리 `1px solid #E5E7EB`, radius `8px`
- 포커스 시 테두리 `#2563EB` + 얇은 outline/box-shadow (`0 0 0 3px rgba(37,99,235,0.15)`)
- 높이 최소 48px (모바일 터치 타겟 확보), 내부 padding `12px 16px`
- 라벨은 입력창 위쪽에 배치, Body Small 크기 + Text Secondary 컬러
- 에러 상태: 테두리 `#F59E0B` 또는 Red 500(`#EF4444`), 하단에 에러 메시지 Caption 크기로 표시

### 3-2. 결과 표시 영역 (가장 중요 — 시각적 위계 최상위)
- 카드 형태, 배경은 살짝 구분되는 톤(`#F0FDF4` 연한 그린 틴트 or `#EFF6FF` 연한 블루 틴트) + 좌측 또는 상단에 Accent 컬러 강조선(4px)
- 결과 숫자는 Result 스케일(36~40px, Bold), 컬러는 Accent(`#10B981`) 또는 Primary
- 단위(원, %, kg 등)는 결과 숫자보다 작은 크기(Body)로 옆에 배치, Text Secondary 컬러
- 결과 카드는 입력 필드 바로 아래, 스크롤 없이 보이는 위치(above the fold) 배치 권장 → 체류시간·재사용성 확보
- 결과가 갱신될 때 살짝 배경색 페이드 or 미세한 scale 트랜지션(0.15~0.2s) 제안 — 입력 변화 인지 향상 (기능 로직 아님, CSS transition 범위)

### 3-3. FAQ 아코디언
- 기본 상태: 질문(Body, SemiBold) + 우측 chevron 아이콘, 하단 얇은 구분선(`#E5E7EB`)
- 펼침 상태: 답변(Body Small, Text Secondary), padding `16px`, 배경은 Surface 유지
- 아코디언 항목 간 간격 `8px`, 전체를 카드 하나로 감싸 하나의 블록처럼 보이게 구성
- 클릭 영역은 질문 전체 줄(텍스트+아이콘)로 확보 — 모바일 터치 편의성

### 3-4. 관련 계산기 링크 카드
- Grid 배치: 데스크톱 3~4열, 태블릿 2열, 모바일 1열(섹션 4 참고)
- 카드 스타일: 배경 White, 테두리 `#E5E7EB`, radius `12px`, padding `16px`, hover 시 그림자(`shadow-sm → shadow-md`)와 테두리 색이 Primary로 살짝 변화
- 카드 내부: 아이콘(선택) + 계산기 이름(Body, SemiBold) + 한 줄 설명(Caption, Text Secondary)
- 카드 자체가 링크 전체이며, 광고 배너와 시각적으로 명확히 구분되는 위치/스타일 유지 (아래 5번 항목 참고)

### 3-5. 브레드크럼 (현재 위치 내비)

계산기 상세·블로그 상세 등 하위 페이지 최상단에 놓는 "홈 / 카테고리" 경로 표시.
**`/calculator/*`·`/blog/*` 전 상세 페이지가 이 정의 하나를 공유한다.**

```
<nav aria-label="현재 위치"
     className="-ml-3 mb-2 flex flex-wrap items-center gap-2 text-sm text-brand-text-secondary">
  <Link href="/" className="inline-flex min-h-9 items-center px-3 transition-colors hover:text-brand-primary {FOCUS_RING_LINK_ROUNDED}">홈</Link>
  <span aria-hidden="true">/</span>
  <Link href={...} className="(위와 동일)">{카테고리명}</Link>
</nav>
```

| 값 | 역할 |
|---|---|
| `text-sm` (Body Small 14px) | 대화형 요소의 **최소** 텍스트 토큰. Caption 12px는 쓰지 않는다(아래 규칙). |
| `min-h-9` (36px) | 크럼 1개의 세로 터치 타깃 하한 — 사이트 내비게이션 표준값(§4 원칙 3 주석 참고). |
| `px-3` | 가로 터치 폭 확보. `홈`처럼 글자 폭 11px짜리 짧은 라벨도 약 37px 폭이 된다. |
| `gap-2` (8px) | 인접 크럼 사이 최소 간격 — 오분류 탭 방지. |
| `-ml-3` | 첫 크럼의 `px-3` 좌패딩을 상쇄해, 크럼 텍스트 시작점을 H1과 같은 좌측 정렬선에 맞춘다. 빼면 브레드크럼만 12px 안쪽으로 밀려 보인다. |
| `flex-wrap` | 모바일 좁은 폭에서 경로가 길어지면 줄바꿈. 가로 스크롤을 만들지 않는다. |

- 구분자 `/`는 링크가 아니므로 **터치 타깃 대상이 아니며** `aria-hidden="true"`로 스크린리더에서 숨긴다.
  경로 구조는 `<nav aria-label="현재 위치">`가 전달한다.
- 포커스 링은 `lib/focusRing.ts`의 **Tier B-1 계열 `FOCUS_RING_LINK_ROUNDED`**
  (자체 테두리 없는 링크 — `design/focus-visible-ring-ui-spec.md` 분류 그대로. 새 티어 없음).
- 현재 상태 크럼(마지막 항목)을 링크가 아닌 텍스트로 둘지는 페이지 스펙에서 정한다. 링크가 아닌
  크럼에는 `min-h-9 px-3`을 붙이지 않아도 된다(터치 대상이 아니므로).

> **규칙 — 대화형 요소에 Caption 스케일 금지 (§2 스케일 보충)**
> - Caption(12px)의 용도는 §2 표에 적힌 대로 **출처·저작권·광고 라벨 등 읽기 전용 보조 텍스트**에 한정한다.
> - 링크·버튼 등 **사용자가 누르는 요소의 최소 텍스트 토큰은 Body Small(14px)** 이다.
>   더 작게 보이게 하고 싶다면 글자 크기가 아니라 색(Text Secondary)·여백·배치로 위계를 낮춘다.
> - 대화형 요소는 **텍스트 크기와 별개로** 터치 타깃 치수(`min-h-*`·padding)를 명시적으로 확보한다.
>   글자 크기만 정하고 넘어가면 라벨이 짧을 때 타깃이 그대로 무너진다(브레드크럼 `홈` = 11×16px 사례).

> **규칙 — 스펙은 코드를 인용하지 않는다**
> 페이지별 UI 스펙(`design/*-ui-spec.md`)은 이 §3의 컴포넌트 정의를 **참조**만 하고 클래스 문자열을
> 다시 선언하지 않는다. "`<파일> N행` 그대로" 식으로 구현 코드를 출처 삼아 스펙을 쓰면 규범이
> 코드에서 역류하고, 코드의 결함이 스펙의 권위를 얻어 새 페이지로 복제된다. 공유 컴포넌트에
> 변경이 필요하면 **이 문서를 먼저 고친다.**

### 3-6. 데이터 표 (Table)

블로그 본문·계산기 설명글에서 **여러 조건의 수치를 나란히 비교**할 때 쓰는 데이터 표.
`/blog/*` 본문의 `table` 섹션이 이 정의 하나를 공유한다.
(데이터 스키마의 정본은 기획측 `planning/blog-spec.md` §1, 이 절은 **화면 표현의 정본**이다.)

> **배경 — 왜 이 절이 생겼나 (2026-08-02)**
> 본문 데이터 모델에 표 타입이 아예 없어서 **블로그 36장 전부 `<table>` 0개**였다. 그런데 산문은
> "표를 세로로 훑어보면", "위 표에서" 같은 표현을 **22곳**에서 써 존재하지 않는 표를 가리키고
> 있었다. 표가 없어서 수치가 문장 안에 나열되면 비교가 불가능하고 체류시간·이해도 모두 떨어진다.

#### (1) 마크업 (정본)

```
<figure className="my-6">
  <div role="region" aria-labelledby={`blog-table-${index}`} tabIndex={0}
       className="-mx-4 overflow-x-auto px-4 sm:mx-0 sm:px-0 {FOCUS_RING_INSET}">
    <table className="w-full min-w-max border-collapse text-sm">
      <caption id={`blog-table-${index}`} className="mb-2 text-left text-sm text-brand-text-secondary">{caption}</caption>
      <thead>
        <tr className="border-b border-brand-border">
          <th scope="col" className="px-3 py-2 font-semibold text-brand-text {text-left|text-right}">…</th>
        </tr>
      </thead>
      <tbody>
        <tr className="border-b border-brand-border/60">
          <th scope="row" className="px-3 py-2 font-medium text-brand-text {text-left|text-right}">…</th>   {/* 각 행 첫 셀 */}
          <td className="px-3 py-2 tabular-nums text-brand-text {text-left|text-right}">…</td>
        </tr>
      </tbody>
    </table>
  </div>
  {note && <figcaption className="mt-2 text-xs text-brand-text-secondary">{note}</figcaption>}
</figure>
```

- `{text-left|text-right}`는 열 정렬 값이다. **기본 정렬 규칙: 0번 열 `left`, 나머지 전부 `right`.**
  데이터 배열이 `align`을 명시하면 그 값을 쓴다(스키마는 아래 (2)).
  헤더 셀의 정렬은 **같은 열의 데이터 셀 정렬과 반드시 일치**시킨다(헤더만 좌측인 표 금지).
- 포커스 링은 `lib/focusRing.ts`의 **Tier B-2 `FOCUS_RING_INSET`**
  (`design/focus-visible-ring-ui-spec.md` 분류 그대로 — **새 티어를 만들지 않는다**).
  **왜 B-1(outset)이 아니라 B-2(inset)인가** — 이 블록은 모바일에서 `-mx-4`로 뷰포트 좌우 끝까지
  블리드한다. outset 링은 요소 바깥에 그려지므로 **좌우 2px이 화면 밖으로 잘려** 포커스 위치를
  알 수 없게 된다. inset 링은 박스 안쪽에 그려져 어떤 폭에서도 잘리지 않는다.
  (2026-08-02 개발팀이 구현 중 발견해 보고 → 마스터 판정. 브레드크럼은 블리드하지 않으므로
  B-1을 그대로 쓴다 — 같은 블로그 상세 페이지 안에서 두 상수가 공존하는 것이 정상이다.)
  스크롤 영역은 `tabIndex={0}` 때문에 키보드 포커스를 실제로 받으므로, 포커스 링이 없으면
  "포커스는 갔는데 아무 표시가 없는" 상태가 된다.
- **색 토큰은 전부 기존 것 재사용**: `brand-border`, `brand-text`, `brand-text-secondary`.
  **신규 색 토큰 금지.** 얼룩말 줄무늬(zebra) 배경도 쓰지 않는다 — 새 배경 토큰이 필요해지고,
  §5 광고 규범의 "광고와 콘텐츠 카드는 배경으로 구분한다"는 신호를 흐린다.

#### (2) 데이터 스키마 (기획·개발 공통 계약)

```ts
| {
    type: "table";
    caption: string;              // 표 제목. 필수
    headers: string[];            // 헤더 행. 필수
    rows: string[][];             // 각 행 길이 === headers.length
    align?: ("left" | "right")[]; // 생략 시 [0번 열 left, 나머지 right]
    note?: string;                // 표 하단 가정·출처. 선택
  }
```

- `caption`이 **선택이 아니라 필수**인 이유는 접근성이다. 아래 (4) 참고.
- 셀 값은 전부 플레인 문자열이다(단위·기호 포함해 완성된 문자열로 데이터에 넣는다).
  셀 안에서 링크·강조 마크업을 쓰지 않는다 — 표는 스캔 대상이지 산문이 아니다.

#### (3) 치수·타이포

| 값 | 역할 |
|---|---|
| `text-sm` (14px) | 표 전체 기본 스케일. **§2 "본문 16px 축소 금지"의 명시적 예외**(아래 (5)). |
| `px-3 py-2` | 셀 패딩. 세로 40px 안팎이 되어 행 구분이 눈으로 잡히고, 3열이 375px에 들어간다. |
| `tabular-nums` (데이터 셀) | §2의 기존 규정 재사용. 자릿수가 세로로 정렬돼야 열 스캔이 성립한다. **신규 토큰 아님.** |
| `border-b border-brand-border` (헤더) / `border-brand-border/60` (본문 행) | 헤더 구분선을 진하게, 행 구분선을 옅게 두어 **세로 격자선 없이도** 표로 읽힌다. 세로선은 긋지 않는다(격자가 촘촘하면 광고 오인 위험 ↑ — §5). |
| `my-6` (figure) | 앞뒤 문단과 24px 간격. 표가 어느 문단에 속하는지 모호해지지 않게 문단 간격(16px)보다 넓게. |
| `mb-2` (caption) / `mt-2 text-xs` (note) | 표 제목은 표 위 Body Small, 하단 가정·출처는 Caption(읽기 전용 보조 텍스트라 12px 허용 — §3-5 규칙의 Caption 용도에 부합). |

#### (4) 접근성 (WCAG) — 임의로 뺄 수 없는 항목

| 속성 | 이유 |
|---|---|
| `role="region"` + `aria-labelledby` | 스크롤 가능한 영역은 **이름 있는 랜드마크**여야 보조기기 사용자가 "여기에 스크롤할 내용이 있다"는 것을 안다. 이름 소스가 필요해서 `caption`을 **필수 필드**로 뒀다. |
| `tabIndex={0}` | 가로 스크롤 영역은 **키보드만으로 스크롤할 수 있어야 한다.** 포커스를 못 받으면 마우스·터치 없이는 잘린 열에 영원히 접근할 수 없다. |
| 각 행 첫 셀 `<th scope="row">` | 스크린리더가 데이터 셀을 읽을 때 **행 레이블을 함께** 읽는다("연봉 3000만원 — 실수령액 …"). 첫 셀을 `<td>`로 두면 숫자만 나열돼 의미가 사라진다. |
| 헤더 `<th scope="col">` | 열 레이블 연결. `<td>`+굵은 글씨로 흉내 내지 않는다. |
| `<caption>` (시각적으로도 노출) | 표 제목을 `<p>`로 표 위에 두면 프로그램적 연결이 끊긴다. 반드시 `<caption>`. |

#### (5) 설계 근거 — 왜 이 값이어야 하나

- **`-mx-4 … px-4 sm:mx-0 sm:px-0` (모바일 블리드)**
  본문 컨테이너는 `px-4`(좌우 16px씩)를 쓰므로 375px 기기에서 유효 폭이 343px밖에 안 된다.
  스크롤 영역만 컨테이너 패딩 바깥으로 넘겨 **유효 폭 343 → 375px**를 확보하고, `px-4`를 안쪽에
  다시 줘서 표 자체는 여전히 본문 정렬선에서 시작하게 만든다. `sm` 이상에서는 폭이 충분하므로
  `sm:mx-0 sm:px-0`으로 해제한다(데스크톱에서 표만 튀어나와 보이는 것을 막는다).
- **`overflow-x-auto` + `min-w-max` (넘침 격리)**
  §4 원칙 5의 구체적 적용이다. **문서 레벨 가로 스크롤을 절대 만들지 않고**, 넘치는 폭은
  이 블록 안에서만 스크롤한다. 블로그 상세 페이지 컨테이너에는 overflow 제어가 없어서, 이 두
  클래스가 없으면 표 하나가 페이지 전체 폭을 늘려 **헤더·브레드크럼까지 함께 밀려난다.**
  `min-w-max`는 열이 억지로 줄바꿈돼 숫자가 두 줄로 쪼개지는 것을 막는다(표의 스캔성 유지).
- **셀 14px 예외**
  §2에 예외로 함께 기록했다. 이 예외를 문서에 적지 않으면 개발팀이 임의로 쓴 값이 되고,
  다음 판 스펙이 **그 코드를 인용해** 규범을 만든다 — 2026-08-01 브레드크럼 12px 사고의 재현이다.
- **열 개수 상한 3열 (행 레이블 1 + 데이터 2)**
  375px에서 콘텐츠 폭 343px, 셀 패딩 24px(`px-3`×2)를 빼면 열당 실질 텍스트 폭이 90px 안팎이다.
  3열이 "가로 스크롤 없이 한 화면에서 비교 가능한" 경계선이다. **4열 이상이 필요하면 표를
  쪼갠다**(예: 조건별로 표 2개). 스크롤로 해결하지 않는다 — 스크롤된 열은 비교 대상에서
  사실상 사라지기 때문이다. (스크롤은 예외 상황의 안전장치이지 설계 전제가 아니다.)
- **광고 인접 금지** — §5 및 `design/blog-layout-spec.md` §4 참고. 테두리·격자를 가진 표 바로
  위/아래에 디스플레이 광고를 붙이면 광고가 표의 연장으로 읽힌다(콘텐츠 오인 배치).

---

## 4. 모바일 우선 반응형 브레이크포인트 원칙

기준은 Tailwind 기본 브레이크포인트를 그대로 채택 (개발팀 커스텀 없을 시 기본값 사용 권장):

| 브레이크포인트 | 최소 너비 | 대상 |
|---|---|---|
| 기본(no prefix) | 0px~ | 모바일 (기본 설계 기준, 1열 레이아웃) |
| `sm` | 640px~ | 큰 모바일 / 작은 태블릿 |
| `md` | 768px~ | 태블릿 (관련 링크 카드 2열 전환) |
| `lg` | 1024px~ | 데스크톱 (3~4열 전환, 사이드바 광고 노출 가능) |
| `xl` | 1280px~ | 와이드 데스크톱 |

### 설계 원칙
1. 모바일 화면에서 "입력 필드 → 결과 표시"가 스크롤 없이 한 화면(1스크린)에 들어오는 것을 최우선 목표로 한다.
2. 계산기 핵심 영역(입력+결과)은 항상 최상단, FAQ/관련 링크/광고는 그 아래 순서로 배치.
3. 터치 타겟 최소 44x44px 확보 (버튼, 입력필드, 아코디언 헤더 등).
   > **⚠️ 선언값 44px vs 내비류 실채택값 36px — 충돌 존재, 미해결(별건 티켓).**
   > 위 44px 선언은 삭제하지 않는다. 다만 **현재 라이브 사이트의 내비게이션 링크류는 36px
   > (`min-h-9`/`h-9`)를 하한으로 채택**해 구현돼 있다는 사실을 함께 기록한다:
   > - §3-5 브레드크럼 — `/calculator/*` 16개 + `/blog/*` 36개 = **52개 라우트**(2026-08-01 적용)
   > - `SiteHeader` nav — 약 36px. `design/site-header-mobile-nav-spec.md` §8-4에 이미 별건으로 제기됨
   > - 기간 단위 토글(`h-9`) — `design/loan-interest-calculator-ui-spec.md` §2-3(B)에 예외로 명시됨
   >
   > 어느 쪽을 정본으로 삼을지는 **사이트 전 nav를 다시 그리는 결정**이라 마스터가 별건으로 분리했다.
   > 디자인팀이 이 문서 안에서 임의로 한쪽을 지우지 않는다. 정본화 전까지의 실무 기준은 아래 이원 관례이며,
   > **이 이원 관례 자체가 그 별건의 대상**이다.
   > - **본문 조작 요소**(입력필드·주요 버튼·아코디언 헤더·카드) = **44px 이상** — 위 선언 그대로.
   >   (예: 입력 `h-12` 48px, 카드 `p-4`)
   > - **내비게이션 링크**(브레드크럼·헤더 nav·단위 토글) = **36px 하한** — 현행 채택값.
4. 데스크톱에서는 좌측 콘텐츠(2/3) + 우측 사이드바(1/3, 광고·관련 링크) 2단 레이아웃 고려 가능.
5. **본문 콘텐츠는 어떤 경우에도 문서 레벨 가로 스크롤을 만들지 않는다.**
   폭을 넘기는 요소(표·코드·긴 URL·넓은 이미지 등)가 있으면 **그 요소를 감싼 블록 안에서만**
   스크롤하게 격리한다(`overflow-x-auto`). 페이지 전체가 좌우로 흔들리면 헤더·브레드크럼·광고까지
   같이 밀려 레이아웃 전체가 무너지고, 모바일에서 세로 스크롤 중 의도치 않은 가로 이동이 생긴다.
   - 텍스트 계열은 애초에 **줄바꿈으로 흡수**한다(브레드크럼 `flex-wrap` — §3-5).
   - 폭이 본질인 데이터는 **블록 내부 스크롤**로 격리한다(데이터 표 — §3-6).
   - 어느 쪽도 안 되면 **콘텐츠를 쪼갠다**(표 3열 상한 — §3-6 (5)). 문서 폭을 늘리는 선택지는 없다.
   - 새 컴포넌트를 스펙에 올릴 때, 가로로 커질 수 있는 요소면 **이 원칙의 처리 방식을 스펙에
     명시**한다(명시가 없으면 개발팀이 임의 판단하게 되고, 그 임의 판단이 규범으로 역류한다).

---

## 5. 광고(애드센스) 배치 가이드라인

**원칙: 사용자가 계산기를 쓰는 흐름을 방해하지 않는 선에서, 자연스러운 콘텐츠 여백에 배치한다.**
**클릭 유도 배치, 콘텐츠로 오인되는 배치, 실수 클릭 유발 배치는 애드센스 정책 위반이므로 절대 금지.**

### 권장 배치 위치
1. **상단 배너 (선택적)**: 헤더 아래, 계산기 입력 영역 위. 크기를 크게 잡아 입력 영역을 아래로 밀어내지 않도록 높이 제한(모바일 기준 최대 100px 권장).
2. **본문 중간 (콘텐츠 사이 자연 삽입)**: 결과 표시 영역과 FAQ 섹션 "사이", 또는 FAQ와 관련 링크 카드 "사이"의 여백에 배치. 반드시 위아래에 명확한 여백(margin 24px 이상)을 두어 콘텐츠와 시각적으로 분리.
3. **사이드바 (데스크톱 lg 이상에서만)**: 우측 사이드바 영역에 sticky 광고 1개. 스크롤 시 따라오되 화면의 30% 이하 크기.
4. **하단 (푸터 위)**: 관련 링크 카드 섹션 다음, 페이지 최하단.

### 금지 사항 (정책 준수)
- 계산기 "결과 표시 영역" 바로 위/아래에 결과처럼 보이게 디자인된 광고 배치 금지 (오인 클릭 유도).
- 버튼(계산하기, 초기화 등) 근처 30px 이내 광고 배치 금지.
- 광고 주변에 "여기를 클릭하세요", 화살표 등 클릭 유도 그래픽/문구 배치 금지.
- **데이터 표(§3-6) 바로 위/아래에 인접한 광고 배치 금지.** 테두리·행 구분선을 가진 표에 광고가
  붙으면 광고 블록이 **표의 마지막 행 또는 표의 주석으로 오인**된다(콘텐츠 오인 배치). 표 전후로
  광고를 두어야 한다면 사이에 최소 32px 여백 + 다른 배경 + "광고" 라벨로 명확히 끊는다.
- 광고와 콘텐츠 카드의 스타일(테두리, 배경)을 유사하게 만들어 구분을 어렵게 하는 디자인 금지 — 광고 영역은 콘텐츠 카드와 다른 여백/배경으로 명확히 분리하고, 필요시 작은 "광고" 라벨(Caption, Text Secondary) 표기.
- 모바일에서 화면 전체를 덮는 팝업/인터스티셜 형태의 자체 삽입 광고 레이아웃 제안 금지 (UX 저해 + 정책 리스크).

### 레이아웃 우선순위 요약
콘텐츠(입력→결과) 최우선 노출 > 자연스러운 여백 활용 광고 배치 > 광고 개수보다 사용자 체류시간·재방문 우선.

---

## 다음 단계 (제안)
- 개발팀 스캐폴딩 완료 후, 위 컬러/폰트 값을 `tailwind.config` 테마 토큰으로 매핑하는 논의 필요.
- 큰 톤앤매너(예: 완전히 다른 컬러 계열, 다크모드 우선 등) 변경 요청 시 마스터 팀장 사전 확인 필요 — 현재는 변경 없음.
