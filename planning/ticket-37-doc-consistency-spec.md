# 티켓 #37 — #36 잔여 문서 정합성 6건 정정 패치 스펙

- 작성: 마스터 (2026-08-05 06:0x 회차)
- 성격: **문서 정합성 전용. 계산 로직·상수·입력 스키마 무변경.** W-1 1건만 라이브 카피(`lib/blog.ts`).
- 인수: `docs/team-log/2026-08-05.md` 05:05 회차 §7-3 (QA R2 이월 6건)
- 목적: 전부 **재오염 벡터** — 문서가 코드/조문과 어긋난 채 남아 있으면 "명세대로 구현"이 결함을 되살린다(#32·#35·#36 발원지 패턴).

---

## 0. 마스터 사전 실측 (표준 ㋌ — dev 착수 전에 닫는다)

| 명제 | 도구 | 실측 |
|---|---|---|
| `8~20세` 리포 전체 | Grep(count, `!node_modules`) | **57행 / 8파일** — cs 1 · spec 34 · qa-report 2 · team-log-0714 1 · constant-audit 4 · content 1 · copy-fixes 10 · team-log-0805 4 |
| `8~20세` 라이브 경로(`components/ lib/ app/`) | Grep | **0건** ✅ (R2 만족) |
| `8세 이상 자녀·손자녀 수` in `components/` | Grep | **2건** — `SalaryNetCalculator.tsx` L213(JSX 주석) · L252(라벨) |
| 〃 in `components/ + lib/calculators.ts` | Grep | **3건** (위 2 + `calculators.ts` 1) |
| `제59조의2제1항 각 호` | Grep | `lib/salary.ts:294` **1건**. `content.md` **0건** → R12 절반 미충족 |
| `SalaryNetCalculator.tsx` 그리드 | Read L214 | `grid grid-cols-2 gap-3 sm:gap-4` — **반응형 열 변경 없음, 1열 폴백 미구현** |

### 0-1. 🔴 QA 이월값 자체의 오류 (마스터 정정)

QA R2가 제안한 재베이스라인 `R1 13→18`은 **틀렸다. 실측 57이다.**
원인은 QA가 지목한 것("정정문이 `8~20세`를 재도입")과 같은 종류이되 **규모가 3배**다 —
`child-tax-credit-copy-fix-spec.md` 한 파일에만 **34행**이 있다(§1-4 경계 대조표·§5 문서 패치 표·
§8 점검표가 전부 구 라벨을 인용해야 하는 문서다).

> **그래서 숫자만 갈아끼우면 안 된다.** R1은 "정정 작업 자체가 늘리는 값"이라
> **리포 전체 원시 카운트로는 영구히 불안정**하다. 아래 P-2에서 **행을 재정의**한다.

### 0-2. 1차 출처 재확인 (F-1 정밀도 보강의 근거)

`law.go.kr DRF/lawService.do?OC=test&target=law&MST=280405&JO=005000&type=XML`
**소득세법 제50조제1항제3호**를 마스터가 이번 회차에 직접 수신:

- **본문 괄호 ①**: `(제51조제1항제2호의 장애인에 해당되는 경우에는 나이의 제한을 받지 아니한다)`
  → **나이제한 면제. 가~마목 공통.** #36의 정정이 옳았음을 재확인.
- **본문 괄호 ②**: `(총급여액 500만원 이하의 근로소득만 있는 부양가족을 포함한다)`
  → F-2 정정이 옳았음을 재확인.
- **나목**: `… 20세 이하(20세가 되는 날과 그 이전 기간을 말한다 …)인 사람.`
  `이 경우 해당 직계비속 또는 입양자와 그 배우자가 모두 제51조제1항제2호에 따른 장애인에`
  `해당하는 경우에는 그 배우자를 포함한다.`

> **핵심**: 나목에는 **장애인 문구가 실재한다.** 다만 그것은 **나이예외가 아니라 배우자 포함 규정**이다.
> 스펙 §0-3의 현행 서술("나목의 **괄호 2개**에는 장애인 문구가 없다")은 **괄호에 한정하면 참**이지만,
> 나목 본문을 읽은 사람이 장애인 문구를 발견하는 순간 "역시 나목이 근거였네"로 **F-1 오인용이 부활**한다.
> → 이것이 QA가 요구한 한 줄의 실제 값어치다. **표준 ㋑ 준수: 1차 출처에서 확인한 범위만 적는다.**

---

## 1. 패치 표

### P-1. `planning/child-tax-credit-copy-fix-spec.md` §0-3 — F-1 정밀도 보강

**위치**: L37 셀 말미(`⚠️ **20세 이하 상한의 근거는 나목이 맞다** — 그 인용은 유지했다.` 바로 뒤)
**추가**(같은 셀 안, 문장 1개):

```
**⚠️ 나목 본문에도 장애인 문구가 있으나 그것은 나이예외가 아니라 "배우자 포함" 규정이다**(`이 경우 해당 직계비속 또는 입양자와 그 배우자가 모두 제51조제1항제2호에 따른 장애인에 해당하는 경우에는 그 배우자를 포함한다`, 2026-08-05 마스터 1차 출처 재수신) — 나목에서 장애인 문구를 발견하고 나이예외의 근거로 되돌리지 말 것.
```

- 기존 문장 **삭제 0**. 순수 추가.
- 셀 안이므로 **개행 금지**(마크다운 표 파손). 한 줄로 이어 붙일 것.

### P-2. 같은 파일 §8 — R1 / R3 / R11 재베이스라인

**R1 (L551)**: 현행 행은 리포 전체 원시 카운트라 **정정 문서가 늘리면 영구히 어긋난다.**
숫자 교체가 아니라 **명제를 재정의**한다.

| 열 | 변경 후 |
|---|---|
| 검증 명제 | `구 라벨 8~20세가 **정본·규범 문서**에 잔존하지 않음 (이력·정정 문서는 제외 — 구 라벨을 인용해야 하는 문서다)` |
| 재현 명령 | **(rev.3 최종)** `rg -c "8~20세" --glob '!node_modules' --glob '!docs/team-log/**' --glob '!docs/qa/**' --glob '!cs/**' --glob '!planning/*-fix-spec.md' --glob '!planning/*-patch-spec.md' --glob '!planning/ticket-*' --glob '!planning/salary-tax-constant-audit.md'` |
| 실측(현재) | `content.md 1 · copy-fixes 10 = **11행 / 2파일**` |
| 패치 후 기대 | **동일 11행 / 2파일** (이번 티켓 범위 밖 — §2 참조) |

**R1-a (L552)**: 실측 열을 이번 회차 실측으로 교체 —
`spec 34 · copy-fixes 10 · constant-audit 4 · team-log-0805 4 · qa-report 2 · content 1 · team-log-0714 1 · cs 1 = 57행 / 8파일`.
"패치 후 기대" 열은 `측정 시점 의존 — R1으로 갈음`으로 대체.

**R3 (L554)**: 패치 후 기대 `1회` → **`2회`**.
사유 병기: `L252 라벨 + L213 JSX 주석. 주석도 같은 문자열을 쓰는 것이 정상이며 라벨 1회 가정이 오류였다.`

**R11 (L562)**: 패치 후 기대 `2회` → **`3회`**.
사유 병기: `tsx 2(라벨+주석) + calculators.ts K3 1`.

**§8 말미 "미검증 항목 정직 고지"**에 1줄 추가:

```
- **R1은 자기지시적(self-referential) 지표였다.** 구 라벨을 정정하는 문서는 구 라벨을 인용할 수밖에 없어, 리포 전체 원시 카운트는 정정 작업이 진행될수록 **증가**한다(2026-08-05 실측 57행, rev.2 기대값 13의 4배). 2026-08-05 R3에서 **정본·규범 문서로 범위를 좁혀** 재정의했다. 잔존 카운트를 KPI로 쓸 때는 **측정 대상이 측정 행위에 오염되는지** 먼저 볼 것.
```

### P-3. 같은 파일 §7-3 백로그 2번 (D-2) — **재확인만, 편집 없음**

마스터 실측: L537~539가 이미 `~~취소선~~ → **종결(2026-08-05 R2)**`로 표기돼 있다.
→ **조치 불요.** dev는 **아무것도 고치지 말 것.** 팀로그에 "재확인 완료"만 남긴다.
(이 항목을 "고칠 게 있다"고 오인해 중복 표기를 넣지 말 것 — 과잉정정 방지.)

### P-4. `planning/salary-net-calculator-content.md` L130 (P8) — CTC 출처 승격 고지

현행 L130 말미는 `… (2026 계산기들 확인).` — **타사 계산기 = 2차 출처**로 값이 확정된 이력이 남아 있다.
**기존 문장 삭제 없이**, L130의 마지막에 다음을 이어 붙인다:

```
 🔴 **출처 승격(2026-08-05, 티켓 #36)**: 이 금액은 원래 위와 같이 **2차 출처(타사 계산기)**로 확정된 이력이 있다. 마스터가 `law.go.kr MST=280405 / JO=005902`에서 **소득세법 제59조의2제1항 각 호**를 직접 수신해 `250,000 / 550,000 / +400,000`과 **축자 일치**함을 확인했다(날조 아님). 앞으로 이 값을 인용할 때는 **타사 계산기가 아니라 제59조의2제1항 각 호를 근거로 적을 것**(표준 ㋍).
```

- 이로써 §8 **R12**(`제59조의2제1항 각 호`가 `lib/salary.ts`·`content.md` 각 1회 이상)가 충족된다.
  현재 `salary.ts:294`만 1건, `content.md` 0건.

### P-5. `design/salary-net-calculator-ui-spec.md` §5-3 (D6) — 코드와 모순 2건

**대상**: L323~327. **결함이 1건이 아니라 2건이다.**

| # | 현행 서술 | 반증 |
|---|---|---|
| a | `라벨(…14자)은 375px에서 **2줄**이 되므로` | 마스터 05:05 DOM 실측(375×667, in-app browser + `javascript_tool`): **1줄, 137.9px**(칸 148.5px). 표준 ㋔ — 한글환산 추정 모델이 약 20% 과대예측 |
| b | `좁으면 sm 미만에서 1열(`grid-cols-1`) **폴백도 허용**하나` | `SalaryNetCalculator.tsx:214`는 `grid grid-cols-2 gap-3 sm:gap-4` — **폴백 미구현.** 문서가 코드에 없는 선택지를 규정 중 |

**교체문** (L323 `"3명" 정도로 짧아 2열에서도 잘림 없음.` 이후 ~L327까지):

```
라벨("8세 이상 자녀·손자녀 수", 14자)은 **375px에서 1줄로 렌더된다** — 라벨 실폭 137.9px / 칸 148.5px
(2026-08-05 마스터 DOM 실측, 375×667 in-app browser + `javascript_tool`, 표준 ⓧ). 설계 시점의
"2줄" 예측은 한글환산 추정 모델의 과대예측이었다(표준 ㋔). 다만 라벨 길이가 바뀌면 2줄이 될 수
있으므로, 두 셀렉트의 상단선은 라벨 줄수와 무관하게 맞아야 한다 — **라벨 컨테이너의 `min-h-10`이
이 역할을 하고 있으며(`SalaryNetCalculator.tsx:218`), 제거하지 말 것.**
**1열(`grid-cols-1`) 폴백은 채택하지 않았다** — 라이브는 `grid grid-cols-2 gap-3 sm:gap-4`로
전 뷰포트 2열 고정이다(`:214`). 두 값의 의존관계(자녀 ≤ 부양−1)를 나란히 보여주는 이점 때문이며,
375px에서 잘림·오버플로가 0으로 실측됐다. **폴백을 "허용"으로 서술하지 말 것** — 미구현 선택지를
문서가 규정하면 다음 구현자가 도입해 좌우 정렬 전제를 깨뜨린다.
```

- ⚠️ 줄번호 인용(`:214`·`:218`)을 새로 넣는다. 표준 ㉰가 "스펙이 코드를 N행으로 인용하면 사고 신호"라 했으나,
  **이 두 곳은 클래스 문자열 자체를 함께 적었으므로** 줄번호가 드리프트해도 grep으로 복구된다. 파일명+클래스가 본체, 줄번호는 보조다.

### P-6. `lib/blog.ts` L1404 (W-1) — 라이브 카피 용어 통일

같은 글(**`salary-net-by-bracket-2026`** — rev.3에서 slug 오기 정정, `salary-net-income-guide-2026`은 L69에서 시작하는 **다른 글**이다) 안에서 L1384는 `자녀·손자녀 0명`, L1404는 `자녀 0명`이다.
**값이 0이라 사실오류는 아니지만**, #36이 "자녀만 쓰면 조손가정이 손자녀를 못 센다"를 고친 직후라
**같은 글이 두 용어를 병용하면 정정의 메시지가 무너진다.**

**변경 (L1404 `callout.text` 내 2곳):**

| 현행 | 변경 후 |
|---|---|
| `부양가족 본인 1인, **자녀 0명**)으로 계산한` | `부양가족 본인 1인, **자녀·손자녀 0명**)으로 계산한` |
| `부양가족·**자녀 수**가 많으면` | `부양가족·**자녀·손자녀 수**가 많으면` |

**추가 (rev.2 — dev 반증 ②로 범위 확대):** 같은 글 **L1469 면책 문구**에도 같은 결함이 1건 더 있다.
마스터 최초 실측이 `lib/blog.ts` 전체가 아니라 L1404 주변만 본 탓에 누락했다(현재 2건, 스펙 기재 1건은 오류).

| 현행 | 변경 후 |
|---|---|
| `개인의 부양가족·**자녀 수**, 비과세액에 따라` | `개인의 부양가족·**자녀·손자녀 수**, 비과세액에 따라` |

범위에 넣는 근거(표준 ⓜ — 문자수 중립이 아니라 **의미 적합**으로 고른다): **같은 글·같은 결함 클래스**이고,
이 티켓의 목적 자체가 "한 글 안에서 두 용어 병용 해소"다. L1469만 남기면 티켓이 겨냥한 상태가 그대로 존치된다.

**위 3개 문자열 외에는 어떤 글자도 바꾸지 말 것.**

> ⚠️ **변경 금지 (과잉정정 방지)**: 같은 문장의 `공제가 줄어 실수령액은 표보다 늘어납니다`는
> 이 글이 "공제 = 실수령에서 빠지는 금액(원천징수액)"으로 일관되게 쓰고 있어 **내부 정합**하다.
> 이번 티켓 범위 밖이다. 고치지 마라. (QA가 별건 티켓 후보로 볼지 판단만 하면 된다.)

---

## 2. 변경 금지 목록

1. **`copy-fixes.md`의 `8~20세` 10건 · `content.md` 1건** — R1 재정의 후에도 남는 값이다.
   이번 티켓은 **지표 재정의**이지 잔존 제거가 아니다. 별건으로 다룬다(§3-2).
2. **`lib/salary.ts` · `lib/calculators.ts` · `components/SalaryNetCalculator.tsx`** — 이번 티켓 무변경.
3. **아카이브 8곳**(spec §7-1: constant-audit·qa-report·team-log-0714·cs) — 시점 기록물, 정정 대상 아님.
4. **`scripts/regression/engines.test.ts`** — 무변경. 앵커 불가침.
5. **`min-h-10`** — P-5가 명시적으로 보존을 규정한다. 제거 금지.

---

## 3. 검증 (dev가 패치 후 전 행 실행, QA가 독립 재현)

### 3-1. grep 재현 표 (표준 ⓖ — 전 행 재현 가능해야 함)

| # | 명제 | 명령 | 기대 |
|---|---|---|---|
| V1 | 계산 무변경 | `npx tsx scripts/regression/engines.test.ts` | **420 assertions ALL PASS** |
| V2 | 빌드 | `npm run build` | **69/69 성공, 신규 경고 0** |
| V3 | 구 문구 잔존 (**dev 반증 ①로 교체**) | `rg -c "인, 자녀 0명" lib/blog.ts` | **0건** (현재 1) |
| V4 | 신 용어 | `rg -o "자녀·손자녀 0명" lib/blog.ts` | **2건** (현재 1) |
| V5 | `부양가족·자녀 수` 잔존 (**dev 반증 ②로 범위 확대**) | `rg -c "부양가족·자녀 수" lib/blog.ts` | **0건** (현재 **2** — L1404·L1469) |
| V6 | R12 충족 | `rg -c "제59조의2제1항 각 호" lib/salary.ts planning/salary-net-calculator-content.md` | **각 ≥1** (현재 salary만 1) |
| V7 | 폴백 서술 제거 | `rg -c "폴백도 허용" design/salary-net-calculator-ui-spec.md` | **0건** (현재 1) |
| V8 | 나목 정밀도 한 줄 | `rg -c "배우자 포함.*규정" planning/child-tax-credit-copy-fix-spec.md` | **≥1** |
| V9 | 라이브 `8~20세` | `rg "8~20세" components/ lib/ app/` | **0건 유지** |
| V10 | 코드 파일 무변경 | `git diff --stat lib/salary.ts lib/calculators.ts components/` | **출력 없음** |
| V11 | 표 파손 없음 | `npm run build` 후 `/blog/<slug>` HTML에 리터럴 `|` 행 없음 | 정상 렌더 |

### 3-2. 이번 티켓이 **닫지 않는** 것 (정직 고지)

- `copy-fixes.md` 10건 · `content.md` 1건의 구 라벨 잔존 — **R1 재정의로 "보이게" 만들었을 뿐 제거하지 않았다.**
  두 파일은 규범 문서이므로 **별건 티켓**이 필요하다(다음 회차 후보).
- P-5는 **문서만** 고친다. 코드(`grid-cols-2` 고정)는 옳으므로 무변경이다.
- V11은 마크다운 표 셀에 개행이 들어가면 깨지는 것을 막기 위한 것이지, 블로그 렌더러와는 무관하다
  (`lib/blog.ts`는 마크다운 파서를 쓰지 않는 판별 유니온이다). **P-6은 표가 아니라 문자열 리터럴이다.**

---

## 4. rev.2 — dev-team 반증 3건 (전부 채택, 마스터 독립 재현 완료)

표준 ㉹가 **4회차 연속** 값을 했다. 셋 다 **마스터 스펙의 오류**였고 dev는 지시대로 고치지 않고 반증했다.

| # | dev 반증 | 마스터 독립 재현 | 처분 |
|---|---|---|---|
| ① | **V3의 기대값 0은 문자열상 도달 불가** — `자녀 0명`은 `손자녀 0명`의 **부분문자열**이라, P-6이 의도대로 적용될수록 카운트가 오히려 **늘어난다**(패치 전 실측도 1이 아니라 2였다) | `rg -c "인, 자녀 0명" lib/blog.ts` → **exit 1 (0건)** 확인 | **채택.** V3을 판별력 있는 `"인, 자녀 0명"`으로 교체 |
| ② | **L1469에 `부양가족·자녀 수` 1건 잔존** — 같은 글 면책 문구. 스펙 "현재 1"은 오류, 실제 2 | `rg -n` 으로 L1469 직접 확인 | **채택.** P-6 범위 확대(위 rev.2 블록) |
| ③ | 🔴 **R1 재정의가 이 스펙 파일 자신 때문에 이미 틀렸다** — 지정 glob 실행 시 `ticket-37-doc-consistency-spec.md:7`이 걸려 **18행/3파일**, 리포 전체도 57이 아니라 **64행/9파일** | 일반화 glob `--glob '!planning/*-spec.md'` 실행 → **정확히 11행/2파일** 재현 | **채택, 단 dev 제안 (a)가 아니라 (b)+(c).** 파일명 열거는 **다음 정정 스펙에서 또 깨진다**(그게 결함의 본체다). `planning/*-spec.md` 패턴으로 일반화하고 실측일자를 병기 |

### 4-1. ③이 왜 이번 회차 최대 교훈인가

P-2e는 "R1은 자기지시적 지표라 정정 작업이 진행될수록 증가한다"는 **교훈을 문서에 적는** 편집이었다.
그런데 **그 교훈을 적는 문서 자신이 같은 회차에 R1을 7행 오염시켰고, 마스터는 그것을 계산에 넣지 않았다.**

> 교훈을 **서술하는 것**과 그 교훈을 **자기 산출물에 적용하는 것**은 다른 행위다.
> 표준 ㋓("결함을 진단한 문장이 발원지를 고쳤는지 따로 확인하라")의 **자기적용 버전**이며,
> 표준 ㋎(정정문은 같은 종류의 오류를 반복한다)의 **5연속** 발동이다
> (#30 승수 → #35 부등호 → #36-R1 조문위치 → #36-R2 문서6건 → **#37 자기지시 지표**).

**그래서 (a)를 쓰지 않았다.** 파일명을 하나 더 열거하는 해법은 오류의 *인스턴스*만 지우고
*생성 규칙*을 남긴다 — 다음 정정 스펙이 생기는 순간 동일하게 깨진다.
`planning/*-spec.md`는 "정정 스펙은 구조적으로 구 라벨을 인용한다"는 **원인**을 직접 배제한다.

### 4-2. 마스터 자기점검 결과

스펙 §0 실측표의 `8~20세` **57행/8파일**은 **스펙 파일을 쓰기 직전 값**이었고,
스펙을 저장한 순간 **64행/9파일**이 됐다. §0 표의 값은 "측정 시점" 표기 없이 적혀 있었다 —
**표준 ⓧ(폭 실측은 도구를 명시)의 시간축 버전이 필요하다**: *자기지시적 지표는 측정 시점을 함께 적어라.*
R1·R1-a 두 행에 `(2026-08-05 06시 실측)`을 병기했다.

---

## 5. rev.3 — qa-team FAIL 6건 (전부 채택, 5건이 마스터 책임)

QA가 A(무회귀)·B(V1~V11)·D(표 무결성)·E(과잉정정)를 전항 PASS로 판정한 뒤,
**C(마스터 rev.2 산출물 검증)에서만 FAIL 6건**을 냈다. 코드·계산·라이브 카피 정확성은 전부 PASS였고
반송 사유는 **전부 문서 정합성** — 즉 이 티켓의 대상 그 자체다.

| # | FAIL | 책임 | 처분 |
|---|---|---|---|
| **F-3** 🔴 | **반증된 "375px 2줄"이 4곳 잔존.** 그중 2곳은 **이번에 편집한 파일·표**다 — `ui-spec:101`(정본 입력필드 표)·`copy-fix-spec:561`(P-2가 손댄 §8 R10 행). 나머지 `:188`·`:213` | **마스터**(범위를 §5-3과 4개 행으로 좁힘) / dev 부책임(5회차 만에 반증 누락) | **채택.** 4곳 전부 실측값으로 정정 |
| **F-1** | **R1 일반화 glob의 과잉 배제.** `!planning/*-spec.md`가 `planning/*-spec.md` **21개 전부**를 배제 — 정정 스펙 5개만이 아니라 **정본 기획안 16개**(`blog-post-29~35-spec.md`, `blog-spec.md`, `og-metadata-spec.md` 등)까지. 지표가 감시 대상의 76%를 구조적으로 못 봄 | **마스터** | **채택.** `!planning/*-fix-spec.md` + `!planning/*-patch-spec.md` + `!planning/ticket-*`로 **정정 문서 명명 규칙만** 배제. 재실행 → 정확히 11행/2파일, 정본 스펙 16개는 감시 범위 복귀 |
| **F-2** | **R1-a가 7이 아니라 8** (총 64 ≠ 65). 8번째를 만든 문장은 **§4-2 자기지시 교훈을 쓰면서 마스터가 추가한 줄**이고, dev는 재측정 없이 전사 | dev 1차 / 마스터 원인제공 | **채택하되 dev 제안 이상으로.** R1-a를 **[비검증·참고]로 강등** — 기대값 고정이 원리적으로 불가능한 행이다 |
| **F-4** | `min-h-10` 보호 지시가 **필요한 2곳 중 1곳만** 지목(`:218`만, 정작 §5-3이 논하는 자녀 라벨은 `:250`) | 마스터 | **채택.** 양쪽 모두 명시 |
| **F-5** | P-6의 대상 글 slug 오기 — 실제는 `salary-net-by-bracket-2026` | 마스터 | **채택.** 정정 |
| **F-6** | 스펙 **자기모순** — §1 P-2의 재현 명령 열이 폐기된 구 glob을 여전히 지시 | 마스터 | **채택.** rev.3 명령으로 교체 |

### 5-1. F-3이 이번 회차의 본질적 실패다

이 티켓의 §0은 **"문서가 코드와 어긋난 채 남으면 명세대로 구현이 결함을 되살린다"**로 시작한다.
그런데 마스터는 **바로 그 실패를 이 티켓 안에서 재현했다** — `ui-spec` §5-3의 "2줄"은 고치면서
같은 파일 **L101 정본 입력필드 표**의 "2줄"은 남겼고, §8에서 R1·R1-a·R3·R11 네 행을 편집하면서
**바로 아래 R10의 반증된 "2줄" 명제**는 건드리지 않았다.

> **표준 ㋓의 재발**(결함을 진단한 문장이 발원지를 고쳤는지 따로 확인하라) —
> 이번엔 "발원지"가 아니라 **"같은 파일의 다른 곳"**이었다.
> 그리고 표준 ㋎(정정문은 같은 종류의 오류를 반복한다)의 **6연속** 발동이다.
> #30 승수 → #35 부등호 → #36-R1 조문위치 → #36-R2 문서6건 → #37-rev2 자기지시 지표 → **#37-rev3 부분정정**.

**교훈(신규 표준 후보 ㋕)**: *한 명제를 정정할 때는 그 명제의 **모든 인스턴스**를 먼저 grep으로
열거한 뒤 착수하라 — 눈에 띈 곳부터 고치면 "정본 문서"가 마지막까지 남는다.*
실제로 이번에 남은 4곳 중 2곳(`ui-spec:101`, `R10`)은 **가장 규범적인 위치**였다.
`rg -n "2줄" design/ planning/` 한 번이면 착수 전에 전부 보였다.

### 5-2. QA가 발견한 별건 라이브 결함 1건 — **이번 회차에 함께 정정**

`lib/blog.ts:146` (`/blog/salary-net-income-guide-2026`, 라이브):

> 종전: `… 1년에 약 500만원 이상이 세금·보험료로 빠지는 셈입니다. 부양가족이나 자녀가 더 많거나 비과세액이 크면 **이 금액은 늘어납니다.**`

`이 금액`의 최근접 선행사가 **"세금·보험료 500만원"**인데, 부양가족·자녀가 늘거나 비과세액이 크면
세부담은 **줄어든다**. 세 조건 **전부**에 대해 방향이 반대로 읽힌다 — **세부담 과대 = 사용자 불리 방향**.

> 정정: `… 부양가족이나 **자녀·손자녀**가 더 많거나 비과세액이 크면 **공제가 줄어 실수령액은 이보다 늘어납니다.**`

지시대명사를 없애고 주어를 **실수령액으로 명시**했다. 자매 글 L1404가 이미 쓰던 표현과 같은 형태로 통일했고,
덤으로 이 문장은 `lib/blog.ts`에서 `손자녀` 없이 `자녀`만 쓰던 **마지막 잔존 지점**이었다(QA 실측).

### 5-3. rev.3이 닫지 않는 것 (정직 고지)

- **라벨 폭 137.9px 실측 자체는 이번 QA가 재현하지 못했다** — QA 세션에 DOM 계측 도구가 없었다.
  05:05 회차 마스터 실측이 유일한 근거이며, QA는 "문서 간 1줄/2줄 주장 일치" 만 검증했다.
- QA 별건 #2(`R8` 줄번호 L298·300·302 → 실제 L299·301·303 드리프트)는 **미조치**. 다음 회차 이월.
- `copy-fixes.md` 10건 / `content.md` 1건 구 라벨 잔존은 **의도된 미종결**(§3-2).
