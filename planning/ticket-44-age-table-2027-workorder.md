# 티켓 #44 — `age-by-birth-year-2026` 2027 갱신 작업지시서

**작성**: 2026-08-06 08:00 GSC 일일 점검 회차 (마스터)
**마감**: **2026-12-31** (2027-01-01 0시부로 라이브가 틀려진다)
**대상 파일**: `lib/blog.ts` 단일
**줄번호 기준 커밋**: **`58632df`** — 이 문서의 모든 줄번호는 이 커밋 기준이다. 착수 시 HEAD가 다르면 **줄번호를 먼저 재확인**하고 이 문서를 갱신할 것.

> **이 문서가 왜 별도로 존재하는가**
> 이 작업지시서는 원래 `planning/blog-year-slug-policy.md`(연도 표기 정책) 안에 들어 있었고, 그 상태로 **QA 2라운드 연속 FAIL**을 받았다. 3라운드 소진 후 판정된 원인은 담당 팀의 역량이 아니라 **설계 결함** — 사이트 전역 표기 정책과 단일 글의 작업지시서를 한 문서에 섞은 것 자체다. 전역 정책의 "연도 토큰을 스윕한다"는 지시가 이 글에서는 **거짓 문장을 만들어내기 때문**이다(§4). 그래서 티켓으로 분리했다. **두 문서를 다시 합치지 말 것.**

---

## 1. 무엇이 문제인가

블로그 글 `age-by-birth-year-2026`(제목 「2026년 출생연도별 나이 조견표」)의 본문은 **연 나이 = `2026 − 출생연도`를 하드코딩**하고 있다. 2027-01-01이 되는 순간 **조견표 15행 전부와 산문 예시가 한 살씩 틀린다.**

이 항목의 성격이 다른 시한 항목과 다른 점:

- **외부 고시를 기다릴 필요가 없다.** 최저임금·4대보험 요율 등은 정부 고시 확정을 기다려야 하지만, 이 글은 달력만 넘어가면 값이 정해진다. **12월을 기다릴 이유가 전혀 없고, 지금 착수할 수 있다.**
- **사용자 유리/불리 방향이 아니라 단순 오류다.** 제보로 발견될 유형이며, 조견표 전 행이 동시에 틀리므로 눈에 잘 띈다.
- **sitemap 등재된 색인 대상 URL**이다(라이브 62 URL 중 1건).

---

## 2. 선행 결정 — **U-KEEP 확정** (제자리 갱신)

2027 갱신 시 URL을 어떻게 할 것인가에 대한 택1이 선행 조건이었다. **기획팀 진단 + 마스터 판정으로 확정한다.**

### 2-1. 기호 (⚠️ 먼저 읽을 것)

`blog-year-slug-policy.md`가 `(가)(나)(다)`를 **버킷 이름**으로 이미 쓰고 있다. 같은 기호를 URL 축에 재사용하면 하류가 확실히 오독한다. 이 축은 **기호를 바꿔 쓴다**:

| 기호 | 뜻 |
| --- | --- |
| **`U-KEEP`** | URL 무변경. 본문·title·description만 갱신 |
| **`U-NEW`** | `-2027` 신규 URL 발행 (구 URL은 유지 또는 리다이렉트) |

### 2-2. 판정: **`U-KEEP`**. 그리고 이것은 단건이 아니라 **사이트 전역 정책**이다.

결정적 근거는 비용 비교가 아니라 **포함 관계**다:

```
U-NEW = U-KEEP(본문 전량 갱신) + 리다이렉트 인프라 신설 + 색인 이관 리스크 + 문서자산 동기화
```

U-NEW를 골라도 §3의 30줄은 **똑같이 전부 고쳐야 한다**(새 URL에 올릴 본문도 정확해야 하므로). 즉 U-NEW는 U-KEEP의 상위집합이고, 추가 비용을 상쇄할 고유 이득이 아래 어디에도 없다.

**색인**: 이 글은 `publishedDate: 2026-07-18`로 색인 이력이 쌓인 채 2027-01-01을 맞는다. U-NEW는 그 축적을 리셋하고 구글이 "a few weeks"로 명시한 재색인 변동 구간에 넣는다. **그리고 이 사이트에는 GSC 데이터가 없어 그 손실을 사전에도 사후에도 측정할 수 없다** — 탐지기가 0인 상태에서 비가역 변경을 하는 것은 이 저장소의 네거티브 컨트롤 표준(㉲)에 정면으로 어긋난다. U-KEEP은 URL 이벤트가 0건이다.

**중복 콘텐츠**: U-NEW에서 구 URL을 유지하면 문단 구조·헤딩·CTA·FAQ가 100% 같고 **숫자만 1씩 다른 두 페이지**가 생긴다. 두 URL 모두 self-canonical(`app/blog/[slug]/page.tsx:40`)이라 선호 지정이 없는 상태이고, 구글이 링크 이력이 있는 **구 URL을 canonical로 고를 수 있다.** 그러면 2027년 사용자가 2026년 표를 보게 되어 **U-NEW를 택한 이유가 그대로 실패한다.** 구 URL을 리다이렉트하면 최종 상태가 U-KEEP과 같아지면서 인프라 비용만 더 든다. 어느 갈래도 U-KEEP을 이기지 못한다.

**인프라**: `next.config.ts`에 redirects 설정이 **아예 없고**(파일 전문이 빈 config), 루트 `middleware.ts`도 없다. U-NEW는 "설정 추가"가 아니라 **리다이렉트 체계의 신규 도입**이다. 그리고 엔트리는 매년 누적되며 체인(`2026→2027→2028`)을 피하려면 **매년 기존 전량의 타깃을 재작성**해야 한다 — 단순 append가 아니라 연 O(36n) 재작성이다.

**애드센스**: 사이트는 2026-07-31 「가치가 낮은 콘텐츠」로 거절됐고 재신청 타깃이 8월말~9월초다. U-NEW를 전역 적용하면 3년 뒤 108 URL 중 72개가 90% 이상 동일 구조가 된다 — **이미 받은 거절 사유와 정확히 같은 방향**이다.
※ 정직하게 구분: 구글 스팸 정책의 scaled content abuse 정의는 *"for the primary purpose of manipulating search rankings"*를 핵심 요건으로 하므로, 연 1회 정직한 갱신본이 여기 **직접 해당한다고 단정할 수는 없다.** 그럼에도 심사 표면상의 리스크는 실재하며, 이는 U-NEW 기각의 **보조** 사유다(주 사유는 §2-2 앞의 세 항목).

### 2-3. 전역 정책 문장

> **연도 스탬프 slug는 URL을 갱신하지 않는다.** 연도는 title·description·본문에서만 갱신하고, slug의 `-YYYY`는 발행연도 표식으로 고정한다. URL 변경은 커스텀 도메인 이전 회차에서만 1회로 처리한다.

**예외 조항 (반드시 함께 명문화)**:
> **과거 연도의 값 자체가 독립적 조회 대상인 글**에 한해 U-NEW(연도별 별도 URL)를 검토할 수 있다. 예: 연봉 실수령액표(작년 원천징수 대조 수요가 실재). **#27은 여기 해당하지 않는다** — 작년 기준 내 나이를 조견표로 되짚는 수요는 실무상 없고, 있더라도 만 나이 계산기가 생년월일 입력으로 완전히 커버한다.

이 예외 조항이 없으면 나중에 연봉표 담당 회차가 "전역 정책이 금지했다"며 잘못된 방향으로 끌려간다.

### 2-4. 왜 전역인가 (단건 격리 불가)

1. **#27은 최악 케이스다.** 36편 중 연도 의존이 가장 강하다(제목·본문·표 전 행이 기준연도 종속). 최악 케이스에서 U-KEEP이 성립하면 그보다 의존도가 낮은 전부에 성립한다.
2. **예외가 0이다.** 블로그 **36편 중 36편**이 `-2026`으로 끝난다(마스터 실측, §6-1). #27만 U-NEW로 빼면 URL 규칙이 즉시 두 벌이 된다.
3. **인프라 비용의 지배항이 엔트리 수가 아니다.** 리다이렉트 표를 **처음 만드는 것**이 비용의 대부분이라 1건이든 36건이든 문턱이 같다. "일단 1건만"은 존재하지 않는다.

---

## 3. 작업 범위 — 전수 열거 (마스터 작성 → QA 독립 재열거 → 마스터 재검증)

**범위**: `lib/blog.ts` **L4297~L4443**.
⚠️ **L4444는 다음 글 `service-period-milestone-2026`의 여는 `{`다.** 범위를 L4444까지로 적으면 인접 글을 건드린다. (마스터 초안이 L4444로 적었고 QA가 반증, 마스터가 파일에서 직접 재확인.)

**단위 병기 (표준 ㋢)**: 아래는 **줄(line) 단위**다. 치환 작업은 **개소(occurrence) 단위**로 세야 한다 — 아래 참조.

### A. 가시 — `2026` 리터럴 보유 = **14줄 / 17개소**

`L4299, L4301, L4319, L4324, L4338, L4339, L4345, L4349, L4387, L4395, L4403, L4419, L4423, L4428`

🔴 **한 줄에 `2026`이 2회 있는 줄 = `L4319`, `L4349`, `L4423`.**
"14줄"만 보고 줄당 1회 치환하면 **이 3줄에서 두 번째 토큰을 놓친다.** 반드시 **17개소**로 셀 것.

### B. 비가시 — `2026` 리터럴이 없는데 연령 숫자 보유 = **16줄**

`L4355~L4369`(조견표 **15행**) + **`L4391`**

🔴 `L4391` = `"2000년생이면 세는나이 27세, 1995년생이면 세는나이 32세인 식입니다."`
문자열에 `2026`이 **없어서 grep에 안 잡히는데** 2027년엔 27→28, 32→33으로 틀린다. **이 줄은 QA·마스터가 초기 라운드에서 둘 다 놓쳤던 줄이다.**

### C. 제3 범주 — A에 속하면서 **동시에 연령 숫자 보유** = **4줄**

`L4349, L4387, L4395, L4419` (C는 A의 부분집합 — 합계에 중복 가산하지 않음)

**"연도 토큰만 치환"하면 이 4줄은 갱신된 조견표와 같은 화면에서 모순된다.** §4에서 별도 규칙을 적용한다.

### 합계

| 구분 | 값 |
| --- | --- |
| 실제 작업 대상 | **A 14 + B 16 = 30줄** |
| 원시 `grep 2026` 히트 | **16줄** (= 작업대상 14 + **함정 2**: `slug` L4298, `publishedDate` L4310) |
| grep 포착률 | **14/30 ≈ 47%** |

> **정확한 서술**: grep 결과를 그대로 작업 목록으로 쓰면 **필요한 것의 절반을 빠뜨리면서 동시에 건드리면 안 될 2줄을 포함한다.** `publishedDate` 혼입은 이 프로젝트에서 이미 2회 발생한 사고 유형이다.

---

## 4. 🔴 마이그레이션 전략 — 줄 목록만으로는 착수 불가

QA가 잡아낸 핵심: **C 4줄의 정정 방향이 서로 반대다.**

- `L4349` · `L4387` · `L4419` (+ B의 `L4391`) → 출생연도를 고정하고 **연령을 +1** 해야 참이 된다.
- 🔴 **`L4395`는 정반대다.** 성년 경계는 **민법상 만 19세로 법정 고정**이므로, 연령을 고정하고 **출생연도를 +1** 해야 한다. 여기에 "연령 +1"을 적용하면 **「만 20세로 성년」이 되어 민법과 충돌**한다. (`L4395`에는 `만 19세`가 **4회** 등장한다 — 마스터 직접 확인.)

### 4-1. 마스터 판정: **출생연도 고정 + 연령 +1**, 그리고 **2008년생 행 1개 신설**(조견표 15행 → **16행**)

근거:
- title·description·본문 6줄이 **`2000년생`·`95년생`·`2005년생`이라는 검색 키워드에 앵커돼 있다.** 반대 안(연령 고정 + 출생연도 +1)을 택하면 2000년생 행이 표에서 사라지고 그 앵커가 전부 표와 어긋난다. **검색 자산 손실이 결정적이다.**
- 다만 그 안을 그대로 적용하면 `L4395`가 거짓이 된다 — 2027년에 만 19세가 되는 사람은 **2008년생**인데 표에 2008년생 행이 없어 `"표에서 바로 확인됩니다"`가 거짓말이 된다.
- 따라서 **2008년생 행을 추가**해 성년 경계 예시를 살린다. 조견표 행 선택 자체가 의미 앵커를 담고 있다는 점에 유의: `2007=성년 경계`, `2006=이미 성년`, `1966=연 나이 60`, `1960=연 나이 66`.

⚠️ 이 판정은 **12월 실행자가 재검토할 수 있다.** 다만 재검토한다면 위 두 제약(키워드 앵커 / `L4395` 진위)을 **동시에** 만족하는지 반드시 확인할 것 — 한쪽만 보면 반드시 다른 쪽이 깨진다.

---

## 5. 🔴 보호 대상 — 절대 변경 금지 (마스터 초안 1건 → QA FAIL → **4계열**로 확정)

전역 연도 스윕이 이 줄들을 건드리면 **참인 문장을 거짓으로 만든다.**

| # | 대상 | 줄 | 왜 보호되는가 |
| --- | --- | --- | --- |
| 1 | `publishedDate: "2026-07-18"` | `L4310` | **발행 사실.** 바꾸면 발행일을 날조하는 것. 갱신 사실은 `updatedDate`에 적는다(§7) |
| 2 | `2023년 만 나이 통일법` | **`L4324`, `L4340`, `L4375`** | **역사적 사실.** 법 시행 연도라 영구 불변. 🔴 특히 **`L4324`는 A(가시 14줄)에 속해 스윕 사정권 안에 있다** — 같은 줄의 `2026`은 바꾸고 `2023`은 남겨야 한다 |
| 3 | `만 19세` ×4 | **`L4395`** | **민법 제4조 성년 연령.** 이 줄은 C라서 "연령 +1"의 사정권인데, 19→20으로 올리면 **법정 상수를 날조**하게 된다. C 내부에 보호 숫자가 박힌 유일한 줄 |
| 4 | 산술 불변식 | `L4340`(`+1`), `L4387`(`한 살 적습니다`), `L4391`(`+1`) | 규칙 서술이라 연도와 무관하게 항상 참 |

> 마스터 초안의 보호 목록은 **`publishedDate` 1건뿐이었다.** QA가 `2023` 3줄과 `만 19세`를 잡아냈고, 마스터가 파일에서 직접 재확인했다. **보호 조항 부재는 이 프로젝트에서 반복 지적된 결함 유형이다.**

---

## 6. 마스터 직접 실측 (팀 보고를 복창하지 않음 — 표준 ㋥)

아래는 **내가 파일/라이브에서 직접 측정한 값**이다. 팀에게서 전달받았으나 내가 재확인하지 않은 값은 이 절에 넣지 않았다.

### 6-1. 검증한 값

| 항목 | 실측 | 방법 |
| --- | --- | --- |
| 범위 끝 | **L4443** (L4444는 다음 글 여는 `{`) | `awk` L4441~4446 직접 출력 |
| `2023` 보유 줄 | **L4324 · L4340 · L4375** | 범위 내 `2023` 스윕 |
| `L4395`의 `만 19세` | **4회** | `grep -o \| wc -l` |
| A 줄/개소 | **14줄 / 17개소**, 2회 줄 = L4319·L4349·L4423 | `awk gsub` 카운트 |
| `updatedDate` 스키마 | `lib/blog.ts:79 updatedDate?: string;` | grep |
| 폴백 3곳 | `app/sitemap.ts:71`(lastModified) · `lib/blog-jsonld.ts:38`(dateModified) · `lib/og.ts:57`(article:modified_time) | grep |
| 블로그 slug | **36편 / 36편이 `-2026`**, 예외 **0** | grep (⚠️ 원시 히트는 37 — `slug: post.slug,` **헬퍼 1줄 포함**. 표준 ㋢ 함정 실례) |
| `next.config.ts` redirects | **0건** (파일 전문이 빈 config) | 파일 전문 확인 |
| 라이브 sitemap | 200 · 10,608 B · **62 URL** | curl |

### 6-2. 전달받았으나 내가 검증하지 않은 값 (인용 시 재측정할 것)

- 기획팀 보고의 `age-by-birth-year-2026` 문자열이 박힌 **파일 14개**(코드 1 + planning 6 + team-log 7) — **미검증**.
- `blog-year-slug-policy.md` §1-5의 **399 출현 / 357행 / 55파일** — 그 문서는 **QA 2라운드 FAIL 상태**이며 기획팀도 "보증하지 않는다"고 명시했다. **정본으로 인용 금지.**
- SERP에서 URL이 브레드크럼으로 대체 표시된다는 서술 — 기획팀이 **미검증으로 표기**했고 판정 근거로 쓰지 않았다.

---

## 7. `updatedDate` — 티켓 #34가 이 티켓의 전제로 승격됨

`updatedDate?: string` 필드는 **이미 존재하고**, `sitemap.lastModified` · JSON-LD `dateModified` · og `article:modified_time` **3곳이 전부 `updatedDate ?? publishedDate`로 폴백**한다(§6-1에서 마스터 직접 확인). 즉 `updatedDate` 한 줄을 설정하면 **신규 코드 0줄로 3개 채널이 동시에 갱신 신호를 낸다.** 36편 전부 미설정 상태다.

**마스터 판정**: 티켓 #34(`updatedDate` 정책 판정)를 **2026-12 이전 마감으로 승격**한다. 근거 — U-KEEP은 "URL은 그대로 두고 내용만 갱신한다"는 전략이므로, **갱신되었다는 신호가 유일한 대외 표시 수단**이다. #34가 미판정이면 이 티켓이 절반만 완성된다.

**권고**: 본문 상단에 1문장(`"이 표는 2027년 기준으로 갱신되었습니다."`)을 추가한다. slug의 `-2026`과 내용의 2027 사이 간극을 **사용자 눈높이에서 해소하는 유일한 지점**이다.

---

## 8. 착수 전 체크리스트

- [ ] HEAD가 `58632df`가 아니면 **줄번호 전부 재확인** 후 이 문서 갱신
- [ ] `2026` 치환은 **17개소** 기준 (14줄 아님) — L4319·L4349·L4423은 줄당 2회
- [ ] **본문 전 블록 육안 통독** — grep 포착률 47%. B 16줄은 grep에 안 잡힌다
- [ ] C 4줄은 **문장 단위 재작성** (토큰 치환 금지)
- [ ] `L4395`는 **연령 고정 + 출생연도 +1** (다른 줄과 반대 방향)
- [ ] 조견표에 **2008년생 행 신설** (15행 → 16행)
- [ ] 보호 대상 4계열 **무변경** 확인: `L4310` / `L4324`·`L4340`·`L4375`의 `2023` / `L4395`의 `만 19세` / 산술 불변식
- [ ] `slug` **무변경** (U-KEEP)
- [ ] `updatedDate` 설정 (#34 판정 반영)
- [ ] 게이트: `tsc` · `eslint` · `npm test` · `next build`
- [ ] QA 독립 검증 — **수정 전 HEAD에 같은 검사기를 먼저 돌려 FAIL이 나오는지 증명**(표준 ㉲)

---

## 9. 이 티켓이 다루지 **않는** 것

- **연도 표기 정책 전반** — `planning/blog-year-slug-policy.md` 소관. **합치지 말 것**(§ 머리말).
- **나머지 35편의 연도 갱신** — §2-3 전역 정책은 확정했으나, 편별 작업지시서는 각각 필요하다. 이 티켓은 `age-by-birth-year-2026` **단 1편**이다.
- **`MINIMUM_WAGE` 상수 교체**(별도 시한 항목) — 같은 12월 마감이지만 **커밋을 분리**한다.
