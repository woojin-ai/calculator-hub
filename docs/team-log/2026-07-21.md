# 팀 로그 2026-07-21

## 01:06 실행분 (오늘 첫 실행)

### 컨텍스트 파악
- 오늘 첫 스케줄 실행. 어제(07-20) 로그의 인계 우선순위 이월: **#6 마케팅 카드뉴스(30편) = 다음 실행 우선**, **#5 블로그 확장(bmi 세 번째 각도 발굴 선행)**.
- 착수 시점 상태: 블로그 30편, 계산기 16개, `HEAD=4d2d607`. git status 미커밋 = `.claude/*`(표준 사용자확인 항목)만.
- 오늘 08:00 GSC 감사(`2026-07-21-gsc-audit.md`) 이미 실행 완료 → 신규 결함 0·회귀 0·코드변경 0·푸시 없음. canonical 25개 라이브 반영 확인(어제 인계 §7-0 종결). 내 작업과 충돌 없음, 감사가 워킹트리 산출물을 손대지 않음(마스터 세션에 위임).

### 배정 작업 (2건 병렬 착수)

**A. 마케팅 — 30편 카드뉴스 초안** (marketing-team)
- 산출: `marketing/card-news/annual-leave-days-by-service-year-2026.md` (8장, slug 단위 개별 파일 = 07-16 표준 준수).
- 프레임 A(채운 연수→발생일수) / 프레임 B(n년차 호칭)를 카드마다 배지로 명시. **발생일수 숫자는 프레임 A에만** 배치(원문에 B 대응표 없음 → B에 숫자 붙이면 창작). 조견표 21행 규칙 `d(1)=d(2)=15, d(k)=d(k-2)+1, 상한25`로 전수 재검산 일치. 금액 예시(80,000/day 등)도 재검산 일치, 16일(7원 함정) 카드 미포함으로 회피.
- 이미지 생성·게시 없음(사용자 승인 대기). 디자인 인계 3건(배지 고정·B열 숫자 금지·"두 해씩" 음영) 파일 하단 기록.

**B. 블로그 31편 기획→구현→QA** (planning → dev → qa)
- 주제: **"몸무게 1kg에 BMI는 얼마나 움직일까 — 키별 민감도와 측정 오차"** (slug `bmi-weight-change-sensitivity-2026`, life, 07-21).
- 선정: 축2(in-degree) bmi-calculator 2→3(최약체 탈출), 축1(카테고리) life 7→8. **기획팀이 지시문 전제 정정** — dsr는 in-degree 6으로 최약체 아님(실제 최약체는 bmi·weekly-holiday 각 2). 
- 차별화: 기존 #9(BMI 값)·#15(정상 체중 범위)는 **값(level)**, 31편은 **차분(Δ)** — "1kg당 BMI 변화량은 키의 1/h²로만 정해진다"는 성질. 억지 중복 아님(수학적 독립량). 중복차단 규칙 4개 강제.
- 프레임 3개(A 몸무게1kg당 / A′ BMI1당kg / B 키1cm) 라벨 강제(29편 실패 방지).

### QA 검증 — 제품 **PASS**
- QA가 직접 tsx 스크립트로 `calculateBmi` 실행, 숫자 17개(A 9행+A′ 3+B 4+파운드 1) 전수 엔진 대조 **전부 일치**. 개발팀 스크립트 미사용.
- **중간 반올림 함정 통과 확인**: A′ 170cm가 2.89(정답, `1/deltaA원값`)이지 2.86(`1/0.35` 오염)이 아님. 본문에 2.86 없음.
- 몸무게 독립성 교차검증 (55,56)(70,71)(90,91) 모두 0.35. B 비대칭 0.28≠0.29 확인.
- 프레임 라벨(F): B 숫자 문장 전부 `170cm·70kg` 동반, A 문장에 몸무게 조건 미부착, list에 A′/B 미혼입 — grep 확인.
- 중복차단(G)·YMYL 톤(H)·데이터/빌드(I) 전부 PASS. 금지어·전칭어·썩는정보 0건. `git diff` = 순수 append(+115/-0, 기존 30요소 무변경). `next build` 63정적 통과.
- **BMI 3편 캡 확인**: `getBlogPostsForCalculator("bmi-calculator")` = 3편, 31편이 최신순 1순위, #9·#15 밀려남 없음.

### 핑퐁 이슈

| 제기 | → 이관 | 결과 |
| --- | --- | --- |
| 마스터 지시문: 최약체 "bmi·dsr·weekly-holiday" | → planning | **정정**(dsr는 in-degree 6, 최약체 아님. planning 실측 §0-2) |
| planning: BMI 클러스터 4편째는 노출이득 0 (마스터 확인요청) | → 마스터 | **승인**(BMI는 31편으로 종료. 3편 캡 = 4편째 thin content 리스크만) |
| QA: 본문 오탈자 "BMI를 1**을** 움직이려면"(조사 중복) | → 마스터 | **해결**(마스터가 "1만큼"으로 수정, heading·FAQ 표현과 정합) |
| QA: 기획안 §7-G-4 "30자 연속일치 FAIL"이 §6-2(f) 공공기관 인용 강제와 논리충돌 | → 마스터(기획안=마스터 산출물) | **해결**(§7-G-4에 고유명사 carve-out 추가. 콘텐츠 재작업 불요 — 면책 산문은 이미 재작성됨) |

- QA가 **마스터/기획 산출물(기획안 체크리스트)에서 결함 1건**을 냈다(§7-G-4 논리충돌). 07-20 표준 ⓒ("마스터 산출물도 검증 대상") 계속 작동.

### 마스터 §7 최종 검증
- **본문 직접 Read**(lib/blog.ts 5108~5172): 21섹션, 프레임 라벨 무오염, 면책 callout `variant:"warning"` (a)~(f) 6항목 실재 확인.
- **오탈자 수정 + 빌드 재실행**: `Compiled successfully in 3.7s` · **63/63 정적** · 에러 0.
- **미확인**: 픽셀 스크린샷(`computer` 타임아웃 — 전 실행 동일 패턴). DOM 정량 + 빌드로 대체. 그 외 미확인 없음.

### 마케팅 카드뉴스 게이팅
- **START 유지.** 이번 실행에서 30편 카드뉴스 초안 완성(위 A). 소재 적합성 높음(조견표가 카드 단위로 분리, 날짜 산술 없음). 프레임 A/B 구분을 카드에 명시 완료.
- 사용자 승인 대기 카드뉴스 누적 **6편**(4 + 29편 + 30편). 이미지 생성·게시 전부 승인 대기.

### CS 문의 채널
변동 없음(`/support` Resend 발신 전용, 인바운드 큐 부재). 지어낼 문의 없음.

### 배포(push)
- 대상: `lib/blog.ts`(31편 append + 오탈자 정정), `marketing/card-news/annual-leave-days-by-service-year-2026.md`(신규), `planning/blog-post-31-spec.md`(신규 + G-4 carve-out), `docs/team-log/2026-07-21.md`, `docs/team-log/2026-07-21-gsc-audit.md`(감사 세션이 push 안 함 → 문서 트래킹 위해 동반 커밋).
- QA 제품 PASS + 마스터 §7(미확인=픽셀 스크린샷 1건, 대체측정 완료) → §8 auto-push 조건 충족.
- **제외 유지**: `.claude/settings.local.json`·`.claude/settings.json`·`.claude/launch.json`(전부 사용자 확인 대상, 경로명시 스테이징으로 배제). `.env*` 미포함·시크릿 0.

### 다음 실행 인계

1. **[블로그 32편 — 유력]** 31편 반영 후 분포 salary 8·loan 8·**date 7**·life 8 → 축1 최소 date 단독. 축2 date 최약체 = **age-calculator(4)**. **date + age-calculator** 조합 권고(기존 date 글 #4·#27 age 중복검사 선행 필수). 대안 weekly-holiday(2, salary)는 축1 충돌 → 착수 시 축 우선순위 판단. 착수 전 형제글 Read 필수.
2. **[BMI 종료 확정]** BMI 클러스터 31편으로 종료(3편 캡). 4편째 만들지 말 것.
3. **[마케팅 — 다음 우선]** 31편(BMI 민감도) 카드뉴스 초안. 프레임 A/A′/B 3개 라벨 카드에 명시 필수, 숫자는 엔진값(2.89 함정 주의).
4. **[운영 표준 — 유지]** ⓒ 마스터 산출물도 검증 대상(이번에도 G-4 결함 검출). ⓕ 정정 전 동일사실 위치 전수 grep. ⓖ 전칭 주장은 전수로만 검증.
5. **[GSC 인계 — 감사 §7]** GSC API 자격증명 부재(6일 연속, 사용자 확인). "푸시 직후 미반영→익일 정상" 2연속 확정 패턴 → 익일 첫 확인으로 이월. OG/metadataBase 전면 부재(티켓 #2, 첫 SNS 게시 승인 전 처리, design+사용자 승인).
6. **[유지 백로그]** `.claude/settings.local.json` bypassPermissions(사용자 확인) · `.claude/launch.json` 타프로젝트 유입 · `relatedSlugs` 꼬리 레이아웃 · 헤더 터치타깃 36px·다크모드 부재 · AdSense 심사 대기(광고 스크립트=승인 후) · 로드맵 #16 취득세(高YMYL 보류) · 마케팅 게시 승인 대기(6편).
