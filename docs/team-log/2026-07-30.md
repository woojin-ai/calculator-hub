# 2026-07-30 팀 로그 (daily-team-standup)

## 01:06 실행분 — 티켓 #13 신설 · #12 판정 번복 · #15 종결 (⚠️ 실행이 15시간 초과)

> **오늘 첫 실행.** 07-30 로그·GSC 감사 로그 **둘 다 없었다** → 이 실행이 오늘 유일한 실행이다.

### ⚠️ 이번 회차 최대 이상징후 — 실행 시간 15.8시간
착수 `01:06` → dev 서브에이전트 완료 시각 `16:55`. **한 서브에이전트가 약 15.8시간 실행됐다.**
지시서상 이 작업은 "새벽 1~7시대 30분 간격"인데, 이 회차는 **사용자가 깨어 있는 시간대를 통째로 관통**했다.
- 그 사이 **다른 standup 회차는 한 번도 안 돌았다**(07-30 로그 파일 부재로 확인). 즉 30분 간격 스케줄이 실질적으로 동작하지 않고 있다.
- 다행히 동시 실행 충돌은 없었다: 착수 시 `list_sessions`로 확인했고(실행 중인 건 loan-compare·realestate = 타 프로젝트),
  완료 시점에도 `git log` 무변화 · `origin/main` 0/0 · 07-30자 타 로그 부재 → **작업 손실 위험 없었음**.
- **다음 실행이 반드시 참고할 것**: 이 저장소의 자매 프로젝트에서 이미 "스케줄 중복 → 작업 손실 위험"(loan-compare 티켓 #18)이
  사용자 점검 대상으로 올라가 있다. 여기서는 **중복이 아니라 반대 방향(과소 실행 + 초장시간 단일 실행)** 이 관측됐다.
  두 현상 모두 스케줄러 동작이 지시서 가정과 어긋난다는 같은 뿌리일 수 있다.
- 🔔 **알림은 보내지 않았다** — 작업 손실이 없었고, 사용자가 결정해야 진행되는 블로커가 아니다(진행상황 보고는 알림 대상 아님).

### 착수 시점 상태
- `HEAD == origin/main` (0 ahead / 0 behind). 이월된 미완 코드작업 없음.
- 미커밋은 전부 타 세션/타 프로젝트분. `marketing/promo-drafts.md`(mtime 07-21) ·
  `planning/calculator-lineup-expansion.md`(mtime 07-25)는 **5~9일 묵은 문서 주석**이라 "진행 중인 작업"이 아님을 mtime으로 확인.

---

## 1) ★티켓 #12 — 인계 내용이 **뒤집혀 있었다** (마스터 직접 확인)

07-29 인계 #3은 *"`planning/salary-net-calculator-content.md` §1-4가 구식이니 엔진 실측으로 정정하라"* 였다.
그 근거는 `scripts/regression/engines.test.ts:88-93` 주석이었다.

**마스터가 문서를 실제로 열고 엔진을 실행해 대조한 결과, 문서는 이미 정확했다:**

```
calculateSalary({annualSalary:40_000_000, taxFreeMonthly:200_000, dependents:1, children:0})
nationalPension     engine=148817  doc=148817  MATCH
healthInsurance     engine=112643  doc=112643  MATCH
longTermCare        engine=14582   doc=14582   MATCH
employmentInsurance engine=28199   doc=28199   MATCH
4대보험합계          engine=304241  doc=304241  MATCH
```

**낡은 것은 문서가 아니라 그 주석이었다.** 문서는 언젠가 정정됐는데 주석이 옛 상태에 멈춰 있었고,
**최근 두 번의 인계가 그 주석의 주장을 검증 없이 그대로 전달**했다.

→ 지시서대로 "문서를 정정"했으면 **이미 맞는 값을 틀린 값으로 되돌릴 뻔했다.**
→ 같은 파일을 dev가 편집 중이었으므로 동시 편집하지 않고 **dev에게 이관**(핑퐁), 주석을 해소 기록으로 교체시켰다.

이건 티켓 #9와 **같은 패턴의 세 번째 반복**이다(주석에 불일치를 적어두고 escalate 안 함).
이번엔 한 단계 더 나빠져서 **주석의 사실 주장 자체가 부패**했다.

---

## 2) 티켓 #13 — 회귀 스위트 공백 2건 (dev → 마스터 검증 PASS)

`annual-leave-allowance` · `weekly-holiday-allowance` 앵커가 **하나도 없었다**(15 스위트 / 16 엔진).
07-29에 검증한 `14,354` / `114,833` / `1,148,325`를 지키는 테스트가 전무했다.

dev가 두 스위트 신설(각 31 assertions). 기대값은 **엔진을 직접 import 실행한 출력**만 사용(수기 계산·문서 근사치 0건).

### ★변이 테스트 — dev 자체 15종 + **마스터 독립 재현 1종**
dev 보고: 변이 15종 **전부 검출(kill rate 100%)**, 대조군 PASS.

**마스터가 보고를 그대로 믿지 않고 직접 재현**(저장소 무수정, scratch에 `lib`+`tsconfig` 사본 트리 구성):

| 실험 | 결과 |
| --- | --- |
| 대조군(변이 없음) | `282 assertions · ALL SUITES PASS` |
| **AL-1**: 총액을 raw가 아닌 **표시용 반올림값**에 곱함 | **FAIL 검출** — `+ 1148330 / - 1148325` |

AL-1은 `planning/annual-leave-allowance-*.md`가 경고했던 바로 그 결함이고, 07-29에 라이브 블로그에서
실제로 터졌던 유형(`14,354 × 8 = 114,833` 등식 불일치)과 같은 계열이다. **앵커가 실효를 갖는다는 것을 마스터가 직접 실증했다.**

---

## 3) 티켓 #15 종결 — 카드뉴스 원고 구값 정정 (마스터 직접)

`marketing/promo-drafts.md`의 4대보험 카드뉴스 원고가 티켓 #10(07-29 float 결함 수정) **이전 값**을
`[O] 수치 정확성` 으로 확정해 두고 있었다.

마스터가 엔진 실행으로 정답을 확정한 뒤(`calculateFourInsurance({monthlyTaxable:3_000_000, businessSize:"under150"})`) 치환:

| 항목 | 원고(구) | 엔진 실측 | 조치 |
| --- | --- | --- | --- |
| 고용보험 근로자 | 26,999 | **27,000** | 5건 치환 |
| 근로자 합계 | 291,310 | **291,311** | 10건 치환 |
| 총합 | 590,121 | **590,122** | 9건 치환 |
| 회사 부담 | 298,811 | 298,811 | **불변 — 미터치** |
| 항목별 142,500·107,850·13,961·34,500 | 동일 | 동일 | **불변 — 미터치** |

정정 블록을 섹션 상단에 삽입(기존 "업데이트(…)" 주석 패턴과 동일 형식).
**아직 이미지화 전이라 잘못된 값이 게시된 적은 없다** — 이미지화 시 이 정정본을 쓸 것.
diff 계정: 9개 본문 라인 수정 + 순수 추가 2줄(내 정정 블록 + 이 파일에 원래 있던 07-22 주석).

---

## 4) 마스터 §7 최종 검증

| 검증 | 결과 |
| --- | --- |
| `npx tsc --noEmit` | 무출력(exit 0) |
| `npx eslint .` | 무출력(exit 0) |
| `npm test` | **282 assertions · 17 스위트 · ALL SUITES PASS** (직전 220 / 15 스위트 → **+62 / +2**) |
| `npm run build` | `✓ Compiled successfully in 5.6s`, **정적 69/69**, 에러·경고 0 |
| `git diff -- lib/` | **비어 있음** — 엔진 무변경(앵커 추가지 로직 변경 아님)을 변이 실험 후에도 재확인 |
| 티켓 #12 주석 실물 | `sed`로 원문 확인 — 구식 수치 나열 삭제 + 해소 기록·교훈 명문화 확인 |

**화면 검증 없음** — 이번 회차 변경은 테스트 파일 + 마케팅 마크다운뿐이라 **Next 빌드 그래프에 들어가는 파일이 0건**이다
(`app/`·`lib/`·`components/` 무변경). 렌더 결과가 바뀔 수 없으므로 브라우저 실측을 생략했다. 빌드는 그래도 돌려 69/69 확인.

**미확인**: 픽셀 스크린샷 육안 검증 — 무인 실행이라 프레임 합성 불가(**17일째**). 단 위 사유로 이번 회차는 화면 변경 자체가 없다.

---

## 팀 핑퐁 요약 (3건)
1. **마스터 → dev(티켓 #12)**: 인계가 "문서가 틀렸다"였으나 마스터 검증 결과 **주석이 틀림** → 판정 번복 후 dev에 이관 → 주석 해소 완료
2. **dev → 마스터(범위 밖 3건)**: 임의 수정 없이 **보고만** 올림(07-29 표준 ⓧ 준수). 아래 인계로 승계
3. **마스터 → 마스터(표준 ⓨ)**: dev의 변이표를 그대로 신뢰하지 않고 **AL-1을 독립 재현**해 앵커 실효 실증

## CS팀
문의 채널 미연동 그대로(`/support` → `/api/contact` 발신 전용, 조회 가능한 수신함 없음). 읽을 신규 문의가 구조적으로 없어 배정 없음(지어내지 않음).

## 마케팅 게이팅 — **대기 (신규 초안 배정 안 함)**
이번 산출물은 회귀 테스트·주석·원고 정정이라 **홍보 소재가 아니다**(새 화면·기능 0건).
`marketing/card-news/` 초안 **9편이 07-14부터 16일째 사용자 이미지화·게시 승인 대기** — 병목은 생산이 아니라 승인이라 10번째 초안은 미승인 재고만 늘린다.
다만 이번에 **그 미승인 재고 중 1편의 수치 오류를 잡아 고쳤다**(티켓 #15) — 승인 전에 잡아서 다행인 사례.
🔔 **알림 재발송 안 함**(07-26 1회 발송 완료, 이슈당 1회 원칙).

---

## 배포(push)
- **커밋(경로 명시 스테이징, `git add -A` 미사용)**: `scripts/regression/engines.test.ts` · `marketing/promo-drafts.md` · `docs/team-log/2026-07-30.md`
- **제외**: `.claude/settings.local.json`(**🔴 평문 크론 시크릿**) · `.claude/launch.json` · `.claude/settings.json` ·
  `planning/calculator-lineup-expansion.md`(타 세션, mtime 07-25) · `.codex/`·`ebook/`·`trading/`(타 프로젝트)
- ⚠️ `marketing/promo-drafts.md`에는 **타 세션이 남긴 07-22 주석 1줄이 함께 커밋된다** — 같은 파일을 내가 수정해야 해서 분리가 불가능했다.
  내용은 카드뉴스 비주얼 템플릿 확정 기록으로 무해하고 5일 이상 방치돼 있던 것이라 포함이 오히려 정상화다.
- `.env*` 포함 **0건** 확인

---

## 다음 실행 인계

1. **[★티켓 #11 — 진행 중, 결과 회수 필요]** 블로그 36편 본문 수치 전수 감사를 qa-team에 배정했고 **이 회차 종료 시점에 아직 실행 중**이다.
   결과 리포트 예정 경로: `docs/team-log/2026-07-30-blog-audit.md`. **다음 실행은 이 파일 존재 여부를 먼저 확인**하고,
   있으면 그 결함 목록부터 dev에 배정할 것. 없으면 재배정. 알려진 미수정 결함 1건: `lib/blog.ts:2435` `≈ 8,812,388` → 실측 **8,812,380**.
2. **[티켓 #16 — 신규, dev 보고]** **주휴수당·연차수당 입력 상한 정책이 미해결인 채 라이브 중.**
   `planning/weekly-holiday-allowance-design.md` §7-4가 "비현실적 값(예: 소정근로시간 200) 입력 시 에러/클램프 정책은 **개발이 정해 회신 바람**"으로
   디자인→개발 핑퐁을 걸어 뒀는데 **회신 흔적이 없고 엔진에도 상한이 없다.** 현재 `weeklyHours: 200`이 정상 통과하고
   실질시급 10,733원·월 예상급여 약 900만원이 그대로 출력된다. 연차 `unusedDays`도 상한이 없어 500일이 통과한다.
   dev가 **정책 결정 사안이라 임의 clamp/에러를 넣지 않고 보고만** 했다(옳은 판단). → **기획/디자인 회신 필요.**
3. **[티켓 #17 — 신규, dev 보고]** `lib/annual-leave-allowance.ts`의 `UNDER_ONE_YEAR_MAX_DAYS = 11`이 **바인딩되지 않는 방어 코드**.
   `serviceYears < 1` 분기에서 `Math.floor(serviceYears*12)`는 구조상 최대 11이라 `Math.min(months, 11)`이 클램프로 동작하는 경우가 없다.
   무해하나 정리 대상. **회귀 보호는 됨**(상수를 10으로 낮추는 변이는 앵커가 잡는다).
4. **[티켓 #18 — 신규, dev 보고]** **테스트 하네스 한계**: `suite()`가 전체를 하나의 try/catch로 감싸서
   한 assertion이 실패하면 **그 스위트의 나머지가 평가되지 않는다.** 결함 검출엔 지장 없으나
   "몇 개 앵커가 잡았는지"는 이 하네스로 측정 불가 — 변이표를 읽을 때 이 점을 감안할 것.
5. **[티켓 #7 — 유지]** `planning/title-optimization-spec.md` 구현 미착수. planning이 Bash 없이 **수기 계수**했으니
   QA가 제안 6건 문자수를 ±0자로 재검산해야 한다(차단 항목). 블로그 title 여유 1자(최장 59/60), 신규 제목 51자 이하 가드레일.
6. **[★스케줄 이상 — 신규 관측]** 위 "실행 시간 15.8시간" 참조. 오늘 standup이 **1회만** 돌았고 GSC 감사는 **0회**다.
   3일 연속 어긋나던 실행 빈도 문제가 이번엔 **단일 초장시간 실행**으로 나타났다. 다음 실행이 같은 패턴이면 사용자 알림을 고려할 것.
7. **[★사용자 결정 대기 — 재발송 금지]** 근속기간 (A) 반복차용 vs (B-2) 민법 §160③ 시맨틱(07-27 알림 1회).
   결정 전까지 `planning/service-period-boundary-fix-spec.md` 구현 금지.
8. **[★사용자 확인 필요 — 재발송 금지]** 공개 저장소 `.claude/settings.local.json` 평문 크론 시크릿(07-27 알림 1회).
   **사용자 조치 전까지 어떤 실행도 `git add -A` 금지** — 지시서 §8 문구를 무시하고 경로 명시 스테이징을 쓸 것. 오늘도 그렇게 했다.
   부가 사실: 같은 파일에 `"defaultMode": "bypassPermissions"`가 07-29에 들어갔다(이 세션이 만든 변경 아님).
9. **[★사용자 확인 필요 — 재발송 금지]** GSC API 자격증명 부재(07-26 알림 1회, 15일째).
10. **[운영 표준 — 신규 ⓩ]** **주석·인계 문서의 "사실 주장"에는 유통기한이 있다.**
    티켓 #12에서 주석이 낡아 두 번의 인계가 틀린 지시를 전달했다. **인계받은 "X가 틀렸다"는 주장은 X를 직접 열어 확인한 뒤 착수할 것.**
    특히 "문서를 엔진값으로 고쳐라" 류는 고치기 전에 대조부터 해라 — 맞는 값을 틀리게 되돌릴 수 있다.
11. **[운영 표준 — 유지]** 앵커는 값이 아니라 **결함을 잡는지**로 검증(ⓨ, 이번에 AL-1로 독립 재현).
    "이 값만 바뀐다"는 마스터 지시를 팀이 반증하면 지시가 틀린 것(ⓧ). 주석 수치는 실행값만(ⓥ).
    스크래치는 **Write + `node`/`npx tsx`**(Bash heredoc 금지). `git -C` 사용, `cd`+`git` 조합 금지.
    **변이 테스트는 저장소가 아니라 scratch 사본에서** — `lib`+`tsconfig.json`을 함께 복사해야 `@/` 별칭이 해결된다(이번에 실제로 걸렸다).
12. **[백로그 — 유지]** og:image 2단계 · 헤더 터치타깃 36px · 칩 `focus-visible` 부재 · 다크모드 부재 · AdSense 심사 대기 ·
    카드뉴스 9편 승인 대기(16일째) · `/blog` 목록 `ƒ (Dynamic)` · #16 취득세(/loan 확정, 착수 가능) ·
    `age-calculator` `<input max>` 빌드시각 고정 · 티켓 #5 `.claude/worktrees/` 스테일 · 티켓 #8 JSON-LD 썩는 수치 3건 ·
    **픽셀 스크린샷 육안 검증(무인 실행 불가, 17일째)**.

## 제약 준수
확정 컨셉/스택/카테고리 구조 임의 변경 없음. Vercel CLI 직접 배포 안 함(git push만). **`git add -A` 미사용.**
타 세션 워킹트리 무접촉(단 promo-drafts.md는 위 사유로 07-22 주석 동반 커밋). 엔진 파일(`lib/`) 무접촉 — 변이 실험 후 재확인.
이미지/영상 생성 도구 미호출.

---

## 17:1x 실행분(같은 회차 연속) — 티켓 #11 종결 + 신규 티켓 #19·#20

> 앞 구간에서 배정한 qa-team 전수 감사가 이 시점에 회수됐다. 결과가 커서 같은 회차 안에서 이어 처리했다.

## 1) ★티켓 #11 감사 결과 — 결함 3건, **전부 라이브 노출 중**

QA가 **36/36편 · 숫자 포함 1,306줄 · 토큰 출현 5,545회(고유 860종) · 엔진 16개 350여 회 호출**로 전수 감사(샘플링 아님).

| | slug | 결함 | 정답 |
| --- | --- | --- | --- |
| 🔴 F-1 | electricity-usage-marginal-cost-2026 | 절감액에 **다른 구간 값**을 붙임(36,200 = 600→500 구간) | **37,420** |
| 🟡 F-2 | severance-pay-guide-2026 | 등식 좌변이 우변을 못 만듦 | 우변 8,812,388은 정답, **좌변**이 결함 |
| 🟡 F-3 | loan-prepayment-break-even-2026 | 표시용 반올림 비율 66.7%를 등식에 사용(±234원) | 분수형 `24 ÷ 36` |

### ★★ 인계문이 또 틀렸다 (오늘 두 번째)
인계 #1은 F-2를 *"`≈ 8,812,388원`(실측 8,812,380)"* 이라 적었다. **거꾸로였다.**
QA가 특정한 바: **8,812,380은 결함 있는 구 좌변을 축자 계산한 값**이다(`97,826 × 30 × 1,096 ÷ 365 = 8,812,380.49`).
엔진 실측은 **8,812,388**이고(날짜쌍 6개 전수 탐색 → 값 단일), 바로 다음 줄 `L2440`이 *"8,812,388원이 더 정확한 값"* 이라고 명시한다.
→ **인계대로 우변을 8,812,380으로 고쳤으면 그게 진짜 회귀였다.** 티켓 #12와 정확히 같은 구조가 하루에 두 번 나왔다(운영 표준 ⓩ).

## 2) 마스터 직접 수정 (dev 세션 미가동 — 07-29 선례 승계)

`lib/blog.ts` **3줄**. 각 패턴이 정확히 1건 매치됨을 확인 후 치환.
- L5671 `36,200 → 37,420` (라벨 유지, 수치 정정 — 같은 글 L5521 데이터표와 정합)
- L2435 좌변 `97,826원 × 30` → **`9,000,000 ÷ 92 × 30`** (**우변 8,812,388 무접촉**). 07-29 L1115와 동일 처방
- L4116 `× 66.7% = 466,666원` → **`× 24 ÷ 36 = 466,666원(원 미만 절사)`**

## 3) QA 독립 재검증 — **조건부 PASS**

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| A 결함 3건 해소 | **PASS** | 엔진 실행 대조 3/3. electricity 데이터표 6행 전수 재검산 통과 |
| B ★되돌림 사고 | **PASS** | 사고 0. 인계문 오류를 QA가 독립 확인, 구 좌변 오차 −8원까지 특정 |
| C 범위 이탈 | **PASS** | 라인 diff·토큰 멀티셋 **독립 재현** — 예상 밖 변동 0, `8,812,388`·`466,666` 불변 |
| D 새 등식 성립 | **PASS** | 엔진이 `Math.floor`임을 소스로 확인 → 466,666 정당 |
| E 게이트 4종 | **PASS** | tsc/eslint/test 282/build 69p 전부 exit 0 |
| **F 패턴 잔존 스윕** | 🔴 **FAIL 3건** | 아래 |

## 4) ★F 스윕 FAIL — 내가 만든 결함 1건 포함, 즉시 처리

QA가 스윕 범위를 **제품 UI까지** 넓혀서 잡아냈다. 이게 이번 회차 최대 수확이다.

- 🔴 **FAIL-1 → 티켓 #19 신설**: `components/LoanPrepaymentFeeCalculator.tsx` L51-54·L542-546.
  `formatRatio`가 `toFixed(1)` 표시값을 **등식 좌변에 그대로 꽂고** 우변은 전정밀도 엔진값.
  블로그 F-3과 **문자 그대로 동일한 결함**인데 노출도는 계산기 결과화면이 더 높다. 비면제 입력 거의 전부에서 등식이 깨진다(최대 234원).
  **범위 밖이라 이번에 고치지 않고 티켓으로 분리**했다.
- 🟠 **FAIL-2 → 티켓 #20 신설**: 잔존비율 자릿수 표준 부재(형제 글 2자리 / 제품 UI 1자리 / SET B 1자리). 기획팀 확정 필요.
- 🟡 **FAIL-3 → 즉시 되돌림**: **내 수정 #4(L4159 `66.7% → 66.67%`)가 만든 결함이다.**
  등식이 아니라 산술오류가 아니었는데(QA 최초 감사도 PASS 판정) 형제 글과 맞추려다
  **같은 리스트(83.3/50.0/33.3/16.7) 안에서 그 줄만 튀게** 만들었다. 제품 UI도 1자리라 되돌리는 쪽이 화면과도 맞는다.
  → **원상 복구.** 표기 표준(#20)이 정해지기 전에 한 줄만 손대지 않는다.
  복구 후 실측: `83.3% / 66.7% / 50.0% / 33.3% / 16.7%` — 리스트 일관성 회복.

## 5) 마스터 §7 최종 검증 (되돌림 반영 후 재실행)

| 검증 | 결과 |
| --- | --- |
| 변경 라인 | **3줄**(L2435·L4116·L5671) — L4159는 HEAD로 복구돼 diff에서 사라짐 |
| 토큰 멀티셋 대조 | 변동 9종 전부 3줄로 설명됨, **예상 밖 0종**. `8,812,388` 3→3 · `466,666` 18→18 **불변** |
| `tsc` / `eslint` | 무출력(exit 0) |
| `npm test` | **282 assertions · ALL SUITES PASS** |
| `npm run build` | `✓ Compiled successfully`, 정적 **69/69** |
| 빌드 산출물 문자열 | 구 3종 **0건** / 신 3종 **각 9건** 렌더 확인 |

**미확인**: 픽셀 육안·모바일 375px 실측 — 무인 실행(17일째). QA 지적: L4116이 원문보다 길어져 모바일 줄바꿈이 1줄 늘 수 있으나 **실측 안 함**.

## 6) 팀 핑퐁 (이 구간 3건)
1. **QA → 마스터**: 인계문 F-2 전제가 뒤집혔음을 지적 → 마스터 독립 확인 후 **인계문 폐기**, 좌변만 수정
2. **QA → 마스터(FAIL-3)**: 내 수정이 만든 리스트 불일치 반려 → **즉시 되돌림**
3. **QA → dev/기획(FAIL-1·2)**: 제품 UI 잔존 결함 → **티켓 #19·#20으로 분리**(범위 밖 임의 수정 안 함)

## 7) ★QA가 명시한 미검사 영역 — 다음 회차 필독
- **법령 원문 대조 0건** (근로기준법 18③·55·60·61, 만 나이 통일법 시행일 등 인용 진위 미검증)
- **외부 요율표 대조 0건** — 한전 단가·4대보험 요율·이자소득세율·최저시급 10,320·자동차세액표는
  **`lib/*.ts` 상수와 일치함만** 확인했다. **상수 자체가 2026년 실제 고시와 맞는지는 미검증**이다.
  🔴 **상수가 틀리면 블로그와 계산기가 똑같이 틀린 채 "정합"이라 이 감사로는 절대 안 잡힌다.** 별도 축의 검증이 필요하다.
- 등식 자동 스윕이 괄호 중첩식·혼합식은 파서가 스킵(blog.ts 기준 스킵 0건이나 커버리지 100% 아님)

## 8) 배포
- 커밋: `lib/blog.ts` · `docs/team-log/2026-07-30.md` (경로 명시 스테이징)
- QA 조건부 PASS의 조건(FAIL 3건)을 **전부 처리**했다: FAIL-3 되돌림 완료, FAIL-1·2는 티켓 분리.

## 9) 인계 추가 (앞 구간 인계에 더함)
- **[🔴 티켓 #19 최우선]** `LoanPrepaymentFeeCalculator.tsx` 등식 결함. 라이브·HIGH. 블로그보다 노출도 높다.
- **[티켓 #20]** 잔존비율 자릿수 표준 확정(기획) → 확정 후 블로그·UI 일괄 적용. **확정 전 개별 줄 수정 금지**(오늘 그래서 되돌렸다).
- **[운영 표준 — 신규 ㉮]** **같은 결함 클래스는 `lib/` + `components/` 를 함께 스윕해라.**
  07-29에 L1115만 고치고 형제 라인을 안 훑어 F-2·F-3이 11일 더 살았고, 이번엔 블로그 안은 훑었지만 제품 코드까지 안 넓혀 티켓 #19가 남을 뻔했다.
- **[운영 표준 — 신규 ㉯]** **일관성 목적의 "겸사겸사 수정"은 표준이 확정된 뒤에만 해라.**
  오늘 FAIL-3이 정확히 그 사고다 — 결함도 아닌 줄을 형제 글에 맞추려다 자기 리스트를 깨뜨렸다.
- **[다음 감사 축]** 외부 고시값 ↔ `lib/` 상수 대조(위 §7). 지금까지의 감사는 전부 "내부 정합"만 봤다.
