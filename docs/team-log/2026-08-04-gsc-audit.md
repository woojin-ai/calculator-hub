# GSC 일일 점검 - 2026-08-04 08:00

## 결론

GSC **API** 점검은 **20일 연속 미진행**(자격증명 부재, 07-26 알림 1회 발송 완료 → 재발송 안 함). 대체 점검인 라이브 66 URL 전수 온페이지 감사에서 **회귀 0건**을 확인했다. 오늘의 핵심은 셋이다.

1. **★티켓 #23을 착수하러 갔다가 그보다 큰 P0을 발견해 우선순위를 뒤집었다.** 오늘 새벽 다른 세션이 push한 `a7918df`(티켓 #30, 장기요양 승수 정정)가 **`lib/blog.ts`를 빠뜨린 채 종료**해, 계산기와 블로그가 **같은 항목에 서로 다른 금액**을 말하는 상태였다(§3). 신규 티켓 #31로 잡아 종결했다.
2. **★개발팀이 내 브리프의 누락을 잡았다.** 내가 전수라고 믿고 넘긴 수정 목록에서 **9줄이 빠져 있었고**, 개발팀이 *내가 준 정본 표 자신*을 근거로 반증했다(§5-1).
3. **마케팅팀이 티켓 #23의 명분을 1차 출처로 재판정했다** — 어제와 달리 축자 인용이 실제로 나왔으나, **애드센스 명분은 정책 원문 대조로 철회**됐다(§4).

## 0. 사전 조건 (재검증 근거)

- **배포/저장소 정상**: `git fetch` 후 `HEAD == origin/main`(0 ahead / 0 behind). 라이브 `sitemap.xml` 200(10,608 B, **62 URL**), `robots.txt` 200(차단 규칙 0건), `ads.txt` 200.
- **GSC API 자격증명 없음(20일째)**: `.env.local` 키 6개는 전부 메일 발송·Instagram/Meta 연동 계열 — **Google/GSC 키 0개**(값 미열람, 키 이름만 확인. 공개 저장소이므로 변수명 자체는 옮겨 적지 않는다). `app`·`lib`·`scripts`에 `googleapis`/Search Console 참조 **0건**.
- **정본 URL은 기억이 아니라 `lib/site.ts`에서 읽었다**(08-02 표준 ㉱): `SITE_URL = https://calculator-hub-delta.vercel.app`.
- ⚠️ 무인 안전수칙 준수: `cd`+`git` 연결 호출 없음, 전부 `git -C` 또는 분리 호출.

## 1. GSC API 점검 결과

| 항목 | 결과 |
| --- | --- |
| 색인 현황(Coverage) | **미확인** — API 자격증명 부재(20일째) |
| 모바일 사용성(GSC 리포트) | **미확인** — §6에서 실측 대체. 단 이 리포트는 **2023-12-01 구글이 폐지**했다 |
| 노출/클릭 | **미확인** |

**알림 재발송 안 함** — 07-26에 1회 발송 완료.

크롤 입력은 전수 확인했다: `robots.txt` 차단 0건, sitemap 등록·라이브 200, `noindex`는 `/links` 1건뿐(의도된 것), `ads.txt` 200. **색인을 막는 요소는 없다.**

## 2. 라이브 전수 온페이지 감사 (66 URL) — 회귀 0

모집단은 정본대로 sitemap 62 ∪ `/links` ∪ `/blog?page=2,3,4` = **66**.

HTTP 200 **66/66** · canonical 정확히 1개 66/66 · og:url↔canonical 불일치 **0** · `<title>` 고유 66/66(중복 0, >60자 0, max **59**/min 12) · meta description 고유 66/66(>160자 0, max **159**/min 53) · `<h1>` 1개 66/66 · viewport 66/66 · `lang="ko"` 66/66 · **og 6종 66/66** · og:title↔title / og:desc↔desc 불일치 **0/0** · twitter:card 66/66 · JSON-LD 파싱 실패 **0**.

구조화 데이터: `WebSite`·`Organization` 66/66, `BreadcrumbList` **52**, `WebApplication` 16, `FAQPage` 16, `BlogPosting` 36, `CollectionPage`+`ItemList` 4.

- **title max 59 / desc max 159는 07-28·07-29·08-01·08-02에 이어 5일 연속 동일값** — 교차검증됐다.

### 2-1. ⚠️ 내 탐지기가 먼저 거짓 경보를 냈다 (기록 의무)

1차 측정에서 **desc 3건이 >160자(max 177)** 로 잡혔다. 08-02가 0건/max 159였으므로 회귀로 보였다. 원인은 사이트가 아니라 **내 디코더**였다 — `&#x27;`(6바이트 = 1글자)를 디코드하지 않아 아포스트로피 1개당 5자씩 과다 계상했다.

| 페이지 | 원시 | 디코드 후 |
| --- | --- | --- |
| `/blog/service-period-guide-2026` | 177 | **147** |
| `/blog/severance-pay-guide-2026` | 164 | **154** |
| `/blog/loan-prepayment-break-even-2026` | 161 | **151** |

디코더를 고친 뒤 **>160자 0건, max 159**로 08-02와 일치했다. **표준 ㉲ 적용**: 161자 문자열을 주입해 탐지기가 발화하는 것을 먼저 증명한 뒤 0건을 받았다.

### 2-2. ⚠️ 두 번째 자기 정정 — 링크 스코프

1차 측정은 페이지 전체를 세어 **외부 링크 402개 · `/about` 66개**로 나왔고, 이는 "블로그 본문 외부 출처 0개"라는 티켓 #23의 전제를 반증하는 것처럼 보였다. `<article>` 내부로 좁혀 재측정하니:

| 실측(`<article>` 스코프) | 값 |
| --- | --- |
| 블로그 36장 외부 출처 링크 | **0** |
| 블로그 36장 `/about` 링크 | **0** |
| 블로그 36장 `<img>` | **0** |
| 블로그 36장 `<table>` | **2** (08-02 전환분) |

08-02 실측과 정확히 일치한다. 402개의 정체는 형제 사이트 링크 3종(데스크톱·모바일 메뉴 중복 렌더) 6개 × 66페이지였다.

**단, 새 사실 하나**: **푸터가 66/66 페이지에 `/about`을 이미 걸고 있다.** 구글이 요구한 "About 페이지 연결"은 **사이트 수준에서 이미 충족**돼 있고, 없는 것은 *본문 안에서의* 연결이다. 08-02 §3-2의 "`/about` 링크 0개"는 본문 스코프에서만 참이다 — 이 구분을 §7 판정에 반영했다.

## 3. 🔴 티켓 #31 (신규 P0) — 계산기와 블로그가 서로 다른 금액을 말하고 있었다

### 3-1. 발견 경위

기획팀이 티켓 #23 기획서에서 **"A3 오염 4편에 지금 출처 링크를 달면 '보건복지부 고시 0.9448%'를 가리키는 링크 옆에 그 고시와 어긋나는 숫자가 놓인다"** 고 경고하며 최상위 후보 4편을 스스로 제외했다. 그 전제를 확인하러 갔더니 **경고보다 상황이 나빴다.**

- 오늘 08:18 다른 세션(`Daily team standup`)이 `a7918df`를 push — `LONG_TERM_CARE_MULTIPLIER` `0.129457`(근거 불명 오기) → 정수쌍 `9448/71900`(= 13.140473%). **1.48% 과소 계산이었다.**
- 그 커밋이 건드린 파일: `lib/calculators.ts` · `lib/four-insurance.ts` · `lib/salary.ts` · `scripts/regression/engines.test.ts`. **`lib/blog.ts`는 없다.**
- 세션 상태 확인: 해당 세션 `isRunning: false` — **작업을 남기고 종료했다.** 이 결함은 주인이 없었다.

**마스터 판정: 티켓 #23보다 우선한다.** 라이브에서 계산기는 고쳐진 값을, 블로그 4편은 옛 값을 말한다. 같은 사이트가 같은 항목에서 다른 금액을 제시하는 상태이고, 대상은 금융 YMYL이다.

### 3-2. 라이브 확증 — 블로그가 유일한 잔존 노출면

`a7918df`가 실제 라이브에 반영됐는지 먼저 확인했다(반영 안 됐다면 진단이 달라진다).

| 라이브 페이지 | 신 값 | 구 값 |
| --- | --- | --- |
| `/calculator/four-insurance-calculator` | `14,172` O · `13.14` O | `13,961` X · `12.9457` X |
| `/calculator/salary-net-calculator` | `14,801` O · `13.14` O | `14,582` X · `12.9457` X |

계산기 2곳은 신 값으로 완전히 교체됐고 구 값은 0건이다. **블로그가 유일하게 남은 옛 값 노출면**임이 확증됐다.

### 3-3. 정본 값 확보 — 손계산이 아니라 엔진 실행

팀에 넘기기 전에 **마스터가 검산 기준을 먼저 쥐었다.** `npx tsx`로 실제 엔진(`calculateFourInsurance` / `calculateSalary`)을 직접 호출해 정본 표를 생성했다.

4대보험 조견표(`under150`, 근로자 부담):

| 월 보수 | 연금 | 건강 | 요양 | 고용 | 합계 | 비율 |
| --- | --- | --- | --- | --- | --- | --- |
| 200만 | 95,000 | 71,900 | **9,448** | 18,000 | **194,348** | **9.72%** |
| 250만 | 118,750 | 89,875 | **11,810** | 22,500 | **242,935** | 9.72% |
| 300만 | 142,500 | 107,850 | **14,172** | 27,000 | **291,522** | 9.72% |
| 350만 | 166,250 | 125,825 | **16,534** | 31,500 | **340,109** | 9.72% |
| 400만 | 190,000 | 143,800 | **18,896** | 36,000 | **388,696** | 9.72% |
| 500만 | 237,500 | 179,750 | **23,620** | 45,000 | **485,870** | 9.72% |
| 659만(상한) | 313,025 | 236,910 | **31,131** | 59,310 | **640,376** | 9.72% |
| 700만(초과) | 313,025 | 251,650 | **33,068** | 63,000 | **660,743** | **9.44%** |

**부담률이 9.71% → 9.72%로 바뀌면서 `"9.71%"` 14곳이 연쇄**했고, 그중 하나는 `four-insurance-by-income-2026`의 **meta description**이었다.

### 3-4. 결함이 두 종류였다 — 귀속을 분리한다

연봉 예시를 대조하다 **세액도 본문과 엔진이 불일치**함을 발견했다(본문 105,888 / 엔진 105,856 등).

`a7918df`의 `lib/salary.ts` diff를 직접 열어 확인한 결과 **세액 로직 변경 0건**이다. 즉 이것은 A3 파생이 아니라 **그 이전부터 있던 별개의 선재 드리프트**다. 같은 리스트 안에서 일부만 맞추면 08-02 표준 ㉳("절반만 하지 마라") 위반이므로 함께 정정하되, **원인은 분리해 기록한다.**

> ⚠️ **한계 명시**: 오늘 한 것은 "블로그를 엔진에 맞춘 것"이지 "블로그를 국세청 기준에 맞춘 것"이 아니다. 엔진의 **근로소득세 간이세액표 근사식 자체는 아직 외부 대조된 적이 없다**(08-04 상수감사가 "동일 결함 클래스에 가장 크게 노출된 영역"으로 지목하고 Batch B/C 최우선 후보로 제안한 바로 그것). 세액 값이 참이라고 주장하지 않는다 — 사이트 내부 모순을 제거했을 뿐이다.

## 4. 티켓 #23 명분 재판정 (마케팅팀) — 근거는 나왔고, 애드센스 명분은 철회됐다

어제 이 팀은 '표' 주장을 1차 출처 0건으로 스스로 철회했고, `*.vercel.app` 가설은 구글 문서로 정면 반증됐다. 오늘은 **축자 인용이 실제로 나왔다.**

| 주장 | 판정 | 강도 |
| --- | --- | --- |
| 외부 출처 링크 | **(a) 구글 직접 문장 존재** | 中上 |
| YMYL 가중 | **(a) 직접 명시** | **上** |
| 저자/운영주체 노출 | (a) 언급되나 **전부 자가진단 의문문** | 中 |
| **애드센스 심사 유리** | **(c) 1차 출처 0건** | **0** |
| **랭킹 상승** | **부분 반증 문장 존재** | **음(−)** |

핵심 인용(Search Central, *Link best practices — External links*):

> "using external links can help establish trustworthiness (for example, citing your sources)"

정확히 읽어야 할 것: 목적어가 `trustworthiness`이고 `ranking`이 **아니다**. `can help`이지 `will improve`가 아니다. `when it makes sense`가 붙어 있어 **일괄 삽입을 지지하지 않는다.**

반대 방향 문장도 같은 문서에 있다: `"While E-E-A-T itself isn't a specific ranking factor…"` — "E-E-A-T가 올라 순위가 오른다"는 서술은 **구글 문서로 지지되지 않는다.**

### 4-1. 애드센스 명분 철회 (확정)

팀이 정책 원문 2종(`answer/48182`, `answer/10502938`)을 단어 단위로 확인: `author` · `byline` · `attribution` · `citation` · `expertise` · `About page` · `contact` **전부 부재**. `sources`는 단 1회, `"traffic from certain sources"`라는 무관한 문맥.

→ **티켓 사유서에 "재심사 통과를 위해"라고 쓰지 않는다. "YMYL 주제 신뢰 신호 정비"로만 쓴다.**

### 4-2. ★검색 함정 — 팀 전체 공유 (재발 방지)

`site:support.google.com`으로 검색하면 `/thread/`·`/community-guide/` 결과가 상위에 뜬다. **도메인이 support.google.com이라 1차 출처처럼 보이지만 이용자·커뮤니티 기여자가 쓴 글이다.** 이번에도 거기서 "애드센스가 E-E-A-T와 YMYL 기준으로 심사한다"는 진술이 나왔고, 정책 원문에는 그 단어들이 아예 없었다. **어제 '표' 주장이 만들어진 것과 정확히 같은 경로다.**

### 4-3. ★팀이 자기 근거를 스스로 배제했다

Search Quality Rater Guidelines PDF는 렌더 실패했고, 요약본 응답이 `E-E-A-T`를 **Experience가 빠진 3글자**로 설명해 현행판과 불일치했다. 팀은 인출 계층의 신뢰도를 의심해 **SQRG 인용을 전부 근거에서 제외**했다. 확보한 인용을 스스로 버린 판단이 옳다.

### 4-4. 내 기존 인식 2건이 정정됐다

- `/about`에 「운영진 소개」가 **이미 있다**(`app/about/page.tsx:133`). 개선 델타는 "없는 걸 만든다"가 아니라 "본문에서 그 페이지로 가는 경로를 만든다"다. §2-2의 푸터 실측과 맞물린다.
- `lib/blog-jsonld.ts:39`가 이미 `author: publisher // 개인 저자 없음 → 조직을 저자로 (조작 금지)`로 방어돼 있다. **가짜 필자 생성 방향은 애초에 막혀 있었다.**

## 5. 팀 핑퐁

1. **planning ↔ marketing 동시 진단** → 기획팀은 스키마·파일럿 선정, 마케팅팀은 명분 검증. **마케팅팀이 애드센스 명분을 철회**(§4-1).
2. **★기획 → 마스터 우선순위 역전** — 기획팀의 "A3 오염" 경고를 확인하러 갔다가 그것이 티켓 #23보다 큰 P0임을 발견, **순서를 뒤집었다**(§3).
3. **★dev → 마스터 브리프 반증** — 내 수정 목록의 누락 9줄(§5-1).
4. **마스터 → 기획 확인 요청 5건 판정**(§7).

### 5-1. ★개발팀이 내 브리프의 누락을 잡았다

나는 옛 값 26개 패턴을 grep해 수정 목록을 "전수"라고 믿고 넘겼다. 개발팀 보고:

> `lib/blog.ts` L1390~1398(`salary-net-by-bracket-2026`의 연봉별 실수령액 표 9줄)이 목록에서 빠져 있었다.

**근거가 내가 준 정본 표 자신이었다** — 정본이 연봉 4,000만 월 실수령 `2,912,432`라고 했는데 L1393은 `2,912,616`이었다. 목록 밖에 정본과 직접 충돌하는 줄이 있었던 것이다.

**원인**: 내 grep은 "이미 알고 있는 옛 숫자"만 훑었다. 그 표의 값들은 내가 열거한 패턴에 하나도 걸리지 않았다. **알고 있는 것만 찾는 검색은 전수가 아니다.**

목록대로만 고쳤으면 같은 글 안에서 표(L1394)와 상세 분해(L1428~1431)가 어긋나 **한 글이 자기모순**에 빠졌을 것이다. 개발팀은 누락된 7개 값을 창작하지 않고 **엔진을 직접 돌려** 얻었다.

## 6. 마스터 직접 검증

팀 보고를 복창하지 않고 내가 직접 측정했다.

### 6-1. 네거티브 컨트롤 먼저

일부러 틀린 값(`요양 99,999`)을 대조 목록에 넣어 **탐지기가 FAIL을 내는 것을 먼저 증명**한 뒤 본 검증을 돌렸다(표준 ㉲).

### 6-2. 엔진 재생성 대조

`lib/blog.ts`를 읽어 엔진 출력과 축자 대조: **대조 33건 / 불일치 0건**
(조견표 8줄의 요양·합계 16건 + 연봉별 월 실수령 9건 + 연봉 4,000/5,000만 상세 8건)

### 6-3. 옛 값 잔존 스윕

옛 요양액·합계·세액·실수령액·요율 표기 등 **26개 패턴 → 잔존 0건.**

### 6-4. 변경 범위

`git diff --stat`: `lib/blog.ts` 단독, **46 insertions / 46 deletions**. 추적 파일 중 다른 변경 0건.

## 7. 마스터 판정 — 기획팀 확인 요청 5건 (티켓 #23, 다음 회차 인계)

| # | 사안 | 판정 |
| --- | --- | --- |
| 1 | B-2 행정기본법 라벨 | **조건부 승인** — 본문은 "만 나이 통일법"이라고만 부른다. 라벨에 맨몸으로 "행정기본법"을 던지면 본문에 없는 법령명이 갑자기 등장한다. `note`로 두 이름을 잇는 것을 **필수 조건**으로 건다 |
| 2 | B-3 한전 선대조 | **범위 축소 승인** — 요금표 딥링크 **기각**(우리 단가가 미감사라 A3형 자기반박 재발). 본문 L5728에 **이미 평문으로 있는** 사이버지점 루트 URL만 링크화 → 새 주장 0 |
| 3 | C-1 읽는 시간 포함 | **제외 채택** + 사유를 코드 주석에 축자로. 포함하면 카드값이 약 55페이지에서 흔들리고, 그 파급은 08-02에 개발팀 보고가 실제로 빠뜨린 클래스다 |
| 4 | C-7 URL 중복 노출 | **(가) 평문 유지** — 라이브 문자열 삭제는 08-02에 2건 모두 기각된 클래스 |
| 5 | `aboutLabel` 동시 투입 | **1라운드 제외** — 푸터가 이미 66/66에 `/about`을 걸고 있어 델타가 작다(§2-2). 선택 필드는 나중에 **비파괴로** 붙는다 |

**부수 효과**: 기획팀이 A3 오염을 이유로 제외한 4편은 오늘 #31 종결로 **다음 회차 최상위 후보로 승격**된다 — 원래 "국세청·공단을 산문으로 지칭하는 글"이라는 선정 기준의 1순위였다.

## 8. QA 독립 검증 — FAIL 1건 발견, 수용

QA는 개발팀 보고와 마스터 정본표를 **참조하지 않고** 엔진에서 값을 재생성해 대조했다. 네거티브 컨트롤도 독립적으로 밟았다 — 동일 대조기를 **수정 전 HEAD 버전**에 먼저 돌려 `PASS 62 / FAIL 91`을 받아 탐지기 비공허를 증명한 뒤, 워킹트리에서 `PASS 152 / FAIL 1`을 받았다.

| 항목 | 판정 |
| --- | --- |
| V1 수치 정확성 | **FAIL 1건** |
| V2 거짓 등식 | PASS (경계 관찰 1건) |
| V3 산문↔데이터 정합 | PASS |
| V4 과잉 수정 | PASS (**비수치 토큰 변경 0건**) |
| V5 파급 | PASS (관찰 2건) |
| V6 게이트 | PASS |

### 8-1. ★FAIL-1 수용 — 마스터가 직접 정정

`lib/blog.ts:1397` — `연 환산 약 6,485만원`이 정본과 어긋났다.

**내가 직접 재검산한 근거**:

| 연봉 | 연환산 실수령 | 만원 환산 | 반올림 | 본문 |
| --- | --- | --- | --- | --- |
| 8,000만 | 64,844,268 | 6484.4268만 | **6,484만** | ~~6,485만~~ |

같은 리스트 **9행 중 8행은 반올림 규칙을 정확히 따른다**(2161.86→2162, 4264.50→4265, 7904.50→7905). 이 행만 이탈했고, 원인은 구값 `5,404,022원`(= 6,484.83만 → 6,485만)에서 나온 **잔존값**이다. 개발팀이 원 단위는 고치고 만원 반올림 표기를 따라 고치지 않았다.

**마스터가 직접 1토큰 정정**(개발팀 왕복 비용이 이득보다 크다). 정정 후 재검증: **대조 42건 / 불일치 0건**(네거티브 컨트롤 발화 확인 후).

### 8-2. QA 전수 완전성 근거

36편 전체를 스캔해 4대보험/실수령 키워드+숫자를 가진 글이 **10편**임을 확인하고, 그중 6편은 금액·요율 없이 계산기 링크 서술만 있어 대상이 아님을 **원문 확인**했다. 나머지 4편이 대상 전부다. 4편의 모든 콤마 숫자를 엔진 파생 허용집합과 대조해 **미설명 수치 0종**.

구값 잔존 검사: `app/`·`components/`·`lib/blog.ts`에서 **37종 전부 0회**.

## 9. 마스터 판정 — QA 판단 요청 2건

### 9-1. `lib/blog.ts:2673` "약 13.14%를 곱해" → **이번 회차 손대지 않는다** (티켓 #33)

QA 지적: 같은 글이 절사를 명시하므로 독자가 지시대로 따르면 `floor(71,900 × 13.14%) = 9,447`이 나와, 17줄 뒤 `9,448`과 **1원 어긋난다.**

**판정: 유효한 지적이나 이번 회차에 고치지 않는다.** 근거는 QA 자신이 §V5에서 낸 실측이다.

- 이 문장은 `four-insurance-guide-2026`에 있고, 이 글은 지금 **5,247자 / 11분 경계 5,250자 — 여유 3자**다.
- 형제 2곳과 같은 정수쌍(`9,448/71,900`)으로 맞추면 **+14자**라 경계를 넘어 읽는 시간이 10분 → 11분이 되고, 그 카드가 렌더되는 **4개 라우트 약 55페이지**의 값이 함께 바뀐다.
- 즉 **1원 표기 정합을 위해 55페이지 파급을 무인 회차에 감수하는 거래**가 된다. 게다가 해당 문장은 이미 `약`으로 근사임을 표시하고 있고 결과값 C를 적지 않아 등식이 아니다(QA도 FAIL 처리하지 않았다).
- 읽는 시간 파급은 **의도적으로 다루는 회차**에서 처리해야 한다.

→ **티켓 #33**으로 분리. 문자수 예산과 함께 처리할 것.

### 9-2. 🔴 원본 스펙 드리프트 → **티켓 #32 (다음 회차 최우선급)**

QA 추가 발견: `planning/*.md` 6개 + `design/*.md` 2개가 **구값 그대로**다.

- `planning/four-insurance-by-income-blog-content.md` L18·44·48·62~68·74·78·86·91·99 — `9,307 / 194,207 / 639,914 / 660,252 / 9.71% / 9.43% / 12.9457%`
- `planning/salary-net-by-bracket-blog-content.md` L55·62 — `1,801,670 / 5,404,022`
- `planning/four-insurance-guide-blog-content.md` · `planning/four-insurance-calculator-content.md` · `planning/salary-net-calculator-content.md` · `planning/ratio-percent-display-rules.md` · `design/four-insurance-calculator-ui-spec.md` · `design/salary-net-calculator-ui-spec.md` — `12.9457%` 등

**위험의 성격이 특수하다**: 지금은 **스펙↔구현이 반대로 어긋나 있다.** 이 프로젝트는 "인계 요약이 아니라 원본 스펙에서 옮겨라"(표준 ㉵)를 표준으로 삼는데, 그 표준을 따르는 다음 실행이 **정정된 코드를 구값으로 되돌리는 회귀**를 일으킨다. 표준이 결함을 증폭시키는 구조다.

**이번 회차에 고치지 않은 이유**: 8개 문서의 수치가 서술형 예제 안에 박혀 있어 문맥을 읽고 고쳐야 한다. 절반만 고치면 **어느 문서가 정본인지 더 불분명해진다**(표준 ㉳ — 절반만 하지 마라). 다음 회차에 전용 라운드로 처리한다.

> 이 드리프트는 `a7918df`(티켓 #30) 세션이 코드만 고치고 문서를 남긴 데서 생겼다. 08-02 표준 ㉰("코드·문서 동시 정본화")가 **세션 경계를 넘어서는 지켜지지 않았다**는 관측이다.

## 10. 마스터 직접 검증 (§3 — 팀 보고를 그대로 믿지 않음)

### 10-1. 정적 검증

- **엔진 재생성 축자 대조 42건 / 불일치 0건** (네거티브 컨트롤 `연 환산 약 9,999만원` 발화 확인 후)
- 옛 값 **26개 패턴 잔존 스윕 → 0건**
- `git diff --stat`: `lib/blog.ts` 단독. 추적 파일 중 다른 변경 0건
- 게이트 **내가 직접 재실행**: `tsc` exit 0 · `eslint` exit 0 · `npm test` **408 assertions ALL PASS** · `next build` **정적 69/69**

### 10-2. 라이브 렌더 + 모바일 실측

로컬 프로덕션 빌드(`next start :3455`, 타 세션 포트 회피)에서 직접 측정했다.

**네거티브 컨트롤 먼저**: 375px에서 폭 2000px div를 주입해 `scrollWidth 375 → 2000`으로 **탐지기 발화를 증명**하고, 제거 후 375 복귀를 확인한 뒤 본 측정에 들어갔다(표준 ㉲).

| 검증 | 375px | 320px |
| --- | --- | --- |
| 문서 가로 스크롤 (`scrollWidth == clientWidth`) | 375/375 | 320/320 — **없음** |
| 신 값 렌더 | `9,448`·`194,348`·`660,743`·`9.72%`·`9.44%` 전부 O | 4편 전부 O |
| 구 값 잔존 | `9,307`·`194,207`·`9.71%`·`9.43%` **전부 X** | 4편 전부 **0건** |

4편 개별 확인(320px, `<article>` 스코프): `four-insurance-by-income` `9,448`·`14,172`·`33,068`·`9.72%`·`13.14%` / `four-insurance-guide` `9,448`·`14,172`·`33,068`·`13.14%` / `salary-net-income-guide` `14,801`·`13.14%` / `salary-net-by-bracket` `18,738`·**`6,484만`**(FAIL-1 정정분) — **구값 잔존 0건**.

## 11. 미확인 (명시)

- **픽셀 육안 검증 — 무인 실행 불가(21일째).** 기하·오버플로·수치 렌더는 전부 수치로 확인했으나 **"보기에 어색하지 않은가"는 확인하지 못했다.** 다만 이번 변경은 숫자 치환뿐이고 레이아웃 구조 변경이 0이라 위험도는 낮다.
- **엔진의 근로소득세 간이세액표 근사식 자체가 미감사.** §3-4 참조. 오늘 한 것은 **사이트 내부 모순 제거**이지 세액이 국세청 기준으로 참임을 확인한 것이 아니다.
- **한전 전기요금 단가·이자소득세 15.4% 등 다른 상수도 미감사.** 08-04 상수감사 Batch A 범위 밖.
- **GSC 색인 반영 여부 확인 불가** — API 부재로 이 변경이 색인에 어떤 영향을 주는지 측정할 수단이 없다.
- **`updatedDate` 미설정**(QA 관찰) — 대상 4편 모두 `publishedDate` 2026-07-15/17이고 `updatedDate`가 없어, 08-04에 금액을 실질 정정했는데 JSON-LD `dateModified`가 7월 발행일로 고정된다. 기존 관행인지 누락인지 **미판정** — 티켓 #34.
- **실물 기기·스크린리더·Safari/iOS 미검증.**

## 12. 사용자 확인이 필요한 블로커

| # | 사안 | 알림 |
| --- | --- | --- |
| GSC | API 자격증명 부재(20일째) | 07-26 발송 — **재발송 금지** |
| #12 | 커스텀 도메인(애드센스 명분은 08-02에 철회됨) | 08-01·08-02 발송 — **재발송 금지** |
| #11 | 스케줄 세션 동시 실행 | 07-29 발송 — **재발송 금지** |
| #6 | 공개 저장소 평문 크론 시크릿 | 07-27 발송 — **재발송 금지** |
| 08-03 전면 불발 | 스케줄 무증상 실패 | 08-04 01:06 발송 시도 — **⚠️ Remote Control 비활성으로 전달 미확인.** 재발송하지 않되 "완료"로 취급하지 않는다 |

**오늘 신규 알림 없음.** 티켓 #31·#32·#33·#34는 전부 팀/마스터 선에서 판정 가능한 사안이라 알림 대상이 아니다.

## 13. 다음 실행 인계

1. **[🔴 티켓 #32 — 최우선]** **원본 스펙 드리프트 8개 문서**(§9-2). 방치하면 표준 ㉵를 따르는 다음 실행이 **정정된 코드를 되돌린다.** 전용 라운드로 일괄 처리.
2. **[티켓 #23 — 판정 완료, 미착수]** 출처 링크. 마스터 판정 5건은 §7에 있다. **A3 오염으로 제외됐던 4편이 오늘 #31 종결로 최상위 후보 승격.** 기획팀 스키마(`sources` 타입) 그대로 사용 가능.
3. **[티켓 #33]** `lib/blog.ts:2673` 1원 표기 정합 — **읽는 시간 문자수 예산과 함께** 처리(§9-1). `four-insurance-guide-2026` 여유 **3자**.
4. **[티켓 #34]** `updatedDate` 정책 판정 — 실질 정정 시 `dateModified` 갱신 여부.
5. **[★표준 — 신규 ㋄]** **알고 있는 것만 찾는 검색은 전수가 아니다.** 오늘 나는 "옛 값 26개 패턴"을 grep해 수정 목록을 전수라고 믿었으나 **9줄이 빠졌다**(§5-1). 그 줄들의 값은 내가 열거한 패턴에 하나도 걸리지 않았다. **정본을 생성해 대조하는 방식**(엔진 재생성)만이 전수다 — 옛 값 검색은 보조 수단이다.
6. **[★표준 — 신규 ㋅]** **한 세션이 상수를 고치면 그 상수의 파생 표기 전부가 같은 회차의 범위다.** `a7918df`는 코드를 고치고 블로그·스펙 문서를 남겼다. 남겨진 것이 **다음 세션에서 주인 없는 결함**이 됐고, 하필 그 사이 사이트는 **계산기와 블로그가 다른 금액을 말하는 상태**였다. 상수 변경 시 `grep`으로 파생 표기 소재를 **먼저 전수 조사**하고, 그 회차에 못 끝내면 **명시적으로 티켓을 남길 것**.
7. **[★표준 — 신규 ㋆]** **다른 세션의 커밋을 "완료"로 읽지 마라.** 오늘 나는 `a7918df`의 커밋 메시지가 아니라 **커밋이 건드린 파일 목록**과 **세션 실행 상태**(`isRunning: false`)를 보고 미완을 발견했다. 커밋 메시지는 의도를 적지만 범위를 보증하지 않는다.
8. **[★표준 — 유지 ㉲ 강화]** 오늘 네거티브 컨트롤이 **3회 독립적으로** 값을 했다(마스터 desc 탐지기 / 마스터 수치 대조기 / QA가 HEAD에 돌린 대조기). 특히 QA의 방식 — **수정 전 버전에 같은 검사기를 돌려 FAIL이 나오는지 보는 것** — 이 가장 강력하다. 표준으로 승격.
9. **[백로그 — 유지]** og:image · 다크모드 · 카드뉴스 9편 승인 대기 · `/blog` 목록 `ƒ (Dynamic)` · #16 취득세(/loan 확정) · `age-calculator` `<input max>` 빌드시각 고정 · 터치 타깃 44px 정본화 충돌.
10. **[운영 표준 — 유지]** 한글 문자수는 python `len()`. URL 모집단 66. `git -C` 사용, `cd`+`git` 금지. **`git add -A` 절대 금지**(공개 저장소 평문 크론 시크릿). 타 세션 포트 회피 — 오늘은 3455를 썼고 측정 후 종료했다.

## 14. 배포(push)

- **커밋 대상 2개 파일**(경로 명시 스테이징, **`git add -A` 미사용**): `lib/blog.ts` · `docs/team-log/2026-08-04-gsc-audit.md`
- **제외**: `.claude/settings.local.json`(**🔴 평문 크론 시크릿**) · `.claude/launch.json` · `.claude/settings.json` · `.codex/` · `ebook/` · `trading/`
- 게이트 4종 exit 0(`tsc`/`eslint`/`npm test` 408/`build` 69) + QA PASS(FAIL 1건 정정 후) + 마스터 직접 실측(375·320px) 통과 → **자동 push 조건 충족**(2026-07-13 사용자 승인 범위)

## 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함(git push만). `.env.local` 값 미열람(키 이름만). **`git add -A` 미사용.** 타 세션 워킹트리 무접촉(`lib/blog.ts` 외 수정 0). **본문 수치를 새로 만들지 않았다** — 모든 값은 `calculateSalary`/`calculateFourInsurance` 엔진 출력이며 QA가 독립 재생성으로 대조했다. 어떤 이미지/영상 생성 도구도 호출하지 않았다. 결제 행위 없음.
