# GSC 일일 점검 - 2026-07-22 08:00

## 결론
GSC **API** 점검은 여전히 미진행(자격증명 부재, **7일 연속 동일 블로커**). 대체 점검으로 **라이브 59개 URL 전수 온페이지 감사 + 블로그 도달성(고아 페이지) 검증 + 375px 실뷰포트 계측**을 수행. **신규 결함 0건 · 회귀 0건 · 코드 변경 0건 → 코드 푸시 없음.** 팀 간 핑퐁 불필요(라우팅할 문제 없음). 신규 관찰 1건은 티켓으로 등재(메타 description 길이, 아래 §6-2).

## 0. 사전 조건 (재검증 근거)
- **배포/저장소 정상**: 라이브 sitemap 200, `git fetch` 후 `HEAD == origin/main == e5bd02b` (0 ahead / 0 behind).
- **GSC API 자격증명 여전히 없음**:
  - `.env.local` 실제 내용 = `RESEND_API_KEY`, `CONTACT_TO_EMAIL`, `IG_*`/`META_*` 4종(인스타 게시용). **GSC/구글 관련 키 0개.**
  - 리포 전체 grep(`GSC_|SEARCH_CONSOLE|searchconsole|service_account|GOOGLE_APPLICATION|private_key`, node_modules·.git·docs·.claude 제외) → **히트 0**.
  - 서비스계정 `.json` 부재(루트 json = package/package-lock/tsconfig뿐).
  - → Coverage / 노출·클릭 / 모바일사용성 리포트 3종 **미확인**.
- ⚠️ `layout.tsx`의 `google-site-verification`은 **소유권 확인용이지 API 접근 권한이 아니다**(전일들과 동일 구분, 7일째 유지).
- ⚠️ 무인 안전수칙 준수: git 조회는 전부 `git -C` 형태, `cd`+`git` 연결 없음. 승인 프롬프트 없이 완주.

## 1. GSC API 점검 결과
| 항목 | 결과 |
| --- | --- |
| 색인 현황(Coverage) | **미확인** — API 자격증명 부재 |
| 모바일 사용성(GSC 리포트) | **미확인** — API 자격증명 부재 (실뷰포트는 §4에서 직접 측정) |
| 노출/클릭 | **미확인** — API 자격증명 부재 |

## 2. 07-21 인계 항목 처리
- **[해소] 표준 ⓖ 적용 — 신규 푸시분 라이브 반영 확인**: 07-21 이후 커밋(30~33편 블로그 등)이 라이브에 반영됨. sitemap **56 → 59 URL**(+3), 33편 `weekly-holiday-allowance-extra-hour-2026`가 sitemap·리스팅·상세 모두 정상 출현. → 07-19/07-20의 "푸시 직후 미반영 → 익일 정상" 패턴 **3연속 확정**. 푸시 직후 폴링에 매달리지 않은 판단이 옳았다.
- **[유지] GSC 자격증명 부재** — 7일 연속, 사용자 조치 없이는 해소 불가(§6-1).
- **[유지] 티켓 #2 OG/metadataBase 부재** — 오늘 실측으로 재확인(59/59 `og:` 태그 **0개**). 회귀가 아니라 기 문서화된 오픈 티켓.

## 3. 라이브 전수 온페이지 감사 (59개 URL)

### 방법 — 표준 ⓕ 준수
URL을 손으로 나열하지 않고 **라이브 `sitemap.xml`의 `<loc>`에서 59개를 기계 추출**해 프로브했다. (07-20·07-21 두 번 연속 손 나열로 존재하지 않는 URL을 프로브한 전례가 있어 이번엔 처음부터 단일 출처 사용 → **오늘 마스터 자기 오류 0건**.)

### 결과 — 전 항목 통과
| 신호 | 결과 |
| --- | --- |
| HTTP 상태 | **59/59 = 200** (비정상 0) |
| canonical | **59/59 정확히 1개**, 전부 **self 타깃 일치**(문자단위 대조, 트레일링 슬래시 불일치 0) |
| `<title>` | 59/59 정확히 1개, **59개 전부 고유**(중복 0), 60자 초과 0 |
| meta description | 59/59 존재, **59개 전부 고유**(중복 0), 빈 값 0 |
| `<h1>` | **59/59 정확히 1개** (헤딩위계 정상) |
| robots noindex | 색인대상 **59/59 = 0** (실수 deindex 없음) |
| JSON-LD | **59/59 존재** (블록 1개 이상) |
| viewport meta | **59/59 정확히 1개** |
| `og:` 태그 | **59/59 = 0개** ← 유일한 공백, 기 등재 티켓 #2 |

### ★파싱 오류 1건 (즉시 자가 검출·교정)
1차 집계에서 `|` 구분자로 필드를 분리했는데 **title에 `... | 계산기 허브`가 들어 있어 필드가 밀렸다.** h1 결과 자리에 description이 출력되는 걸 보고 즉시 발견 → 앞 5개·뒤 5개 필드를 위치 고정하고 가운데를 title로 재조립해 재파싱. **원시 응답을 다시 받지 않고 저장된 출력만 재해석**해 재측정 비용 0.
- **교훈(표준 ⓗ 신규)**: 사람이 읽는 텍스트(제목·설명)를 담는 집계는 `|`처럼 콘텐츠에 실제로 등장하는 문자를 구분자로 쓰지 말 것. 그리고 **결과가 자리에 안 맞아 보이면 데이터가 아니라 파서를 먼저 의심**할 것(기존 표준 ⓔ의 확장).

### 인프라 회귀 점검
- `robots.txt` 200, `User-Agent: * / Allow: /` 전체 허용, Sitemap 지시자 정상.
- 404 가드: `/calculator/does-not-exist`, `/blog/does-not-exist`, `/nope` → **전부 404 + noindex 1 + canonical 0** (noindex 페이지에 canonical 안 붙는 설계 유지 — 07-20 §7-5 우려 지점이 정상 동작).

## 4. 신규 점검 — 블로그 도달성(고아 페이지) 검증

sitemap에 있어도 **내부 링크로 도달 불가능하면 색인 품질이 떨어진다**(GSC "발견됨 - 현재 색인 생성되지 않음"의 전형적 원인). 지금까지 감사에서 한 번도 측정하지 않은 표면이라 오늘 신규 추가했다.

- 블로그 리스팅 전 페이지 크롤: `/blog`(9) + `?page=2`(9) + `?page=3`(9) + `?page=4`(6) = **33개 링크**
- sitemap의 블로그 URL = **33개**
- **고아 페이지(sitemap에 있으나 리스팅 미링크): 0건**
- **유령 링크(링크는 있으나 sitemap 미등재): 0건**
- 페이지네이션 경계: `?page=1` → **308 리다이렉트 → `/blog`**(중복 URL 방지, 정상), `?page=5`·`?page=6` → **404**(빈 페이지 양산 없음)
- → **블로그 33편 전량이 크롤러에게 도달 가능. 색인성 구조 결함 없음.**

## 5. 모바일 실뷰포트 직접 검증 (375×812, 마스터 직접)

### 홈
- `horizontalOverflowPx = **0**`, 초과 요소 **0건**
- nav 링크 **7개 전원 visible**, `고객센터` 우측 끝 **325px**(<375) — 07-20(325.2px)·07-21(325px)과 **3일 연속 일치**
- `h1` 1개, canonical 1개

### 33편 블로그 상세 (신규 콘텐츠 — 최초 모바일 검증)
07-21 이후 추가된 콘텐츠라 아직 어떤 감사도 모바일에서 보지 않았던 페이지. 블로그 본문은 **표·코드블록이 모바일 오버플로의 주 원인**이라 표 유무를 함께 계측했다.
- `horizontalOverflowPx = **0**`, 초과 요소 **0건**
- `<table>` **0개** → 표 오버플로 위험 자체가 없음(이 글은 구간 설명을 텍스트+리스트로 구성)
- 헤딩위계 `H1 → H2×7 → H3×3 → H2 → H3×4` — **건너뜀 없음, h1 1개**
- canonical = 자기 자신 정확
- 계산기 내부링크 **6개**(블로그→계산기 동선 정상)
- **콘솔 에러 0건**

### 관찰(결함 아님)
헤더 링크 터치타깃 높이 **36px**(권장 44px 미만), 로고 28px. **기 등재 백로그 항목이며 오늘 신규 발생 아님.** 오버플로·가림 없음이라 기능 결함은 아니다.

### 미확인
- 픽셀 스크린샷: 이전 실행들과 동일하게 `computer` 캡처를 시도하지 않고 **DOM 정량 계측으로 대체**했다. 시각적 깨짐(색 대비·겹침 등 DOM으로 안 잡히는 유형)은 **미확인**.

## 6. 오늘의 GSC 점검 요약
| 항목 | 결과 |
| --- | --- |
| 색인/모바일사용성/노출 (GSC API) | **미확인** — 자격증명 부재 **7일** |
| 온페이지 색인성(대체 점검) | **59/59 전항목 통과** — 200/canonical self/title·desc 전부 고유/h1 1개/noindex 오염 0/JSON-LD 존재 |
| 크롤 도달성(신규) | 블로그 33편 **고아 0건**, 페이지네이션 경계 정상 |
| 모바일 실뷰포트(대체 점검) | 홈·33편 상세 오버플로 **0px**, 초과 요소 0건, 콘솔 에러 0 |
| 인프라 | sitemap 200/59 URL, robots 전체 Allow, 404+noindex 가드 정상 |

## 7. 배포(push)
- **오늘 코드 변경 0건.** 신규 결함 미검출 → 수정 대상 없음 → **코드 커밋·푸시 없음**. (자동 push 승인은 "검증 통과한 수정이 있을 때" 발동하며, 오늘은 수정 자체가 존재하지 않는다.)
- 이 감사 로그 파일만 **경로 명시 스테이징**으로 커밋·푸시.
- **타 세션 산출물 회피**: 워킹트리의 `marketing/card-news/*`(4건 수정), `marketing/promo-drafts.md`, `.claude/settings*.json`·`launch.json`은 **손대지 않았다.** `git add -A` 미사용 — 07-19/07-20 교훈 준수.

## 8. 팀 핑퐁
**발생 없음.** 색인 오류·노출 문제·모바일 사용성 문제가 **모두 미검출**이라 planning/marketing에 진단을 의뢰할 대상이 없었고, 따라서 dev·qa 라우팅도 불필요했다. (문제가 없는데 팀을 호출하는 것은 절차 소모일 뿐이므로 의도적으로 생략했다.)

## 9. 미확인 / 다음 실행 인계
1. **[★사용자 확인 필요 — 유지, 7일 연속]** **GSC API 자격증명 부재.** 서비스계정 `.json` 배치 또는 OAuth 토큰 env 설정이 없으면 Coverage/노출·클릭/모바일사용성 3종은 **영구 미확인**. 이것이 매일 감사의 **최대 구조적 공백**이며, 대체 점검(온페이지·도달성·실뷰포트)은 "우리 쪽 결함 없음"까지만 증명할 뿐 **구글이 실제로 어떻게 색인했는지는 알 수 없다.** 사용자 조치 없이는 다음 실행도 동일하게 막힌다.
2. **[신규 티켓 #3 — 낮음]** **meta description 길이 편차.** 59개 전부 고유·존재하나: **9개 블로그 글이 160자 초과**(최대 206자 `loan-prepayment-break-even-2026`, 200자 `service-period-guide-2026`) → SERP에서 잘림. 반대로 **16개 페이지가 70자 미만**(최소 `/blog` 28자, `/calculator/dday-calculator`·`/calculator/bmi-calculator` 26자) → 스니펫 공간 낭비.
   - **결함이 아니라 카피 품질 이슈**로 판단해 오늘 직접 수정하지 않았다. description은 마케팅/기획 소관 콘텐츠이고, 9~16개 카피를 감사 실행이 단독으로 재작성하는 것은 범위를 넘는다(제약: 확정된 구조·컨셉 임의 변경 금지).
   - **권고**: planning/marketing 정규 슬롯에서 처리. 특히 **짧은 쪽(계산기 상세 26~35자)이 개선 여지가 크다** — 클릭률에 직접 영향.
3. **[티켓 #2 — 유지]** **OG/metadataBase 전면 부재** (오늘 59/59 `og:` 0개 실측 재확인). 카드뉴스 CTA가 전부 "프로필 링크" 의존이라 링크 언펄 시 썸네일·제목 카드가 없다. **첫 SNS 게시 승인 전 처리 권장.** og:image 에셋 필요 → design 소관 + 이미지 사용자 승인.
4. **[운영 표준 — 신규 ⓗ]** 집계 스크립트에서 **콘텐츠에 등장하는 문자(`|` 등)를 구분자로 쓰지 말 것.** 오늘 title의 `| 계산기 허브` 때문에 필드가 밀렸다. 그리고 **결과가 자리에 안 맞으면 데이터가 아니라 파서를 먼저 의심**(ⓔ 확장).
5. **[운영 표준 — 오늘 작동 ⓕ·ⓖ]** ⓕ(URL을 손으로 나열하지 말고 단일 출처에서 생성) → 오늘 sitemap 기계 추출로 **2일 연속 반복하던 자기 오류를 원천 차단**. ⓖ(푸시 직후 미반영은 익일 확인으로 이월) → 오늘 3연속 확정 패턴으로 검증됨.
6. **[백로그 — 유지]** canonical 생성 2종 공존(`canonicalUrl()` 헬퍼 vs 블로그 2곳 직접 조립) 드리프트 위험 / `coming-soon` 스텁의 self-canonical+noindex 신호충돌(현재 도달불가, 로드맵 #16 추가 시 발현) / 프리뷰 배포 `x-robots-tag` **미확인** / 프로덕션 Linux 대소문자 slug 동작 **미확인** / 픽셀 스크린샷 기반 시각 검증 **미확인**.
7. **[유지 백로그]** `.claude/settings.local.json` `defaultMode: bypassPermissions`(사용자 확인 필요), 헤더 터치타깃 36px·다크모드 부재, `relatedSlugs` 꼬리 레이아웃, AdSense 심사 대기, 마케팅 게시 승인 대기, 로드맵 #16 취득세(高YMYL 보류).

## 제약 준수
확정된 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. 코드 변경 0건이라 코드 push 대상 없음. `.env*`·시크릿 미포함.
