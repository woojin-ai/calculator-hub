# 2026-07-26 팀 로그 (계산기 허브 / daily-team-standup)

## 01:05 실행분 (오늘 첫 실행)

### 컨텍스트 확인
- `docs/team-log/2026-07-26.md` 부재 → **오늘 첫 실행**. 07-25 로그 하단 "다음 실행 인계" 1번부터 이어받음.
- 라이브 상태: 계산기 17개 / 블로그 **35편**(salary 9 · loan 9 · life 9 · **date 8**) / static 68 / HEAD `4b0d6f4`.
- `git status`: 미커밋 4건(`.claude/launch.json`·`.claude/settings.local.json`·`marketing/promo-drafts.md`·`planning/calculator-lineup-expansion.md`) + 미추적 3건(`.codex/`·`ebook/`·`trading/`). **전부 타 세션/타 프로젝트 산출물** → 07-23·07-25 선례대로 이번 회차 커밋에서 제외. 검증 전 중단된 이 저장소 작업물은 없음(소스 코드 변경 0건).
- 07-26 GSC 감사 로그는 아직 없음(해당 작업은 오전 실행) → 중복 지시 없음.

### 이번 회차 배정
인계 1번 **블로그 36편 — date 카테고리**(단독 최저 8편). 종료 클러스터 4편째 금지(BMI·주휴수당·age·savings). date 잔여 각도: service-period(2)·dday(2)·annual-leave-days(1).

### planning-team → `planning/blog-36-spec.md`
주제 **경력 합산 계산법**(`career-period-sum-2026`, date). 갭 실증: `Grep "경력" lib/blog.ts` → **0건**(35편 통틀어 미등장). 형제 service-period 2편이 **둘 다 "한 회사·한 구간"** 전제라 **여러 구간 합산**이 통째로 비어 있었다. 고유 프레임 4가지 함정: ①`N년 M개월` 합산 불가 ②반올림 소수 누적오차 ③통합계산 공백 과대계상 ④퇴사일=입사일 겹침 이중계상. 형제글 원리(+1·윤년·만근일)는 [2] callout에서 명시적으로 넘기고 재서술하지 않음(중복 회피).
planning 자진 보고: **Bash 미보유 → 전 수치 손 유도**. §3-3에 재검산 대상 20셀 표를 정리해 마스터 사전검산을 요청. 교차검산 2쌍을 스스로 설계해 넣음(2,929+135=3,064 / 61=61 — 독립 경로라 손 유도 오류 시 깨짐).

### 마스터 사전검산 (dev 착수 **전** — 07-25에 확립한 삽입 지점)
스펙 md 코드펜스를 **스크립트로 파싱**해 대조(손 전사 금지, 표준 ⓚ). `npx tsx`로 `calculateServicePeriod` 실제 실행:
- **20셀 전건 일치, FAIL 0건.** 교차검산 ①2929+135=3064 ②61=61 둘 다 성립.
- 자수 **실측 title 38 / description 146** — 기획 자기보고와 **오차 0**(07-25의 148↔147 사례와 달리 이번엔 정확).
- 유사문자 오염 0건(en dash·중점·curly quote), title의 `—`는 의도된 U+2014.
- 부수 3건([19] 7년11개월38일 · [23] 약36일 · [38] 2,126일) 및 [21] 소수합 8.0 전건 일치.

### ★별건 — `calculateServicePeriod` 결함 실행 재현 (기획팀 손추적 → 마스터 실행 확인)
```
("2026-01-31","2026-02-28") → totalDays 29 / 표시 "0년 1개월 -2일"  ★음수★
("2026-01-30","2026-02-28") → totalDays 30 / 표시 "0년 1개월 -1일"  ★음수★
("2024-01-31","2024-02-29") → totalDays 30 / 표시 "0년 1개월 -1일"  ★음수★
```
`lib/service-period.ts` L88~101이 `days<0`일 때 직전 달을 **1회만** 빌려 보정 후에도 음수가 남는다. **라이브 계산기에 노출되는 실사용자 영향 결함**(totalDays는 정상, 표시만 깨짐). → **티켓 #2** 생성. 36편은 이 경로를 밟지 않아 발행 차단 사유 아님.

### dev-team 이식
`lib/blog.ts` **Edit 1회 append**(Write 통째 재작성 금지, 표준 ⓗ). tsc 0 / eslint 0 / **클린빌드**(`rm -rf .next`, 증분 불신) 에러 0 / **static 69**(68→+1) / 블로그 36편. dev가 스펙 50블록을 스크립트로 파싱해 1:1 역이스케이프 대조 → **축자 FAIL 0건**. 특수문자(`÷` `−` em dash) 빌드 HTML까지 보존 확인.

### qa-team 1차 → **발행 차단 FAIL 0 / 비차단 FAIL 1 / 관찰 8**
- **A 계산**: 20셀 + 교차검산 2쌍 + 부수 3건 **전건 PASS**(마스터 결과를 믿지 않고 독립 재실행 — 표준 ⓒ). 엣지 8종(같은날·역순·잘못된 날짜·윤일·9999년) 정상.
- **B 프레임 귀속**: 렌더 `<li>` 31개를 HTML에서 추출해 **값 산술과 분리 검증** → 라벨 오귀속 **0건**. 라벨 없는 6줄은 전부 구간행이 아니라 연산행이며, [30] "차이" 행은 프레임 간 차분이라 단일 라벨이 오히려 오귀속이 됨을 논증. 29편 FAIL 재발 없음.
- **C lint**: **36편 전수** title>51 0건 / 렌더 `<title>`>60 0건 / desc>160 0건.
- **D 검색**: 태그 유니크 **174개 질의**로 35편↔36편 결과집합 대조 → **오염 0건**. 신규 어휘 10종 전부 회수.
- **E 렌더**: 클린빌드 후 실제 `next start` + curl → **HTTP 200**(404 회귀 없음), 관련글·역방향 링크·sitemap·페이지네이션 전부 200. **375px 실측은 "미검증"으로 정직 보고**(puppeteer 미설치) — 추정을 검증으로 적지 않음.
- **★FAIL-1(비차단)**: `[13] heading` "경력 합산 **3단계**" ↔ `[14] ordered list` **5항목**. `<ol class="list-decimal">`에 1~5가 찍혀 자기모순이고, **description에도 "합산하는 3단계"가 있어 블로그 카드까지 모순이 노출**됨. 소관 planning(원고 유래, dev는 축자 이식만). → **티켓 #1**.

### 핑퐁: QA → planning (FAIL-1 반송)
정정 제약을 명시해 되돌림(신규 숫자·사실 도입 금지 / description 135~160 유지 / **description 어휘 삭제 금지**(검색 인덱스, 표준 ⓙ) / [14] 항목 텍스트 보존).
planning이 **`3단계`→`5단계`(자수 불변) 안을 스스로 기각** — [14]의 5번째 항목은 합산이 아니라 **공백 확인(선택)**이라 description이 "합산하는 5단계"가 되면 **공백을 합산 단계로 이중 계상하는 새 부정확**이 생긴다는 이유. 채택안: **[13]에서 `3단계 ` 4자 삭제 + [4] 말미에 "정리하면 다음 3단계입니다." 앵커 추가 + description 무수정**([5]의 3항목에 앵커링). 마스터가 논거를 검토하고 승인.
- planning **도구 제약 자진 보고**: 이번 창에 **Edit 미배정** → 스펙을 Write로 통째 재작성하면 QA 통과분에 전사 오류가 섞이므로 **원본을 건드리지 않고** 패치 파일 `planning/blog-36-spec-r1-fix.md`로 제출. 올바른 판단.
- planning이 QA 지적 N2도 정정: 종전 "예시 입사일을 전부 28일 이하로 고정" 서술이 **사실 아님**([35]에 `2026-03-31` 존재). 실제 안전 근거는 (a) 차용 후 `days=0`으로 경계 착지 (b) [35]가 근속기간을 표기하지 않음. **종전 재현 조건도 오탐**임을 발견해 `종료월의 일수 < 입사일의 일 − 1`로 정정 → 마스터가 엔진 실행으로 반증 시도, 4케이스 전부 일치 확인.

### dev-team r1 패치 적용 + **dev가 마스터 지시 오류 검출**
`lib/blog.ts` Edit 2회(DEV-1/DEV-2) + 스펙 D1~D7 적용, 축자차 0건. tsc 0 / eslint 0 / 클린빌드 69페이지 / 36편 유지.
- **마스터 지시 오류**: 내가 완료조건에 `git diff lib/blog.ts` = `+2 −2`를 기대값으로 적었으나, **36편 자체가 미커밋 신규 추가분**(HEAD는 35편)이라 구조상 `+276`이 나올 수밖에 없다. dev가 전제 오류를 규명하고 멈춰 보고한 뒤, 대신 스펙 §2 축자 대조(불일치 0건)로 2줄 변경을 입증. **표준 ⓒ가 또 실효**.

### 마케팅 게이팅 판단 + 🔔 알림 발송(01:5x)
신규 콘텐츠(36편)가 있으나 **카드뉴스 신규 초안 배정은 하지 않음.** 이유: `marketing/card-news/` 초안 **9편이 전부 QA 통과 상태로 07-14부터 12일째 사용자 이미지화·게시 승인 대기** 중이다. 10번째 초안을 더해도 병목(사용자 승인)은 그대로이고 미승인 산출물만 누적된다.
대신 **PushNotification 발송** — 9편 12일 방치는 07-25에 사용자가 지적한 "#16 취득세가 문서에만 적힌 채 9일 방치" 사고와 **동일한 실패 유형**이고, 게시는 계정 행위라 사용자 본인만 결정 가능. 결과: **데스크톱 알림 발송, 모바일 푸시는 미발송(Remote Control 비활성)**. 팀 로그 전체에 이전 알림 발송 기록 0건이었음을 확인하고 보낸 최초 1회다 — 다음 회차는 **재발송 금지**.

### ⚠ 동시 실행 세션 감지 (이번 회차 중 발생)
실행 도중 `components/CrossSiteNav.tsx`가 새로 `M` 상태가 됐다(실행 시작 스냅샷에는 없었음). 내용은 `text-[9px]` → `text-[11px]` 2곳(현재/준비 중 배지 폰트 확대) — **이번 회차 어느 팀에도 배정하지 않은 변경**이라 다른 세션(자매 사이트 standup 또는 디자인 수정)이 동시에 작업 중인 것으로 판단.
처리: **커밋에서 제외하되 되돌리지도 않는다**(다른 세션 소유물, 내 QA 파이프라인을 거치지 않음). `git add -A` 대신 **경로 명시 스테이징**으로 격리 — 07-23·07-25와 동일 원칙. 스케줄 §8의 `git add -A` 지시는 이 선례로 대체한다. QA 2차도 이를 독립 발견해 N3으로 보고(마스터 판단과 일치).

### qa-team 2차 → **PASS (발행 차단 FAIL 0건 / 비차단 3건)**
- **A 해소 확인**: 렌더 페이지 전체에 `경력 합산 3단계` 문자열 **부재**. `[4]` 앵커 바로 다음 DOM 형제가 `<ul>` li=3. `[4]`와 `[14]` 사이에 9블록·H2 2개가 끼어 있어 앵커가 `[14]`를 가리켜 읽힐 여지 **없음**(내가 우려로 제시한 위험이 실측상 불성립).
- **B 무변경**: 스펙 50블록 ↔ 코드 파싱 대조 `0 mismatches`. r1 관계식 역검증(`code[4] === before4 + 앵커문장`, `before13.replace("3단계 ","") === code[13]`) 둘 다 true → 변경 블록 = **[4][13] 2개뿐** 확정. 수치 list 7개 무변경.
- **★QA의 자체 판단으로 20셀 재검산을 생략하지 않음**: 스펙이 untracked 신규 파일이라 "스펙 자체가 r1 전후로 안 바뀌었다"를 git diff로 증명할 수 없어 무변경 논증의 근거가 닫히지 않는다고 보고 **엔진을 직접 재실행**(전건 일치). 올바른 판단.
- **D 반증 시도**: planning이 정정한 재현조건을 **브루트포스 147,188조합**(입사일 1~31 × 종료일 2019~2031)으로 검증 → `days<0` 실측 33건, **일반식 예측 불일치 0건**(반증 실패 = 조건 정확).
- **비차단 N1**: `[5]`가 `ordered:false`라 불릿으로 렌더 → "3단계"라 부르는 목록엔 번호가 없고, 화면에서 번호가 붙은 유일한 목록은 `[14]` 5항목. QA가 보정안(`[5].ordered=true`)을 **처방하지 않고 관찰만 보고**(번호목록 2개가 되는 트레이드오프 때문) — 표준 ⓘ(QA 제안도 검증 대상) 준수.
  → **마스터 판단: 현행 유지.** 렌더 페이지에 틀린 개수를 **주장하는 문장이 0건**이고 `[4]→[5]` 인접성이 DOM에서 확정적이다. `[5]`를 번호목록으로 바꾸면 3항목·5항목 번호목록이 둘 생겨 방금 없앤 혼동을 재도입할 위험이 더 크다. 관찰로만 인계.
- **비차단 N2 (티켓 #2에 반영)**: D5의 **볼드 결론**(`종료월의 일수 < 입사일의 일 − 1`)은 `종료일=월말`로 스코프가 걸려 있는데 **이 버그는 종료일이 월말이 아닐 때도 난다**. 반례 `("2000-01-31","2026-03-01")` → `26년 1개월 -1일`. 일반식은 이 계열을 정확히 예측하므로 문서가 틀린 건 아니지만, **개발자가 실제로 보고 착수할 한 줄이 계열 하나를 누락**한다.

### 마스터 §7 최종검증 (QA PASS 후에도 직접 실행)
독립 스크립트(`npx tsx`, lib 직접 import) + 클린빌드 + 실서버 + **브라우저 실측**:
- 36편 / date **9편** → salary·loan·life·date **9·9·9·9 완전 균형 달성** / slug 중복 0
- lint 전수: title>51 **0건**(max 50) / 렌더 `<title>`>60 **0건**(max 59) / desc>160 **0건**(max 159). 36편 = 38 / 47 / 146. `updatedDate` 미설정 확인
- 구조: 50블록, H2 **10개**(D7 정정치와 일치), list `[5]ul×3 … [14]ol×5 …` 9개, callout info2/warning2
- FAIL-1 정정 직접 확인: `[13]="경력 합산 — 계산기로 하는 순서"`(단계 수 주장 0), `[4]` 앵커 존재, `[5]`=3항목 일치, **본문 내 "N단계" 출현 1회뿐**, description "3단계" 유지
- CTA/related 3종 전부 `status:"live"` / 검색 질의 6종 전부 36편 회수 / 특수문자 `—`·`÷`×2·`−`×1 보존
- **클린빌드**(`rm -rf .next`) 에러 0 → blog HTML **36개**, **`.meta` 비200 0건**, 36편 산출물 **109,378 bytes**(07-25의 23KB 404 스텁 아님), sitemap 등재 1건, 실서버 curl **200**
- **★브라우저 실측으로 375px 갭 해소** — QA가 1·2차 모두 "미검증"으로 남긴 항목. 375×812: **가로 오버플로 0px**, article 343px, 넘치는 요소 0개. 1280×800: 오버플로 0px. 라이브 DOM에서 앵커 문단의 **다음 형제가 실제로 `UL.list-disc` li=3**임을 확인(앵커가 [5]를 가리킴을 렌더 수준에서 입증), `<ol>` list-style `decimal` 5항목, meta description 146자·"3단계" 포함
**FAIL 0건 → 판정: 완료.**

### CS팀
문의 채널 미연동 상태 그대로 — `/support` 폼은 `/api/contact`(Resend)로 **발신**만 하고 **조회 가능한 수신함이 없다.** cs-team이 읽을 신규 문의가 구조적으로 존재하지 않으므로 이번 회차 배정 없음(지어내지 않음).

### 팀 핑퐁 요약 (이번 회차 4건, 전부 당일 해소)
1. **planning → 마스터**: Bash 부재 자진 보고 + 20셀 재검산 요청 → 마스터 사전검산(dev 착수 전) → 오차 0건, 통과.
2. **QA 1차 → planning → dev → QA 2차**: FAIL-1(3단계↔5항목) 반송 → planning이 `5단계` 안을 **스스로 기각**(공백 확인을 합산 단계로 이중 계상하는 새 부정확 발생)하고 앵커링 안 채택 → dev 적용 → QA 2차 PASS.
3. **planning → 마스터 (도구 제약)**: Edit 미배정으로 스펙 정정 불가 → Write 통째 재작성 대신 **패치 파일**로 제출(QA 통과분 전사 오류 방지) → 마스터 검토 후 dev가 D1~D7 적용. 올바른 우회.
4. **dev → 마스터 (마스터 지시 오류)**: 내가 완료조건에 적은 `git diff = +2 −2`가 **전제 오류**(36편 자체가 미커밋 신규분이라 구조상 +276). dev가 멈추고 원인 규명 후 대체 입증. **표준 ⓒ가 이번에도 실효.**

### 이번 회차의 표준화 후보
- **ⓛ 미커밋 신규 파일에는 "diff 무변경" 논증이 성립하지 않는다.** 마스터 지시(#4)와 QA 2차 B절이 같은 지점에서 각각 걸렸다. 신규 추가분의 무변경·최소변경을 증명하려면 git diff가 아니라 **원본 스펙 ↔ 코드 파싱 대조**(+ 관계식 역검증 `after === before + 추가분`)를 써야 한다. 스펙 자체도 untracked면 무변경 논증의 근거가 닫히지 않으므로 **엔진 재검산을 생략하지 마라**(QA 2차의 판단이 옳았다).
- **ⓜ 정정안은 "자수 불변"이 아니라 "의미 정합"으로 골라라.** `3단계→5단계`는 자수 불변이라 가장 싸 보였지만, `[14]` 5번째 항목이 합산이 아니라 공백 확인이라 description이 **새 부정확**을 얻는다. 싼 정정이 사실관계를 바꾸는지 먼저 보라.
