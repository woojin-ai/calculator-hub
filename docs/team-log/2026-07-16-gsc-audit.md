# GSC 일일 점검 - 2026-07-16 08:00

## 결론: GSC **API** 점검은 여전히 미진행(연동 정보 없음). 대신 07-15 대비 **더 성장한 라이브 사이트**(sitemap 26→35 URL)의 색인 인프라를 마스터가 직접 회귀 검증 → **결함 0**. 최신 계산기·블로그를 실제 375px 뷰포트에서 렌더 실측(오버플로 0). 코드 수정 없음 → push 없음.

## 0. 사전 조건 (재검증 근거)
- **배포/저장소 정상**: 라이브 `https://calculator-hub-delta.vercel.app` 홈 `GET /` → 200, 65KB.
- **GSC API 인증정보 여전히 없음**: `.env.local` = `RESEND_API_KEY`, `CONTACT_TO_EMAIL` 2줄뿐(89B). 서비스계정 키(.json/.pem) 없음. `GSC_/SEARCH_CONSOLE/service_account/GOOGLE_APPLICATION/OAUTH` grep(site-verification 제외) → 히트 0. → GSC API 조회 불가.
- ⚠️ `layout.tsx`의 `google-site-verification`은 소유권 확인용이며 API 접근 권한 아님(전일과 동일 구분).

## 1. GSC API 점검 결과
- 색인 현황(Coverage): **미확인** (API 접근 불가)
- 모바일 사용성 리포트: **미확인** (API 접근 불가) — 단 온페이지/실뷰포트 요소는 2·2b에서 직접 확인
- 노출/클릭 데이터: **미확인** (API 접근 불가)

## 2. 마스터 직접 검증 — 라이브 색인 인프라 (전부 통과)
기준: `https://calculator-hub-delta.vercel.app`
- **sitemap.xml** → 200, **URL 35개**(07-15는 26개). 내역: 정적 10 + 라이브 계산기 **16** + 블로그 **9**. `app/sitemap.ts`가 `lib/calculators`(status==="live")·`lib/blog` 데이터 기반으로 생성 → 라이브 URL 유형별 집계와 **정확히 일치**(누락/고아 0, coming-soon 스텁 0).
- **robots.txt** → 200, `User-Agent:* / Allow:/` + `Sitemap:` 지정. 크롤 차단 없음.
- **신규 페이지 색인성**(07-15 이후 추가분: annual-leave-allowance 계산기, car-tax, bmi 블로그 등):
  - `/calculator/annual-leave-allowance-calculator`, `/calculator/car-tax-calculator` → 200 + `robots: index, follow` 명시.
  - `/blog/bmi-guide-2026`, `/blog/annual-leave-allowance-guide-2026` → 200 + 자기참조 `canonical` 있음.
  - `/life`, `/salary` 카테고리 → 200, robots 메타 없음(=기본 색인).
- **404 가드**: `/calculator/nonexistent-xyz`, `/blog/nonexistent-post` → **404 + noindex**. 소프트-404/thin content 색인 위험 없음.
- **viewport 메타**: 홈·최신 계산기·블로그·카테고리 전부 `width=device-width, initial-scale=1` 서빙 확인.

## 2b. 모바일 375px 실제 렌더 검증
브라우저 뷰포트 375×812 실측:
- **최신 계산기** `/calculator/annual-leave-allowance-calculator`: `docScrollW==innerW==375`, **가로 오버플로 0px**, 초과폭 요소 0, h1="연차수당 계산기", input 3개 정상.
- **신규 블로그** `/blog/bmi-guide-2026`: `docScrollW==innerW==375`, **가로 오버플로 0px**, 초과폭 요소 0, h1 정상.
- **콘솔 에러 0건**(onlyErrors 조회).
- ※ 스크린샷 캡처는 렌더러 타임아웃으로 이번에도 실패 → DOM 정량 측정(overflowPx/wideElements)으로 오버플로 없음 확정.

## 3. 발견된 문제 / 핑퐁 루프
- **SEO/색인 결함 0건.** 라우팅할 이슈 없음 → planning/marketing/dev 핑퐁·TaskCreate·qa-team 검증 미발동(정상 — 대응할 문제 없음).
- (비결함 메모) 계산기 상세 페이지엔 명시적 `canonical` 없음 — 그러나 신·구 페이지(salary/severance/dsr/savings/car-tax) **전부 동일**하므로 신규 회귀가 아닌 기존 설계(자기참조 canonical 기본동작). GSC 위험 아님 → 미조치. 블로그 상세엔 canonical 있음.

## 4. 배포(push)
- 코드 수정 없음(점검·검증만) → 커밋/푸시 대상 없음. 자동 push 미실행.
- working tree: `.claude/settings.local.json`(M) + 신규 team-log만 존재 — GSC 감사 소관 코드 아님.

## 5. 미확인 / 다음 실행 인계
- **GSC API 텔레메트리 3종(Coverage·노출/클릭·Mobile Usability)은 계속 미확인.** 실조회하려면 사용자가 GSC 서비스계정 키(.json)를 프로젝트에 두거나 OAuth 토큰을 env에 설정해야 함.
- 온페이지 인프라 + 375px 모바일 렌더는 오늘 정상. 다음 실행: (a) GSC 크리덴셜 생겼는지 먼저 확인 → 있으면 실 API 점검 시작, (b) 없으면 오늘처럼 라이브 인프라 회귀(sitemap 개수·데이터 일치/robots/viewport/신규 라이브 페이지 색인성/404 가드) + 375px 오버플로 재확인.
- 실기기 스크린샷 시각 검증은 렌더러 타임아웃으로 3일 연속 미수집(DOM 정량 측정으로 대체). 시각 증빙이 필요하면 렌더러 안정화(타임아웃 원인) 별도 조사 필요.
