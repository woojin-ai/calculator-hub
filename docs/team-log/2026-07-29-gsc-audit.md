# GSC 일일 점검 - 2026-07-29 08:00 (실제 실행 09:39~)

## 결론

GSC **API** 점검은 **14일 연속 미진행**(자격증명 부재). 07-26에 이 블로커로 알림 1회를 이미 보냈으므로 **이슈당 1회 원칙에 따라 재발송하지 않았다.**

대체 점검(라이브 66 URL 전수 온페이지 감사 + 6개 라우트 375px 실측)에서 **회귀 0건**을 확인했다. 오늘의 핵심은 세 가지다.

1. **🔴 스케줄 세션 동시 실행을 발견해 코드작업을 의도적으로 전면 보류했다.** `daily-team-standup` 세션이 **실행 중인 상태로** 같은 작업트리에서 `lib/calculators.ts`를 편집하고 있었고, 남은 티켓 #7·#8·#9가 **전부 그 파일을 대상**으로 한다(§2). 사용자 알림 1회 발송.
2. **감사 스크립트 자체 결함 2건을 지시 전에 잡았다** — 16진 엔티티 미디코딩(`&#x27;`)과 "og 6종" 정의 오류. 둘 다 그대로 믿었으면 없는 회귀 3건 + 66건을 dev에 지시할 뻔했다(§3-1). **5일 연속 측정도구 함정 계열.**
3. **신규 결함 1건 발견**: 브레드크럼 탭 타깃 11×16px — 사이트가 07-28에 스스로 채택한 표준 ⓣ 위반(§5). 오늘은 충돌 회피로 티켓화만 했다.

## 0. 사전 조건 (재검증 근거)

- **배포/저장소 정상**: `git fetch` 후 `HEAD == origin/main`(0 ahead / 0 behind). 라이브 `sitemap.xml` 200(10,608 B, **62 URL**).
- 베이스 URL 정본 `calculator-hub-delta.vercel.app` 유지.
- **GSC API 자격증명 여전히 없음(14일째)**: `.env.local` 키 6개(`RESEND_API_KEY`/`CONTACT_TO_EMAIL`/`IG_BUSINESS_ACCOUNT_ID`/`IG_ACCESS_TOKEN`/`META_APP_ID`/`META_APP_SECRET`) — **Google/GSC 키 0개**(값 미열람, 키 이름만 확인).
- ⚠️ 무인 안전수칙 준수: `cd`+`git` 연결 호출 없음, 전부 `git -C`.

## 1. GSC API 점검 결과

| 항목 | 결과 |
| --- | --- |
| 색인 현황(Coverage) | **미확인** — API 자격증명 부재(14일째) |
| 모바일 사용성(GSC 리포트) | **미확인** — §4에서 실측 대체 |
| 노출/클릭 | **미확인** |

**알림 재발송 안 함** — 07-26에 이 이슈로 1회 발송 완료(이슈당 1회 원칙).

## 2. 🔴 스케줄 세션 동시 실행 — 오늘 코드작업을 전면 보류한 이유

세션 시작 직후 워킹트리에 **다른 세션의 미완성 작업**이 있었다.

| 근거 | 실측 |
| --- | --- |
| `mcp__ccd_session_mgmt__list_sessions` | **"Daily team standup" `isRunning: true`**, cwd `C:\claude code webside`, 최종활동 09:39 → 09:46(내 세션 진행 중에도 계속 갱신) |
| `lib/calculators.ts` | **M**(uncommitted), mtime **09:30** — 내 세션 시작 9분 전 |
| `planning/title-optimization-spec.md` | **??**(신규), mtime **09:22**, 머리말 "기획팀 / 2026-07-29", 근거 티켓 **#7** |
| `lib/calculators.ts` diff 내용 | loan-interest 총이자 `2,368,790` → **`2,368,572`** = **티켓 #9 그 자체**. 그 외 dday interpretation 하드코딩 제거, 연차수당 반올림 주석 추가 |

즉 **standup 세션이 티켓 #7(기획)과 #9(구현)를 이미 진행 중**이다.

### 2-1. 왜 "다른 티켓을 하면 되지 않나"가 성립하지 않는가

남은 티켓의 대상 파일을 전수 확인했다.

| 티켓 | 대상 | 파일 |
| --- | --- | --- |
| #7 title 최적화 | 카테고리 4개 `metaTitle?` 신설 | **`lib/calculators.ts:37` `categoryInfo`** |
| #8 JSON-LD 썩는 수치 | four-insurance / savings-interest / dsr | **`lib/calculators.ts`** + `lib/blog.ts` |
| #9 interpretation 수치 | loan-interest | **`lib/calculators.ts`** (standup이 이미 수정함) |

**세 티켓 전부가 standup이 지금 편집 중인 파일 하나에 몰려 있다.** 내가 손대면 상호 클로버이거나, 한쪽이 다른 쪽의 미완성 상태를 커밋한다.

### 2-2. 마스터 판정

- **오늘 코드 변경 0건.** 티켓 #7·#8·#9 전부 standup 소유로 두고 건드리지 않는다.
- **타 세션 산출물을 대신 커밋하지 않는다**(07-28 §0 선례 준수). `lib/calculators.ts`·`planning/title-optimization-spec.md`는 스테이징조차 하지 않았다.
- **worktree 격리로 우회하지 않았다.** 티켓 #5가 이미 스테일 워크트리 2개를 위생 문제로 올려둔 상태라 3번째를 무인으로 늘리는 것은 이득보다 부채가 크다.
- 이것은 형제 프로젝트 loan-compare의 **티켓 #18과 동일한 사고 유형**이다(2026-07-26, 16:11·16:21 두 실행이 같은 트리를 동시 편집해 "상호 회피로 겨우 무손실"). 그쪽에서 근접 발생한 것이 **여기서 실제로 발생했다.**
- **구조적 해결은 사용자만 가능** → 알림 발송(§7).

## 3. 라이브 전수 온페이지 감사 (66 URL)

모집단은 정본대로 sitemap `<loc>` 62 ∪ `/links` ∪ `/blog?page=2,3,4` = **66**.

| 신호 | 결과 |
| --- | --- |
| HTTP 상태 | **66/66 = 200** |
| canonical | 66/66 정확히 1개, og:url 불일치 **0** |
| `<title>` | 66/66 고유, 중복 0, **>60자 0건**(max 59 / min 12) |
| meta description | 66/66 고유, 태그 중복 0, **>160자 0건**(max **159** / min 53) |
| `<h1>` | 66/66 정확히 1개 |
| JSON-LD / viewport / `<html lang>` | 66/66 존재 / 66/66 정상 / 66/66 `ko` |
| robots noindex | `/links` 1건(의도) |
| **og 6종**(title·description·url·site_name·locale·type) | **66/66 완비** — 티켓 #2 회귀 0 |
| og:title↔title / og:desc↔desc / og:url↔canonical | **불일치 0 / 0 / 0** |
| og:type | article 36 / website 30 |
| twitter:card | 66/66 |
| og:image | **0/66** — 회귀 아님, 백로그 미착수 항목(§3-1) |

**desc max 159자는 07-28 실측치와 정확히 일치**한다. 이것이 어제 측정이 옳았고 오늘 내 1차 측정이 틀렸다는 교차검증이 됐다.

### 3-1. ★감사 스크립트 자체 결함 2건 — 지시 전에 차단

1차 실행이 **FAIL 2종(desc>160자 3건, og 6종 미완비 66건)** 을 뱉었다. 둘 다 **내 스크립트 결함**이었다.

| # | 오탐 | 원인 | 실제 |
| --- | --- | --- | --- |
| 1 | `desc > 160자` **3건** (`service-period-guide` 177 / `severance-pay-guide` 164 / `loan-prepayment-break-even` 161) | 디코더가 **10진 수치참조(`&#39;`)만 처리하고 16진(`&#x27;`)을 놓쳤다.** Next.js는 작은따옴표를 `&#x27;`로 출력한다 → **건당 1자가 6자로 계산돼 발생당 +5자 부풀림** | 4·2·2회 등장 → 157 / 154 / 151자. **전부 정상, 위반 0건** |
| 2 | `og 6종 미완비` **66건**(전량) | 내가 "6종"에 **og:image를 임의로 포함**시켰다. 정본 6종은 title·description·url·site_name·locale·**type**이고, og:image는 별도 백로그(미착수) | og:image 0/66은 **알려진 미착수 상태**이지 회귀가 아니다 |

100% 실패율(66/66)은 결함이 아니라 측정 오류의 서명이다. 디코더를 수치참조 일반(10진·16진) 처리로 고치고 6종 정의를 정정한 뒤 재실행 → **전 항목 위반 0건.**

07-25 `innerText` · 07-26 `getClientRects` · 07-27 `html.unescape` · 07-28 `next dev` 워밍업 + `resize_window` preset에 이은 **5일 연속 측정도구 함정**이고, 이번에도 팀에 지시하기 전에 잡았다.

## 4. 마스터 직접 모바일 실측 (375×812 실브라우저, 라이브)

표준 ⓠ에 따라 **매 측정 전 `clientWidth`를 먼저 확인**했다(전부 375 확인, 0 아님).

| 페이지 | 가로스크롤 | 오버플로 요소 | 12px 미만 폰트(가시) |
| --- | --- | --- | --- |
| `/about` | **없음**(375=375) | **0** | **0** |
| `/` | **없음** | **0** | **0** |
| `/salary` | **없음** | **0** | **0** |
| `/calculator/severance-pay-calculator` | **없음** | **0** | **0** |
| `/blog/severance-pay-guide-2026` | **없음** | **0** | **0** |
| `/blog/savings-maturity-by-monthly-amount-2026`(조견표) | **없음** | **0** | **0** |

- **07-28 `/about` 칩 수정 회귀 0**: 칩 16개, **높이 36px**, 인접 최소간격 **8px**, 문서높이 **2445px**, "준비 중" 문구 **부재** — 어제 수정 후 수치와 **완전 일치**.
- **조견표 포스트 검증**: 라이브 블로그 36편에 `<table>` 요소는 **0개**(전부 반응형 구조로 렌더). 조견표 최다 포스트에서 **내부 가로스크롤 컨테이너 0개, 오버플로 0** — 표 오버플로는 존재하지 않는 문제다.
- **계산기 입력 필드**: 높이 **48px**, 폰트 **16px**(iOS 자동확대 방지선 충족).
- 데스크톱 1280×800 교차확인(clientWidth 1265): 오버플로 0, **홈 카테고리 카드 4장 전부 197px**(높이 집합 크기 1) — 07-28 실측치와 일치.

### 4-1. 측정 오탐 1건 추가 차단

`/about` 1차 측정에서 **12px 미만 폰트 6건**이 잡혔다(어제는 0건이라 회귀로 보였다). 확인 결과 6건 전부 **`getBoundingClientRect()` 0×0 = 비가시**인 형제사이트 레일(`관련 서비스` 10px, 사이트명 11px×4)이었고, **375px에서는 렌더되지 않는다.** 내 필터에 가시성 조건이 빠진 것이 원인이며 어제 측정에는 있었다. **회귀 아님.**

## 5. 신규 결함 — 티켓 #10 (오늘은 티켓화만)

계산기 상세 `/calculator/*`의 **브레드크럼 탭 타깃이 표준 ⓣ를 위반**한다. 375px 라이브 실측:

| 링크 | 크기 | 폰트 |
| --- | --- | --- |
| `홈` | **11 × 16px** | 12px |
| `급여 계산기` | 58 × **16px** | 12px |

07-28에 **사이트가 스스로 채택한 표준 ⓣ**는 "높이 ≥36px, 인접 간격 ≥8px"이다. 16px은 그 절반 미만이고, `홈`의 **너비 11px**은 손가락으로 정확히 누르기 어렵다. 부수적으로 해당 `<nav>`에 **`aria-label`이 없다.**

- **회귀 아님**(기존 상태), 긴급하지 않음. 07-28 §7 선례대로 **결함 근거 없이 바꾸지 않되, 여기서는 자사 표준이라는 명시적 근거가 있다**(어제 "70자 하한"처럼 발명한 임계값이 아니다).
- **오늘 미착수 사유는 §2 충돌 회피뿐이다.** 수정 시 07-28 `/about` 칩 선례(`min-h-9` + `flex-wrap` + `gap-2`, `/support` 알약 클래스 어휘 재사용)를 따를 것.
- 헤더 로고 링크 **28px**도 재확인됐다 — 기존 백로그 "헤더 터치타깃 36px"와 동일 건이며 티켓 #10과 함께 처리하는 것이 자연스럽다.

## 6. 팀 호출 / 핑퐁 — 오늘 없음 (사유 명시)

지시서 §2는 "**문제 발견 시**" planning·marketing 진단 → dev 구현 → qa 검증의 핑퐁을 요구한다. 오늘은 **감사에서 나온 FAIL 2종이 전부 내 스크립트 결함**이었고(§3-1), 라이브 회귀는 **0건**이었다. 실재하는 신규 결함(티켓 #10)은 §2의 동시 편집 위험 때문에 착수 자체를 보류했다.

**없는 문제로 팀을 부르지 않았고, 부를 수 있는 문제는 착수가 막혀 있었다.** 팀 호출 0건은 절차 누락이 아니라 이 두 사실의 결과다.

## 7. 사용자 확인이 필요한 블로커

| # | 사안 | 알림 |
| --- | --- | --- |
| **#11 (신규)** | **스케줄 세션 동시 실행 → 같은 작업트리 동시 편집.** 구조적 해결은 (a) 스케줄 시간 분리 또는 (b) 세션별 worktree 격리 — 둘 다 사용자 설정 영역 | **2026-07-29 발송(1회).** ⚠️ 도구 응답: *"Mobile push not sent (Remote Control inactive)"* — **휴대폰 전달 실패**, 데스크톱 알림만 |
| #6 | 공개 저장소 `.claude/settings.local.json` 평문 크론 시크릿 | 07-27 발송 완료 — **재발송 금지** |
| GSC | API 자격증명 부재(14일째) | 07-26 발송 완료 — **재발송 금지** |

## 8. 배포(push)

- **커밋 대상**: `docs/team-log/2026-07-29-gsc-audit.md` **1개 파일뿐**(경로 명시 스테이징, `git add -A` 미사용)
- **코드 변경 0건** — 빌드·테스트 불필요(문서 전용)
- **제외**: `lib/calculators.ts` · `planning/title-optimization-spec.md`(**타 세션 진행 중, §2**) · `.claude/settings.local.json`(**🔴 평문 크론 시크릿**) · `.claude/launch.json` · `.claude/settings.json` · `marketing/promo-drafts.md` · `planning/calculator-lineup-expansion.md` · `.codex/` · `ebook/` · `trading/`
- `.env*` 포함 0건 확인

## 9. 미확인 / 다음 실행 인계

1. **[🔴 ★사용자 확인 필요 — 티켓 #11, 07-29 알림 1회(휴대폰 미전달). 재발송 금지]** 스케줄 세션 동시 실행. **다음 실행은 세션 시작 시 `list_sessions`로 다른 세션의 `isRunning`을 먼저 확인할 것** — 이번에 이 확인이 유일한 검출 지점이었다.
2. **[★표준 — 신규 ⓤ]** **작업 시작 전 `list_sessions` + 대상 파일 mtime을 확인한다.** `git status`에 남의 미완성 변경이 있으면 그 파일과 **그 파일을 대상으로 하는 모든 티켓**을 오늘 범위에서 제외한다. 타 세션 산출물은 대신 커밋하지 않는다.
3. **[★표준 — 신규 ⓥ]** **HTML 메타 길이 측정은 수치참조를 10진·16진 **둘 다** 디코딩한 뒤 센다.** Next.js는 `'`를 `&#x27;`로 출력하므로 16진을 빠뜨리면 **발생당 +5자**가 부풀어 정상 문서가 위반으로 잡힌다. 5일 연속 측정도구 함정 계열.
4. **[★표준 — 신규 ⓦ]** **"og 6종"은 title·description·url·site_name·locale·type이다. og:image는 미착수 백로그이지 결함이 아니다.** 감사에서 매일 재계상하지 말 것. 그리고 **100% 실패율(66/66)은 대개 결함이 아니라 측정 오류의 서명**이다.
5. **[★표준 — 유지]** 폰트·크기 실측 필터에는 **가시성 조건**(`width>0 && height>0`)을 반드시 넣는다 — 없으면 비가시 레일 요소 6건이 회귀로 잡힌다(§4-1).
6. **[티켓 #10 — 신규]** 브레드크럼 탭 타깃 11×16px + `nav` `aria-label` 부재 + 헤더 로고 28px(§5).
7. **[티켓 #7·#8·#9 — standup 소유, 오늘 무접촉]** 전부 `lib/calculators.ts` 대상. **standup 세션의 커밋 결과를 먼저 확인한 뒤** 잔여분을 판단할 것.
8. **[티켓 #5 — 유지]** `.claude/worktrees/` 스테일 워크트리 2개(`cranky-feistel-94e116`, `jolly-leavitt-3b98ee`). 오늘 grep 노이즈를 유발했다. #6과 함께 `.claude/` 위생 문제로 묶어 올릴 만하다.
9. **[백로그 — 유지]** og:image 2단계 미착수 · 다크모드 부재 · AdSense 심사 대기 · 카드뉴스 9편 승인 대기 · `/blog` 목록 `ƒ (Dynamic)` · #16 취득세(/loan 확정) · `age-calculator` `<input max>` 빌드시각 고정 · **픽셀 스크린샷 육안 검증(무인 실행 불가, 15일째)**.
10. **[운영 표준 — 유지]** C 로케일 Git Bash의 `wc -m`·`${#}`·`awk length`는 바이트를 센다 — 한글 문자수는 python `len()` 또는 JS `[...s].length`. URL 모집단 66(sitemap 62 ∪ `/links` ∪ `?page=2,3,4`). `git -C` 사용, `cd`+`git` 금지. **dev 서버 살아있을 때 `rm -rf .next` 금지.**

## 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. **코드 변경 0건.** Vercel CLI 직접 배포 안 함(git push만). `.env.local` 값 미열람(키 이름만). **`git add -A` 미사용.** **타 세션 워킹트리 무접촉**(스테이징조차 안 함). 어떤 이미지/영상 생성 도구도 호출하지 않았다.
