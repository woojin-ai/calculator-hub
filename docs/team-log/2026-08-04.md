# 2026-08-04 팀 로그

## 01:06 실행분 (오늘 첫 실행)

**이번 회차 결론: 티켓 #22 종결 → 커밋 `fd9db53` push 완료.**
라이브 UI의 거짓 등식 제거 + 재오염원 3곳 동시 차단.

---

## 0) ★08-03 하루 통째로 실행 0회 — 알림 발송

`docs/team-log/2026-08-03.md` **파일 없음** + `git log --since=08-03 --until=08-04` **커밋 0건**.
즉 어제 14회 슬롯이 전부 불발했다. 08-01 메모리에 기록된 **좀비 세션 → `global_limit` 포화 → 조용한 실패**와 같은 클래스로 보인다. 그때 설치한 WTS `ClaudeReaper-StuckSessions`가 막지 못했다는 뜻이다.

- `mcp__scheduled-tasks__list_scheduled_tasks`로 `enabled`/`nextRunAt`을 확인하려 했으나 **권한 거부**(don't ask 모드). 상태 파일도 `scheduled-tasks/` 아래엔 `SKILL.md`뿐이라 직접 확인 불가.
- **🔔 알림 시도(01:06 회차) — ⚠️ 실제 전달 실패**: `PushNotification` 반환값이
  `Mobile push not sent (Remote Control inactive)`. 즉 **휴대폰으로 안 갔다.** 08-01 알림은
  "동시 실행/중복" 건이었고 이번은 **countermeasure 설치 후의 재발**이라 새 정보로 판단해 발송했으나,
  Remote Control이 꺼져 있어 사용자에게 도달했는지 불확실하다.
  → **다음 실행 지침**: 30분마다 재발송하지 말 것. 다만 **이 이슈는 "알림 완료"로 취급하지 말고
  "전달 미확인"으로 남길 것.** 사용자가 대화로 나타나면 그때 구두로 전달하는 게 확실하다.
  (부수 관측: Remote Control이 꺼져 있으면 **모든 무인 실행의 블로커 알림이 조용히 유실된다** —
  07-26 이후 발송했다고 기록된 알림 4~5건도 실제로는 도달 안 했을 수 있다. 이 자체가 점검 대상.)
- 오늘(08-04) 01:06 회차는 정상 발동했다 → 상시 정지가 아니라 **간헐적 무증상 실패**.

---

## 1) 이어받은 지점

08-02 이월 1번 = **티켓 #22 미착수**. `components/LoanPrepaymentFeeCalculator.tsx` 3건이며
**라이브 UI에 거짓 등식이 노출 중**이라 최우선으로 잡았다.

### 마스터 선행 검증 — 요약이 아니라 원본 스펙에서 옮겼다 (표준 ㉵ 적용)
08-02가 신설한 ㉵("인계 요약이 아니라 원본 스펙에서 작업 목록을 옮겨라")를 실제로 적용했고, **즉시 값을 했다.**

인계 요약(08-02 로그 L158)은 `#15 … '=' → '≈'`라고만 적혀 있었다. 그런데 원본 스펙
`planning/ratio-percent-display-rules.md` §2-3 **L141**을 직접 열어보니:

> `=` 대신 **`≈`(또는 "약")** 을 쓴다. **정확히 나누어떨어지는 경우(`18 ÷ 24 = 75%`)만 `=`를 쓴다.**

**요약대로 무조건 `≈`로 바꿨으면 `18개월 ÷ 24개월 = 75%`를 거짓으로 만들 뻔했다.**
→ 브리프를 "무조건 치환"이 아니라 **조건부 분기**로 작성. ㉵가 하루 만에 2회째 적중.

---

## 2) 실행 결과 (dev-team)

| # | 내용 |
| --- | --- |
| **#15** | 잔존기간 행 등호를 분기로 전환. 인라인 템플릿 → `formatRemainingRatioLine` 추출(회귀 앵커가 잡을 수 있게) |
| **#16** | `formatRatio` 정수 판정을 `toFixed(1)` **이후**로 이동 (`formatDsr`와 동형) |
| **#17** | `formatRatio` 주석에 "표시 전용, 등식 사용 금지 — 등식은 분수형(R÷D)" |

### ★마스터 브리프의 사실 오류를 dev가 정정했다
브리프에 부동소수 함정 예시로 `3/30 → 10.000000000000002`를 적었는데, **dev가 실측으로 반증**했다(`3/30 === 0.1`, `0.1*100 === 10`). 다만 **결론(정수 산술로 판정하라)은 유효**했고, dev가 D≤36 전 조합을 실측해 **진짜 함정 3건**을 찾아냈다:

`11/20 → 55.00000000000001` · `7/25 → 28.000000000000004` · `14/25 → 56.00000000000001`

셋 다 **정확값인데 실수 비교로 짰으면 `≈`로 오판**됐을 케이스다. 최종 구현은 정수 산술
`R × 1000 % D === 0`. 앵커 예시도 `3/30`이 아니라 실측 검증된 `11/20`으로 교체됐다.

**교훈: 마스터 브리프의 예시도 검증 대상이다. 팀이 반증하면 그게 정상 작동이다.**

---

## 3) QA 검증 — 2라운드, FAIL 4건 전부 해소

### 1라운드: 코드 로직 **전부 PASS**, 문서 정합 FAIL 2건
QA가 전수 탐색으로 독립 재검산한 항목:
- `=`/`≈` 판정식 ↔ BigInt 진리값 **702조합 불일치 0**
- 실수 비교로 짰을 때 갈리는 조합 = **`11/20`·`7/25`·`14/25` 3건** (dev 주장과 숫자까지 일치)
- #16으로 표시가 바뀌는 조합 = **15개** (dev 주장과 구성까지 완전 일치)
- 헬퍼 추출 전후 렌더 문자열 **810건 코드포인트 대조**, 부호 외 불일치 0
- `formatPrepaymentFormulaLine`의 `=` 정당성 **29,160조합 위반 0** (건드리면 안 되는 행 — 보존 확인)
- 앵커 비공허성: 구 구현으로 되돌려 **결함 3클래스 전부 최소 1앵커가 FAIL**함을 증명

| FAIL | 내용 | 반환처 |
| --- | --- | --- |
| **F-1** | 코드 주석이 "Batch 3 **표 행번호**"를 "실재 **티켓 번호**"로 표기. `#15/#16/#17`은 이미 **무관한 실제 티켓**이 점유 중(카드뉴스 구값 / 입력 상한 / 미바인딩 방어코드) | dev-team |
| **F-2** | `design/loan-prepayment-fee-ui-spec.md` 4곳이 구 문자열 `= 66.7%`를 여전히 규정. **L596은 "일치"라고 단언하는데 그게 거짓**이 됨 | design-team |

두 건 다 **마스터가 직접 grep으로 재현 확인 후** 반환했다(표준 ㉴).

### 2라운드: FAIL 3건 → 2건 조치 + 1건 마스터 판단으로 백로그

| FAIL | 판정 | 처리 |
| --- | --- | --- |
| **R2-F1** | design §8-2 좌열은 **기획 §7-2 인용 열**인데 design이 고쳐 씀 → 오귀속 + "일치"가 디자인↔디자인 자기대조로 **공허해짐** | design-team 반환, 해소 |
| **R2-F2** | design팀의 "ASCII 상자 내부 폭 56칸" 보고가 실측과 불일치(56/57/58/60) | **마스터 판단: 조치 제외.** v1.0(07-14) 초판부터의 기존 결함이고 이번 편집이 만든 게 아니며(QA도 "신 (d) 2줄은 정렬 안 깼다"로 PASS), **문서 안에 "56칸"이라는 문구가 있는 것도 아니다**(마스터 grep 확인 = 0건). 보고서에만 존재한 부정확한 주장 → 백로그 |
| **R2-F3** | **진짜 상류 재오염원**: `planning/loan-prepayment-fee-content.md:243`이 금지형 2개를 그대로 규정 중 | planning-team 반환, 해소 |

**R2-F3이 이번 회차 최대 수확이다.** QA의 지적이 날카로웠다 — design이 §8-2의 거울상만 고치면
"재오염 확률을 낮춘 게 아니라 **탐지 확률을 낮춘다**"(§8-2에서 "일치"를 본 사람은 상류를 확인하러 가지 않는다).

---

## 4) ★마스터 실책 — 병렬 배정이 만든 모순 (자기 지적)

R2-F1(design)과 R2-F3(planning)을 **동시에 배정**했다. 그 결과:

- design은 §8-2 좌열에 **구 문구를 복원**하고 "기획팀 반환 대상"이라고 적었는데,
- **바로 그 시각에** planning이 그 구 문구(L243)를 **정정 완료**했다.

→ 커밋 직전 마스터가 두 파일을 직접 대조해 발견. design에 3차 지시로 좌열을 **정정본 기준으로 갱신 + 판정을 `일치`로 복원 + "상류 §7-2는 08-04 정정 완료" 반영**시켜 해소했다.

**교훈(신규 표준 ㉷): 같은 인용 관계로 묶인 두 문서를 서로 다른 팀에 병렬 배정하지 마라.**
상류(피인용)를 먼저 확정한 뒤 하류(인용)를 배정해야 한다. 병렬로 돌리면 **한쪽이 다른 쪽의
"현재 상태"를 사실로 적는 순간 그 문장이 즉시 거짓**이 된다. 이번엔 커밋 전에 잡았지만
운이 좋았다 — 두 파일을 대조하지 않았으면 "미해결"이라고 단언하는 문서가 라이브로 나갔다.

---

## 5) 팀 핑퐁 이력

| # | 제기 | → 이관 | 결과 |
| --- | --- | --- | --- |
| 1 | 마스터(원본 스펙 직독, ㉵) | → dev-team | `≈` 무조건 치환이 아니라 **조건부 분기**로 브리프 교정 |
| 2 | **dev-team**(마스터 브리프의 float 예시가 사실무근) | → 마스터 | **반증 수용.** 실측 함정 3건으로 예시 교체 |
| 3 | qa-team R1 F-1(티켓번호 오표기) | → 마스터 재현 → dev-team | 해소 |
| 4 | qa-team R1 F-2(디자인 스펙 드리프트) | → 마스터 재현 → design-team | 해소 + design이 **범위 밖 (d) 4곳 자체 발견·정정** |
| 5 | **design-team**(근거 (d)도 금지형 — 지시 안 했는데 발견) | → 마스터 | **채택.** QA가 "217원 차이로 등식 거짓" 재검산 확인 |
| 6 | qa-team R2-F1(§8-2 오귀속) | → design-team | 해소 |
| 7 | qa-team R2-F3(상류 원천 + **표준 Batch 2 감사 구멍**) | → planning-team | 해소 |
| 8 | qa-team R2-F2(폭 주장 부정확) | → 마스터 | **백로그 판정**(기존 결함, 사용자 영향 0) |
| 9 | **planning-team**(§8-3 가드 문구에 상류 정정 반영 필요) | → design-team | 해소 |
| 10 | 마스터(병렬 배정 모순 자기발견) | → design-team 3차 | 해소 |
| 11 | qa-team(프로세스 권고: 라운드별 WIP 커밋 없으면 델타 검증 불가) | → 마스터 | **수용, 아래 ㉸** |

---

## 6) 마스터 최종 검증 (§7)

| 항목 | 근거 |
| --- | --- |
| 회귀 스위트 | 마스터 직접 실행 — **353 assertions · ALL SUITES PASS** (직전 322 → +31) |
| 빌드 | 마스터 직접 실행 — `✓ Compiled` + **69/69** 정적 페이지 |
| **실렌더 축자 확인** | 마스터가 `formatRemainingRatioLine`·`formatPrepaymentFormulaLine`을 **실호출**해 문자열 확인 (아래) |
| F-1 재현 | `grep "티켓 #1[567]"` ↔ 팀로그 실제 티켓 대조 — 충돌 확인 후 반환 |
| F-2 재현 | `grep "= 66.7%"` design 스펙 4곳 확인 후 반환 |
| R2-F2 반증 | `grep "56칸\|내부 폭"` design 스펙 → **0건**(문서엔 그 주장 없음) → 백로그 판정 |
| R2-F3 재현 | `sed -n '243p' planning/...` 금지형 2개 확인 + 표준 L263이 **L243을 감사한 적 없음** 확인 |
| 병렬 모순 | 커밋 전 두 파일 대조로 발견 → 3차 지시로 해소 확인 |
| 시크릿 | 대상 5파일 diff hex 32자+ **0건**, 스테이징에 `.claude`/`.env` **0건** |
| 커밋 범위 | `git add`에 **5개 경로 명시**(`git add -A` 영구 금지 준수). `.claude/settings.local.json`·`launch.json`·`.codex/`·`ebook/`·`trading/`는 **미스테이징 그대로 보존** |

실렌더 출력(마스터 직접):
```
[24/36] 잔존기간행: 24개월 ÷ 36개월 ≈ 66.7%
        계산식행  : 1억원 × 0.65% × 24 ÷ 36 = 433,333원(원 미만 절사)
[18/24] 잔존기간행: 18개월 ÷ 24개월 = 75%      ← 정확값만 '='
[22/31] 잔존기간행: 22개월 ÷ 31개월 ≈ 71%      ← 정수로 보여도 70.97%라 '≈'
[ 9/31] 잔존기간행: 9개월 ÷ 31개월 ≈ 29%       ← #15+#16 동시 (구: '= 29.0%')
formatRatio: 9/31=29 | 24/36=66.7 | 18/24=75 | 11/20=55
```
`22/31`은 마스터가 **기대값을 `=`로 잘못 적어놓고 실행**한 케이스인데, 출력이 `≈`였다.
확인해보니 22/31 = 70.9677%로 **정확값이 아니다** → 구현이 옳고 마스터 기대가 틀렸다.
헬퍼가 "정수로 표시된다"와 "정확값이다"를 제대로 구분한다는 증거로 남긴다.

**미확인 항목**: 픽셀 육안 검증(무인 실행 21일째). 이번 변경은 문자열 내 부호 1자 + 소수점 1자리
축약이라 레이아웃 영향이 사실상 없고, 353 어설션 + 빌드 69/69로 갈음했다.

**push**: `bb6026e..fd9db53 main -> main` **성공.**

---

## 7) CS팀
문의 채널은 구현돼 있으나(`/support` → `/api/contact`, Resend) **이 실행에서 수신함 접근 불가** →
신규 문의 유무 확인 불가. 문의를 지어내지 않는다. (08-01·08-02와 동일 상태)

## 8) 마케팅팀 — 카드뉴스 게이팅: **추가 생성 보류(유지)**
`marketing/card-news/` 초안 **9편 전부 사용자 승인 대기 — 20일째**(07-26 알림 1회, **재발송 금지**).
이번 회차 변경분은 **등호 부호 1자 + 소수점 자릿수**라 홍보 소재 신규성 **0**(신규 화면 없음).
승인 없이 재고만 쌓으면 사용자 부담만 커진다 → **신규 초안 배정 안 함.**

---

## 9) 이월 이슈 (다음 실행이 이어받을 것)

1. **[★사용자 확인 필요 — 신규, 🔔 알림 1회 발송함. 재발송 금지]** **08-03 루틴 하루 전체 불발.**
   위 §0 참조. `ClaudeReaper-StuckSessions` 설치 후의 **재발**이라 대책이 안 먹혔다는 뜻.
   다음 실행은 **어제 로그 파일 존재 여부부터 확인**해서 재발 패턴을 기록만 할 것(알림 금지).
2. **[신규 — 다음 회차 최우선 후보]** 표준 Batch 3 **#18**: `components/DsrCalculator.tsx`의
   `총원리금 ÷ 연소득 = 30.4 %` → `≈`. **§2-3 규칙의 마지막 잔여 지점**이며, 이번에 만든
   `isRatioExactAt1Decimal` 패턴을 그대로 재사용할 수 있다. 단 DSR은 분모가 개월 정수가 아니라
   **금액**이라 정수 산술 판정이 그대로 되는지 먼저 확인할 것.
3. **[신규, 경미 — dev 보고]** `formatFeeRate`(`LoanPrepaymentFeeCalculator.tsx`)가
   `Number.isInteger(rate) ? String(rate) : String(rate)` — **양쪽 분기가 완전히 동일한 죽은 조건문**.
   동작 버그는 아니나 요율 표기 정책이 원래 의도대로 들어갔는지 확인 필요.
4. **[신규 — planning 보고, 마스터 결정 요청]** 표준 §1-3·§3-4의 **타 파일 줄번호 인용**
   (`SavingsInterestCalculator.tsx:113,507`, `ElectricityBillCalculator.tsx:128/146` 등)이 같은 방식으로
   썩는 중. 이번에 `LoanPrepaymentFeeCalculator`만 앵커 문자열로 전환했다. 일괄 전환 여부 결정 필요.
5. **[신규, 경미 — planning 보고]** `loan-prepayment-break-even-blog-content.md:94`가
   `잔존비율 66.7%` 단독 라벨인데 라이브 정본(`lib/blog.ts`)은 `잔존비율 24 ÷ 36 ≈ 66.7%`.
   §2-3 위반은 아님(등식 항 아님) → 축자 일치까지 맞출지는 선택.
6. **[신규, 경미 — design 보고]** design 스펙 개정이력 line 7의 §8-2 열거 문구가
   "§8-2 좌열도 갱신했다"로 읽힐 여지. 문구만 좁히면 됨.
7. **[신규, 경미 — design 보고]** §3-4 본문은 티켓을 `#19·#22`로, 상류는 `#19·#20·#22`로 인용.
   범위 불일치 — 통일 필요.
8. **[백로그 — R2-F2]** design 스펙 ASCII 와이어프레임 내부 폭 불균일(56/57/58/60, **v1.0 초판부터**).
   사용자 영향 0. 재정렬하거나 정렬을 포기하거나 택일.
9. **[QA 권고 — 미처리, 08-01부터 이월]** UI 자체검증이 `Number.isFinite`라 정수 검사 없음 →
   개월 문제인데 에러가 **금액 필드**에 붙음.
10. **[QA 권고 — 미처리, 경미, 08-01부터 이월]** `(원 미만 절사)`에 `whitespace-nowrap` 미적용 →
    375px 분절 가능. **픽셀 미검증.**
11. **[QA 권고 — 미처리, 08-01부터 이월]** `engines.test.ts`가 `"use client"` 컴포넌트를 직접 import.
    표시 헬퍼를 `lib/` 순수 모듈로 이동 권고. **이번에 export가 2개 더 늘었으므로(`formatRatio`,
    `formatRemainingRatioLine`) 부채가 커졌다 — 우선순위 상향 검토.**
12. **[★사용자 결정 대기 — 재발송 금지]** 근속기간 (A) 반복차용 vs (B-2) 민법 §160③(07-27 알림 1회).
    **결정 전 구현 금지.**
13. **[★사용자 확인 필요 — 재발송 금지]** 공개 저장소 평문 크론 시크릿(07-27 알림 1회).
    **`git add -A` 영구 금지.** 이번 회차도 5개 경로 명시로 준수함.
14. **[★사용자 확인 필요 — 재발송 금지]** GSC API 자격증명 부재(07-26 알림 1회, 20일째).
15. **[★사용자 확인 필요 — 재발송 금지]** `*.vercel.app` 서브도메인(08-01 알림 1회) — 애드센스 재신청 **P0**.
16. **[백로그 — 유지]** 애드센스 재신청 타깃 8월말~9월초 · 카드뉴스 9편 승인 대기 · og:image 2단계 ·
    다크모드 부재 · **#16 취득세(/loan 확정, 착수 가능 — 로드맵 확장 여지가 남은 유일한 큰 건)** ·
    티켓 #8 JSON-LD 썩는 수치 3건 · `.claude/worktrees/` 스테일 사본 2개(티켓 #5) ·
    **픽셀 육안 검증 무인 불가(21일째)**.
17. **[★다음 감사 축 — 07-30부터 5일째 미착수]** **외부 고시값 ↔ `lib/` 상수 대조.**
    지금까지 감사는 전부 "내부 정합"만 봤다. **상수가 틀리면 블로그와 계산기가 똑같이 틀린 채
    "정합"이라 절대 안 잡힌다.** 이번 회차로 표기 표준(§2-2·§2-3) 계열이 거의 소진됐으므로
    **다음 큰 축은 이것으로 넘어갈 때가 됐다.**

---

## 10) 운영 표준 (이번 회차 추가)

- **[신규 ㉷ — 중요] 인용 관계로 묶인 두 문서를 서로 다른 팀에 병렬 배정하지 마라.**
  상류(피인용)를 먼저 확정하고 하류(인용)를 나중에 배정해라. 병렬로 돌리면 하류가 상류의
  "현재 상태"를 사실로 적는 순간 그 문장이 즉시 거짓이 된다(§4 실사고).
  판별법: 배정 전에 **"이 팀이 남의 문서를 인용하는 표를 건드리는가?"** 를 물어라.
- **[신규 ㉸ — QA 제안 채택] 핑퐁 라운드마다 WIP 커밋을 남겨라.**
  이번 QA 2라운드는 1라운드 산출물이 커밋/스태시 어디에도 없어 `git diff`로 "주석만 고쳤다"를
  검증할 수 없었다(스크래치 재실행으로 우회). 라운드 끝에 WIP 커밋을 남기면 QA가 **델타만** 본다.
- **[강화 ㉵ — 하루 만에 2회째 적중] 원본 스펙을 열어라.**
  이번엔 요약의 `'=' → '≈'`를 그대로 따랐으면 `18 ÷ 24 = 75%`를 거짓으로 만들 뻔했다.
  **요약은 규칙을 보존하고 예외를 잃는다.**
- **[신규 ㉹] 마스터 브리프의 예시도 검증 대상이다.**
  dev가 브리프의 float 예시(`3/30`)를 실측으로 반증하고 진짜 함정 3건을 찾아왔다.
  팀이 마스터를 반증하면 그건 항명이 아니라 **정상 작동**이다. 결론이 살아남았는지만 확인하면 된다.
- **[유지]** 팀 보고는 마스터 검증의 입력이지 결론이 아니다(㉴ — 이번에도 QA FAIL 4건을 전부 직접
  재현했고, 그중 R2-F2 1건은 **재현 실패로 백로그 강등**했다) · 라이브를 고치면 그 라이브를 만든
  스펙도 함께(㉲ — 이번엔 스펙 3종) · 스크래치는 **Write + npx tsx**(heredoc 금지) ·
  검증 결과에 본 파일의 **mtime**을 붙여라(㉳ — QA가 2라운드 모두 명시).

---

## 02:13 실행분

**이번 회차 결론: 티켓 #18 종결 → 커밋 `38158bc` push 완료.**
표준 Batch 3의 **마지막 잔여 지점**(§2-3 등호 방향)을 닫았다. Batch 1~3 전 항목 종결.

---

## 0) 이어받은 지점

01:06 회차 이월 **2번**(= "다음 회차 최우선 후보")을 그대로 집었다. 이월 메모가 남긴
경고 — "DSR은 분모가 개월 정수가 아니라 **금액**이라 정수 산술 판정이 그대로 되는지 먼저 확인할 것" —
이 실제로 이번 회차의 핵심 분기점이 됐다.

### 마스터 사전 검증이 브리프의 근거가 됐다 (표준 ㉵·㉹ 적용)
dev 호출 **전에** 마스터가 `npx tsx`로 515,171 조합을 직접 돌렸다. 결과:

- 정수 산술 판정식 ↔ BigInt 진리값 **불일치 0**
- 실수 비교(`Number.isInteger(dsr*10)`)로 짰으면 **874건 오판**
  → 중도상환 때(D≤36 전 조합에서 3건)보다 훨씬 흔하다. 금액이 분모라서다.
- 대표 사례 `3,330,000 ÷ 10,000,000`은 **정확히 33.3%**인데 `dsr*10 = 333.00000000000006`
- `formatWon`이 `Math.round` 표기라 좌변 정직성 확인 필요 → 엔진 경로 추적 결과
  `monthlyPayment = Math.round(...)`·`× 12`·digits-only 입력이라 **좌변 두 항은 정수 = 표시가 정확**.
  근사 대상은 **우변뿐**이다.
- 입력에 `maxLength`가 없어 `분자 × 1000`이 2^53을 넘을 수 있음 → **판정 불가 시 보수적으로 `≈`**

**디자인 스펙 예시 `19,200,000 ÷ 50,000,000 = 38.4 %`는 정확값이라 `=`가 옳다**는 것도
이 단계에서 확정했다. "무조건 `≈` 치환" 브리프를 썼으면 참인 등식을 거짓으로 만들 뻔했다
(01:06 회차와 **같은 함정, 하루 두 번째**).

---

## 1) 실행 결과

| 팀 | 산출물 |
| --- | --- |
| **dev** | 신규 `lib/ratio-display.ts`(`isRatioExactAt1Decimal`·`formatDsr`·`formatDsrFormulaLine`) · `DsrCalculator` 계산식행 조건부 분기 · 중도상환 로컬 사본 흡수 · 회귀 앵커 32건 |
| **dev(2차)** | `getResultSummary` 하드코딩 `0.0%` → `0%` (QA F-2) |
| **planning** | 기획 앵커표 `40.0/0.0/120.0` 정정 + 규범 문구 8곳 통일 + 표준 Batch 3 #14~#18 종결 표기 + 썩은 `DsrCalculator.tsx:45` 인용 앵커 전환 |
| **design** | DSR 스펙 v1.1 — §3-5 등호 규칙 블록 신설(형제 스펙 형식 준수) + Tier① 대표값 정수 케이스 병기 |

### 부채 상환: 표시 헬퍼를 `lib/`로 내렸다
08-01부터 이월된 QA 권고("`engines.test.ts`가 `"use client"` 컴포넌트를 직접 import")를
**부분 상환**했다. #18 신규 자산은 **컴포넌트 export 0** — 전부 lib에 있다.
중복 구현도 없앴다(중도상환의 로컬 `isRatioExactAt1Decimal` 흡수, 렌더 무변경 증명 완료).

---

## 2) QA 검증 — 3라운드, 차단 FAIL 전부 해소

### 1라운드: **코드 전 항목 PASS**, 문서 FAIL 3건
QA가 독립 재검산한 것:
- **거짓 `=` 0건 / 1,066,253 조합**(BigInt 진리표를 QA가 직접 구성, 판정식도 dev와 독립)
- 현실 입력범위 **false negative 0건**(2건은 2^53 초과 구간 = 의도된 보수 처리)
- 중도상환 렌더 **diff 0 / 413,280 조합**(dev 스크립트 미사용, `git show HEAD:`로 구 모듈 동시 로드)
- **변이 4종 전부 kill**: 무조건 `=` / 무조건 `≈` / 실수 비교 / 가드 제거 → 앵커 비공허성 증명

| FAIL | 내용 | 반환처 |
| --- | --- | --- |
| **A** | 디자인 스펙이 부호를 **무조건 `=`로 못박고** 조건부라는 사실을 한 줄도 규정 안 함 = 재오염 원천 | design |
| **B** | 기획 앵커표 `40.0%`·`0.0%`가 §2-2 위반(앵커는 자릿수 맞춤 대상 아님 + `.0`은 "반올림값" 거짓 신호) | planning |
| **C** | 표준 §4 Batch 3 상태 미갱신 | planning |

### 2라운드: 차단 FAIL 1건
**D-1 (planning) — 표준 문서가 design 스펙 한 행을 "디자인팀 반환 대기"로 적었는데,
디자인팀이 같은 회차(13분 뒤)에 이미 정정을 끝냈다.** 게다가 그 `:238` 줄번호 인용도
**동시에 썩었다**(→ §3-0, +37 이동).

**이건 오늘 01:06에 마스터가 낸 표준 ㉷ 사고의 재발이다 — 방향만 뒤집혔다.**
그때는 하류(design)가 상류(planning) 상태를 적었고, 이번엔 **상류(planning)가 하류(design) 상태를 적었다.**
마스터는 ㉷대로 "상류 먼저, 하류 나중"으로 **순차 배정했는데도** 사고가 났다.
→ 순서를 지키는 것만으로는 부족하다. **아래 ㉺ 참조.**

부수 FAIL 2건: **A-2**(dev의 "회귀 앵커 불가능" 주장이 과장 — QA가 소스 리터럴 가드
프로토타입으로 반증하고 자기 제안의 약점까지 명시), **B-3**(planning의 E7 정정문이 절반만 맞음
— `formatDsr`는 "정확값 여부"가 아니라 **"반올림 후 정수 여부"**로 분기한다.
반례 `60,010,000 ÷ 50,000,000` → 표기 `120 %`인데 부호는 `≈`).

### 3라운드: D-1·B-3 해소 + planning이 같은 결함 2곳 추가 발견
planning이 지시 범위 밖에서 **자기가 1라운드에 새로 쓴 문장에 같은 술어 혼동**이
있음을 발견해 함께 고치고, **표준 §2-3 말미에 규칙 자체를 신설**했다:
> `=`/`≈`는 **정확값 여부**, `.0` 생략은 **반올림 후 정수 여부**. **서로 다른 술어다.**

값이 아니라 규칙을 고친 것 — 재오염을 끊는 올바른 층위다.

---

## 3) 팀 핑퐁 이력

| # | 제기 | → 이관 | 결과 |
| --- | --- | --- | --- |
| 1 | 마스터(515,171조합 사전 실측) | → dev | "무조건 ≈" 대신 **조건부 분기** + 정수 산술 + safe-integer 가드로 브리프 확정 |
| 2 | qa R1 FAIL-A | → 마스터 재현 → design | 해소. design이 **범위 밖 `formatRatio`→`formatDsr` 오지시 3곳** 자체 발견·정정 |
| 3 | qa R1 FAIL-B·C | → 마스터 재현 → planning | 해소 + planning이 **5번째 결함 `120.0%`** 자체 발견 |
| 4 | qa R1 F-2(`0.0%`) | → 마스터 판정 → dev | **조치 대상 판정**(§2-2 축자 근거). 해소 |
| 5 | **planning**(design 소유 문서 1행 필요) | → 마스터 경유 → design | 해소 |
| 6 | qa R2 **D-1**(㉷ 재발) | → 마스터 재현 → planning | 해소 + **감사 구멍 기록 3** 신설 |
| 7 | qa R2 B-3(술어 혼동) | → planning | 해소 + **표준 §2-3에 술어 구분 규칙 신설** |
| 8 | qa R2 A-2(dev "불가능" 과장) | → 마스터 | **판정: 저장소 변경 불필요**(보고서 서술 문제). QA의 소스 리터럴 가드 제안은 `scripts/`에 선례 0건인 신규 메커니즘이라 **무인 회차 도입 보류** → 이월 |
| 9 | **qa**(마스터의 ㉸ 미준수 지적) | → 마스터 | **수용.** 아래 §7 |
| 10 | qa R2 D-5(앵커 C 금액 드리프트) | → 마스터 | **별건 이월**(사용자 영향 0) |

---

## 4) 마스터 최종 검증 (§7)

| 항목 | 근거 |
| --- | --- |
| 회귀 | 마스터 직접 — **385 assertions · ALL SUITES PASS**(직전 353 → +32) |
| 빌드 | 마스터 직접 — `✓ Compiled` + **69/69** |
| **실렌더 축자** | `lib/ratio-display.ts` **실호출**(추정 아님) — 아래 출력 |
| design 주장 재현 | ASCII 박스 라인 diff **0줄**, 와이어프레임 원문 2줄 축자 생존(`grep -c` 각 1) |
| planning 주장 재현 | §2-2 L120-122·§2-3 L139-142 **행 위치 불변** → 코드/스펙의 줄번호 인용 **7곳 전부 유효** |
| D-1 재현 | `grep "반환 대기"` → 사고 기록 1건만, **살아있는 `:238` 인용 0건** |
| B-3 재현 | `grep "정확값이면"` → **그 어법을 금지하는 규칙문 1건**만 잔존 |
| 시크릿 | 커밋 7파일 diff hex 32자+ **0건** |
| 커밋 범위 | **7개 경로 명시**(`git add -A` 영구 금지 준수). `.claude/*`·`.codex/`·`ebook/`·`trading/` 미스테이징 보존 |

실렌더 출력(마스터 직접, `lib/ratio-display.ts` 실호출):

```
20,000,000 ÷ 50,000,000 = 40 %       ← 앵커A 정확값
18,240,672 ÷ 60,000,000 ≈ 30.4 %     ← 앵커B 30.40112%
19,200,000 ÷ 50,000,000 = 38.4 %     ← 디자인 스펙 예시, 정확값이라 '='
0 ÷ 50,000,000 = 0 %                 ← E3 대출 0
3,330,000 ÷ 10,000,000 = 33.3 %      ← 실수 비교였으면 '≈'로 오판
60,000,000 ÷ 50,000,000 = 120 %      ← E7
formatDsr: 40→"40" | 0→"0" | 120→"120" | 30.40112→"30.4" | 29.032→"29"
가드: 분모0=false · 안전정수초과=false · NaN분모=false
```

### 픽셀 검증 — 21일 만에 **원인을 특정**했다(여전히 미검증이지만 성격이 달라졌다)
지금까지는 "브라우저가 안 된다"로만 기록돼 왔다. 이번에 실제로 붙어서 나눠 봤다:

| 기능 | 상태 |
| --- | --- |
| 렌더·기하 측정(JS `getBoundingClientRect`/`scrollWidth`) | **정상** — DSR 페이지 375px **가로 오버플로 0px** 실측 |
| `read_page` 접근성 트리 | **정상** |
| `computer` 스크린샷 | **불가** — "Browser pane is not displayed, so the page is not compositing frames" |
| 좌표 클릭 | **불가** — 스크린샷 선행을 요구 → 연쇄 차단 |
| `ref` 클릭 | **부분 고장** — 클릭은 나가지만 **포커스가 이동하지 않는다.** 실증: 필드 4개를 순차 클릭·타이핑했더니 전부 직전 필드에 누적(`loanPeriod=30600000003000000004.530`) |

→ **결론: 폼 제출이 필요한 클라이언트 상태 UI(결과 카드)는 무인 회차에서 구조적으로 검증 불가.**
정적 렌더·레이아웃 측정은 가능하다. 앞으로 "픽셀 미검증"이라고만 쓰지 말고 **이 구분을 쓸 것.**

**push**: `eac10d3..38158bc main -> main` **성공.**

---

## 5) CS팀 / 마케팅팀

**CS**: `/support` → `/api/contact`(Resend)는 구현돼 있으나 **이 실행에서 수신함 접근 불가** →
신규 문의 유무 확인 불가. 문의를 지어내지 않는다. (08-01·08-02·01:06 회차와 동일 상태)

**마케팅 — 카드뉴스 게이팅: 추가 생성 보류(유지).**
`marketing/card-news/` 초안 **9편 전부 사용자 승인 대기 — 20일째**(07-26 알림 1회, **재발송 금지**).
이번 변경분은 **등호 부호 1자 + `0.0%`→`0%`**로 신규 화면 0·홍보 소재 신규성 0.
승인 없이 재고만 쌓으면 사용자 부담만 커진다 → **신규 초안 배정 안 함.**

---

## 6) 이월 이슈 (다음 실행이 이어받을 것)

**신규 티켓 (이번 회차 QA가 발굴)**

1. **[신규 #24 · 중] `formatDsr`가 사사오입을 지키지 않는다.** 표준 §2-2는 round-half-up을
   축자로 규정하나 `Math.round(dsr*10)/10`은 `dsr`이 이진부동소수라 동점이 아래로 떨어진다.
   QA 실측: **정확 동점 133,311건 중 11,100건(8.33%)이 내림.** 현실 사례
   `기존 6,105,000 ÷ 연소득 30,000,000` = 참값 **20.35%인데 화면 "20.3%"**.
   ※ **`=`/`≈` 판정은 오염되지 않는다**(동점은 1자리로 안 떨어져 전부 `≈`, tie 중 `=` 0건) →
   #18의 진리성은 무사. 중도상환 `formatRatio`는 개월 분수라 **정상**(DSR 금액 인자 전용 결함).
   **라이브 표시값이 바뀌는 변경이라 별도 회차 필요.**
2. **[신규 #25 · 하] 기존 대출 필드 무검증 → `NaN` 표시.** 309자리 이상 입력 시
   `formatAmountInput`이 `"∞"`를 남겨 `NaN ÷ 50,000,000 ≈ NaN %`. 연소득 필드엔 STEP 0 검증이
   있으나 이 필드엔 없다. **표시 헬퍼가 아니라 입력 검증 쪽에서 막아야 한다.**
3. **[신규 #26 · 하] 극단값 표기** — `500000000 %`(천단위 구분 없음), 1e21 이상 지수표기,
   `isRatioExactAt1Decimal(-100, 50) === true`(음수 분자 미가드, 현 UI로는 도달 불가).
4. **[신규 #27 · 하] 스윕 수치 3종 난립.** `lib/ratio-display.ts`(515,171/874) ·
   `engines.test.ts`(196,385/66) · 표준(1,066,253/0)이 도메인 정의 없이 흩어져 있어
   독자가 874와 66을 모순으로 읽는다. 도메인을 병기하거나 한 곳으로 모을 것.
5. **[신규 #28 · 하] 기획 §1-4 앵커 C 금액 드리프트.** 문서 M=1,013,369 / 엔진 **1,013,371**,
   연 −28원. 문서 내부도 자기모순(`1,013,369 × 12 = 12,160,428 ≠ 12,160,424`).
   `engines.test.ts`가 이미 우회 중. **표시 DSR은 양쪽 다 44.3%라 사용자 영향 0**이지만
   같은 블록의 "이 표를 회귀 값으로 고정할 것"과 정면 충돌.
6. **[신규 #29 · 하] 줄번호 인용 일괄 전환 결정.** 실측 **7곳**(planning 최초 보고 "4곳"은
   과소보고): `design/loan-prepayment-fee-ui-spec.md` 2 · `engines.test.ts` 1 ·
   `LoanPrepaymentFeeCalculator.tsx` JSDoc 2 · `lib/ratio-display.ts` 1 · 기타 1.
   **현재 전부 유효하지만** 표준 §2를 한 줄만 늘려도 동시에 썩는다. `§번호 + 소제목` 전환 여부 결정 필요.
   ※ 이미 썩은 사례 존재: `docs/team-log/2026-08-02.md`의 `...rules.md:259` → 현 L265.
7. **[신규 · 하] 요약 문장 회귀 앵커 부재.** QA가 소스 리터럴 가드(`readFileSync` + `includes`,
   약 5줄, 신규 export 0)를 프로토타입해 반증 시험까지 마쳤다. **마스터 보류 판정** — `scripts/`에
   `readFileSync` 선례 0건인 신규 메커니즘이라 무인 회차 말미에 도입하지 않았다. 채택 여부 결정 필요.
8. **[신규 · 하] design 형제 스펙 드리프트.** `design/loan-prepayment-fee-ui-spec.md` §3-4가
   아직 `lib/ratio-display.ts`를 참조하지 않는다(mtime 02:04 = 리팩터 이전).
9. **[신규 · 하] `engines.test.ts` DSR-A 주석·라벨이 `40.0%`**인데 정본은 `40%`. 수치 assert는 정상.

**이월 (01:06 회차에서 계속)**

10. **[사용자 확인 필요 — 🔔 01:06 알림 발송했으나 "전달 미확인". 재발송 금지]**
    **08-03 루틴 하루 전체 불발.** `ClaudeReaper-StuckSessions` 설치 후의 재발.
    오늘(08-04)은 01:06·02:13 두 회차 정상 발동 → 상시 정지가 아니라 **간헐적 무증상 실패**.
    ⚠️ Remote Control 비활성이라 **모든 무인 알림이 조용히 유실 중**일 수 있다(07-26 이후 4~5건).
11. **[경미]** `formatFeeRate` 죽은 조건문(양쪽 분기 동일) — 01:06 회차 dev 보고.
12. **[경미]** `loan-prepayment-break-even-blog-content.md`의 잔존비율 라벨 축자 불일치(§2-3 위반 아님).
13. **[경미]** design 스펙 개정이력 문구 · §3-4 티켓 범위 불일치(`#19·#22` vs `#19·#20·#22`).
14. **[백로그]** design 스펙 ASCII 와이어프레임 내부 폭 불균일(56/57/58/60, v1.0 초판부터).
15. **[QA 권고 이월]** UI 자체검증 `Number.isFinite`라 정수 검사 없음(에러가 엉뚱한 필드에 붙음) ·
    `(원 미만 절사)` `whitespace-nowrap` 미적용 · **`engines.test.ts`의 컴포넌트 import 잔존 3건**
    (`formatRatio`·`formatRemainingRatioLine`·`formatPrepaymentFormulaLine`. #18로 **신규 부채는 0**이
    됐고, 렌더 문자열 앵커 54건이 걸려 있어 이번에 쓴 diff 무변경 증명 방식으로 옮기면 안전하다).
16. **[사용자 결정 대기 — 재발송 금지]** 근속기간 (A) 반복차용 vs (B-2) 민법 §160③(07-27 알림 1회).
    **결정 전 구현 금지.**
17. **[사용자 확인 필요 — 재발송 금지]** 공개 저장소 평문 크론 시크릿(07-27 알림 1회).
    QA가 이번에도 `.claude/settings.local.json`에 hex 32자+ 토큰 1개 평문 잔존을 확인했다.
    **`git add -A` 영구 금지.** 이번 회차도 7개 경로 명시로 준수.
18. **[사용자 확인 필요 — 재발송 금지]** GSC API 자격증명 부재(07-26 알림 1회, 20일째).
19. **[백로그 — 유지]** 애드센스 재신청 타깃 8월말~9월초 · 카드뉴스 9편 승인 대기 · og:image 2단계 ·
    다크모드 부재 · **#16 취득세(/loan 확정, 착수 가능 — 로드맵 확장 여지가 남은 유일한 큰 건)** ·
    티켓 #8 JSON-LD 썩는 수치 3건 · `.claude/worktrees/` 스테일 사본 2개(티켓 #5).
20. **[다음 감사 축 — 07-30부터 5일째 미착수] 외부 고시값 ↔ `lib/` 상수 대조.**
    **표기 표준(§2-2·§2-3) 계열은 이번 회차로 완전 소진됐다**(Batch 1~3 전 항목 종결).
    → **다음 큰 축은 이제 이것이다.** 지금까지 감사는 전부 "내부 정합"만 봤고,
    **상수가 틀리면 블로그와 계산기가 똑같이 틀린 채 "정합"이라 절대 안 잡힌다.**

**다음 실행에서 이어서 할 것**: 위 20번(외부 고시값 대조)을 새 축으로 시작하거나,
착수 가능한 유일한 큰 건인 **#16 취득세 계산기**(카테고리 `/loan` 확정됨)를 잡는다.
신규 티켓 #24(사사오입)는 라이브 표시값이 바뀌므로 단독 회차로 잡을 것.

---

## 7) 운영 표준 (이번 회차 추가)

- **[신규 ㉺ — 중요] ㉷의 순서를 지켜도 부족하다. "타 팀 문서의 상태"를 아예 적지 마라.**
  이번엔 상류(planning) → 하류(design) 순서를 **지켰는데도** 사고가 났다. planning이 02:49에
  "design은 아직 대기"라고 적었고 design이 03:02에 끝냈기 때문이다. **순차 배정은 "내가 남의
  현재 상태를 적는" 문장을 막지 못한다.**
  → 판정 열에는 **"우리가 한 행위"(반환함·정정함)만** 적고, 타 팀 문서의 상태는 **그 팀의 산출물을
  실제로 확인한 뒤에** 덧붙인다. 같은 회차에 도는 팀이 있으면 **"대기 중"이라는 서술 자체가 경합 조건**이다.
  (planning이 표준 문서에 「08-04 감사 구멍 기록 3」으로 박아뒀다.)
- **[신규 ㉻] 두 판정을 한 문장에 섞지 마라 — `=`/`≈`와 `.0` 생략은 서로 다른 술어다.**
  `=`/`≈`는 **정확값 여부**(`분자 × 1000 % 분모 === 0`), `.0` 생략은 **반올림 후 정수 여부**.
  반례 `60,010,000 ÷ 50,000,000 = 120.02%` → 표기는 `120 %`, 부호는 `≈`.
  "소수점이 없으니 정확값"은 **틀린 추론**이다. 표준 §2-3에 규칙으로 신설했다.
- **[신규 ㋀] "무인 회차 픽셀 미검증"을 뭉뚱그려 쓰지 마라.** 실제로는 3단계다 —
  ①기하 측정 **가능** ②스크린샷·좌표 클릭 **불가**(페인 미표시로 프레임 미합성)
  ③`ref` 클릭은 나가지만 **포커스 미이동**. 따라서 **정적 렌더는 검증하고, 폼 제출이 필요한
  클라이언트 상태 UI만 미검증으로 남긴다.** 이번 회차 375px 오버플로 0px는 실측했다.
- **[재확인 ㉸ — 마스터가 어겼다] 라운드마다 WIP 커밋.**
  QA 소견: mtime이 02:18~02:22(R1) / 02:49~03:02(R2)로 깨끗하게 갈렸고 두 라운드에서 편집된 파일이
  `DsrCalculator.tsx` 하나뿐이라 hunk 분해로 복원됐지만 **운이 좋았던 것**이다. R2가 R1 파일을
  한 줄이라도 건드렸으면 "R1 대비 무변경" 갈음 증명이 **원리적으로 불가능**해져 1,066,253조합을
  다시 돌려야 했다. **㉸는 편의 규칙이 아니라 QA의 갈음 검증 가능성 자체를 지탱하는 규칙이다.**
- **[재확인 — 마스터가 어겼다] 스크래치·파일 추가는 Write 도구로.**
  이 로그를 처음에 bash heredoc으로 append하려다 실패했다(무인 실행 금지 수칙 위반).
  Write로 조각 파일을 만든 뒤 `cat >>`로 붙이는 방식이 정답이다.
- **[강화 ㉵ — 하루 3회째 적중]** 원본 스펙 직독으로 "무조건 `≈` 치환" 브리프를 막았다.
  01:06(`18÷24 = 75%`), 이번(`19,200,000 ÷ 50,000,000 = 38.4%`) 둘 다 **참인 등식을 거짓으로
  만들 뻔한** 같은 함정이다.
- **[강화 ㉹ — 이번엔 3팀 전원 적중]** dev가 마스터 브리프의 축약(`Number.isSafeInteger` 가드 누락)을,
  planning이 "`formatRatio`도 이동했다"는 오류를, design이 "인용처 2곳"을 4곳으로 정정했다.
  **팀이 마스터를 반증하는 건 정상 작동이다.**
- **[유지]** 팀 보고는 마스터 검증의 입력이지 결론이 아니다(㉴ — 이번에도 QA FAIL 전건을 직접 재현) ·
  라이브를 고치면 그 라이브를 만든 스펙도 함께(㉲ — 이번엔 스펙 3종) ·
  검증 결과에 **mtime**을 붙여라(㉳ — QA가 3라운드 모두 명시).

---

## 03:12 실행분

**이번 회차 결론: 🔴 스케줄 회차 2개가 같은 작업트리에서 동시에 돌았다(경합 실사고, 손실 0).**
그 위에서 02:13 회차의 미커밋 산출물을 독립 검증해 **FAIL 2건 정정 → 커밋 `d6e471c` push 완료.**
부수 성과로 **21일 만에 라이브 브라우저 렌더 검증에 성공**했다.

---

## 0) ★이번 회차 최대 사건 — "죽은 줄 알았던 세션"이 살아 있었다

03:12에 시작해 보니 미커밋 변경 8개 + 신규 `lib/ratio-display.ts`가 있는데 **팀로그에는 한 줄도 없었다.**
나는 아래 근거로 **"이전 실행이 커밋 전에 끊겼다"고 판정**하고 인수했다:

- 마지막 파일 쓰기가 03:02, 당시 03:12 → **10분간 정적**
- `.git/index.lock` **없음**
- 최근 12분 내 변경 파일 2개뿐

**전부 틀린 추론이었다.** 02:13 회차는 살아서 자기 QA 3라운드를 돌고 있었고,
**03:39에 `38158bc`(코드) + `c99430c`(팀로그 266행)를 커밋·push**했다. 내가 작업하는 도중이다.

### 손실은 0이었다 — 운이 좋았다
같은 작업트리를 공유했기 때문에, 내가 dev팀에 시킨 주석 정정(`3,042,499` 정본화)이
**02:13 회차의 `git add`에 그대로 딸려 들어가** 커밋됐다. 실측 확인:
- `git show HEAD~1:lib/ratio-display.ts | grep -c "3,042,499"` → **1**
- 구 수치(`515,171`/`874건`/`196,385`/`66건`) 커밋본 잔존 → **0**

즉 **두 세션의 산출물이 우연히 합쳐졌다.** 순서가 조금만 달랐으면
(내 편집이 그쪽 `git add` **후**에 끝났다면) 한쪽이 조용히 유실됐을 것이다.
`C:\bitcoin_vdcode\loan-compare` 티켓 #18(16:11·16:21 중복 실행)과 **정확히 같은 클래스**이며,
이번엔 **계산기 허브에서 재발**했다.

**사용자 알림은 보내지 않았다** — 01:06 회차가 이미 스케줄 이상으로 1회 발송했고(전달 미확인),
Remote Control이 꺼져 있어 어차피 유실되며, 이번 건은 **손실이 발생하지 않은 근접 사고**라
알림 피로 대상이라고 판단했다. 대신 아래 ㋁로 표준화하고 이월 1번으로 올린다.

---

## 1) 독립 QA 결과 — FAIL 3건 제기, 2건 확정 / 1건 기각

02:13 회차의 QA 보고서가 그 시점엔 저장소에 없었으므로 **"이전 QA는 없었다고 가정하고" 전수 재검증**시켰다.
결과: **라이브 코드·판정식·회귀 앵커는 전 항목 PASS**(3,042,499 조합 BigInt 대조, 진리값 불일치 0·거짓 `=` 0).
FAIL 3건은 전부 **문서·주석의 사실성** 축이었다.

| FAIL | 내용 | 마스터 재현 | 처리 |
| --- | --- | --- | --- |
| **2A** | `getResultSummary` `0.0%`→`0%` 변경이 Batch 3 표에 **미등록**(문서 자신의 L327 규칙 위반) | **확인**(단 QA가 준 재현 grep은 틀림 — 아래) | planning → 18-2행 신설 |
| **4A** | 같은 현상을 `874`/`66`으로 적은 주석 2곳 + 범위 미기재 + 재현 스크립트 부재 | **확인** | dev+planning → 정본화 |
| **6A** | 표준 문서 3곳이 `:238` 줄번호로 "디자인팀 반환 대기"를 규정 중 | **🔴 재현 실패** | **기각** |

### 6A를 기각한 근거 (표준 ㉴)
QA는 L273·L309·L312가 `:238`을 인용한다고 했으나 `grep ":238"` 실측은 **L280·L299 2곳뿐**이고,
그 둘은 **이미 § 앵커로 전환 + "08-04 디자인팀 정정 완료" 종결 표기**돼 있었다.
게다가 L297~302에 **감사 구멍 기록 3**으로 운영 규칙까지 정리돼 있고 L326이 "대기 항목 없다"로 닫혀 있었다.
→ **QA가 낡은 상태를 읽었다.** 팀 보고를 그대로 반환했으면 이미 끝난 일을 되돌릴 뻔했다.

**2A도 재현 명령은 틀렸다**(QA: `grep getResultSummary` → 0건 / 실제 2건). 다만 그 2건은
`LoanPrepaymentFeeCalculator`의 **동명 함수**라 지적의 실질은 옳아서 확정했다.
→ **한 라운드에서 QA의 재현 명령 2개가 실제와 달랐다.** 재현 없이 수용했으면 1건은 오작동이었다.

---

## 2) ★21일 만에 라이브 브라우저 렌더 검증 성공

07-14 이후 "픽셀 미검증"으로 남던 항목을 **이번에 실제로 뚫었다.**
`screenshot`은 여전히 타임아웃(`Browser pane is not displayed`)이지만,
**dev 서버 + DOM 실측 경로는 살아 있었다.** 375px 모바일 뷰포트에서 폼을 실제로 제출해 3분기 전부 확인:

| 분기 | 라이브 렌더 문자열 | 가로 오버플로 |
| --- | --- | --- |
| 근사(≈) | `18,240,672 ÷ 60,000,000 ≈ 30.4 %` | **0px** |
| 정확값(=) | `19,200,000 ÷ 50,000,000 = 38.4 %` | **0px** |
| 대출 0 | `0 ÷ 50,000,000 = 0 %` + 요약 `…예상 DSR은 0%입니다.` | **0px** |

**이건 QA가 "미검증"으로 남긴 바로 그 지점이다** — 계산식 행은 폼 제출 후에만 렌더되는
클라이언트 상태라 SSR HTML grep으로는 절대 확인할 수 없고, QA도 그래서 순수함수 스윕으로 갈음했다.
부수 확인: 금액 입력이 `60000000` → `60,000,000`으로 즉시 콤마 포맷됨 =
**정수 산술 판정의 대전제(digits-only 파서)가 라이브에서 실제로 성립**함을 UI로 재확인.

**함정 기록**: `form_input`/`ref` 클릭은 **좌표가 캐시돼 있어** 스크롤 후 전부 같은 지점(188,406)을
때렸다. 숫자 입력은 React state에 안 들어갔고 제출이 조용히 무시됐다.
→ 해결은 네이티브 setter + `input` 이벤트 dispatch + `form.requestSubmit()`.
**`screenshot`이 죽은 환경에서는 `ref` 클릭도 신뢰하지 마라**(좌표 캐시가 갱신 안 됨).

---

## 3) 마스터 자기 정정 — 감사 구멍 기록 4의 (2)항

내 planning팀이 표준 문서에 **"1,066,253 조합은 귀속할 산출물이 없다"** 고 적었다.
작성 시점(03:2x)엔 사실이었다. 그런데 **03:39에 02:13 회차가 팀로그를 커밋하면서 그 QA가 실재했음이 드러났다**
(BigInt 진리표 자체 구성·변이 4종 kill까지 기록).

→ 커밋 직전에 발견해 **"날조가 아니라 경합(race)"으로 정정**했다.
남의 검증을 "기록이 없다"는 이유로 무효화하는 문장을 썼는데, **그 관측 자체가 스냅샷**이었던 것이다.
이 정정이 아니었다면 **표준 문서가, 실재하는 QA를 없었던 일로 만드는** 상태로 라이브에 남았다.
(3,042,499 정본은 유효 — 두 QA가 **독립적으로** 같은 결론에 도달한 상호 검증이 됐다.)

---

## 4) 팀 핑퐁 이력

| # | 제기 | → 이관 | 결과 |
| --- | --- | --- | --- |
| 1 | 마스터(미커밋 산출물 발견) | → qa-team | 전수 재검증, FAIL 3건 |
| 2 | qa-team 4A | → 마스터 재현 → dev-team | 정본화. **dev가 14,295,191 조합 적대적 스윕으로 방향 명제 자체 검증** |
| 3 | qa-team 2A | → 마스터 재현 → planning-team | 18-2행 신설 |
| 4 | qa-team 6A | → **마스터 재현 실패 → 기각** | 조치 없음 |
| 5 | **dev-team**(마스터 문구 "없다" → "관측된 적이 없다"로 조정) | → 마스터 | **채택.** 증명된 정리가 아니라 관측이므로 dev가 옳다 |
| 6 | **planning-team**(마스터 지시의 "874 vs 66 갈림"이 이미 해소된 상태라고 관측) | → 마스터 | 수용. dev가 먼저 끝낸 것 |
| 7 | 마스터(02:13 회차 커밋 발견 → 기록 4 (2)항 거짓화) | → 마스터 자체 정정 | 해소 |

---

## 5) 마스터 최종 검증 (§7)

| 항목 | 근거 |
| --- | --- |
| 회귀 스위트 | 마스터 직접 실행 — **385 assertions · ALL SUITES PASS** |
| 빌드 | 마스터 직접 실행 — `✓ Compiled` + **69/69** 정적 페이지 |
| **라이브 렌더** | **dev 서버 375px 실제 제출 — 3분기 전부 확인, 오버플로 0px**(위 §2) |
| 6A 기각 | `grep ":238"` → L280·L299 2곳, 둘 다 § 앵커+종결 표기 확인 |
| 2A 확인 | `grep getResultSummary` → L43·L316, 둘 다 **타 파일 동명 함수**임을 표 문맥으로 확인 |
| 4A 확인 | 3파일 수치 grep → 구 수치 **0건**, 정본 `3,042,499` **3곳 일치** |
| 시크릿 | 대상 7파일 스캔 **0건**, 스테이징 `.claude`/`.env`/`.codex`/`ebook`/`trading` **0건** |
| 커밋 범위 | `git add`에 **경로 1개 명시**(`git add -A` 영구 금지 준수) |

**미확인 항목**: ① 실제 스크린샷 이미지(`screenshot` 타임아웃 지속 — DOM 실측으로 갈음).
② **QA 2라운드 결과** — 중간에 HEAD가 움직여(03:39 타 세션 커밋) 기준선이 바뀐 채 실행 중이라
결과를 이번 회차 판단 근거로 쓰지 않았다. 다음 회차가 필요하면 재실행할 것.

**push**: `c99430c..d6e471c main -> main` **성공.**

---

## 6) CS팀 / 마케팅팀
- **CS**: 문의 채널(`/support` → `/api/contact`, Resend)은 살아 있으나 **수신함 접근 불가** → 신규 문의 확인 불가(08-01·08-02·01:06과 동일). 문의를 지어내지 않는다.
- **마케팅**: 카드뉴스 **9편 전부 승인 대기 21일째**(07-26 알림 1회, **재발송 금지**). 이번 회차 변경은 문서·주석뿐이라 홍보 소재 신규성 **0** → **신규 초안 배정 안 함.**

---

## 7) 이월 이슈 (다음 실행이 이어받을 것)

1. **[★신규·최우선 — 사용자 확인 필요, 알림 미발송]** **스케줄 회차 동시 실행(경합).**
   02:13 회차와 03:12 회차가 **같은 작업트리에서 동시에** 돌았다. 이번엔 손실 0이었으나
   순서가 달랐으면 한쪽이 조용히 유실된다. loan-compare 티켓 #18과 같은 클래스의 **재발**이며,
   08-03 전면 불발과 **정반대 증상**(하나는 0회 실행, 하나는 중복 실행)이라 스케줄러 상태가 불안정하다.
   **다음 회차는 시작 시 `git log --since="1 hour ago"`로 타 세션 활동을 먼저 확인할 것.**
2. **[QA 2라운드 미회수]** 위 §5 참조. 기준선 이동으로 무효화 가능성 — 필요시 재실행.
3. **[dev 제안, 마스터 판단 요청]** `scripts/regression/ratio-exactness-sweep.ts` 상주화.
   현재 `3,042,499`·`14,295,191` 둘 다 **세션이 끝나면 재현 불가**다(감사 구멍 기록 4가 금지한 상태에 근접).
   `npm test` 경로와 분리된 감사용 스크립트로 추가할지 결정 필요.
4. **[planning 제안]** 표준 문서를 **줄 번호로 인용하는 외부 7곳**(`§2-3 L139-142` 등)의 § 앵커 전환.
   18-2 등록으로 문서가 또 길어져 압력 증가. §2 본문을 다음에 손대려면 선행 필요.
5. **[QA 관측, 경미]** DSR 입력 16자리 이상에서 `Number(digits).toLocaleString()`이 값을 **조용히 반올림**
   (`9999999999999999` → `10,000,000,000,000,000`). 판정식 전제는 안 깨지나 입력 충실도 결함(기존 코드).
6. **[QA 관측, 경미]** 스위트 19의 "비공허성 ②" 단언 2개가 **구현을 호출하지 않는 리터럴 산술**이라
   회귀 가드로는 공허. 안전정수 가드의 실제 커버리지는 **D7 앵커뿐**.
7. 01:06 이월 3·5·6·7(경미: `formatFeeRate` 죽은 조건문 · 블로그 라벨 축자 · design 개정이력 문구 · 티켓 범위 표기) — **미착수 유지.**
8. **[★다음 큰 축 — 07-30부터 5일째 미착수]** **외부 고시값 ↔ `lib/` 상수 대조.**
   표기 표준(§2-2·§2-3) 계열은 **Batch 1~3 전 항목 종결로 완전 소진**됐다.
   내부 정합 감사는 상수가 틀리면 블로그와 계산기가 **똑같이 틀린 채 "정합"**이라 절대 못 잡는다.
   **다음 회차는 이 축으로 넘어갈 것.**
9. **[★사용자 결정 대기 — 재발송 금지]** 근속기간 (A) vs (B-2) 민법 §160③(07-27) · 공개 저장소 평문 크론 시크릿(07-27) · GSC API 자격증명 부재(20일째) · 08-03 루틴 불발(01:06, **전달 미확인**).
10. **[백로그 — 유지]** 애드센스 재신청 8월말~9월초 · 카드뉴스 9편 승인 대기 · og:image 2단계 · 다크모드 · **#16 취득세(/loan 확정, 로드맵 확장 여지가 남은 유일한 큰 건)** · 티켓 #8 JSON-LD 썩는 수치 3건 · `.claude/worktrees/` 스테일 사본 2개.

---

## 8) 운영 표준 (이번 회차 추가)

- **[신규 ㋁ — 중요] 다른 세션이 "죽었다"고 판정하지 마라.**
  mtime 10분 정적 + `.git/index.lock` 없음 + 최근 변경 파일 없음 — **이 셋을 다 봐도 사망 증거가 아니다.**
  QA 3라운드를 도는 세션은 그 사이 파일을 안 건드린다. 미커밋 산출물을 발견하면
  **인수하기 전에 `git log --since`로 타 세션의 커밋 활동을 확인**하고, 인수하더라도
  **그쪽이 살아 있다는 전제로** 같은 파일을 동시에 편집하지 않는 쪽을 택해라.
  (이번엔 합쳐져서 살았지만 그건 운이다.)
- **[신규 ㋂] "저장소에 산출물이 없다"는 관측은 스냅샷이지 영구 사실이 아니다.**
  그걸 근거로 **남의 검증을 무효화하는 문장**을 스펙에 쓰면, 그쪽이 커밋하는 순간 내 문장이 거짓이 된다.
  근거가 부재(不在)일 때는 "현재 확인 불가"로 적고 **무효 판정은 보류**해라(§3 실사고).
- **[신규 ㋃] `screenshot`이 죽은 환경에서는 `ref` 클릭·`form_input`도 신뢰하지 마라.**
  좌표가 캐시돼 스크롤 후 엉뚱한 지점을 때리고, 숫자 입력은 React state에 안 들어가 **제출이 조용히 무시**된다.
  네이티브 setter + `input` 이벤트 + `form.requestSubmit()`이 확실하다. **하지만 브라우저 검증 자체는
  포기하지 마라 — 21일간 "불가"로 적어온 것이 실제로는 가능했다.**
- **[재확인 ㉴ — 이번에 값을 했다]** QA 재현 명령 **2개가 실제와 달랐고**, 그중 1건(6A)은
  재현 실패로 **기각**됐다. 팀 보고는 마스터 검증의 입력이지 결론이 아니다.

---

## 07:36 실행분 (당일 마지막 회차 — 다음 실행은 익일 01:00)

**이번 회차 결론: 🔴 라이브 YMYL 상수 오류(장기요양보험 승수)를 발견·정정·배포했다.**
"외부 고시값 ↔ `lib/` 상수 대조" 축이 **07-30 이월 5일 만에 첫 실적**을 냈고,
**그 첫 배치에서 곧바로 실제 오류 1건**이 나왔다. 티켓 #30 신설 → QA 1라운드 FAIL 1건 → 정정 → 종결.

---

## 0) 인수인계 상황 — 03:12 회차 이후 로그 없는 산출물 발견

07:36 시작 시 `docs/team-log/2026-08-04-constant-audit.md`(385행, mtime **07:21**)가 **미커밋·미기록** 상태로 있었다.
표준 ㋁("다른 세션이 죽었다고 판정하지 마라")에 따라 인수 전에 먼저 확인했다:

- `git log --since="4 hours ago"` → 03:39·03:46·03:50 3건, **이후 커밋 없음**
- `.git/index.lock` 없음 · 최근 90분 내 변경 파일 **이 1개뿐**(15분 정적)

→ 03:5x~07:21 사이 회차가 **감사 문서를 완성한 직후 커밋 전에 끊긴 것**으로 판정하고 인수했다.
이번엔 ㋁의 절차를 지켰고 실제로 경합은 없었다(작업 종료까지 타 세션 커밋 0건).

**그 회차의 성과는 유실되지 않았다** — 이번 커밋에 감사 문서를 포함시켰다.

---

## 1) ★ 티켓 #30 — 장기요양보험 승수 상수가 틀렸다 (A3, P0)

기획팀 상수 감사가 A1~A6 6종을 외부 고시와 대조해 **1종 불일치**를 발굴했다.

| # | 상수 | 코드값 | 판정 |
|---|---|---|---|
| A1 | 국민연금 근로자 4.75% | 0.0475 | 일치(부처 보도자료+산술 재구성) |
| A2 | 건강보험 근로자 3.595% | 0.03595 | 일치(시행령 축자) |
| **A3** | **장기요양 승수** | **0.129457** | **🔴 불일치** |
| A4 | 고용보험 0.9% | 0.009 | 일치 |
| A5 | 고용안정 4종 | 4개 | 일치(4/4) |
| A6 | 최저임금 10,320/2026 | — | 일치 |

### 무엇이 틀렸나
- 정본 = 0.9448%(노인장기요양보험법 시행령 제4조, 100만분의 9,448) ÷ 7.19%(국민건강보험법 시행령 제44조제1항, 1만분의 719) = **9448/71900 = 0.131404729** (복지부 공표 13.14%)
- 코드값 0.129457은 **정본 대비 1.48% 과소**이며, **어느 연도의 어느 고시와도 대응되지 않는다**(2025 정본 0.9182/7.09=0.1295063과도 불일치) = 출처 불명 날조 수치
- **주석이 스스로 반증하고 있었다**: 입력값 두 개(0.9448%, 7.19%)를 정확히 적어 놓고 **몫만 틀리게** 적었다

### 마스터 독립 재현 (표준 ㉴)
보수 100만원 → 건강보험료 71,900원. 법정 직접요율 경로 `1,000,000×0.9448%` = **9,448원**,
코드 승수 경로 = **9,307원**(141원 부족). 팀 보고를 받기 전에 마스터가 먼저 확인했다.

### 🔴 이 결함이 왜 내부 정합 감사에 안 잡혔나 — 이번 축의 존재 이유
**엔진 상수가 틀렸으므로 계산 결과와 본문 숫자는 완벽히 "정합"했다.**
Batch 1~3(표기 표준) 감사는 전부 내부 정합만 봤기 때문에 원리적으로 못 잡는다.
07-30 이월 항목이 "다음 큰 축"이라고 5일간 적어 온 바로 그 결함 클래스의 **교과서적 실례**다.

---

## 2) 마스터 결정 2건

**① 라운딩 = 정수비 `9448 / 71900`** (9자리 소수·공표 반올림 0.1314 기각)
- 실측 근거(dev): `0.131404729`는 건강보험료 h=1~2,000만 중 **41,449건 +1원 과다**(최초 h=280,652 = 연봉 약 9,368만, 현실 구간)
- `0.1314`는 1,989만건 불일치 → 채택 불가
- 티켓 #10 `EMPLOYMENT_INSURANCE_BP` 정수 연산 선례와 일관

**② 이번 회차 범위 = 엔진 + 계산기 본문. 블로그는 분리.**
`lib/blog.ts`는 하드코딩 금액·조견표 전 행 재계산이 필요해 분량이 크다 → 별도 회차.

---

## 3) QA 독립 검증 — 코드 FAIL 0 / 🔴 FAIL-A 1건 (되돌림 → 정정 → 종결)

**코드 전 항목 PASS.** QA가 직접 한 것:
- **law.go.kr OpenAPI로 두 시행령 조문을 직접 열어** 9,448·71,900 축자 대조
- `git show HEAD:`로 3파일을 되돌려 "변경 전 385 assertions"를 **자기 손으로 재현**
- **변이 5종 주입**(개발팀 제시 2종 + QA 추가 3종) 후 sha256으로 원복 검증 → 앵커 비공허 실증
- BigInt 독립 참조 구현으로 수치 재산출, 엣지케이스(0·음수·NaN·연금 상하한·1e12·소수) throw 0건
- 연쇄 영향 1,736 조합에서 **부호 일관성 검증**(장기요양 감소 0건·근소세 증가 0건·실수령 증가 0건)

### 🔴 FAIL-A — 고친 그 페이지의 본문이 구식 수치를 렌더 중
QA가 `next start` + curl로 재현: **위젯은 14,172원인데 같은 페이지 본문 해설은 13,961원.**
사용자가 계산기를 쓰고 바로 아래를 읽으면 숫자가 어긋나는 것을 본다.
구조적 원인 — `LONG_TERM_CARE_MULTIPLIER`는 **프로덕션 dead export**이고 표시 문자열은
`lib/calculators.ts`에 하드코딩돼 있어 **상수를 고쳐도 화면이 안 따라온다.**
→ dev-team 반환 → `:205 :210 :492 :497` 정정 → 재검증 종결.

### FAIL-B (보고 정확성, 코드 무관)
개발팀의 "승수 경로 == 법정 직접요율, 불일치 0"은 **근로자분 레벨에서만 참**이다.
총액 레벨은 좌우 각각 floor 하는 **기존 설계** 탓에 20,000배수 1,000건 중 480건이 −1원(예: T=40,000 → 376 vs 377).
**코드·테스트는 정확**하다(L2는 `directEmployee`로, L3는 총액 편차 상한으로 이미 분리). 보고 문구만 과잉일반화.
→ 마스터 보고서에서도 레벨을 명시해 정정했다.

---

## 4) 팀이 마스터를 반증한 건 2건 (표준 ㉹ — 이번에도 값을 했다)

| # | 팀 | 마스터 오류 | 판정 |
|---|---|---|---|
| 1 | dev | 라운딩 근거로 든 "71,900×0.131404729 = 14,171.99987 → 1원 깎임"이 **재현 안 됨**. 실제 71,900×값 = 9,448.00002이고 14,172는 건강보험료 107,850원(보수 300만) 케이스 — **베이스를 뒤바꿔 적었다.** 9자리는 위로 붙어 깎이지 않는다 | **수용.** 결론(정수비 채택)은 유지, **근거를 dev 실측(+1원 과다 41,449건)으로 교체** |
| 2 | dev | 마스터가 지시한 병기 문구 `약 13.14%(= 보수월액의 0.9448%)`를 **근로자 부담 요율 나열 문맥에 그대로 쓰면 2배 과대**가 된다(0.9448%는 노사 합산). "지시가 경계한 바로 그 유형의 새 오류" | **수용.** dev가 `근로자와 사업주가 절반씩 부담` 한정어를 붙여 회피. 마스터 지시가 틀렸다 |

2번은 특히 중요하다 — **마스터가 오류를 고치는 지시문 안에서 같은 클래스의 오류를 만들 뻔했다.**
dev가 축자 준수 대신 반증을 택하지 않았다면 라이브에 나갔다.

또 dev가 QA의 **좀비 서버**(3517 포트, QA 세션 잔존)를 감지해 fresh 포트로 재검증했다.
`EADDRINUSE`로 기동 실패했는데도 curl이 200을 반환해 **구 빌드를 검증할 뻔한** 함정이다.

---

## 5) 마스터 최종 검증 (§7) — 전부 직접 실행

| 항목 | 근거 |
|---|---|
| 산술 정본성 | 마스터 직접 — 300만: 요양 14,172, 합산 28,344 = 법정직접 28,344 ✔ / 구 승수면 13,961 |
| 상수·주석 | `lib/salary.ts:29-45`, `four-insurance.ts:152-165` 직접 Read — **조문명+항번호+대조일(2026-08-04) 실재** 확인 |
| 회귀 스위트 | 마스터 직접 실행 — **408 assertions · ALL SUITES PASS** (변경 전 385, 신규 23) |
| 빌드 | 마스터 직접 실행 — `✓ Compiled` + **69/69** 정적 페이지 |
| **라이브 렌더** | 마스터가 **fresh 포트 3781**로 prod 기동 후 두 페이지 curl (HTTP 200) |
| ↳ 구식 문자열 | 11개 패턴(`12.9457`·`13,961`·`14,582`·`304,241`·`590,122` 등) → **양 페이지 모두 전부 0건** |
| ↳ 정정값 | salary `14,801`×2·`304,460`×2·`105,856`×2 / four `14,172`×4·`291,522`×2·`590,544`×2 · `13.14%` 8·6회 |
| 병기 문구 | 4곳 전문 추출해 직접 판독 — **"근로자가 보수의 0.9448%"로 읽히는 문장 0건** 확인 |
| 좀비 서버 | 3517 PID 23500 **종료 확인**, 3781도 종료 |
| 시크릿 | 커밋 대상 5파일 스캔 **0건**. `.claude/settings.local.json`(평문 시크릿, 236행 변경) **의도적 제외** |
| 커밋 범위 | 경로 **5개 명시**. `git add -A` 영구 금지 준수 |

**미확인 항목**: ① 실제 스크린샷 이미지(`screenshot` 타임아웃 지속 — curl DOM 실측으로 갈음).
② 폼 제출 후 클라이언트 상태(이번 변경은 SSR 본문 문자열이라 해당 없음).

**push**: 아래 §8.

---

## 6) CS팀 / 마케팅팀
- **CS**: 문의 채널(`/support` → `/api/contact`, Resend) 살아 있으나 **수신함 접근 불가** → 신규 문의 확인 불가(변동 없음). 지어내지 않는다.
- **마케팅**: 카드뉴스 **9편 전부 승인 대기 21일째**(07-26 알림 1회, **재발송 금지**).
  이번 변경은 계산 정확도 수정이라 **신규 화면 0**. → **신규 초안 배정 안 함.**
  ※ 다만 **"2027년 최저임금 10,700원"**은 8월 확정 고시~연말 검색 수요 구간이라 감사가 콘텐츠 기회로 제안했다(확정 고시 확인 후 착수 권고).

---

## 7) 이월 이슈 (다음 실행 = 익일 01:00이 이어받을 것)

1. **[★최우선 · P1 — 다음 회차 즉시] `lib/blog.ts` 구식 수치 정정.**
   계산기 2개는 깨끗해졌으나 **블로그 3편이 여전히 구식을 렌더 중** → 계산기↔블로그 교차 불일치.
   QA가 **파일:줄 단위 작업 지시서를 이미 만들어 뒀다**(위 감사 문서 아님, 이 로그 §7 하위 표 참조 불가 시 QA 보고 재생성 필요):
   - `:108` `약 12.95%` · `:138~141` 14,582/304,241/105,888/10,588 · `:1393` 2,912,616
   - `:2619 :2642 :2666 :2673` `12.9457%` 4회 · `:2693` **9,307원 → 9,448원** · `:2695~2697` 194,207/199,207/393,414
   - `:2716` 13,961/291,311/298,811/590,122 · `:2738` 32,577 → 33,068 · `:2740` 660,252 → 660,743
   - `:1428~1429` 18,460 → **18,738 계열**(연봉별 실수령 표 전 행 재계산 필요)
   - **`:3493~3574` 소득구간별 조견표 전 행 + 부담률 `9.71%`(14회) → `9.72%`, `9.43%` → `9.44%`**
     ※ 부담률은 **글의 핵심 명제**라 소제목까지 바뀐다. 전면 재검토 대상.
   **주의: 모든 수치를 엔진으로 재산출할 것. 손계산 금지(이번 사고의 원인이 손계산이었다).**
2. **[P2] 기획·설계 문서 드리프트 — 재발 방지 관점에서 중요.**
   - `planning/salary-net-calculator-content.md:53`이 **"0.9448%÷7.19% = 13.14%가 *아니라* … 12.9457%임에 유의"** 라고 **적극적으로 틀린 지침**을 준다. **#30 오기의 발원지로 추정.** 최우선 정정.
   - 같은 파일 `:130` 자기모순(`× 12.9457% (= 보수월액 × 0.47240%)` — 괄호 안 0.4724%가 정본)
   - `planning/four-insurance-calculator-content.md:80`이 `LONG_TERM_CARE_MULTIPLIER=0.129457`을 **상수명까지 지목해 고정** → 재발 유도. 정수 쌍으로 교체 필요
   - `planning/ratio-percent-display-rules.md:57,175`가 `12.9457%`를 **"법정 고시값 축자"로 분류** — 이중으로 틀렸다(값도 틀렸고 애초에 고시값이 아니라 파생 승수). **규칙 문서라 파급 큼**
   - `design/*-ui-spec.md`, `docs/qa/salary-net-calculator-qa-report.md:40`, `cs/salary-calculator-review.md:18~26`
3. **[구조 개선 제안 — FAIL-A의 근본 원인]** 표시 문자열을 상수 파생으로 묶어라.
   `LONG_TERM_CARE_MULTIPLIER`가 dead export라 **상수를 고쳐도 화면이 안 따라온다.** 이번 FAIL-A의 구조적 원인이며, 정정 후에도 구조는 그대로다 → **다음에 요율이 바뀌면 똑같이 재발한다.**
4. **[QA 권고]** 회귀 앵커 L3(과다청구 금지) 스윕 범위가 `T=1,000,000~1,002,000` **2,001건**뿐. 결론은 참이나 계약치고 커버리지가 얇다 → 확대 권고.
5. **[감사 Batch B — 다음 축 계속]** 감사가 지목한 후보:
   - **`salary.ts` 근로소득 간이세액표 근사식** ← **동일 결함 클래스에 가장 크게 노출**. 구간표·공제액이 틀려도 내부 정합에 안 걸린다. **Batch B 최우선 권고**
   - 보험료징수법 제13조제2항(고용보험 노사 1/2 배분) 축자 — A4에서 **미시도**로 남김
   - 국민연금법 **부칙 제4조** 축자 — OpenAPI가 장문 절단으로 부칙 미도달(A1은 부처 보도자료+산술 재구성으로 판정, **근거 등급 차이 명시됨**)
   - `NATIONAL_PENSION_BASE_MIN/MAX`(410,000/6,590,000) — 2026.7.1~2027.6.30 적용 확인됨. **2027년 7월 갱신 예약 필요**
6. **[⏰ 시한폭탄 2건 — 날짜에 의해 자동 발생하는 예약된 오류]**
   - **2026-08-05(내일) 최저임금 확정 고시** — 현재 게시된 건 제9조 **"안(案) 고시"**(10,700원)이고 제10조 확정 고시는 미발령. 확정 후 고시번호·본문 확인 필요
   - **2026-12-31 `MINIMUM_WAGE` 교체** — 놓치면 **2027-01-01부터 사이트 전체가 구 최저임금을 안내**한다
7. **[요율 감사 SOP 신설 제안 — 이번 감사의 최대 교훈]**
   - 상수 주석에 **기관명만이 아니라 "무엇을(고시명·조문) 언제 대조했는지"** 필수 기재.
     실측: 6종 중 요율 확인 날짜가 적힌 것은 **A6 하나뿐**이었고, 틀린 A3도 "출처: 보건복지부"라고 적혀 있었다. **기관명만 적힌 출처 주석은 검증의 증거가 되지 못한다.**
   - **공단 "알기쉬운 OO" 안내 페이지를 1차 출처로 쓰지 마라.** 국민연금공단 안내 페이지가 오늘까지 **"9%·근로자 4.5%"로 미갱신** 상태다 — 주석의 출처를 곧이곧대로 열었다면 **"코드 4.75%가 틀렸다"는 정반대 오판**을 냈을 것이다.
   - **승수형 상수(A÷B)는 주석의 나눗셈을 실제로 계산해 보는 것만으로 검증된다** — 저비용 자동 가드 후보. #30이 정확히 이 방법으로 잡혔다.
8. **[미착수 유지 · 경미]** `formatFeeRate` 죽은 조건문 · 블로그 잔존비율 라벨 축자 · design 개정이력 문구 · 티켓 범위 표기(`#19·#22` vs `#19·#20·#22`) · design 스펙 ASCII 폭 불균일 · `engines.test.ts` 컴포넌트 import 3건 · DSR 16자리 이상 조용한 반올림 · 스위트 19 공허 단언 2개.
9. **[신규 #24~#29 · 미착수]** #24 `formatDsr` 사사오입(라이브 표시값 변동 → **단독 회차 필요**) · #25 기존대출 필드 무검증 NaN · #26 극단값 표기 · #27 스윕 수치 난립 · #28 앵커 C 금액 드리프트 · #29 줄번호 인용 7곳 전환 결정.
10. **[★사용자 결정 대기 — 전부 재발송 금지]**
    근속기간 (A) vs (B-2) 민법 §160③(07-27) · 공개 저장소 평문 크론 시크릿(07-27) ·
    GSC API 자격증명 부재(20일째) · 08-03 루틴 불발(01:06, **전달 미확인**) ·
    스케줄 회차 동시 실행 경합(03:12 관측, 알림 미발송) · `*.vercel.app` 서브도메인(08-01).
    ⚠️ Remote Control 비활성이라 **무인 알림이 조용히 유실 중**일 수 있다.
11. **[백로그 — 유지]** 애드센스 재신청 8월말~9월초 · 카드뉴스 9편 승인 대기 · og:image 2단계 · 다크모드 ·
    **#16 취득세(/loan 확정, 착수 가능 — 로드맵 확장 여지가 남은 유일한 큰 건)** ·
    티켓 #8 JSON-LD 썩는 수치 3건 · `.claude/worktrees/` 스테일 사본 2개(#5, 구 `0.129457` 코드 잔존 확인).

**다음 실행에서 이어서 할 것**: **1번(`lib/blog.ts` 구식 수치)을 최우선으로 잡는다** —
계산기만 고쳐 놓은 현 상태는 블로그와 어긋나 있고, QA가 줄 단위 지시서를 이미 만들어 뒀다.
그다음 2번(발원지 `planning/...:53` 정정)으로 재발을 막고, 여유가 있으면 5번 Batch B(간이세액표)로 축을 잇는다.

---

## 8) 운영 표준 (이번 회차 추가)

- **[신규 ㋄ — 가장 중요] 내부 정합 감사는 상수 오류를 원리적으로 못 잡는다.**
  #30은 **계산 결과와 본문 숫자가 완벽히 정합**했기 때문에 Batch 1~3을 전부 통과했다.
  → 정합 감사와 **정오(正誤) 감사는 다른 축**이다. 정합이 아무리 깨끗해도 상수 대조를 대신하지 못한다.
  07-30부터 5일간 "다음 큰 축"으로만 적혀 있던 항목이 **착수 첫 배치에서 실제 오류를 냈다** —
  이월 항목의 우선순위를 "쉬운 것부터"가 아니라 **"안 본 축부터"**로 잡아야 한다는 증거다.
- **[신규 ㋅] 주석이 나눗셈을 적고 있으면 그 나눗셈을 실제로 계산해 봐라.**
  #30의 주석은 입력값 두 개를 **정확히** 적어 놓고 몫만 틀렸다. 즉 **자료를 못 찾아서 생긴 오류가 아니라
  올바른 자료를 손에 쥔 채 계산을 잘못 쓴 것**이다. 외부 출처 없이도 **주석 내부만으로 반증 가능**했고,
  이 검증은 5초면 된다. 승수형 상수(A÷B) 전수에 대해 저비용 자동 가드로 만들 가치가 있다.
- **[신규 ㋆] 기관명만 적힌 출처 주석은 검증의 증거가 아니다.**
  6종 중 요율 확인 날짜가 있던 건 1종뿐이고, **틀린 A3에도 "출처: 보건복지부"가 적혀 있었다.**
  더 나쁜 건 그 출처를 곧이곧대로 열면 **오판을 유도**한다는 것이다 — 국민연금공단 "알기쉬운" 페이지는
  오늘까지 구 요율(9%·4.5%)로 미갱신이라, 주석대로 열었으면 **맞는 코드를 틀렸다고 고칠 뻔**했다.
  → 1차 출처는 **법령(law.go.kr) 또는 소관 부처 보도자료**. 공단 안내 페이지는 2차 자료다.
- **[신규 ㋇] 상수를 고쳐도 화면은 안 따라온다 — dead export를 신뢰하지 마라.**
  `LONG_TERM_CARE_MULTIPLIER`는 프로덕션 소비처가 0이었고 표시 문자열은 전부 하드코딩이었다.
  그 결과 **엔진만 고친 시점에 "위젯 14,172 / 같은 페이지 본문 13,961"이라는 자기모순 페이지**가 만들어졌다.
  → 상수 변경 티켓은 **반드시 라이브 렌더 grep까지 포함**해야 종결된다. QA가 이걸 잡았다.
- **[신규 ㋈] 포트가 안 뜨는데 curl이 200이면 좀비를 검증하고 있는 것이다.**
  dev가 `EADDRINUSE`를 감지하고도 curl 200이 나오는 것을 보고 **fresh 포트로 재검증**했다.
  이걸 놓쳤으면 구 빌드를 "정정 완료"로 보고할 뻔했다. **검증용 서버는 반드시 새 포트로 띄우고,
  기동 로그에서 Ready를 확인해라.** (좀비 2개 이번 회차에 종료)
- **[재확인 ㉹ — 이번엔 마스터 지시 자체가 새 오류를 만들 뻔했다]**
  dev가 마스터의 병기 문구(`= 보수월액의 0.9448%`)를 **근로자 부담 문맥에 쓰면 2배 과대**라고 반증했다.
  **오류를 고치는 지시문 안에서 같은 클래스의 오류를 만드는 일**이 실제로 일어난다.
  축자 준수보다 반증이 우선이라는 원칙이 라이브 오류 1건을 막았다.
- **[재확인 ㉴ — QA가 값을 했다]** QA가 법령 원문을 **직접 열어** 대조했고, `git show HEAD:`로
  "변경 전 385"를 자기 손으로 재현했으며, 변이 5종을 직접 주입해 앵커 비공허를 실증했다.
  개발팀 숫자를 옮겨 적지 않은 결과 **FAIL-A(라이브 모순)**를 잡아냈다.
- **[재확인 ㋁ — 이번엔 지켰다]** 미커밋 산출물(감사 문서 385행)을 발견하고 **인수 전에**
  `git log --since`로 타 세션 활동을 확인했다. 03:12 회차의 교훈이 실제로 적용됐다.
