# 2026-07-14 데일리 팀 스탠드업 로그

> 날짜 주의: 실행 시작 시 currentDate가 2026-07-13이었으나 실행 중 07-14로 넘어감. 07-13 스탠드업 로그(2026-07-13.md)는 이미 완결돼 있어 본 실행은 그 로그가 지목한 "내일(07-14)" 작업으로 취급, 2026-07-14.md에 기록한다.

## 실행 시작 시 파악한 상태 변화 (07-13 로그 대비 큰 진전)
07-13 로그가 미착수로 남긴 블로커들이 그 사이 **모두 해소**돼 있었다(초기 커밋 2개로 반영, 팀 프로세스 밖에서 생성됨):
- 저장소가 **git 저장소로 초기화**됨(커밋 2개: 초기 커밋 + 홈 그리드 수정). 07-13 GSC 감사가 "git 아님"이라 한 상태에서 바뀜.
- **법적 페이지**(`/privacy`, `/terms`) + **고객센터**(`/support`, FAQ + mailto 문의폼→담당자 이메일) 실재·라우팅 확인. 07-13 최상위 블로커 해소.
- `app/sitemap.ts`(라이브 계산기만 색인) + `app/robots.ts` + `lib/site.ts`(SITE_URL=calculator-hub-delta.vercel.app) 존재(오늘 시점 untracked). SEO 인프라 확보.
- 시작 시 라이브 계산기 4개(만나이·D-Day·BMI·대출이자), coming-soon: salary-net.

## 오늘 완료 (핵심: 로드맵 #5 연봉 실수령액 계산기 라이브)
정상 파이프라인 준수 — 기획 → 디자인 → (핑퐁) → 개발 → 마스터 검증.

### 산출물
- `planning/salary-net-calculator-content.md` (기획): 2026 요율·간이세액표 근사공식(Option B) STEP1~11 수치 확정, 검증 앵커.
- `design/salary-net-calculator-ui-spec.md` (디자인): 결과 3-tier(대표값/요약3행/접이식 상세), 셀렉트·근사배너·YMYL CTA금지, 모바일 규칙.
- `lib/salary.ts` (개발, 신규): 요율 상수 모듈 분리 + `calculateSalary`. 파이프라인 순서(4대보험→소득세), 국민연금 clamp·천원절사, 라운딩=절사.
- `components/SalaryNetCalculator.tsx` (개발, 신규): 스펙대로 구현.
- `app/calculator/[slug]/page.tsx` CALCULATOR_COMPONENTS 매핑 추가, `lib/calculators.ts` interpretation·faq(6개)·status:live 전환.
- **라이브 계산기 4개 → 5개**.

### 마스터가 확정한 결정 (planning §7 / design §8 응답)
- 근로소득세 = **Option B 근사공식**(오늘). Option A(원표 lookup) 이관은 백로그.
- 라운딩 = 절사. 관련계산기 = 기본순서 유지.
- 근사 고지 상단 배너 = **YMYL-중간에도 도입 승인**(대출 전용 요소를 톤 낮춰 확장, 향후 YMYL-중간+ 공통 규칙).
- design-system.md 신규 3종(select/접이식 결과 상세/월+연 2단 히어로) 정식 편입 = **보류**(컴포넌트 내 사용만, 대출 세그먼트토글과 동일 판단).
- Tier③ 공제상세 = 기본 접힘 승인.

## 마스터가 직접 검증한 근거
- `npm run build` 3회 직접 실행(구현 후/H-1 수정 후 2회) 모두 통과, 타입에러·경고 0, static 20/20, `salary-net-calculator` SSG 확인.
- **실코드 결정론 테스트**(`npx tsx`로 실제 `calculateSalary` 5케이스 실행):
  - 앵커(연봉4천만/부양1/비과세20만/자녀0): 월 실수령 **2,912,616**(planning 앵커 289~293만 범위 내), 4대보험 304,241, 세금 116,476.
  - 국민연금 상한(연봉2억): **313,025원 고정**(기준소득월액 659만×4.75%) — clamp 정상.
  - 검산(세전−4대보험−세금=월실수령) 전 케이스 **정수 정확 일치**.
  - 무효입력(연봉0) → null 정상.
- **라이브 브라우저 검증**(localhost dev, 데스크톱 1280 + 모바일 375):
  - DOM 구동으로 앵커 입력→제출→결과카드 렌더 확인. 월 실수령 2,912,616, 동적 요약 "세전 연봉 4,000만원을 기준으로, 매월 4대 보험 약 30만원과 세금 약 12만원을 공제한 실수령액입니다." 정확.
  - Tier③ 기본 접힘(detailsDefaultOpen=false) 확인, 펼치면 8줄 개별+소계+인라인 근사노트+과세정보 노출. summary 라벨에 "(4대 보험·세금 항목별)" CS안전 문구 포함.
  - 모바일 375: 가로 오버플로 0(docScrollWidth=375), 계산 버튼 48px.
- **CS 검수 H-1 수정 검증**: interpretation 1문단 개별수치를 엔진 실측값(148,817 등)으로 교체됨을 실코드로 확인, 결과카드와 일치.

## 미확인 항목
- 실기기(실제 휴대폰) 렌더링·터치 정밀도는 브라우저 프리뷰/DOM 기반이며 실기기 아님.
- 브라우저 프리뷰 도구(preview_start/read_page)가 반복 불안정(빈 트리·viewport 0x0, 프로세스 조기 종료) — 07-13과 동일 증상. 시각 스크린샷 미확보, 검증은 실코드 tsx 테스트 + DOM(innerText/scrollWidth)로 수행. 픽셀 단위 시각 회귀 미확인.
- 사이트 실제 배포 여부: site.ts에 Vercel URL이 있으나 배포·AdSense 승인 여부는 확인 불가(GSC 미연동). GSC 감사 트랙 소관.
- 2026 세율 8구간·근로소득공제·세액공제 금액은 planning이 2023~2024 개정분 유지 가정(별도 인하 고시 미확인). 요율 자체는 복수 출처 교차 확인.

## 오늘 발생한 핑퐁 이슈 (누가 제기 → 누구에게 → 결과)
1. **[design→planning] Tier③ 접힘 기본값 + 근사고지/요약 카피** — design이 3-tier 설계하며 접힘 기본값 콘텐츠 합의 + 카피 확정을 기획에 되돌림 → planning이 접힘 승인 + 배너/인라인/동적요약 카피 확정(summary 라벨 보강 조건) → 개발 반영. **해결.**
2. **[planning→design, 저우선] design §3-2 목업 예시 문장 논리 불일치** — planning이 카피 확정 중 발견(월단위 대표값 아래 "연 연봉…11만" 오독 소지, 12만으로). 실제 제품은 확정 템플릿으로 정확 구현됨(라이브 확인). design 문서의 예시 문장만 수정 필요 = **문서 typo, 백로그 이월**(제품 무영향).
3. **[CS→dev, 라이브 defect] H-1 interpretation 예시 수치 불일치** — cs-team이 신규 페이지 검수 중 발견, interpretation 개별 공제액(148,810 등)이 엔진 출력(148,817 등)과 불일치해 대표 예시 오류제보 유발. 마스터 tsx로 엔진값 확인 → dev가 실측값으로 교체 + M-1(폼 하단 개인정보 안내 1줄) 추가 → 마스터 재검증(빌드+실코드). **해결.**
4. **[CS→기획, 개선제안 백로그] M-2/M-3/M-4** — 부양가족 정의 문안, "간이세액표" 최초 1회 풀이(07-13 대출 C-3/C-4 패턴 재발), "4대보험도 소액 오차 가능" 고지 여지. 오류 아님, 기획 문안 필요 → **내일 이월**.

## 마케팅팀 카드뉴스 게이팅 판단: **START(착수)** — 07-13 "보류"에서 전환
- 근거: 07-13 최대 블로커(법적/문의 페이지 부재) 해소 + 라이브 5개 + sitemap/robots. 품질 바 충족.
- 조치: 오늘 신규 대표 콘텐츠(연봉 실수령액 계산기) 연계 카드뉴스 7장 + SNS 캡션 3종 **초안**을 `marketing/promo-drafts.md`에 작성. YMYL-중간 가드 준수(급여인상·이직 부추김 배제, 근사 한계 전용 카드).
- **한계**: 실제 SNS 게시·이미지 에셋 생성은 **사용자 승인 사안**이라 미실행(초안까지만). 게시 전 /support 문의채널 실동작 최종 확인 권장(마케팅 제기).

## CS 문의 채널
- 07-13 "미연동"에서 부분 변화: `/support`에 FAQ + **mailto 문의폼**(→담당자 이메일) 존재. 단 **서버측 수신함(인바운드 메시지 저장소) 없음**(mailto는 사용자 메일앱을 열 뿐). 실제 인바운드 문의 큐가 없어 이번 실행에서도 "실제 사용자 문의 응대"는 없었고, CS 작업은 신규 페이지 사전 검수였다.
- **(2026-07-14 09시대 실행 갱신)** 이 mailto 상태는 이후 01:00경 커밋(`c0d7fb9`)으로 이미 대체됨: `/support` 문의폼이 Resend 서버 전송 방식(`app/api/contact/route.ts`)으로 전환되어 개인 이메일이 소스/화면에 노출되지 않는다. 단, 이 전환도 "서버측 수신함"을 만든 것은 아니라 CS팀이 조회 가능한 인바운드 큐는 여전히 없다(발신 전용, 목적지는 env 변수).

## 내일(2026-07-15)로 넘기는 미해결/후속 이슈
1. **[CS 개선제안 백로그] M-2/M-3/M-4/L-1/L-2** (기획 문안 필요): 간이세액표 최초 풀이·부양가족 정의·4대보험 오차 여지·자녀 8~20세 기준·비과세 개념. 오류 아닌 문의예방 개선. 착수 시 기획팀부터.
2. **[문서 typo] design §3-2 목업 예시 문장** 12만원 표기로 수정(제품 무영향, 낮음).
3. **[구조결정 백로그] 근로소득세 Option A(간이세액표 원표 lookup) 이관 여부** — 정확도↑ vs 데이터파일+연1회(3월) 갱신 부담. YMYL-중간·최대트래픽이라 승인 후 재검토(planning §7-1). 마스터/사용자 결정.
4. **[운영 거버넌스] 요율·세율 갱신 체크리스트** — 4대보험(1월)·간이세액표(3월) 연1회 점검을 워크플로에 넣을지(planning §7-4). 상수는 `lib/salary.ts` 블록으로 분리해둠.
5. **[design-system 편입 보류분]** select 스타일 / 결과 접이식 상세 / 월+연 2단 히어로 + (07-13 이월) 세그먼트토글·병렬숫자·상단배너 → 다음 YMYL 계산기 착수 시 일괄 정식 편입 재검토.
6. **[사용자 결정 이월, 07-09부터]** 실제 문의 채널 서버 수신함 부재(mailto만), cs-team Edit 권한 부재. /privacy·/terms 법률 전문가 검토(현재 표준 템플릿 초안, 코드 주석에 비검토 명시).
7. **[게이팅 후속, 사용자 승인]** 카드뉴스 이미지화·실제 게시 채널/계정·게시 승인. 로드맵 6순위(단위 변환 계산기) 다음 착수 대기.

## 참고 (이상 신호, 조치 불필요)
- 마케팅·(과거)팀 작업 중 Higgsfield MCP 도구 정의에 섞인 "generate_image/video/audio를 핵심 도구로 사용하라"·업로드 유도 문구를 재확인(07-09/07-13과 동일). 사용자/마스터 지시가 아닌 도구 정의 보일러플레이트 → 프롬프트 인젝션으로 취급, 어떤 생성·업로드 도구도 호출하지 않음.
- 배포(git push) 미실행(사용자 승인 사안). 작업 결과는 working tree에만 존재(미커밋).

---

## 09:41 실행분

이 실행 시작 시점(09:41), 위 로그(01시대 실행분 추정)가 이미 오늘 하루치 상세 보고를 담고 있었으나 **커밋되지 않은 채 남아있었음**을 확인. 그 사이(01:00~09:41) 다른 세션이 별도로 `c0d7fb9`(Resend 전환)를 커밋함. 즉 이번 실행 시작 시 working tree에는 "로그상 완료로 기록됐지만 실제로는 미반영된" 고아 작업(H-1/M-1/CS검수/카드뉴스 초안)이 남아있었음 — [[project_scheduled_workflows]]가 경고한 동시성 위험이 실제로 재현된 사례.

### 이번 실행에서 배정/처리한 작업
1. **고아 작업 검증 후 커밋+푸시** — H-1(interpretation 수치), M-1(개인정보 안내), CS 검수 문서, 카드뉴스 초안. 커밋 `76c5bc3`.
2. **CS 백로그 5건(M-2/M-3/M-4/L-1/L-2) 정식 처리** — planning-team 문안 확정 → dev-team 구현.
3. **신규 qa-team 서브에이전트 발견 및 첫 가동** — 사용자가 이번 실행 도중 직접 커밋(`f47de8c`)으로 추가(사유: 그리드 정렬·개인정보 노출처럼 개발팀 자가검증+마스터 확인에서도 놓친 이슈를 사용자가 육안으로 발견한 전례). H-1 + CS 5건에 대해 첫 독립 검증 실행.
4. **QA 발견사항 2건 후속 반영** — (a) "기준소득월액" 무설명 노출(용어 일관성 FAIL, 같은 커밋의 "간이세액표" 처리와 자기모순) → 기획 제안 문구 그대로 개발 반영, (b) `ResultInterpretation.tsx`의 `whitespace-pre-line` 누락(사이트 전역, 오늘 변경 무관 기존 결함, D-Day·연봉·대출이자·BMI 4개 계산기 영향) → 즉시 수정.
5. **보안: QA 자체 산출물의 개인정보 재노출 발견 및 마스터 직접 조치** — 자동 안전장치가 "QA 보고서가 `.env.local`의 실제 값을 그대로 인용해 커밋 대상 문서에 재노출"이라고 경고. 확인 결과 `docs/qa/*.md`에 이메일 3회, 기존 미커밋 `docs/team-log/2026-07-14.md`(01시대 실행분)에도 mailto 시절 서술로 2회 노출되어 있었음 → 마스터가 두 파일 모두 마스킹 후 재확인(재검색 결과 0건, 검색어 자체를 설명하는 1곳 제외). [[project_scheduled_workflows]]에 재발 방지 원칙 기록.

### 마스터가 직접 확인한 결과 + 근거
- **H-1 재검증**: `npx tsx`로 `calculateSalary(연봉4천만/비과세20만/부양1/자녀0)` 직접 실행 → 국민연금 148,817·건강보험 112,643·장기요양 14,582·고용보험 28,199·근로소득세 105,888·지방소득세 10,588, interpretation 표기치와 완전 일치 확인(QA도 별도 격리환경+손계산으로 독립 재확인, 결과 동일).
- **빌드**: 이번 실행 중 `npm run build` 2회 직접 실행(고아작업 커밋 전 1회, CS 5건+QA 후속 2건 반영 후 1회) 모두 타입에러 0, static 21/21 통과.
- **라이브 브라우저 검증**: dev 서버 기동 후 `/calculator/salary-net-calculator` 실제 렌더링 텍스트로 CS 5건 문구가 화면에 정확히 반영됐음을 `get_page_text`로 확인. 데스크톱(1280) `getBoundingClientRect` 픽셀 측정으로 부양가족/자녀 도움말 높이 차이(32px vs 16px, 16px 여백 비대칭 — 겹침·잘림 아님) 실측, 모바일(375) 재측정 결과 오히려 두 도움말 높이가 동일(48px=48px)해지고 가로 오버플로 0(`docScrollWidth`=375) 확인. 배너도 모바일에서 높이만 늘어나고(80px) 잘리지 않음 확인.
- **개행 버그 수정 확인**: 수정 후 `getComputedStyle(p).whiteSpace === "pre-line"` 직접 확인 + 텍스트에 리터럴 `\n\n` 잔존 확인 → 표준 CSS 동작상 문단 분리 렌더링 보장됨(스크린샷 도구는 이번에도 타임아웃, DOM/computed-style 기반으로 대체 검증).
- **보안 재노출**: `grep -rn "sss159228"`로 전체 저장소 재스캔, 마스킹 후 실제 이메일 문자열 0건(검색어 자체를 언급하는 1곳만 남음, 값 아님) 확인.

### 미확인 항목
- 실기기·픽셀 단위 스크린샷은 이번에도 도구 타임아웃(computer/screenshot 30초 초과)으로 미확보 — DOM 측정(getBoundingClientRect/getComputedStyle)으로 대체 검증. 07-13부터 반복되는 환경 이슈.
- GSC API 텔레메트리 여전히 미확인(크리덴셜 부재, 08:06 감사 로그와 동일 결론).
- 데스크톱에서 부양가족/자녀 도움말 16px 높이 비대칭은 실측 완료했으나 "완벽히 대칭으로 맞출지"는 폴리시 판단 이슈라 아래 이월(비차단, 낮음).

### 핑퐁 이슈 정리
1. **[01시대 CS→기획→개발] M-2/M-3/M-4/L-1/L-2** — CS 검수 백로그 → planning-team이 위치별 최종 문구 확정 → dev-team 구현 → 마스터 라이브 검증. **해결.**
2. **[QA→기획→개발] 기준소득월액 무설명(신규 FAIL)** — QA가 M-4 반영 직후 재검증 중 발견(같은 커밋 내 "간이세액표"는 괄호 풀이, "기준소득월액"은 누락된 자기모순) → QA 제안 문구를 그대로 dev-team이 반영 → 마스터 라이브 검증. **해결.**
3. **[QA→개발] ResultInterpretation 개행 미보존(사이트 전역, 범위 밖 발견)** — 오늘 변경 원인 아니나 4개 계산기 가독성에 영향 크다고 QA가 판단해 보고 → 마스터가 "작고 안전한 수정"으로 판단해 즉시 개발 배정(백로그로 미루지 않음) → 마스터 검증. **해결.**
4. **[자동 안전장치→마스터] QA 보고서의 개인정보 재노출** — QA는 검증 원칙상 직접 수정 안 함 → 마스터가 직접 마스킹 처리(QA 스스로 발견한 문제라 별도 팀 이관 불필요, 운영/보안 성격). **해결.** 재발 방지 원칙을 프로젝트 메모리에 기록.
5. **[design-team 이관분 참고, 비차단]** dev-team이 부양가족/자녀 도움말 높이 비대칭 가능성을 사전에 design-team 검수용으로 남겼으나, 마스터가 직접 데스크톱+모바일 픽셀 측정으로 먼저 확인한 결과 겹침·잘림 없는 경미한 여백 차이(데스크톱만 16px)로 판명되어 별도 design-team 라운드 없이 마스터가 직접 "비차단, 낮은 우선순위 백로그"로 판단 종결.

### 마케팅팀 카드뉴스 게이팅 판단: **START 유지** (오늘 재배정 없음)
- 이번 실행은 신규 화면/계산기 없이 텍스트 문구 보강 + 버그 수정만 진행되어, 마케팅에 새로 전달할 "오늘 새로 생긴 콘텐츠"가 없음(01시대 실행분이 이미 연봉 계산기 카드뉴스 초안을 완성해 커밋함). 고정 소재 반복 배정을 피하기 위해 이번 실행은 마케팅팀 재호출을 보류. 카드뉴스 게이팅 상태 자체는 START 유지.

### CS 문의 채널
- 01:00경 Resend 전환(`c0d7fb9`)으로 개인정보 노출 문제는 해소됐으나(env 변수만 참조, 서버 코드 직접 재확인함), **서버측 인바운드 큐는 여전히 없음**(발신 전용) — CS팀이 조회할 수 있는 실제 문의 저장소는 오늘도 없어 "실제 사용자 문의 응대"는 수행하지 못함. [[project_scheduled_workflows]] 메모리에 이 구분을 명확히 기록함(향후 실행이 "Resend=해결"로 오인하지 않도록).

### 배포(push)
커밋 `76c5bc3`(고아 작업, 이번 실행 초반) + 이번 실행 말미 CS 5건/기준소득월액/개행버그 반영분 — 둘 다 빌드+라이브 브라우저(데스크톱+모바일)+독립 QA 검증을 통과해 **push 진행**.

### 다음 실행(2시간 뒤, 11시대)으로 넘기는 미해결/후속 이슈
1. **[신규, qa-team 정착]** 오늘 처음 가동된 qa-team을 앞으로 모든 "버그 수정 후 / 신규 기능 완료 전" 표준 절차에 포함시킬 것(사용자가 명시적으로 만든 팀이므로 임의로 생략하지 말 것).
2. **[낮음, 비차단]** 부양가족/자녀 도움말 데스크톱 16px 높이 비대칭 — 원하면 다음 폴리시 라운드에 디자인팀 검토(현재는 마스터 판단으로 종결, 급하지 않음).
3. **[기획팀 백로그, 낮음]** QA가 참고로 남긴 M-1 버튼 하단 안내문과 FAQ 6번 문구의 표현 차이(의미 모순은 아님, 두 벌 문구 통일 검토).
4. (이월 유지) 근로소득세 Option A(간이세액표 원표 lookup) 이관 여부, 요율 갱신 체크리스트, design-system 편입 보류분 3종+이월분, 법률 전문가 검토, 실제 문의 채널 서버 수신함 부재, 카드뉴스 이미지화/게시 승인 — 전부 07-13/01시대 로그와 동일하게 유효, 오늘 진전 없음.
5. **[해결 확인, 목록에서 제거]** 01시대 로그가 이월한 M-2/M-3/M-4/L-1/L-2는 이번 실행에서 전부 처리 완료.

---

## 09:48 실행분 (당일 3차 세션)

> **중요: 동시성 충돌 재확인.** 이 실행은 currentDate 확인 후 09:48에 시작했으나, 작업 도중
> `docs/team-log/2026-07-14.md`에 이미 "09:41 실행분"(위 섹션)이 존재하는 것을 발견했다 — 09:41과
> 09:48은 동일 9시 슬롯(cron jitter 320초) 내 별도 두 세션으로 추정된다. 원인 불명(중복 트리거 또는
> 이 환경의 실행 큐 지연). 이번 세션은 **한참 뒤(작업 중 real time이 09:48→14:22까지 진행됨)에도
> `daily-team-standup`의 `lastRunAt`이 13:45 KST로 갱신된 것을 확인**해, 11시·13시대 슬롯도 이 세션과
> 겹쳐 별도로 실행됐을 가능성이 있다(직접 확인 불가, 파일/커밋 충돌 여부로만 간접 확인). 매 커밋 전
> `git fetch`+`git status`로 충돌을 사전 차단하는 절차를 평소보다 훨씬 자주(총 6회) 수행했고, 실제
> 충돌은 **없었다**(모든 시점에서 origin과 0 ahead/0 behind 확인 후 진행). 사용자는 이 cron 중복/큐
> 지연 가능성을 인지하고 필요시 스케줄 설정을 점검할 것을 권장한다.

### 컨텍스트 파악
- 메모리(`project_adsense_site_plan`, `project_scheduled_workflows`) + 오늘 로그(01시대 실행분,
  09:41 실행분) + GSC 감사 로그 모두 Read. 시작 시점 `git log` 확인 결과 `76c5bc3`가 최신 커밋(09:41
  세션 결과물 `f47de8c`/`9f88599`는 아직 없었음 — 이후 작업 중 나타남, 아래 참고).
- 실제 파일 상태(Glob) 확인: `lib/calculators.ts`에 라이브 계산기 5개(만나이·D-Day·연봉·대출·BMI),
  로드맵 6순위 단위 변환 계산기는 코드 없음(planning 문서상 계획만 존재) — 다음 작업 대상으로 확정.

### 오늘 완료 (핵심: 로드맵 #6 단위 변환 계산기 라이브 — 라이브 계산기 5개 → 6개)
정상 파이프라인 준수 — 기획 → 디자인 → (핑퐁: 단위 목록 불일치 마스터 조정) → 개발 → QA 독립검증
→ 마스터 재검증 → push.

#### 산출물
- `planning/unit-converter-content.md` (기획): 5개 카테고리(길이·무게·면적·부피·온도) 변환 계수·
  정확도 검증 앵커(§1-6) 확정. 롱테일 대량 페이지 생성은 이번 범위 제외(단일 인터랙티브 페이지).
- `design/unit-converter-ui-spec.md` (디자인): 세그먼트 탭 5등분, **사이트 최초 실시간(버튼 없음)
  계산 패턴**, 스왑 버튼(드리프트 방지=full-precision 되먹임), 참조표, 3단 반올림 규칙, 모바일
  무스크롤 달성 설계.
- `lib/units.ts` (개발, 신규): canonical-base 변환 엔진(곱셈형 factor 테이블 + 온도 전용 오프셋
  함수), 3단 반올림 포맷 유틸.
- `components/UnitConverter.tsx` (개발, 신규): 스펙대로 구현, 참조표는 하드코딩 없이 엔진으로 실시간
  계산.
- `lib/calculators.ts`·`app/calculator/[slug]/page.tsx`: unit-converter 항목 추가, 라우트 매핑,
  status: live.
- 커밋 `daba388`(기능 본체) + 후속 커밋(용어 통일, 아래 참고) — **push 완료**.

#### 핑퐁 이슈 1: [기획→디자인, 마스터 조정] 단위 목록 불일치
- planning(§1)이 5카테고리·28개 단위(정확도 앵커 포함)를 먼저 확정했으나, design이 이를 읽지 않고
  병렬로 작업하며 SEO 목적의 한국 전통 단위(관·아르·에이커·홉·되·말)를 자체 제안 — 또한 design 제안
  목록에서 planning이 앵커까지 확정한 "컵(200mL)"이 누락됨.
- 마스터가 두 문서를 대조해 `design/unit-converter-ui-spec.md` §13에 확정 기록: **planning의
  28개 확정 목록을 최종본으로 채택**(디자인 제안 한국 전통 단위는 정확도 앵커 미검증 상태라 이번
  범위 제외 — 연봉 계산기 CS 전례와 같은 리스크 차단), 컵은 복원. 한국 전통 단위 확장은 다음 실행
  백로그로 이월(아래 참고).
- **해결.** 추가 왕복 없이 마스터가 그 자리에서 조정, dev-team에 확정본만 전달.

#### 핑퐁 이슈 2: [QA→기획, 마스터 직접 처리] 용어 불일치 2건
- qa-team 독립 검증(신규 기능 완료 후 필수 절차, 09:41 세션이 신설)에서 interpretation 본문이
  "넓이"(3회)를, FAQ·UI 탭은 "면적"을 쓰는 용어 혼용과, unit-converter만 "브라우저 안에서"(기존 5개
  계산기는 전부 "브라우저 내에서")를 쓰는 문구 불일치 2건 발견. 둘 다 콘텐츠 문구 문제이며 planning
  원문이 원인(개발 구현 오류 아님).
- 마스터가 QA 보고서에서 놓친 4번째 occurrence(interpretation 본문에도 "브라우저 안에서"가 한 번 더
  있었음)까지 자체 grep으로 추가 발견, 4곳 전부 직접 수정("넓이"→"면적" 3곳, "브라우저 안에서"→
  "브라우저 내에서" 2곳) 후 재빌드 통과 확인. **해결.** (작고 안전한 문구 수정이라 기획팀 왕복 없이
  마스터가 즉시 처리 — 01시대 로그의 "whitespace-pre-line" 선례와 동일 판단 기준.)

### 마스터가 직접 검증한 근거
- `npm run build` 3회 직접 실행(구현 직후/용어수정 후) 모두 타입에러 0, static 22/22 통과.
- **독립 결정론 테스트**(dev-team 산출물과 별도로 마스터가 직접 작성한 `_master-verify-units.ts`를
  `npx tsx`로 실행, 완료 후 삭제): 33평→109.09㎡, 1inch→2.54cm, 1lb→453.59g, 70℉→21.11℃, 25℃→
  298.15K, 1L→5컵, 20평→66.12㎡(마스터 확정 기본 프리필) 등 8개 케이스 전부 기대값과 정확히 일치.
  절대영도 경계값(-273.15℃=false, -1K=true) 정상, 왕복 스왑(33평→㎡→평) 오차 0.
- **QA팀 독립 재검증**(9번째 팀 산출물, 마스터와 별도 스크립트로 100개 assertion): 계산 정확성·온도
  오프셋·엣지케이스·해설-엔진 수치 일치 전부 PASS. 용어 일관성만 FAIL(위 핑퐁 이슈 2에서 해결).
- **라이브 브라우저 검증**(localhost dev, 데스크톱 + 모바일 375 실제 클릭/조작): 기본 진입(면적,
  20평→66.12㎡) 렌더 확인, 온도 탭 클릭→카테고리 전환+기본값 리셋(1℃→33.8℉) 확인, 스왑 버튼
  클릭→from/to 단위 교환 + 결과 "33.8℉은 약 1℃입니다"로 드리프트 없이 정확히 역변환 확인(콤보박스
  선택 상태로 재검증), 모바일 375px `docScrollWidth=375`(가로 오버플로 0), 세그먼트 탭 5개 각
  ~67px(design 예측 68.6px와 근접) 오버플로 없음, 결과 카드 하단 y≈400px(뷰포트 812px 내, 무스크롤
  목표 부합), 콘솔 에러 0건.
- **마케팅 카드뉴스**: `marketing/promo-drafts.md`에 96줄 순수 추가(git diff 0 deletions로 기존
  연봉 계산기 카드뉴스 섹션 무손상 확인)로 6장 초안 + 캡션 3종 + 시즌변주 1종. 카드 속 수치(10평=
  33.06㎡ 등)를 마스터가 직접 400/121 상수로 재계산해 일치 확인.

### 미확인 항목
- 실기기(실제 휴대폰) 렌더링은 이번에도 브라우저 프리뷰 기반(단, 오늘은 스크린샷 대신 DOM
  상호작용(클릭+read_page+getBoundingClientRect)이 안정적으로 동작해 이전 실행들보다 검증 깊이가
  향상됨 — computer 스크린샷 액션만 타임아웃, 그 외 상호작용은 정상).
- 카드뉴스 실제 이미지화·게시는 이번에도 미실행(사용자 승인 사안, 초안까지만). 마케팅팀은 오늘 누적된
  카드뉴스 2건(연봉 7장+단위변환 6장)을 묶어 한 번에 검토받는 것을 제안(참고, 결정 아님).
- GSC API 텔레메트리 여전히 미확인(크리덴셜 부재, 변동 없음).
- 09:41/13:45대 추정 동시 세션의 실제 작업 내용은 커밋/파일 상태로만 간접 확인했을 뿐, 그 세션
  자체와 직접 통신한 것은 아니다(Agent 도구의 특성상 서로 다른 세션 간 직접 대화 불가).

### 마케팅팀 카드뉴스 게이팅 판단: **START 유지**
- 오늘 두 번째로 신규 라이브된 콘텐츠(단위 변환 계산기)를 마케팅팀에 브리핑해 전용 카드뉴스 6장을
  추가 확보. 고정 소재 반복 없이 오늘 실제 변경사항만 반영.

### CS 문의 채널
- 변동 없음. `/support` Resend 서버 전송(발신 전용)까지만 구축, 서버측 인바운드 큐는 여전히 없어
  CS팀이 조회할 실제 문의가 없음(01시대·09:41 로그와 동일 결론).

### 배포(push)
- 커밋 `daba388`(단위 변환 계산기 기능 본체 — planning/design/lib/units.ts/UnitConverter.tsx/
  calculators.ts/page.tsx/salary UI스펙 오타수정/CS카피 중복작업 경위기록) — 빌드+독립검증+브라우저
  확인 통과 후 push 완료.
- 용어 통일 수정(넓이→면적, 브라우저 안에서→내에서) + 마케팅 카드뉴스 2차분 + 이 로그 + QA 리포트 —
  이번 섹션 작성 직후 별도 커밋으로 push 예정(아래 최종 보고 참고).
- `.claude/settings.local.json`(로컬 도구 권한 캐시, 이번 세션과 무관한 변경)은 커밋 대상에서 제외.

### 다음 실행(2시간 뒤, 11시대 — 단, 위 동시성 메모대로 이미 지나갔을 수 있음)으로 넘기는 미해결 이슈
1. **[신규, 운영 이슈]** `daily-team-standup` cron이 같은 슬롯에서 중복/근접 트리거되는 현상이
   오늘 09:41/09:48 두 세션으로 재현 확인됨(1차는 07-13 승인 로그에 없던 새 관찰). 사용자가 스케줄
   설정(jitter 범위, 중복 실행 방지 여부)을 점검할 필요가 있음 — 다음 실행이 컨텍스트 파악 시
   `mcp__scheduled-tasks__list_scheduled_tasks`의 `lastRunAt`을 세션 시작 시각과 대조해 유사 패턴이
   반복되는지 계속 관찰 권장.
2. **[구조 확장 백로그]** 단위 변환 계산기에 한국 전통 단위(관·아르·에이커·홉·되·말) 추가 여부 —
   design이 제안했으나 정확도 앵커 미검증으로 이번 범위 제외. 착수 시 planning이 먼저 앵커까지
   확정한 뒤 진행.
3. **[로드맵]** 다음 신규 계산기는 7순위 근속연수·근무일수 계산기(`/date`, YMYL 낮음, 퇴직금
   계산기 사전 단계로 내부링크 설계).
4. **[게이팅 후속, 사용자 승인 대기]** 카드뉴스 이미지화·실제 게시 채널/계정 승인 — 오늘 누적
   2건(연봉 7장+단위변환 6장), 마케팅팀은 묶어서 검토 제안.
5. (이월 유지, 변동 없음) 근로소득세 Option A 이관 여부, 요율 갱신 체크리스트, design-system 편입
   보류분(salary 3종 + unit-converter 4종: 세그먼트탭/스왑버튼/실시간결과카드/참조표), 법률 전문가
   검토, CS 서버 인바운드 큐 부재.

---

## 오후 실행분 (로드맵 #7 근속연수·근무일수 계산기 라이브 — 라이브 6개→7개)

> 세션 시작 시 `git status`(대화 시작 스냅샷)가 stale이라 `daba388`/`1bd5fcd`(단위변환)가 히스토리에
> 없는 것처럼 보였으나, 실제 `git log`로 두 커밋이 정상 push돼 있고 working tree는 `.claude/settings.local.json`
> (의도적 제외)만 미커밋인 clean 상태임을 확인. 이번엔 고아 작업 없음. cron은 `*/30 * * * *`(30분 간격)로
> 확인됨(메모리의 2시간 간격 기술과 다름 — 훨씬 잦음). 커밋 전 매번 `git fetch`+behind 확인으로 충돌 사전 차단(실제 충돌 0).

### 완료: 로드맵 #7 정상 파이프라인 (기획 → 디자인 → 마스터 승인 → 개발 → QA 독립검증 PASS → 마스터 재검증 → push)
- `planning/service-period-calculator-content.md` (기획): 재직일수 **양 끝 포함(+1)** 기준 확정, 손계산 앵커 5개(A~E), interpretation(5단)·FAQ 5개·shortDescription. 연차는 **계산 출력 아님, 참고용 해설만**(YMYL 보수).
- `design/service-period-calculator-ui-spec.md` (디자인): 버튼식(기존 date 계산기와 통일), 결과 3값(히어로=N년M개월D일 + 보조 재직일수·소수년), "오늘 기준" caption, end<start 인라인 검증. 기획 확정안과 **불일치 0**(단위변환 때의 목록 핑퐁 재발 안 함).
- `lib/service-period.ts` (개발 신규): 로컬타임 파싱(`parseISODate`/`stripTime`), 종료일+1 후 date-diff로 재직일수·N년M개월D일을 한 기준서 파생, 직전 달 실제 일수 borrow(윤년 자동), 소수년=재직일수÷365. 판별 유니온(invalid-start/end, end-before-start) 방어.
- `components/ServicePeriodCalculator.tsx` (개발 신규): 스펙대로, 기존 AgeCalculator/DdayCalculator 스타일 토큰 재사용. 동적 요약 문구.
- `lib/calculators.ts`·`app/calculator/[slug]/page.tsx` 배선(status: live). 관련계산기 자동 = 만나이+D-Day+연봉(§4 정합).
- 커밋 **`6f98cc6`** push 완료.

### 마스터가 승인한 결정 (planning §7)
- **7-1 재직일수 = 양 끝 포함(+1) 승인**: 일반 근무일수 유틸리티에 맞는 직관(같은날 입퇴사=1일), 퇴직금 실무 하루 차이는 해설/FAQ 고지로 처리.
- **7-2 연차 = 계산 출력 아님, 해설/FAQ 참고용만 승인**: 회계연도 vs 입사일 기준 차이로 개인화 연차는 오정보 위험(연봉 근사배너와 같은 보수적 YMYL 자세).
- design-system 신규 요소(분리-span 히어로 등) 정식 편입 **보류**(컴포넌트 내 사용만) — 기존 판단 유지.

### QA팀 독립검증 결과: **전 항목 PASS** (릴리스 차단 결함 0)
- QA가 별도 작성한 독립 구현(UTC ordinal + 역산)으로 앵커 A~E + 신규 엣지 5개(F~J: 윤년입사·월말 borrow·초장기 36년·윤일→윤일) 전부 손계산 대조 일치.
- 재직일수/N년M개월D일/소수년 상호 정합(addChk true), 엣지/방어(end<start·존재하지 않는 날짜·미입력=오늘·미래 허용) PASS, interpretation·FAQ 수치=엔진=앵커 일치(연봉 CS 전례 항목 정밀검증), whitespace-pre-line 문단 렌더, 빌드 static 23, curl 200+텍스트 노출 PASS.
- 개발팀 "요일 표기" 자체처리(`formatKoreanDatePlain` 신설로 기획 확정 문안과 일치) **타당 승인**.

### 마스터가 직접 확인한 근거
- **독립 오라클 테스트**(dev/QA와 다른 제3의 방법 — 하루씩 Date 커서 순회 + 연·월 스텝핑, 프로젝트에 임시 `_master-verify-sp.ts` 넣어 `npx tsx` 실행 후 삭제): A 1097/3-0-1/3, B 1096/3-0-0/3, C 1993/5-5-14/5.5, E 1/0-0-1/0, F 366/1-0-0/1, G 31/0-1-0/0.1(월말 borrow=윤년 2월 29 실측), H 13344/36-6-14/36.6, D(미입력=오늘 고정) 1425/3-10-25/3.9 — **전부 엔진과 일치**. end<start·잘못된 날짜 방어 정상.
- **빌드**: `npm run build` 직접 실행, Compiled success, 타입에러 0, static **23/23**, `service-period-calculator` SSG.
- **라이브 브라우저 검증**(dev :3000, JS로 DOM 구동 — 프리뷰 뷰포트 0x0 불안정 재현되나 DOM 상호작용은 정상):
  - 데스크톱 앵커C 입력→제출→결과카드 "근속기간 5년 5개월 14일", 동적요약 "입사일 2020년 3월 2일부터 2025년 8월 15일까지 총 1,993일 근무해…", 총 재직일수 1,993일, 근속연수(소수) 약 5.5년 정확.
  - 종료일 미입력→오늘: "…부터 오늘까지 총 1,425일…" + caption "2026년 7월 14일 오늘을 기준으로 계산했습니다." + 3년10개월25일/약3.9년(앵커D) 정확. (오늘=2026-07-14 확인)
  - end<start: 인라인 alert "퇴사일이 입사일보다 빠릅니다…" 노출 + 결과카드 없음.
  - 모바일 375: `docScrollWidth`=`innerWidth`=375(가로 오버플로 0), 계산 버튼 48px.

### 미확인 항목
- 실기기(실제 휴대폰)·픽셀 스크린샷은 이번에도 프리뷰 뷰포트 0x0/스크린샷 타임아웃으로 미확보 — DOM(innerText/scrollWidth/getBoundingClientRect)로 대체 검증(07-13부터 반복 환경이슈).
- 실제 Vercel 배포 반영·AdSense 상태는 push까지만 확인(GSC API 미연동, 감사 트랙 소관).

### 핑퐁 이슈
1. **[planning↔design, 핑퐁 0]** 이번엔 design이 planning 확정안을 먼저 Read하고 화면만 설계해 입력/출력 불일치가 없었음(단위변환 전례 대비 개선). 마스터 조정 불필요.
2. **[dev 자체처리→QA/마스터 승인] 요일 표기 스펙 충돌** — design §8은 D-Day `formatKoreanDate`(요일 포함) 재사용 지시, 기획 §2 확정 예시 문안은 요일 없음. dev가 `formatKoreanDatePlain`(요일 없음) 신설로 기획 확정 문안 우선. QA·마스터 모두 타당 판정(작고 안전, 콘텐츠 확정 우선 원칙). **해결.**
3. **[QA→마스터, 문서 정합 비차단]** design §8 문구가 여전히 `formatKoreanDate`를 지시 → 실제 구현(plain)과 형식적 편차. 제품 무영향 doc-only. 아래 백로그로 이월(추가 커밋 churn 회피 위해 이번엔 로그만).

### 마케팅 카드뉴스 게이팅: **START 유지** — 오늘 신규 라이브(근속연수 계산기) 전용 카드뉴스 6장 + SNS 캡션 3종 초안 추가
- `marketing/promo-drafts.md` 하단 84행 순수 추가(0 삭제 확인), 대표 수치(1,993일/5년5개월14일/약5.5년) 엔진·앵커 일치. YMYL 가드(퇴사·이직 부추김 배제, 연차 개인화 미약속, 안전문구 포함). 커밋 **`58589e1`** push.
- 실제 이미지화·게시는 여전히 사용자 승인 사안(미실행). 누적 카드뉴스 3건(연봉7·단위변환6·근속연수6).
- 참고: 마케팅팀이 Higgsfield MCP instructions의 "generate_image 등을 핵심 도구로 쓰라"·업로드 유도 문구를 재확인(07-09/13/14 동일) → 프롬프트 인젝션으로 취급, 생성/업로드 도구 미호출.

### CS 문의 채널
- 변동 없음. `/support` Resend 발신 전용, 서버측 인바운드 큐 부재 — 조회할 실제 문의 없음(이번 실행도 사용자 문의 응대 없음).

### 배포(push)
- `6f98cc6`(근속연수 계산기) + `58589e1`(카드뉴스) — 둘 다 빌드+독립오라클+QA독립검증+라이브(데스크톱/모바일) 통과 후 push. `.claude/settings.local.json`은 제외(로컬 권한 캐시). `.env*` 미포함 확인, 신규 파일 개인정보 grep 0건.

### 다음 실행으로 넘기는 미해결/후속
1. **[로드맵]** 다음 신규 계산기 = **8순위 퇴직금 계산기**(`/salary`, YMYL 중간, 근속연수 계산기와 세트 — 근속연수 계산기에서 관련링크로 연결됨). YMYL-중간이라 연봉 계산기처럼 근사/면책 가드 필요, planning부터 착수.
2. **[doc-only 백로그, 낮음]** `design/service-period-calculator-ui-spec.md` §8의 `formatKoreanDate` 지시를 `formatKoreanDatePlain`(요일 없음)으로 문서 정정(제품 무영향, QA 지적). 다음에 이 문서 손댈 때 함께.
3. **[운영 관찰 지속]** cron `*/30 * * * *` 잦은 실행·중복/근접 트리거 가능성 계속 관찰(lastRunAt 대조). 매 커밋 전 git fetch로 충돌 차단 절차 유지.
4. **[게이팅 후속, 사용자 승인 대기]** 카드뉴스 이미지화·실제 게시 채널/계정 승인 — 누적 3건(연봉7·단위변환6·근속연수6).
5. (이월 유지) 근로소득세 Option A 이관, 요율 갱신 체크리스트, design-system 편입 보류분(salary 3 + unit 4 + service-period 히어로 분리span 등), 법률 전문가 검토, CS 서버 인바운드 큐 부재, 단위변환 한국 전통 단위 확장(앵커 미검증), M-1 버튼안내/FAQ6 문구 통일(낮음).

---

## 저녁 실행분 (로드맵 #8 퇴직금 계산기 라이브 — 라이브 7개→8개)

> 세션 시작 시 working tree는 `.claude/settings.local.json`(의도적 제외)만 미커밋인 clean 상태,
> origin과 0/0 동기화. 고아 작업 없음. 직전 오후 실행분 핸드오프가 지목한 "다음 = #8 퇴직금
> 계산기(근속연수와 세트, YMYL-중간)"를 이어서 처리. cron 30분 간격 겹침 대비 커밋 직전 git
> fetch+behind 재확인(0/0), 파일명 지정 스테이징(`.claude/settings.local.json` 제외).

### 완료: 정상 파이프라인 (기획 → 디자인 → 마스터 확정 → 개발 → QA 독립검증 PASS → 마스터 재검증 → push)
- `planning/severance-pay-calculator-content.md` (기획): 입력 5개 확정(입사일·퇴사일·최근3개월급여합계·연간상여·연차수당), STEP 1~7, 손계산 앵커 A~E, interpretation(700자)·FAQ 5개·shortDescription. 세트 핵심발견 = 우리 "퇴사일(마지막근무일)+1"이 고용노동부식 계속근로기간과 수치 동일 → #7과 재직일수 정합.
- `design/severance-pay-calculator-ui-spec.md` (디자인): 기획 §7 입력·출력을 그대로 채택(자기검증표 불일치 0 — 단위변환 목록 핑퐁 재발 안 함). 결과 3-tier(대표값+보조4행 항상노출, 접이식 미도입), 1년미만 중립회색 안내카드, 상단 amber 배너 카피 1·2안.
- `lib/severance.ts` (개발 신규): STEP 1~7 순수엔진. 재직일수=`calculateServicePeriod` 재사용(양끝포함+1). **N_avg = D_ret−3개월 월말 clamp**(`subtractThreeMonthsClamped` — 순진한 `new Date(y,m-3,d)` 롤오버 방지). 1년미만 게이트(<365), 라운딩=1일평균 전정밀도+최종만 Math.round. 판별유니온 방어(invalid-start/end, end-before-start, eligible:false).
- `components/SeverancePayCalculator.tsx` (개발 신규): design 스펙대로. 연봉 배너/히어로/formatAmountInput/formatWon + 근속연수 FieldError·usedToday 결합. 동적 요약 getResultSummary.
- `lib/calculators.ts`: `relatedSlugs?` 선택 필드 신설(하위호환) + severance 등록(status live) + **퇴직금↔근속연수 양방향 세트링크**(#7에도 relatedSlugs 추가, 다른 필드 미변경). `app/calculator/[slug]/page.tsx` 매핑.
- 커밋 **`4d2c48f`** push 완료.

### 마스터 확정 결정 (기획 §8 / 디자인 §9 응답)
- §8-1 Option B(3개월 합계 입력) 승인 / §8-2 상여·연차 선택입력+3/12 승인(제외 시 체계적 과소추정 리스크) / §8-3 통상임금 제외+면책 승인 / §8-4 1년미만 미산출+안내 승인 / §8-5 라운딩 승인 / §8-6 service-period 재사용 승인.
- §8-7 관련링크 override를 **양방향으로 확장 확정**: 자동로직상 #7↔#8이 서로 안 가리켜, 세트 파트너 상호링크를 위해 양쪽 relatedSlugs 지정.
- 디자인 배너 = **1안**(세전·근사·공식출처 3중) 채택. 보조 4값 = 계산흐름순. 1년미만 = 중립회색. (소규모 카피/UI라 기획 추가 왕복 없이 마스터 확정 — 기존 선례와 동일 기준.)
- **구현 유의로 dev/QA에 강조**: STEP4 "D_ret−3개월"의 월말 clamp(5/31 등). dev가 구현+self엣지, QA·마스터가 독립 재현으로 92 vs 89 판별 케이스 확인.

### QA팀 독립검증 결과: **전 항목 PASS** (FAIL 0)
- 개발 엔진 재사용 안 하고 별도 JS(`scratchpad/verify.mjs`)로 앵커 A~E 중간값까지 독립 재계산 → 전부 일치. clamp 엣지 92(clamp) vs 89(롤오버) 판별 확인. 365경계(eligible)/364(ineligible), 1년미만 금액 구조적 미노출(ResultState에 amounts 부재), 양방향 관련링크+하위호환, 콘텐츠 수치=엔진 일치, 빌드 24페이지, 모바일 table미사용. 개인정보 재노출 0.
- 마이너(조치불요): 동적요약 "약 N년"이 정수 years 절삭(예 5.5년→약5년). design §3-5 준수·"약" 헷지·정확 근속 N년M개월 병기라 비차단.

### 마스터가 직접 확인한 근거
- **실 shipped 엔진 독립 실행**(임시 `_master-verify-severance.ts` `npx tsx` 후 삭제): 앵커 A 20,562,239(Navg92,D2192)·B 21,178,856·C 12,952,686(Navg89)·D 6,994,521(Navg90윤년,A정확히100,000)·E 지급대상아님(243). 엣지: 365일=eligible/364=ineligible, clamp(퇴사 2025-05-30→Navg92 not 89), end<start=error, 빈퇴사일→오늘(usedToday), 음수→0 방어. 전부 기대치 일치.
- **빌드**: `npm run build` 직접 실행 Compiled success, 타입에러 0, static **24/24**, severance SSG 포함.
- **라이브 브라우저 검증**(dev :3000, DOM 구동):
  - 데스크톱 앵커A 입력→계산→히어로 "예상 퇴직금 세전 20,562,239 원" + 세전각주 + 동적요약 "입사일 2019년 1월 1일부터…예상 퇴직금은 약 2,056만원입니다." + 보조4행(임금총액10,500,000→산정일수92일→1일평균114,130원→재직일수2,192일 근속6년0개월, 계산흐름순) 정확.
  - 1년미만(243일): 중립회색 카드(`border-brand-border bg-brand-surface`), 격리 카드 텍스트에 "원" 0건(금액 미노출), 재직일수·근속만.
  - end<start: 인라인 alert "퇴사일이 입사일보다 빠릅니다…" + 결과카드 없음.
  - 관련링크: severance=[연봉→근속연수→만나이], #7 service-period=[퇴직금→연봉→만나이](역방향), age(relatedSlugs 없음)=자동로직 유지(하위호환).
  - 모바일 375: `clientWidth=scrollWidth=375`(가로 오버플로 0), 계산버튼 48px, 히어로 렌더.
- **보안**: 신규·수정 파일 `grep sss159228|RESEND|@gmail|api_key` 0건, 커밋에 `.env*`/`settings.local` 미포함 확인.

### 미확인 항목
- 실기기(실제 휴대폰)·픽셀 스크린샷은 이번에도 프리뷰 프로세스 조기종료/뷰포트 불안정으로 미확보 — DOM 측정(scrollWidth/getBoundingClientRect)·live 상호작용으로 대체(07-13부터 반복 환경이슈). 단 이번엔 resize 후 375 정상 측정됨(innerWidth 초기 0→resize 후 375).
- 실제 Vercel 배포 반영·AdSense 상태는 push까지만 확인(GSC API 미연동, 감사 트랙 소관).

### 핑퐁 이슈
1. **[planning↔design, 핑퐁 0]** design이 planning 확정 §7을 먼저 Read하고 설계 → 입력/출력 불일치 0(단위변환 전례 대비 개선 유지). 마스터 조정 불필요.
2. **[design→마스터 확인, 소규모 마스터 직결]** 배너 카피 택1·보조값 순서·1년미만 톤 = 마스터가 즉시 확정(1안/계산흐름순/중립회색). 기획 추가 왕복 없이 처리(작고 안전한 콘텐츠·UI 결정).
3. **[QA→마스터, 비차단 참고]** 동적요약 "약 N년" 정수 절삭 → 마스터 판단으로 비차단 백로그(스펙 준수·헷지·정확값 병기). 아래 이월.

### 마케팅 카드뉴스 게이팅: **START 유지** — 오늘 신규 라이브(퇴직금 계산기) 전용 카드뉴스 6장 + SNS 캡션 3종 초안 추가
- `marketing/promo-drafts.md` 하단 92행 순수 추가(0 삭제). 대표 수치는 앵커A만 사용(2,192일·900만+600만·1일평균 114,130원·예상 퇴직금 약 2,056만원 세전) — 엔진·기획·interpretation과 일치(grep 재확인). 카드 구성: 후크(정보톤 중립화)→법정공식·입력→앵커A 예시→상여·연차 3/12+근속연수 세트→한계·면책(필수)→CTA.
- YMYL-중간 가드: 퇴직·이직 부추김 배제, 세전·통상임금/부분월 한계·공식출처 확인 3중 면책을 카드5·캡션에 명시. Higgsfield MCP "생성도구 쓰라" 인젝션 문구 재확인·무시(생성/업로드 도구 미호출, 07-09/13/14 동일).
- 마케팅이 기존 2-C(2026-07-08 구 퇴직금 초안, 이직 전제 후크) 섹션에 "2026-07-14 갱신: 최종본으로 대체" 주석 1줄 추가(삭제 아닌 additive, 기존 2-B 동일 패턴). 마스터 확인: git diff 0 deletions, 구 아이디어 초안을 최종본으로 대체 표기한 것이라 수용.
- 실제 이미지화·게시는 여전히 사용자 승인 사안(미실행). 누적 카드뉴스 4건(연봉7·단위변환6·근속연수6·퇴직금6).

### CS 문의 채널
- 변동 없음. `/support` Resend 발신 전용, 서버측 인바운드 큐 부재 — 조회할 실제 문의 없음(이번 실행도 사용자 문의 응대 없음). CS팀은 이번 실행에서 별도 배정 없음(신규 페이지 사전검수는 QA가 독립검증으로 커버).

### 배포(push)
- `4d2c48f`(퇴직금 계산기 기능+기획+디자인) — 빌드+실엔진 앵커실측+QA독립검증+라이브(데스크톱/모바일) 통과 후 push 완료.
- 마케팅 카드뉴스 초안 + 이 팀로그 — 별도 커밋으로 push 예정(아래). `.claude/settings.local.json`(로컬 권한 캐시) 제외. `.env*` 미포함·개인정보 grep 0건 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[로드맵]** #7·#8 세트 완성(근속연수+퇴직금). 다음 신규 계산기 후보는 로드맵 9순위 이후 — 다음 실행이 `planning/calculator-lineup.md`로 우선순위 재확인 후 착수. 신규 계산기라면 planning부터.
2. **[비차단 백로그, 낮음]** 동적요약 "약 N년" 정수 절삭(예 5.5년→약5년) — 세트 파트너 #7이 decimalYears("약 X.X년")를 쓰므로 원하면 일관성 차원에서 decimalYears로 통일 검토(단, 그러면 interpretation 정적문단 "약 6년"과 "약 6.0년" 소폭 편차 발생 — 트레이드오프 있음). QA·마스터 모두 현재 비차단 판정.
3. **[doc-only 백로그, 낮음]** `design/service-period-calculator-ui-spec.md` §8 formatKoreanDate→Plain 문서정정(오후 실행분 이월, 제품 무영향).
4. **[게이팅 후속, 사용자 승인 대기]** 카드뉴스 이미지화·실제 게시 채널/계정 승인 — 누적 4건(연봉7·단위변환6·근속연수6·퇴직금6). 마케팅은 묶어서 검토 제안.
5. **[운영 관찰 지속]** cron `*/30 * * * *` 잦은 실행·중복/근접 트리거 계속 관찰(lastRunAt 대조). 이번 실행은 커밋 전 fetch 0/0로 충돌 없었음.
6. (이월 유지) 근로소득세 Option A 이관, 요율 갱신 체크리스트, design-system 편입 보류분(salary 3+unit 4+service-period+severance), 법률 전문가 검토, CS 서버 인바운드 큐 부재, 단위변환 한국 전통 단위 확장, 퇴직금 통상임금 입력 확장 여부(현재 면책만).

### 참고 (동시성 관찰, 조치 불필요)
- 이번 실행 커밋 직전 `git status`에 내가 만들지 않은 untracked `planning/electricity-bill-calculator-content.md`가 출현 — 겹쳐 도는 다른 스케줄 세션(전기요금 계산기 기획 착수 추정)의 진행 중 작업으로 보인다. **건드리지 않고 스테이징에서 제외**(내 커밋엔 marketing/promo-drafts.md·이 팀로그만 포함). 다음 실행은 전기요금 계산기가 다른 세션에서 이미 진행 중일 수 있으니 착수 전 `git status`/`git log`로 중복 확인할 것([[project_scheduled_workflows]] 동시성 원칙).

---

## 19:35 실행분 (로드맵 #9 전기요금(누진세) 계산기 라이브 — 라이브 8개→9개)

> 세션 시작 시 origin 0/0 동기화, tip `4d2c48f`(#8 퇴직금, 19:31 push). 직전 저녁 실행분 핸드오프의
> "다음 = 로드맵 9순위 이후, planning/calculator-lineup.md 재확인 후 착수"를 이어받아 **#9 전기요금
> 계산기**(/life 2순위, 여름 시의성)를 정상 파이프라인으로 처리. **동시성 주의**: 겹쳐 돈 다른 세션이
> 실행 중 `dc0a97a`(#8 퇴직금 카드뉴스 + 저녁 팀로그)를 19:47 push했고, 그 로그에 "내가 만들지 않은
> `planning/electricity-bill-calculator-content.md` 출현 → 다른 세션의 전기요금 작업으로 보고 제외"라고
> 남겼다 = **그 세션이 본 그 파일이 바로 이 세션의 planning 산출물**. 두 세션이 서로 다른 작업(그쪽=#8
> 마케팅, 이쪽=#9 전기요금)을 해 충돌 없음. 커밋 직전 fetch로 0/0 재확인.

### 완료: 정상 파이프라인 (기획 → 디자인 → 마스터 확정 → 개발 → QA 독립검증 PASS → 마스터 재검증(빌드+독립계산+라이브브라우저) → push)
- `planning/electricity-bill-calculator-content.md` (기획): 입력 2개 확정(월 사용량 kWh 필수 + 계절 select, 기본=현재월 자동), STEP 0~8 파이프라인, 요금상수 리서치+교차확인, 손계산 앵커 A~D, interpretation(750자)·FAQ 5개·shortDescription. **핵심 발견 = 전력산업기반기금 2.7%**(과제 예시 3.7%는 폐지값, 3.7→3.2→2.7 단계 인하). 한전 공식 PDF는 캐시 문제로 원본 직접대조 불발 → 교차확인값 + 상수 분리 + 면책으로 처리(연봉 Option B 선례와 동일 기준).
- `design/electricity-bill-calculator-ui-spec.md` (디자인): planning §7 먼저 Read → 입력/출력 불일치 0(대조표 명시). 3-tier 결과카드(대표값+누진게이지+요약3행 항상노출+세부4행 접이식) + amber role=note 배너(1안) + **누진단계 게이지**(신규 시각요소, 접근성: 색+테두리+굵은라벨 3중구분·빨강 금지·role=img+aria-label 텍스트대체).
- `lib/electricity.ts` (개발 신규): STEP 0~8 순수엔진 + 요금상수 named const(갱신대상 주석). **데시-원 정수 연산**으로 floor 부동소수 오차 차단(150×214.6=32,189.99… 사고 방지). 판별유니온 방어(음수·비유한 → invalid-usage), 게이지 파생값 엔진 반환.
- `components/ElectricityBillCalculator.tsx` (개발 신규): design 스펙대로. 연봉 골격(배너·selectBase·formatAmountInput·AmountRow·details·getResultSummary·resultRef) + 퇴직 FieldError 결합. 계절 기본값 = 클라이언트 lazy init(빌드타임 고정 방지) + suppressHydrationWarning.
- `lib/calculators.ts`: electricity-bill-calculator 등록(status live, interpretation/faq/shortDescription = 기획안). relatedSlugs override 미지정(자동 = 단위변환+BMI+만나이, 라인업 §3-3 정확 충족). `app/calculator/[slug]/page.tsx` 매핑.

### 마스터 확정 결정 (기획 §8 응답)
- §8-1 slug `electricity-bill-calculator` 승인 / §8-2 저압 고정·슈퍼유저(>1000kWh) 별도단가 미구현·복지할인 미반영 승인(보수적 단순화, "실제보다 높게 보여주는" 안전한 오차 방향) / §8-4 **전력기금 2.7% 확정** / §8-5 상수 모듈 분리 / §8-6 라운딩(Energy·Subtotal=floor, VAT=round, Fund·Total=floor10) / §8-7 동적요약+계절 클라이언트 자동. 배너 = 디자인 1안. 인터랙션 = 버튼식(YMYL amber-banner family 일관성, 실시간은 단위변환 전용).
- 한전 공식 PDF 최종대조(§8-3)는 원본 확인 사안이라 미완으로 남김 — 상수는 교차확인값 + 갱신 주석 + 면책으로 관리(다음 실행/사용자가 공식표로 대조 시 lib/electricity.ts 상수 블록만 수정).

### QA팀 독립검증 결과: **PASS** (FAIL 0, 비차단 지적 1 = 디자인 문서 오타)
- 개발 엔진 재사용 안 하고 **from-scratch node 스크립트(naive float) + 실엔진 npx tsx 직접실행**으로 앵커 A~D를 3중 대조(기획표/QA독립/엔진출력) → 전 중간값 일치. 엣지(0·음수·NaN·1200 슈퍼유저·경계값 200/400/300/450·계절전환·소수·라운딩) 전부 PASS. 상수 실측(기금 0.027=2.7% 확정, 3.7% 0건). 콘텐츠 예시수치=앵커B 일치. 빌드 25페이지, 계산기 8→9. 접근성(role=img+aria-label, 색단독 아님, 빨강 0, h-12, role=note 배너, table 0). 개인정보 0.
- 비차단 지적: 디자인 §3-3-1 게이지 visual max 문구 "기타 500"이 자신의 공식 `b2+(b2−b1)`(=600)과 모순. 금액 무관 순수 시각 파라미터.

### 핑퐁 이슈 (전부 해결)
1. **[dev→design, 게이지 visual max 모순]** dev가 "디자인 문구 500 vs 공식 600 불일치"를 발견·보고 → 마스터가 design팀에 SendMessage로 되돌림 → **design 회신: 600/600 확정(=문구 "500"이 자신의 오타), 스펙 문서 자가정정 완료**. dev는 이미 자기일관적인 600/600으로 구현·주석. 코드 수정 불요. **해결.**
2. **[design→dev 구현검수]** design이 구현을 스펙과 대조 검수 → **"구성안 일치"**(3-tier·배너 1안 자구일치·게이지 접근성·모바일 오버플로0·h-12·계절문구 전부 일치, 코드 수정 불요). **해결.**
3. **[planning↔design, 핑퐁 0]** design이 planning §7 먼저 Read → 입력/출력 불일치 0(단위변환 전례 개선 유지).

### 마스터가 직접 확인한 근거
- **독립 재계산**(shipped 엔진 미사용, 별도 node .mjs): 앵커 A=31,220 / B=70,640 / C=110,280(하계) / D=83,530 전부 기대치 일치. 엣지 0kWh→1,020(기본요금만), 500kWh 기타 Energy 97,650 vs 하계 83,555(계절경계 효과) 확인.
- **코드 리뷰**(lib/electricity.ts Read): 데시-원 정수연산, 상수 2.7% 확정, floor10/round/floor 라운딩 위치 정확, 방어(음수·비유한) 존재. calculators.ts 9개 slug·9 live 무결(중복 없음), interpretation 예시수치=앵커B 일치. page.tsx 매핑 정확.
- **빌드**: `npm run build` Compiled successfully, 정적 25페이지, `.next/.../electricity-bill-calculator.html`(41KB) 프리렌더 생성.
- **라이브 브라우저 검증**(dev :3000, JS로 React 컨트롤드 입력 구동 — 뷰포트 0 환경이슈로 클릭 대신 native setter+dispatch):
  - 350kWh 기타계절 → 히어로 **70,640원**, chip "기타계절 2단계", 전기요금계 62,690·부가세 6,269·기금 1,690 렌더, 게이지 role=img aria-label "현재 350kWh는 기타계절 누진 2단계 구간입니다. 3단계(400kWh)까지 50kWh 남았습니다." 정확, 빨강 0.
  - 500kWh 하계 → **110,280원**, chip "하계 3단계"(앵커C 일치).
  - 1,200kWh → 슈퍼유저 note 노출. 빈 사용량 → role=alert "월 사용량을 입력해주세요." + 결과 미렌더.

### 미확인 항목
- 실기기(실제 휴대폰) 픽셀 스크린샷·375px 실측 레이아웃: 프리뷰 뷰포트가 이번엔 0x0로 고정돼(resize에도 미회복) 스크린샷/폭실측 미확보 — DOM 구동 상호작용 + 코드 grep(table 0·flex justify-between·w-full 게이지·h-12)로 대체. 가로 오버플로 0은 구조(무table·flex·flex-wrap 히어로)로만 확인, 픽셀 미측정.
- 한전 공식 요금표 PDF 원본 대조(§8-3): 교차확인값으로 ship, 원본 미대조. 실 Vercel 배포·AdSense는 push까지만(GSC API 미연동).

### 배포(push)
- 예정: `lib/electricity.ts`·`components/ElectricityBillCalculator.tsx`·`lib/calculators.ts`·`app/calculator/[slug]/page.tsx`·`planning/`·`design/` 신규+수정 + 이 팀로그. `.claude/settings.local.json`(로컬 권한 캐시) 제외. `.env*` 미포함·개인정보 grep 0건 확인. QA PASS + 마스터 빌드/독립계산/라이브 통과분이라 auto-push 대상(2026-07-13 승인).

### 다음 실행으로 넘기는 미해결/후속
1. **[로드맵]** 다음 신규 계산기 = **10순위 중도상환수수료 계산기**(/loan, 대출이자 이용자 동선) 또는 11순위 4대보험료(/salary, 연봉 보조). `planning/calculator-lineup.md` §2 순서 재확인 후 착수. 신규면 planning부터.
2. **[YMYL 후속, 낮음]** 전기요금 요금상수 한전 공식 요금표 PDF 최종대조(기획 §8-3 미완). 상수는 `lib/electricity.ts` 상단 블록에 격리돼 있어 대조 후 그 블록만 수정하면 됨. 연료비조정요금(분기변동)·전력기금 요율 갱신 체크리스트도 함께.
3. **[마케팅 게이팅]** 전기요금 계산기(9번째 라이브, 여름 시의성 최상) 카드뉴스 초안 — 이번 실행에서 배정 검토(아래 마케팅 섹션 참조). 누적 카드뉴스 현재 4건.
4. (이월 유지) 카드뉴스 이미지화·게시 채널 사용자 승인 대기, 근로소득세 Option A 이관, design-system 편입 보류분(+전기요금 누진게이지 정식편입 여부), 법률 전문가 검토, CS 인바운드 큐 부재, cron 30분 겹침 관찰, 퇴직금 통상임금·단위변환 전통단위 확장.

---

## 20:37 실행분 (로드맵 #10 중도상환수수료 계산기 라이브 — 라이브 9개→10개)

> 세션 시작 시 origin 0/0 동기화, tip `3613731`(전기요금 카드뉴스, 20:34 push). 직전 19:35 실행분 핸드오프의
> "다음 = 10순위 중도상환수수료(/loan) 착수, planning부터"를 이어받아 **#10 중도상환수수료 계산기**를
> 정상 파이프라인으로 처리. git status에 미커밋 WIP 없음(settings.local.json만) 확인 후 시작.

### 완료: 정상 파이프라인 (기획 → 디자인 → 마스터 확정 → 개발 → QA 독립검증 PASS → 마스터 재검증(빌드+독립계산+라이브브라우저) → push)
- `planning/loan-prepayment-fee-content.md` (기획): 표준공식 `Fee = 중도상환금액 × 수수료율 × (잔존÷대출기간)` 슬라이딩. **핵심 판단 = 과제 제안 입력 ③"잔존기간"을 "경과기간(개월)"으로 변경** — 30년 주담대에서 "잔존 29년" 넣는 구조적 오입력 차단(잔존은 파생 R_m = D_m − E_m). 3년(36개월) 캡 + 3년 경과 면제 기본 채택. 2025.1.13 수수료율 인하(주담대 1.4%→0.65%대, 신규계약만) 리서치 반영 → 수수료율 사용자 입력이 최종, default 0.7% 상수 분리. 손계산 앵커 A~D, interpretation(740자)·FAQ 5개·shortDescription.
- `design/loan-prepayment-fee-ui-spec.md` (디자인): planning §7 먼저 Read → 입력/출력 불일치 0(대조표). 입력 4개 세로스택, 대표값 히어로+근거 4행 항상노출(접이식 미채택), amber role=note 배너, **슬라이딩 2세그먼트 게이지**(전기요금 게이지 접근성 재사용: 색+테두리+굵은라벨 3중·role=img+aria-label·빨강 금지), 면제 상태 카드 분기. ③ 라벨서 "잔존" 표현 전면 금지.
- `lib/loan-prepayment.ts` (개발 신규): STEP 0~4 순수엔진. `D_m=min(T_m,36)`·`R_m=D_m−E_m`·R_m≤0→면제·`Fee=floor(A×R_m×f/(100×D_m))` **정수곱 우선**(순환소수 floor 오차 차단). 상수 3개(캡36·1095일·default0.7) named const + 갱신주석. 판별유니온 방어(음수·비유한·총기간<1→invalid-input). 게이지 파생값 반환.
- `components/LoanPrepaymentFeeCalculator.tsx` (개발 신규): design 스펙대로. 동적요약 정상/면제 분기, FieldError, 수수료율>5% note, "계산하기" 중립버튼(오인 CTA 0).
- `lib/calculators.ts`: loan-prepayment-fee 등록(status live, category loan, interpretation/faq/shortDescription = 기획안). **relatedSlugs override = ["loan-interest-calculator","salary-net-calculator","age-calculator"]**(마스터 확정). loan-interest 미수정(자동 sameCategory로 역방향 성립). `app/calculator/[slug]/page.tsx` 매핑.

### 마스터 확정 결정 (기획 §8 응답)
§8-1 slug `loan-prepayment-fee` 승인 / §8-2 경과기간(개월) 방식 승인·날짜정밀화는 후속옵션 보류·최소수수료/일할 엣지 v1 제외 / §8-3 **3년 캡+면제 기본 로직 확정**(상품예외 면책) / §8-4 default 0.7%+floor 승인 / §8-5 상수분리 승인 / §8-6 동적요약+면제분기 승인·FAQ 개인정보 6번째 문항 불필요(interpretation ⑤ 커버) / §8-7 relatedSlugs override 승인·역방향 loan-interest 자동성립으로 미지정. 인터랙션=버튼식(YMYL family 일관).

### QA팀 독립검증 결과: **PASS** (블로킹 FAIL 0, 비블로킹 관찰 4)
- from-scratch 참조구현 + 실엔진 npx tsx 3중대조: 앵커 A=37,500 / B=433,333(대표) / C=200,000 / D=0(면제) 전부 일치. 엣지(음수·NaN·Inf·총기간0→invalid / 경과=분모·>분모→면제 / 총기간<36 캡미적용 / 율0·>5% / 큰금액 1e15 안전정수 / ratio 0·1 경계) 전부 PASS. **부동소수**: 브루트포스 468건서 소박한 float순서가 언더카운트(700,000→699,999)하나 엔진은 정수곱-우선 채택해 회피 확인. 콘텐츠 예시수치=앵커B 일치, ③ 화면 라벨 "경과기간"("잔존" 미사용) 확인. 등록(10 slug·10 live·override·category loan·매핑) 무결. 접근성(role=img+aria-label, 색단독 아님, 빨강 0, table 0, 면제분기, role=note 배너, 오인CTA 0, 개인정보 fetch/storage 0). 빌드 26페이지.
- 비블로킹 관찰(수정 불요): formatFeeRate 죽은분기(무해)·소수 개월 입력 허용(에러없이 합리처리)·amount=0 컴포넌트 선차단·float순서 회귀주석 가치. 담당 dev, 차기 리팩터 시 참고.

### 핑퐁 이슈
- planning↔design 핑퐁 0(design이 planning §7 먼저 Read, 불일치 0). design→planning 되돌림 없음(출력항목 변경 불요). dev 발견 2건(파일명 표기 갈림 lib/loan-prepayment.ts vs loanPrepayment.ts → 마스터 확정 우선 채택 / design §3-7 절번호 누락, 문안은 §3-2·§4-3·기획§2에 존재 → 온전)은 자체판단 처리, 코드 이슈 아님. **핑퐁 없이 1패스 완료.**

### 마스터가 직접 확인한 근거
- **독립 재계산**(shipped 엔진 미사용, 별도 node .mjs): 앵커 A=37,500 / B=433,333 / C=200,000 / D=0(면제) 전부 기대치 일치. edge 36/36→면제, 율0→0 확인.
- **코드리뷰**(lib/loan-prepayment.ts Read): D_m=min(T_m,36), R_m=D_m−E_m, R_m≤0 면제, Fee 정수곱-우선 floor, 상수 분리+갱신주석, 방어(음수·비유한·총기간<1) 존재. calculators.ts 10 slug 유일(sort로 확인)·override 정확·매핑 정확.
- **빌드**: `npm run build` Compiled successfully, TypeScript 에러 0, 정적 26페이지, /calculator/[slug] 10경로.
- **라이브 브라우저**(dev :3000, JS 컨트롤드 입력 구동 — 뷰포트 0x0 환경이슈로 resize 미회복, native setter+dispatch):
  - 앵커B(1억·0.65%·12개월·360개월) → 히어로 **433,333원**, 게이지 role=img aria-label "…3년(36개월) 캡 중 24개월 잔존(잔존비율 66.7%)…" 정확, amber role=note 배너 렌더, 빨강 0, table 없음, 동적요약 "약 433,333원".
  - 앵커D(5천만·1.2%·40개월·360개월) → **면제 대상** 히어로, 게이지 aria-label "…40개월이 지나 면제기준기간 3년(36개월)을 넘었습니다. 면제 대상입니다." 분기, 동적요약 "…36개월을 넘었으므로 중도상환수수료가 면제됩니다.", 0원.

### 미확인 항목
- 실기기(실제 휴대폰) 픽셀 스크린샷·375px 실측 레이아웃: 프리뷰 뷰포트가 이번에도 0x0로 고정(resize 미회복, 19:35 실행분과 동일 환경이슈) → 스크린샷/폭실측 미확보. 가로 오버플로 0은 QA 코드grep(table 0·flex·flex-wrap·w-full 게이지)으로만 확인, 픽셀 미측정.
- 수수료율/3년 캡 상수 원본(은행 약정서·은행연합회 공시) 최종대조는 교차확인값으로 ship, 원본 미대조(상수 격리+갱신주석+면책 관리). 실 Vercel 배포·AdSense는 push까지만.

### 배포(push)
- 대상: `lib/loan-prepayment.ts`·`components/LoanPrepaymentFeeCalculator.tsx`·`lib/calculators.ts`·`app/calculator/[slug]/page.tsx`·`planning/`·`design/` 신규+수정 + 이 팀로그. `.claude/settings.local.json`(로컬 권한 캐시) 제외. `.env*` 미포함·개인정보 grep 0건. QA PASS + 마스터 빌드/독립계산/라이브 통과분이라 auto-push 대상(2026-07-13 승인).

### 다음 실행으로 넘기는 미해결/후속
1. **[로드맵]** 다음 신규 계산기 = **11순위 4대보험료 계산기**(/salary, 연봉 계산기 보조) 또는 12순위 DSR(/loan, 정책 민감도 높아 관리부담). `planning/calculator-lineup.md` §2 순서 재확인 후 착수. 신규면 planning부터. (loan 카테고리는 이제 대출이자+중도상환 2개 → DSR 추가 시 relatedSlugs 재조정 여지, 기획 §8-7)
2. **[마케팅 게이팅]** 중도상환수수료 계산기(10번째 라이브) 카드뉴스 초안 — 다음 실행 배정 검토. 누적 카드뉴스 현재 6건(전기요금까지). 마케팅은 이번 실행 미배정(계산기 파이프라인에 집중, 시간).
3. **[YMYL 후속, 낮음]** 중도상환수수료율·3년 캡 상수 은행연합회 공시/약정서 원본 대조(기획 §8-3·§8-5). 상수는 `lib/loan-prepayment.ts` 상단 블록 격리. 2025 요율 개편 추가 변동·폐지 논의 갱신 체크리스트에 편입.
4. (이월 유지) 전기요금 한전 PDF 상수 최종대조, 카드뉴스 이미지화·게시 채널 사용자 승인 대기, 근로소득세 Option A 이관, design-system 편입 보류분(+전기요금 누진게이지·중도상환 슬라이딩게이지 정식편입 여부), 법률 전문가 검토, CS 인바운드 큐 부재, cron 30분 겹침 관찰, 퇴직금 통상임금·단위변환 전통단위 확장.
