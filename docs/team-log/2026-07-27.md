# 2026-07-27 팀 로그 (daily-team-standup)

## 18:24 실행분 — 티켓 #2 근속기간 월말 경계 결함 **해결**

> ⚠️ **실행 시각 이상**: 이 작업의 지시서는 "새벽 1~7시대 30분 간격, 하루 14회"로 되어 있으나
> 이 회차는 **18:24에 실행**됐다. 오늘 `docs/team-log/2026-07-27.md`가 없었고
> `2026-07-27-gsc-audit.md`(오전 11:57 종료)만 있었으므로 **daily-team-standup의 오늘 첫 실행**으로 판단하고 진행했다.
> 스케줄 설정이 의도대로인지 사용자 확인이 필요할 수 있다(블로커는 아님 — 알림 미발송).

### 배정 근거
07-26 인계 **#1 [★최우선]** 그대로. 라이브 계산기에 음수 일수가 실제 노출되는 실사용자 영향 결함이라 블로그 신규 발행보다 우선.

---

### 결함 재현 (마스터, 착수 전)
`lib/service-period.ts:88` — 양 끝 포함을 위해 `종료일⁺ = 종료일+1`로 만든 뒤 캘린더 분해하는데,
`days < 0`일 때 **`if`로 단 한 번만** 직전 달 일수를 빌린다. 입사일이 30·31일이고 직전 달이 2월이면 1회 차용으로 부족해 **음수가 잔존**.

```
2000-01-31 ~ 2026-03-01 → 26년 1개월 -1일
2020-01-31 ~ 2021-02-28 → 1년 1개월 -2일
```

---

### 파이프라인: planning + design(병렬) → 마스터 검산 → QA 사전검증 → dev → QA 재검증 → 마스터 §7

#### 1) planning — 시맨틱 정의
후보가 둘이 아니라 **셋**임을 밝혀냄: (A) 반복차용 / (B-1) 앵커 clamp(java.time·date-fns 계열) / (B-2) 민법 §160③ 말일만료.
**(B-2) 채택 권고**, 근거 4개(민법 조문 직접 적용 / (A)는 근기법 §60② 판단을 오도 / (B-1)은 2/29 입사자의 만근일을 못 만듦 / `if→while` 한 글자 수정은 제3의 오답).
산출물 `planning/service-period-boundary-fix-spec.md`. **Bash 부재로 실행 검증 불가 → 검산 요청 6건을 마스터로 올림(올바른 처리).**

#### 2) design — 표시 규칙
`components/ServicePeriodCalculator.tsx:20·143·145-168` 3곳이 엔진 값을 **가공 없이** 표시. **표시부 자체엔 버그 없음.**
- 0 값 규칙: **변경 불필요**(확정 스펙 §3-2에 "0인 자리도 생략 안 함" 이미 명시, 코드도 일치)
- **UI 클램프(`Math.max(0, days)`) 명시적 반대** — 계산 오류를 가려 다음 유사 버그를 영영 못 잡게 됨. **마스터 동의, dev에 금지 지시로 전달.**
- **★ design이 단독으로 발견**: `lib/severance.ts`가 같은 엔진을 재사용 → `SeverancePayCalculator.tsx:394·424`의 `근속 N년 M개월`도 영향권. QA 범위 포함 요청.

#### 3) 마스터 검산 — planning 결론 일부 **반증**
planning의 6건 중 **③(INV-11)·④·①이 실측과 불일치**. 다만 **내 1차 검산도 틀렸다** — 내가 구현한 "B-2"가 `boundary = 말일`이었는데 스펙의 B-2는 `boundary = 말일+1`이라 하루 어긋났고, 그 탓에 "months 5,103건 변동"이라는 잘못된 수치를 냈다. **QA가 이 오류를 잡아냄.**

#### 4) QA 사전검증 (수정 착수 **전**) — 6개 주장 독립 재현
`Date` 미사용 독립 구현(Howard Hinnant `days_from_civil`)으로 재작성 후 대조. 판정:

| 주장 | 판정 |
| --- | --- |
| A 음수 출력 | **CONFIRMED** (+ 단조성도 깨짐, 28,060 스텝 위반) |
| B 비음수 오답 대량 존재 | **결론 CONFIRMED / 마스터 예시·왕복논거 REFUTED** |
| C B-1 vs B-2 상이 | **상이 CONFIRMED / 마스터 건수·형태 REFUTED**(위 §3 원인) |
| D B-1은 2/29 만근일 못 만듦 | **CONFIRMED** (블로그 본문 3중 의존 확인) |
| E totalDays 불변 | **CONFIRMED** |
| F INV-11 거짓 | **REFUTED — INV-11은 참**(단, B-2 기준일 때) |

**★ 이 회차 최대 수확**: QA가 **현재 코드의 정체를 규명**했다 — 현재 코드는 "(A) 시맨틱 + 1회차용 버그"이고, 비음수 구간은 (A)와 13,457,601쌍 전부 일치한다. 즉 **(B-2) 채택은 "버그 수정"이 아니라 "시맨틱 변경 + 버그 수정"**이다. 이 성격 규정이 범위 결정을 통째로 바꿨다.
또한 planning §4-1·§4-4·§7이 **FAIL**: "영향 33건 / 노출 창 좁음"은 틀렸고, 실제 노출 창은 **매년 3월 한 달 전체**이며 그중 **28/30일이 무증상 오답**(종료일 2019~2031 한정 199,265건).

#### 5) 마스터 범위 결정 — **(A) 유지, 버그만 수정**
(B-2)는 199,265건의 표시값을 바꾸는 **YMYL 정책 변경**이고 (A)·(B-2) 모두 자체 정합적이라, 무인 실행이 임의로 고를 사안이 아니라고 판단. **명확히 깨진 것(음수)만 이번에 고치고, 시맨틱 선택은 사용자에게 올림.** → §"사용자 결정 필요" 참조.

#### 6) dev — 구현
`lib/service-period.ts` 차용 블록만 `if` → `while` + **차용 달 갱신**(`borrowed` 카운터). 함정(`if→while` 한 글자 수정 = 같은 2월 두 번 차용 → `26년 0개월 27일`이라는 제3의 오답) 회피 확인.
자체 검증 23,360,803쌍: 회귀 위반 0 / 불변식 위반 0 / `totalDays`·`decimalYears` 변경 0. 회귀 앵커 F~I 추가(+12 assertions).

#### 7) QA 재검증 — **PASS**
독립 참조 구현(`Date` 미사용, years/months를 `if` 보정이 아닌 **floor 나눗셈**으로 분해 — 주장 6이 틀리면 자동으로 갈라지는 설계) + **29,543,265쌍**(dev와 별개 표본) 전수 대조.
- 회귀 0 / 불변식 0 / `totalDays`·`decimalYears` 비트 단위 동일 / 4클래스 일치
- **구조공간 936,228조합 완전 열거**(샘플링 아님): `k≥3` **0건**, `months<-12` **0건**
- **변이 시험**으로 앵커 검출력 실증: 구 `if` 1회 / `while`만 바꾼 판 / UI 클램프 / 차용 달 오지정 4종을 각각 3~4개 앵커가 검출 → 앵커가 자기 자신과 비교하는 게 아님을 입증
- `severance` 금액 5,775,369 조합 대조 → **금액 차이 0**
- 발행 콘텐츠 인용값 36건 전수 → **변경 0건**
- 번들 확인: 클라이언트·SSR 청크 양쪽에 수정 루프 반영, 구 패턴 잔존 청크 **0개**

**QA 비차단 지적 3건 → 마스터가 이번 회차에 전부 반영:**
1. dev 주석 "months 최소 −11"이 부정확 — **전역 최솟값은 −12**(k=1·입사 12월·종료일⁺ 1월). 결론(보정 1회로 충분)은 유효하나 여유가 1칸뿐 → **주석 정정 완료**
2. `if (months < 0)` 보정 경로를 밟는 앵커가 **하나도 없음**(이 수정의 유일한 하중선인데 미잠금) → **앵커 J 추가**(`1999-12-31 ~ 2000-01-14` → `0년 0개월 15일`, 보정 전 months = −12 전역 최솟값). 203 assertions PASS
3. 스펙 문서가 여전히 "(B-2) 채택"으로 되어 있어 다음 라운드가 되돌릴 위험 → **스펙 최상단에 마스터 정정 블록 삽입**

---

### 마스터 §7 최종 검증 (QA PASS 후에도 직접 실행)
- **클린빌드**(`rm -rf .next`) → `✓ Compiled successfully`, **정적 69/69**, `tsc --noEmit` 무출력, `eslint` 무출력
- `npm test` → **203 assertions ALL SUITES PASS** (기존 188 + F~I 12 + J 3)
- **실브라우저 실측**(dev 서버 + 실제 입력):
  - `2000-01-31 ~ 2026-03-01` → 화면 `26 년 0 개월 30 일`, **음수 표기 0건**
  - `aria-label` = `"근속기간 26년 0개월 30일"` → QA가 "스크린리더가 마이너스를 낭독 중"이라 지적한 경로 **해소 확인**
  - **375px 모바일**: 가로 오버플로 **0px**, 뷰포트 초과 요소 0개, 히어로 274px **1줄**(높이 40px)
  - **퇴직금 화면 교차 확인**: `2020-01-31 ~ 2021-02-28` → `395일 (근속 1년 0개월)`, 음수 0건
- **스크린샷 미취득**(브라우저 pane 미표시로 컴포지팅 불가) — DOM 실측으로 대체, 육안 검증은 **미확인**으로 남김

#### ★ 마스터가 자기 검증을 뒤집은 지점 (기록)
브라우저 실측 결과 퇴직금 화면이 `근속 1년 0개월`로 표시됨 — 구 버전은 같은 입력에서 `근속 1년 1개월`이었다.
즉 **design이 제기한 "퇴직금 개월 오염" 우려는 (A) 채택 하에서 옳다.** QA의 "REFUTED"는 **(B-2) 기준**이었고 우리는 (A)를 택했으므로 적용되지 않는다.
나는 QA 보고를 받고 design의 우려를 성급히 철회했다가 실측으로 되돌렸다. **design 문서 §1-2는 수정 불필요(그대로 유효).**
영향 범위는 **구 버전이 음수를 찍던 입력에 한정**되고 **퇴직금 금액은 불변**(5,775,369 조합 차이 0).

---

### 팀 핑퐁 요약 (이번 회차 6건)
1. **planning → 마스터**: Bash 부재로 검산 6건 위임 → 마스터 실행 → **3건 불일치 발견**
2. **마스터 → QA**: 내 분석이 planning과 충돌해 독립 검증 의뢰 → **내 B-2 구현이 하루 어긋났음을 QA가 적발**(내 수치가 틀림)
3. **QA → 마스터**: "현재 코드 = (A) + 버그"라는 성격 규정 → **범위를 (A) 유지로 축소하는 결정의 근거가 됨**
4. **design → dev**: UI 클램프 금지 + 0 생략 로직 삽입 금지 → dev 지시서에 반영, QA가 미삽입 확인
5. **QA → dev/planning → 마스터**: 비차단 3건 → **마스터가 직접 반영**(주석·앵커 J·스펙 헤더)
6. **QA → 별건 티켓**: `lib/age.ts` 2/29생 규약 불일치 발견 → 티켓 분리(아래)

### CS팀
문의 채널 미연동 그대로 — `/support` 폼은 `/api/contact`(Resend)로 **발신만** 하고 조회 가능한 수신함이 없다. cs-team이 읽을 신규 문의가 구조적으로 존재하지 않아 이번 회차 배정 없음(지어내지 않음).

### 마케팅 게이팅 판단 — **대기 (신규 초안 배정 안 함)**
이번 회차 산출물은 **계산기 정확성 수정**이라 카드뉴스 소재로 부적합(홍보할 신규 화면·기능 아님).
그리고 `marketing/card-news/` 초안 **9편이 07-14부터 13일째 사용자 이미지화·게시 승인 대기** 중 — 병목은 생산이 아니라 승인이라 10번째 초안은 미승인 산출물만 늘린다.
**🔔 알림 재발송 안 함** (07-26에 이 건으로 1회 발송 완료, 이슈당 1회 원칙).
GSC 감사가 인계한 "스레드 캡션 CTA를 URL 직접 삽입으로 교체" 건은 **9편 승인 후로 이월**(승인 전 편집은 병목을 안 줄임).

---

### 배포(push)
- 커밋 대상(**경로 명시 스테이징, `git add -A` 미사용**): `lib/service-period.ts` · `scripts/regression/engines.test.ts` · `planning/service-period-boundary-fix-spec.md` · `planning/service-period-display-rules.md` · `docs/team-log/2026-07-27.md`
- **제외**: `.claude/settings.local.json`(**🔴 평문 크론 시크릿 — 07-27 GSC 감사 §7**) · `.claude/launch.json` · `.claude/settings.json` · `marketing/promo-drafts.md` · `planning/calculator-lineup-expansion.md`(타 세션) · `.codex/`·`ebook/`·`trading/`(타 프로젝트)
- `.env*` 포함 0건 확인

---

## 사용자 결정 필요 (🔔 이번 회차 알림 1회 발송)

**근속기간 시맨틱을 (A)로 둘 것인가, (B-2) 민법 §160③으로 바꿀 것인가.**

| | (A) 반복차용 — **현재 라이브** | (B-2) 민법 §160③ 말일만료 |
| --- | --- | --- |
| 근거 | 기존 동작 유지, 변경 최소 | 민법 조문 직접 적용, planning 권고 |
| `2020-01-31 ~ 2021-02-28` | `1년 0개월 29일` | `1년 1개월 0일` |
| 표시값 변동 | 0건(현 상태) | **199,265건**(종료일 3월 구간) |
| 체감 | 말일 입사자가 "1개월"을 잃은 것처럼 보임 | 말일 입사자에게 1개월 인정 |

- 양쪽 모두 **자체 정합적**이고 불변식을 전부 만족한다. 정합성으로는 못 고른다 — **정책 선택**이다.
- 퇴직금 **금액**은 어느 쪽이든 불변(`totalDays`만 사용). 바뀌는 건 `N년 M개월 D일` 표시뿐.
- (B-2) 전환 시 `planning/service-period-boundary-fix-spec.md`의 §2-4·§3-4·INV-14/15와 §5-2 FAQ 초안이 그대로 재사용 가능(참조 구현식은 QA 검증 통과).

---

## 다음 실행 인계

1. **[★사용자 결정 대기 — 신규, 오늘 알림 1회 발송함. 재발송 금지]** 위 (A) vs (B-2) 시맨틱 선택. 결정 전까지 `service-period-boundary-fix-spec.md`를 구현하지 마라(최상단 정정 블록 참조).
2. **[신규 티켓 — 별건]** `lib/age.ts` 2/29생 규약 불일치. QA 실측: `2024-02-29생, 기준일 2025-02-28` → **만나이 0세인데 "다음 생일까지 0일"**(생일인데 나이가 안 오름). 원인은 `manAge`(31~39행)는 clamp 없이 3/1에 증가하는데 `getNextBirthday`/`safeDateForYear`(74~92행)는 2/28로 말일 clamp — **한 파일 안에 두 규약 공존**. planning §5-4가 "clamp를 이미 갖춘 것으로 보인다"고 낙관했으나 실행하면 반쪽만 갖췄다. 티켓 #2와 같은 계열이며, 기준 선택(2/28이냐 3/1이냐)이 사용자 결정 사안일 수 있음.
3. **[블로그 37편]** 07-26 인계 그대로 — 카테고리 완전 균형(9·9·9·9) 달성으로 "최저 카테고리" 선정축 소진. 새 선정축 필요(in-degree / 검색수요 / 클러스터 잔여각도). 종료 클러스터(4편째 금지): BMI·주휴수당·age·savings. date는 service-period 3편·dday 2편으로 포화.
4. **[★사용자 확인 필요 — 07-27 GSC 감사가 알림 1회 발송. 재발송 금지]** 공개 저장소 `.claude/settings.local.json` 평문 크론 시크릿. **사용자 조치 전까지 어떤 실행도 `git add -A` 금지** — 지시서 §8의 `git add -A` 문구는 무시하고 경로 명시 스테이징을 쓸 것.
5. **[★사용자 확인 필요 — 07-26 알림 1회 발송. 재발송 금지]** GSC API 자격증명 부재(13일째).
6. **[운영 표준 — 신규 ⓝ]** **자기 검산 구현도 검증 대상이다.** 이번에 마스터의 "B-2" 구현이 스펙과 하루 어긋나 planning을 잘못 반증했고, QA가 이를 잡았다. 팀 산출물을 반증할 때 **내 구현이 스펙 정의와 일치하는지 먼저 확인**할 것.
7. **[운영 표준 — 신규 ⓞ]** **dev 서버가 떠 있는 동안 `rm -rf .next` 금지.** 이번 회차에 마스터가 이걸 해서 `/calculator/...`가 500을 뱉었고, 잠시 코드 결함으로 오인할 뻔했다(서버 재시작으로 해소). 클린빌드 전엔 `preview_stop` 먼저.
8. **[운영 표준 — 유지]** 스크래치 스크립트는 **Write + `npx tsx`**(Bash heredoc은 승인 프롬프트로 무인 실행이 멈춘다). `git -C` 사용, `cd`+`git` 조합 금지.
9. **[백로그 — 유지]** 계산기 desc<70자 16건 · og:image 2단계 미착수 · `.claude/worktrees/` 스테일 2개 · 헤더 터치타깃 36px · 다크모드 부재 · AdSense 심사 대기 · 카드뉴스 9편 승인 대기 · `/blog` 목록 `ƒ (Dynamic)` · #5 계산기 명칭 혼용 · #16 취득세(高YMYL, /loan 확정) · `age-calculator`의 `<input max>` 빌드시각 고정.
