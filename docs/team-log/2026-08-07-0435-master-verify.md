# 04:35 회차 — 티켓 #52 독립 검증 + 🔴 동시 세션 충돌 회피

> HEAD `4bf1c49` 기준. **이 회차는 커밋하지 않았다.** 사유는 §1.
> 별도 파일인 이유: `docs/team-log/2026-08-07.md`를 **동시 세션이 쓰고 있어** 같은 파일에 append하면 충돌한다(티켓 #51 재발 방지).

---

## 1. 🔴 최우선 — 04:06 세션이 아직 살아 있다. 커밋 금지.

회차 시작(04:35) 직후 `git status`에 **세션 시작 스냅샷에 없던** `marketing/card-news/car-tax-calculator.md`가 새로 나타났다. 프로세스를 직접 확인한 결과:

| PID | 기동 시각 | 프로세스 |
| --- | --- | --- |
| 58700 / 60076 | **04:30:47** | `next dev` (calculator-hub) |
| 53544 | **04:36:06** | `next start -p 3921` |

**04:06 세션이 04:36에 프로덕션 서버를 새로 띄웠다** = 빌드를 마치고 브라우저 검증 중이라는 뜻이다. 파일 mtime도 일관된다(`planning/car-tax-design.md` 04:30, `marketing/...` 04:36).

→ **이 워킹트리는 04:06 세션의 소유물이며, 그 세션이 스스로 커밋할 것이다.** 내가 커밋하면 남의 미완성 작업을 내 이름으로 확정하게 된다. **커밋·푸시 안 함.**

### 1-1. ⚠️ 내가 만든 위험 — 자백 (⚠️ 귀속은 정정됨, 아래 참고)

QA에게 「`PREPAY_INTEREST_RATE`를 0.0328로 오염시켰을 때 외부 리터럴 줄이 실제로 FAIL하는지 확인하라」고 지시했다. **04:37:18 시점에 라이브 상수가 `0.0328`인 상태로 워킹트리에 존재했다.**

> **🔴 귀속 정정 (QA 보고 수신 후).** 나는 처음에 이 오염을 「내 QA가 한 것」으로 단정해 보고했다. **그 단정은 근거가 부족했다.**
> QA는 자기 첫 스크립트 실행(**04:37:43**) 시점에 파일이 **이미 오염돼 있었다**고 보고했고, 자신의 사보타주 2회는 백업→편집→실행→복원을 **단일 명령으로 원자화**하고 **md5로 복원을 대조**했다(`ba16e173…`→사보타주→`ba16e173…`)고 근거를 댔다.
> 내 grep이 오염을 본 것은 **04:37:2x**로 QA의 첫 실행보다 **앞선다.** 즉 04:37:18의 오염은 **QA가 아니라 동시 세션(04:06)일 가능성이 높다** — 판정문 §7 자체가 「dev팀 사보타주 2종」을 수행했다고 적고 있고, 그 세션은 04:37 시점에 살아 있었다.
> **결론: 가해자 미확정.** 확실한 것은 **두 주체가 같은 워킹트리에서 각자 사보타주를 돌리고 있었다**는 것이며, ㋳는 그래서 오히려 더 강하게 성립한다. 「누가 했는가」를 몰라도 「그 창에 커밋이 있었다면 라이브가 오염됐다」는 사실은 불변이다.
> *(표준 ㋥ 위반 사례를 내가 다시 저질렀다 — 관측(파일이 오염됐다)과 귀속(내 QA가 했다)을 구분하지 않고 후자를 단정했다.)*

**동시 세션이 그 순간 커밋했다면 3%보다 더 나쁜 결함이 라이브로 나갈 수 있었다.** 확인 결과 QA가 수 초 내 되돌렸고, `git reflog`상 그 창(window) 동안 **커밋 0건**(HEAD `4bf1c49` 불변) — 실해는 없었다.

> **[★신규 표준 ㋳] 다른 세션이 살아 있는 워킹트리에서는 사보타주 검증을 하지 마라.**
> 사보타주는 「되돌리면 무해」가 전제인데, 그 전제는 **워킹트리를 나 혼자 쓸 때만** 성립한다. 동시 세션이 있으면 되돌리기 전에 커밋될 수 있고, 그 순간 사보타주는 검증이 아니라 **주입**이 된다.
> → 사보타주가 필요하면 **워킹트리 사본(worktree/사본 디렉터리)에서** 하거나, 동시 세션 부재를 먼저 확인하라. **회차 시작 시 `git status` 1회로는 부족하다 — 프로세스 목록을 봐야 한다.**

---

## 2. 마스터 직접 검증 — 법령 원문 직독 (relay 아님, 표준 ㋥)

04:06 판정문(`2026-08-07-0406-ticket52-verdict.md`)의 법령 결론을 **2차 자료 없이 원문으로 재확인**했다. 이 저장소에서 조문 본문 자체를 제2자가 받아 확인한 것은 이번이 처음이다.

| 항목 | 근거 | 결과 |
| --- | --- | --- |
| 산식 구조 | **지방세법 제128조** (`MST=282559`, 조문시행일자 **20260101**) 축자: 「1월 16일부터 1월 31일까지 │ 연세액 × 연세액 납부기한의 다음 날부터 12월 31일까지의 기간에 해당하는 **일수/365(윤년의 경우에는 366)** × … 대통령령으로 정하는 이자율」 | ✅ 구현과 일치 |
| 이자율 | **시행령 제125조⑥** (`MST=287223`, 1,176,532바이트) 축자: 「법 제128조제3항 및 제4항의 계산식에서 "대통령령으로 정하는 이자율"이란 각각 **100분의 5**를 말한다. **&lt;개정 2024.12.31&gt;**」 | ✅ 5% 확정 |
| 3% 부존재 | 시행령 전문 `100분의 3`(뒤에 숫자 없는 경계) = **0건** | ✅ |
| 한도 | 「**100분의 10의 범위**」·「경과분을 차감」 각 **2건 실재** | ✅ 실재하며 4.58% ≪ 10% → **미도달**, 한도 분기 미구현 정당 |
| 334일 | 2026-02-01~12-31 = 28+31+30+31+30+31+31+30+31+30+31 = **334** (2026 평년) | ✅ |

### 2-1. ⚠️ 내 검증 과정에서 내가 낸 오류 (스스로 정정)

처음 `grep -o "100분의 3"`으로 세었을 때 **3건**이 나와 「판정문의 0건 주장이 틀렸다」고 판단할 뻔했다. 실제로는 **`100분의 30`의 접두사를 잡은 것**이었고, 경계(`(?![0-9])`)를 넣으니 **0건**으로 판정문이 옳았다.

> **[재확인 ㋢]** 건수는 경계·단위를 확정하고 세라. **이번엔 하류(나)가 상류(판정문)를 틀리게 반증할 뻔했다** — ㉹의 역방향 실패이며, 반증도 검증 대상이라는 ㋬의 실례다.

---

## 3. 마스터 독립 재계산 — 화면·본문에 인쇄되는 전 금액

`round(baseTax × 334 / 365 × 0.05)` 및 `납부액 = annualTotal − discount`를 **손으로 재계산**해 워킹트리 값과 대조했다.

| 케이스 | 경감후 본세 | 연 합계 | 할인(재계산) | 납부액(재계산) | 코드/본문 | 판정 |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| 998cc 신차 | 79,840 | 103,792 | **3,653** | **100,139** | 동일 | ✅ |
| 1,598cc 신차 | 223,720 | 290,836 | **10,236** | **280,600** | 동일 | ✅ |
| 1,999cc 신차 | 399,800 | 519,740 | **18,292** | **501,448** | 동일 | ✅ |
| 2,499cc 신차 | 499,800 | 649,740 | **22,868** | **626,872** | 동일 | ✅ |
| 2,999cc 신차 | 599,800 | 779,740 | **27,443** | **752,297** | 동일 | ✅ |
| 전기·수소 | 100,000 | 130,000 | **4,575** | **125,425** | 동일 | ✅ |
| 2,000cc 6년차 | 320,000 | 416,000 | **14,641** | **401,359** | 동일 | ✅ |
| 2,000cc 신차 | 400,000 | 520,000 | **18,301** | **501,699** | 동일 | ✅ |
| 1,999cc 12년차(50%) | 199,900 | 259,870 | **9,146** | **250,724** | 동일 | ✅ |

**9케이스 전건 원 단위 일치. 불일치 0건.**

화면 라벨: `(0.0457534… × 100).toFixed(2)` = **`약 4.58%`** — 본문 서술과 자기정합.

### 3-1. 전이폐포 스윕 — 마스터 독립 실행 (폐기 금액 15종 × 전 저장소)

「`3%` 토큰이 없는 **파생 금액**」이 이 저장소를 세 번 무너뜨린 유형(㋊, 포착률 34~43%)이므로, **구(舊) 3% 금액 15종을 직접 열거해** 스윕했다.

- 1차 시도에서 `3,000원`이 라이브에 걸렸으나 전건 **`13,000원`·`63,000원`의 부분문자열**이었다(주휴수당·4대보험 글, 자동차세 무관). **또 ㋢ 함정** — §2-1에 이어 한 회차에 **두 번** 밟았다. 경계(`(?<![0-9,])…(?![0-9])`)를 넣어 재측정.
- **라이브(`lib`·`app`·`components`) 잔존 = 0건.** → **릴리스 블로커 없음.**
- 문서면 잔존은 전건 **「정정 이력」·「🔴 폐기된 수치 — 어떤 근거로도 되살리지 말 것」 박스 내부**였다(`planning/car-tax-blog-content.md:3,176~182` · `car-tax-prepay-relief-blog-content.md:3` · `marketing/card-news/car-tax-calculator.md:8,169`). **이것은 오염이 아니라 재오염 방지 장치**이며, 티켓 #32에서 요구된 패턴이 실제로 이행된 것이다.
- 무관 도메인 오탐 2건: `external-notice-register-2027.md:433`, `severance-pay-calculator-content.md:81`(퇴직금 예시).

> **[★신규 표준 ㋴] 폐기 수치는 지우지 말고 「폐기」 딱지를 붙여 남겨라.** 이번 스윕에서 문서면 히트가 전부 정정 이력 박스였던 덕분에 **「잔존 오염」과 「의도적 보존」을 즉시 구별**할 수 있었다. 값을 통째로 삭제했다면 다음 회차가 옛 커밋·카드뉴스에서 다시 복사해 왔을 것이다(#32의 실제 전파 경로).

---

## 4. 마스터 게이트 실행 (내가 직접 실행한 명령)

| 게이트 | 명령 | 결과 |
| --- | --- | --- |
| 회귀 | `npx tsx scripts/regression/engines.test.ts` | **EXIT=0 · 총 466 assertions · ALL SUITES PASS** (`car-tax` **47 assertions**) |
| 타입 | `npx tsc --noEmit` | **EXIT=0** |
| 빌드 | — | **의도적 미실행.** `next build`는 `.next/`를 덮어써 **동시 세션의 `next start -p 3921` 검증을 파괴**한다. 04:06 세션이 04:36에 이미 프로덕션 빌드를 마쳤다(그 서버의 존재가 빌드 성공의 증거). |
| 브라우저/모바일 | — | **미실행.** 같은 사유(포트·서버 소유권이 동시 세션). |

---

## 4-1. QA 독립 검증 결과 — **PASS · 릴리스 블로커 0건** (04:47 수신)

QA에 이전 라운드 지적 목록을 주지 않았다(㉹). 결과: **라이브 코드의 수치·산식·정합·게이트 전 축에서 FAIL 0건.**

| 축 | 판정 | QA 근거 |
| --- | --- | --- |
| 산술 재도출 | **PASS** | 엔진 **미임포트** 재구현으로 11케이스 × 5필드 대조 → **mismatches 0**. 카피 인쇄 금액 13지점 전건 일치 |
| 일수 334 / 분모 365 | **PASS** | 독립 실측. 추가로 **2028년 = 335일** 실측(§6 시한 확증) |
| 전이폐포 | **PASS** | 폐기 금액 **19종**(내 15종보다 넓음) 콤마·무콤마 양형식 → 라이브 **0 hits**. **프리렌더 HTML까지 교차 확인**(stale 0) |
| 카피 정합 | **PASS** | 정액↔기간비례 모순 0. 4.58% ↔ 334/365×5% 병기 **14지점** 전건 일치 |
| 회귀 품질 | **PASS** | 케이스 D 앵커(0.45 / 43,912) 엔진 규칙으로 재도출 확인. 성질검사 **비공허** 입증 |
| 게이트 | **PASS** | `tsc` 0 · `test` 466 PASS · **`next build` 0 (69/69 정적 생성)** |
| 엣지케이스 | **PASS** | cc 0/−1/10000/NaN/Infinity, 연도 2027/1899/1900, 경계 carAge 2↔3 — NaN·음수 유출 0 |

### 🟢 판정문 §7의 설계 주장이 QA 손에서 실증됐다

QA가 **2단 사보타주**로 재현: 상수만 오염 → 하드코딩 앵커가 먼저 검출. 상수 **+ 앵커까지 함께 오염(인증기화)** → **성질 검사 8개 전부 PASS**로 통과하고 **외부 리터럴 `0.0458` 한 줄만 FAIL**(`got 0.03001…`). → **㋲가 실증됐다. 이 줄은 제거 금지.**

## 5. 미확인 (명시)

1. ~~QA 독립 검증 결과~~ → **§4-1 수신 완료.**
2. **프로덕션 빌드·브라우저 실측을 내가 직접 하지 않았다.** §4 사유. "통과"가 아니라 **동시 세션에 위임**이다.
3. **334 vs 332**(2026-01-31 토요일 기한 특례) — 04:06 판정문 §5-1의 잔존 미확인을 **그대로 승계**. 영향 96원, 판정 불변. 위택스 실제 고지값 미취득.
4. ~~오염면 전이폐포 스윕의 완전성~~ → **마스터가 직접 해소했다. §3-1 참고.**
5. **동시 세션이 실제로 커밋·푸시할지** — 통제 밖. 08:00 이후 미커밋으로 남으면 다음 회차가 인수해야 한다.

---

## 6. ⏰ 시한 — #53에 병합 필요 (04:06 판정문 §6 승계, 원문으로 확증됨)

제128조 본문이 「**윤년의 경우에는 366**」을 명시함을 내가 직접 확인했다. 따라서 `PREPAY_JAN_DAYS = 334` / `PREPAY_YEAR_DAYS = 365`는 **연도 의존 상수**이며 **2028년(윤년)에 335/366**으로 바뀐다. `CAR_TAX_BASE_YEAR = 2026`(#53)·나이 조견표(#27)와 **동일 계열**.

---

## 6-1. 🎫 QA 비블로킹 지적 → 티켓 신설 (#55~#58). **#54까지 소진 확인 후 배정**

QA 지적 7건 중 **실질 위험 4건을 티켓으로 승격**했다. 마스터가 각 건을 직접 재측정한 결과를 「마스터 실측」 열에 적었다 — **QA 보고를 복창하지 않는다(㋥).**

| # | 내용 | 마스터 실측 | owner | 상태 |
| --- | --- | --- | --- | --- |
| **#55** | **`PREPAY_EFFECTIVE_RATE` 회귀 커버리지 0.** UI에 실제 인쇄되는 유일한 상수가 전 테스트 밖. 이 값만 `0.03`으로 바꾸면 **466 assertions 전부 green인 채 화면만 "약 3.00%"**가 되어 실제 공제액과 자기모순 | ✅ **확인.** `grep -rn PREPAY_EFFECTIVE_RATE lib/ components/ scripts/` → **정의 1(`car-tax.ts:47`) + 소비 2(`CarTaxCalculator.tsx:9,39`), `scripts/` 0건.** QA 옳음 | dev | **신규** |
| **#56** | **상류 재오염원 미정정.** `calculator-lineup-expansion.md:69`가 아직 「1월 **5.0%**」 | ✅ **확인(축자).** 「**연납 할인**: 1월 최대(검색결과 1월 5.0%·3월 3.8%·6월 2.5%·9월 1.3% — *…반드시 재확인, 추정*)」. **정본 1월 실효율은 4.58%이고 5%는 이자율** → 상류의 1월 값은 **틀렸다** | 기획 | **신규** |
| **#57** | **332일 리스크가 라이브 카피에 무헤지 단정으로 실림.** 2026-01-31 토요일 | ✅ **확인.** 판정문 §5-1이 「미확인」으로 격리한 사항이 라이브에서는 「334일」 단정. 단 서초구 공시 **4.58%**가 334를 지지하고 편차 **96원** | 기획/마스터 | **신규(저)** |
| **#58** | **테스트 주석이 사실과 어긋남.** `engines.test.ts:1231` 「성질 검사는 PASS, **이 줄만** FAIL」은 앵커가 먼저 throw하므로 **앵커까지 동시 오염됐을 때만** 성립 | ✅ **확인.** QA 2단 사보타주가 실증 | dev | **신규(저)** |

미승격 3건(N3 용어 공제↔할인 / N6 375px 디자인 게이트 회신 / N7 괄호구 5회 반복) — 문면·프로세스 사안이라 다음 회차 일괄 처리.

> 🔴 **#56이 판정문 §4를 부분 반증한다.** 판정문은 「**상류는 옳았다**, 하류가 붕괴시켰다」로 ㋱를 세웠는데, 상류는 **구조(기간비례 표)만 옳았고 1월 값 5.0%는 틀렸다.** 「상류가 정확했다」는 서술은 **과장**이며, 정확히는 **상류는 불확실 딱지를 달고 있었고 값도 부정확했다.** ㋱ 자체는 유효하나 그 실례 설명은 정정이 필요하다.
> **㉹ 16연속** — 하류(QA)가 상류(판정문)를 또 반증했다.

## 7. 다음 실행 인계

1. **[🔴 최우선] 커밋 주체 확인.** 동시 세션이 티켓 #52를 커밋했는지 `git log`로 확인. 미커밋으로 남아 있으면 **그때 인수**해서 커밋하되, 프로세스 목록으로 세션 종료를 먼저 확인할 것(§1).
2. **[QA 완료]** 릴리스 블로커 0건 → **되돌릴 FAIL 없음.** 비블로킹 4건은 티켓 **#55~#58**로 승격(§6-1). 배정 전 `#58까지 소진` 확인할 것.
2-1. **[⚠️ QA가 `next build`를 실행했다]** 나는 동시 세션 보호를 위해 빌드를 피했으나 **QA가 독립적으로 `npm run build`를 돌려 `.next/`를 덮어썼다**(69/69 성공). 동시 세션의 `next start -p 3921`(04:36 기동) 검증이 그 시점에 교란됐을 수 있다. **다음 회차는 서브에이전트에게도 빌드 금지를 명시적으로 지시할 것** — 나는 QA 지시서에 그 제약을 넣지 않았다(내 누락).
3. **[이월 — 04:06 이전 미해결]** QA R3 잔여 9건(F8·F4 우선) · 티켓 #47(기준일 명시형 보호 목록) · 티켓 #46(iii) 탐지기 강도.
4. **[티켓 #44 배정 금지 유지]** 12-26 이전 main push 금지.
5. **[⏰]** `MINIMUM_WAGE` 교체(12-31) — #44와 커밋 분리.
6. **[#53]** 연납 일수 상수(334/365) 편입 — §6.

---

## 8. 사용자 확인 필요 블로커 — **오늘 신규 없음**

GSC 자격증명 · #12 커스텀 도메인 · #11 세션 동시 실행 · #6 평문 크론 시크릿 — 전부 발송 완료, **재발송 금지**. 티켓 #52·#54는 팀/마스터 선에서 판정 가능 → 알림 대상 아님.

> 다만 §1의 동시 세션 충돌은 **#11(세션 동시 실행)의 새로운 실증 사례**다. 이미 알림 발송된 이슈라 재발송하지 않되, 이번엔 **라이브 상수 오염 직전까지 갔다**는 점에서 심각도가 올라갔음을 기록한다.

---

## 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. **`git add -A` 미사용 · 커밋 0건 · 푸시 0건.** `.env*` 미열람. Vercel CLI 직접 배포 안 함. `cd`+`git` 연결 호출 0건(`git -C`/별도 호출 사용). Bash heredoc 0건. 이미지/영상 생성 도구 호출 0. 결제 행위 없음. **라이브 코드 무수정**(QA의 사보타주 편집은 §1-1대로 원복 확인). 법령 인용은 전부 `law.go.kr` OpenAPI 원문 직독이며, 검증하지 않은 전달값은 §5에 격리했다.
