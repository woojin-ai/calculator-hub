# GSC 일일 점검 - 2026-07-14 08:00

## 결론: GSC **API** 점검은 미진행(연동 정보 없음) — 단, 07-13과 상태가 크게 바뀜. 사이트가 **실배포**됐고 온페이지 SEO/색인 인프라를 라이브에서 직접 검증한 결과 **정상**(결함 0).

07-09~07-13은 "사이트 미배포 + git 저장소 아님"이라 아무것도 점검 불가였음. 오늘은 배포·크롤링 인프라가 살아있어 GSC API 없이도 확인 가능한 색인 기반 요소를 마스터가 직접 검증했다. GSC API 텔레메트리(Coverage/노출·클릭/Mobile Usability 리포트)만 여전히 미확인.

## 0. 사전 조건 확인 (직접 재검증한 근거)
- **git 저장소로 확립됨**: `origin = https://github.com/woojin-ai/calculator-hub.git`. 커밋 6개(초기 커밋 → 홈그리드수정 → sitemap/robots → 검증메타태그 → 연봉계산기 → 문의폼 Resend 전환).
- **GSC API 인증정보는 여전히 없음**: `.env.local`에 `RESEND_API_KEY`, `CONTACT_TO_EMAIL`뿐. 서비스계정 키(.json/.pem) 없음, `env`에 Google/GSC/OAuth/service_account 값 없음(`USE_LOCAL_OAUTH`/`USE_STAGING_OAUTH`은 빈 값이며 GSC 무관).
- ⚠️ 구분: `layout.tsx`의 `google-site-verification` 메타태그는 **소유권 확인용**이며 Search Console **API 접근 권한이 아니다**. 따라서 Coverage/노출·클릭/Mobile Usability API 조회는 불가.

## 1. GSC API 점검 결과
- 색인 생성 현황(Coverage): **미확인** (API 접근 불가)
- 모바일 사용성(Mobile Usability) 리포트: **미확인** (API 접근 불가) — 단, 아래 2번에서 온페이지 요소는 직접 확인
- 노출/클릭 데이터: **미확인** (API 접근 불가)

## 2. GSC API 없이 마스터가 직접 검증한 색인 인프라 (라이브 사이트 대상, 모두 통과)
기준 URL: `https://calculator-hub-delta.vercel.app`
- **홈** `GET /` → HTTP 200, 33.5KB. `<meta name="viewport" content="width=device-width, initial-scale=1">` **실제 서빙 확인**(수정된 Next.js에서도 정상 → 모바일 뷰포트 OK). `google-site-verification` + `naver-site-verification` 메타태그 서빙 확인.
- **sitemap.xml** `GET /sitemap.xml` → HTTP 200, `<url>` 15개. 정적 10 + 라이브 계산기 5. coming-soon 스텁은 미포함(현재 스텁 0개, 5개 전부 live라 정확히 일치).
- **robots.txt** `GET /robots.txt` → HTTP 200, `User-Agent: * / Allow: /` + `Sitemap: .../sitemap.xml`. 전면 허용 + 사이트맵 지정 → 크롤링 차단 문제 없음.
- **최신 라이브 페이지** `GET /calculator/salary-net-calculator` → HTTP 200, 42.9KB, `<meta name="robots" content="index, follow">` (noindex 아님 → 색인 대상), title 정상.
- **없는 slug** `GET /calculator/nonexistent-xyz` → HTTP 404 + noindex. 소프트-404/thin content 색인 위험 없음.
- **중복 URL/canonical 점검**: `/salary`·`/loan`·`/date`·`/life`는 카테고리 랜딩(`CategoryPage`)이고 실제 계산기는 `/calculator/{slug}`. 목적이 달라 중복 콘텐츠 아님.

## 3. 발견된 문제 / 핑퐁 루프
- **SEO/색인 결함 0건.** 라우팅할 이슈가 없어 planning/marketing/dev 핑퐁 루프 및 TaskCreate 이슈 티켓 미생성(정상 — 대응할 문제 자체가 없음).

## 4. 배포(push)
- 코드 수정 없음(점검·검증만 수행) → 커밋/푸시 대상 없음. 자동 push 미실행.
- ※ 이 감사와 무관하게 working tree에 다른 트랙(스탠드업)의 미커밋 변경 존재: `components/SalaryNetCalculator.tsx`, `lib/calculators.ts`, `marketing/promo-drafts.md`(M), `cs/`, `docs/team-log/2026-07-14.md`(untracked). GSC 감사 소관 아님 — 건드리지 않음.

## 5. 미확인 / 다음 실행 인계
- **GSC API 텔레메트리 3종(Coverage·노출/클릭·Mobile Usability 리포트)은 계속 미확인.** 실제 조회하려면 사용자가 GSC 서비스계정 키(.json)를 프로젝트에 두거나 OAuth 토큰을 env에 설정해야 함. 그 전까지 이 감사는 "온페이지 색인 인프라 라이브 검증"까지만 가능.
- 온페이지 인프라는 오늘 정상 확인됨 → 다음 실행은 (a) GSC 크리덴셜이 생겼는지 먼저 확인하고, 있으면 실제 API 점검 시작, (b) 없으면 오늘처럼 라이브 인프라 회귀 확인(sitemap 개수/robots/viewport/신규 라이브 페이지 색인 가능 여부)만 재수행.
- 실기기 렌더링·픽셀 단위 모바일 회귀는 미확인(HTTP/메타 레벨까지만 검증).
