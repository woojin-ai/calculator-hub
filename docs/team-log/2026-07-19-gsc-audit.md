# GSC 일일 점검 - 2026-07-19 08:00

## 결론: GSC **API** 점검은 여전히 미진행(연동 정보 없음, 4일+ 연속 동일 블로커). 라이브 색인 인프라 회귀 검증 → **결함 0** (sitemap 53→54). 추가로, **이전 세션이 커밋하지 않고 남긴 블로그 내부링크 기능**(상세 페이지 "함께 읽으면 좋은 글")을 발견해 qa-team 독립 검증 + 마스터 직접 375px/768px 실측까지 완료 후 **커밋·푸시**했다.

## 0. 사전 조건 (재검증 근거)
- **배포/저장소 정상**: 라이브 `https://calculator-hub-delta.vercel.app` sitemap `GET` → 200.
- **GSC API 인증정보 여전히 없음**: `.env.local` = `RESEND_API_KEY`, `CONTACT_TO_EMAIL` 2줄뿐. 루트 `.json`은 package/tsconfig/launch/settings뿐(서비스계정 키 아님). `GSC_/SEARCH_CONSOLE/service_account/GOOGLE_APPLICATION/searchconsole` grep → 히트는 **과거 gsc-audit 로그 파일뿐**, 실제 크리덴셜 0. → GSC API 조회 불가.
- ⚠️ `layout.tsx`의 `google-site-verification`은 소유권 확인용이며 API 접근 권한 아님(전일과 동일 구분).

## 1. GSC API 점검 결과
- 색인 현황(Coverage): **미확인** (API 접근 불가)
- 모바일 사용성 리포트: **미확인** (API 접근 불가) — 온페이지/실뷰포트 요소는 2·4에서 직접 확인
- 노출/클릭 데이터: **미확인** (API 접근 불가)

## 2. 마스터 직접 검증 — 라이브 색인 인프라 (전부 통과)
기준: `https://calculator-hub-delta.vercel.app`
- **sitemap.xml** → 200, **URL 54개**(07-18은 53). 내역: 홈+정적 10 + 계산기 **16** + 블로그 **28**. 전일 대비 +1 = `service-period-milestone-2026`(최신 커밋 3d06a03)이 라이브 반영됨 → 배포 파이프라인 정상.
- **robots.txt** → 200, `User-Agent:* / Allow:/` + `Sitemap:` 지정. 크롤 차단 없음.
- **신규/샘플 페이지 색인성**:
  - `/blog/service-period-milestone-2026`, `/blog/age-by-birth-year-2026` → 200 + 자기참조 canonical + viewport 정상
  - `/calculator/unit-converter` → 200 + `robots: index, follow` + viewport 정상
- **404 가드**: `/blog/nonexistent-xyz`, `/calculator/nonexistent-xyz` → **404 + noindex**. 소프트-404 위험 없음.

## 3. 발견 사항 — 미커밋 내부링크 기능 (SEO 소관이라 인수)
working tree에 이전 세션의 미커밋 변경이 있었다: `lib/blog.ts`(`getRelatedBlogPosts` 신규), `components/RelatedBlogPosts.tsx`(headingText/columns props), `app/blog/[slug]/page.tsx`(섹션 추가) + spec 2건. **블로그 상세 28편에 각 4개의 내부링크를 추가하는 변경 = 크롤 경로/색인 소관**이라 GSC 감사 범위로 인수해 검증했다.

### 핑퐁 기록
1. **마스터 → qa-team**: 스펙(planning/design) 대조 독립 검증 지시.
2. **qa-team → 마스터**: 기능 6개 항목 **전부 PASS**. 근거 — 스펙 문장만으로 셀렉터를 독립 재구현해 28편 전수 대조 mismatch 0; 자기참조·중복 0(프리렌더 HTML 28개 직접 검사); 28편 전부 4편 충족·빈 배열 0; **풀 빌드 2회 산출물 바이트 대조로 SSG 결정성 확인**; 기존 호출부(`/salary`, `/calculator/*`) "관련 글"·3열 클래스 바이트 동일 유지; 헤딩 위계 h1 1개·건너뜀 0. 빌드/린트 clean.
   - **FAIL 1건 제기(F-1)**: 코드 `POST_RELATED_BLOG_LIMIT = 4`인데 코드 주석이 인용한 `planning/blog-internal-linking-spec.md`는 **6**을 명시 → 스펙-구현 불일치.
3. **마스터 판단(재작업 라우팅 없이 직접 종결)**: 값 4가 정답. planning 초안의 6은 "3열 그리드에서 2행"이라는 전제였고, 이후 design 스펙이 **2열**로 확정하면서 전제가 무효화됨 — 2열에선 4가 2×2로 정확히 떨어진다. 코드를 6으로 되돌리는 건 오히려 퇴행이므로 **문서를 구현에 맞춰 갱신**(planning 스펙 §1-2에 확정 박스 + §1-2 총노출·§5-3 상수값·§2 실측치 정정). dev-team 재호출 불필요로 판단.
   - qa-team 실측 정정도 함께 반영: 스펙 §2의 "date 카테고리는 4편에서 멈춘다" 서술은 실제로 date 5편 중 **2편**만 해당(나머지 3편은 T2a가 salary로 다리를 놓아 6편이 참).

## 4. 마스터 직접 검증 — 실뷰포트 실측 (qa-team PASS를 그대로 믿지 않음)
로컬 프로덕션 서버(`next start`, :4321)에 브라우저를 붙여 DOM 정량 측정:
- **375×812 / `/blog/age-korean-age-law-2026`**: `scrollW==innerW==375`, **문서 가로 오버플로 0px**, 스크롤 컨테이너 제외 **진짜 초과폭 요소 0건**. 카드 4장이 1열로 스택(w=343, x=16 동일) = 모바일 의도대로. h2 목록 마지막이 "함께 읽으면 좋은 글"로 확인, 링크 4개가 스펙 §3 예시 A의 손계산(`age-by-birth-year` → `service-period-milestone` → `dday-counting-guide` → `service-period-guide`)과 **정확히 일치**.
- **768×1024**: 카드가 **2열×2행**(x=24/385, y 2단), w=345, 오버플로 없음 → design 2열 확정 사양대로 렌더.
- **회귀 `/salary`**: h2 "관련 글" 유지, `lg:grid-cols-3` 존재, "함께 읽으면" 문자열 유출 0, 오버플로 없음 → 기본값 변경에 의한 기존 페이지 파손 없음.
- **콘솔 에러 0건**.
- ※ 스크린샷 캡처는 종전대로 미시도(렌더러 타임아웃 이력) → DOM 정량 측정으로 대체.

## 5. 배포(push)
- 빌드 + qa-team PASS + 마스터 375/768px 실측을 모두 통과 → 2026-07-13 사용자 승인 기준 충족, **자동 커밋·푸시 실행**.
- 푸시 범위: `lib/blog.ts`, `components/RelatedBlogPosts.tsx`, `app/blog/[slug]/page.tsx`, `planning/blog-internal-linking-spec.md`(정정), `design/blog-related-posts-section-spec.md`, team-log.
- **의도적 제외**: `.claude/settings.local.json`, `.claude/settings.json` — 로컬 권한/에디터 설정이라 사이트 코드가 아니고 검증 대상도 아님. 로컬에 그대로 둠.

## 6. 미확인 / 다음 실행 인계
- **GSC API 텔레메트리 3종(Coverage·노출/클릭·Mobile Usability) 계속 미확인.** 실조회하려면 사용자가 GSC 서비스계정 키(.json)를 프로젝트에 두거나 OAuth 토큰을 env에 설정해야 함. **4일+ 연속 동일 블로커** — 사용자 조치 없이는 이 항목이 영구히 "미확인"으로 남는다.
- **T2b·T4 티어는 실행 커버리지 0%** (qa-team O-1). LIMIT=4에서 T1/T2a/T3만으로 4편이 다 차서 두 분기는 한 번도 진입하지 않는다. 로직 일치만 확인됐고 실출력 대조는 불가 — 블로그가 더 늘어 발동하기 시작하면 재검증 필요.
- **육안 픽셀 검수 미실시** (qa-team O-4): 카드 폭·제목 2줄 클램프·메타행 1줄은 클래스/DOM 수준으로만 확인. 디자인팀 육안 검수는 별도로 받는 게 맞음.
- **2열 vs 3열 과도기 불일치**: 블로그 상세는 2열, 계산기 상세 16개는 3열로 갈려 있음. design §9-1이 "의도적, 후속 티켓" 으로 명시한 사항 — 후속 티켓 미생성 상태.
- **블로그 28편**: 향후 GSC 연동되면 신규 블로그들의 실제 색인/노출을 최우선 확인할 것. 이번 내부링크 추가로 고아 글(in-degree 0) **0편**·최소 in-degree 1 확보(qa-team 감사) → 크롤 도달성은 개선됨.
- 다음 실행: (a) GSC 크리덴셜 생겼는지 먼저 확인 → 있으면 실 API 점검 시작, (b) 없으면 오늘처럼 라이브 인프라 회귀 + 375px 오버플로 재확인.
