# GSC 일일 점검 - 2026-07-25 08:00

## 결론
GSC **API** 점검은 여전히 미진행(자격증명 부재, **10일 연속 동일 블로커**). 대체 점검으로 **라이브 60개 URL 전수 온페이지 감사** 수행 → **회귀 0건, 전 신호 통과**. 07-24에 수정한 34편 title이 라이브에서 **54자**로 반영 확인(전체 max 59 ≤60, baseline 복구 완료). 오늘의 성과는 **10일 묵은 "모바일 픽셀/실측 검증 미확인" 공백을 실측으로 부분 종결**한 것: 375px 실제 뷰포트에서 3개 페이지 유형(블로그·계산기·홈)의 레이아웃 기하·탭타겟·폰트·실제 계산 동작을 측정해 **가로 오버플로 0**을 확인했다. 코드 변경 없음 → dev/qa 핑퐁 불필요, **push는 이 로그 1개 파일만**.

## 0. 사전 조건 (재검증 근거)
- **배포/저장소 정상**: `git fetch` 후 `HEAD == origin/main == d6f577e`(0 ahead/0 behind). 라이브 `sitemap.xml` 200(10,231 B, **60 URL** — 07-24와 동일).
- **GSC API 자격증명 여전히 없음(10일째)**. 오늘은 **새 파일이 생겨 정밀 재확인**했다:
  - 신규 `.env.local`(8줄) 발견 → 키 이름만 확인(값 미열람): `RESEND_API_KEY` / `CONTACT_TO_EMAIL` / `IG_BUSINESS_ACCOUNT_ID` / `IG_ACCESS_TOKEN` / `META_APP_ID` / `META_APP_SECRET`. **Google/GSC 관련 키 0개**(문의폼 메일 + 인스타 게시용으로 추정, 타 세션 산출물).
  - env의 `google|oauth|gsc` 매치 4건은 전부 **Claude Code 자체 변수**(`CLAUDE_CODE_OAUTH_SCOPES`, `USE_LOCAL_OAUTH`, `USE_STAGING_OAUTH`, `CLAUDE_CODE_SDK_HAS_OAUTH_REFRESH`) — GSC와 무관. **오검출 주의 항목으로 기록**한다.
  - 루트 `.json` = package/package-lock/tsconfig뿐, 서비스계정 키 부재.
  - → Coverage / 노출·클릭 / 모바일사용성 리포트 3종 **미확인**.
- ⚠️ `layout.tsx`의 `google-site-verification`은 소유권 확인용이지 API 접근 권한이 아니다(10일째 유지).
- ⚠️ 무인 안전수칙 준수: git 조회는 `git -C` 형태, `cd`+`git` 연결 없음.

## 1. GSC API 점검 결과
| 항목 | 결과 |
| --- | --- |
| 색인 현황(Coverage) | **미확인** — API 자격증명 부재 |
| 모바일 사용성(GSC 리포트) | **미확인** — API 자격증명 부재 (단, §3에서 실측 대체) |
| 노출/클릭 | **미확인** — API 자격증명 부재 |

## 2. 라이브 전수 온페이지 감사 (60개 URL)
sitemap `<loc>` 단일 출처에서 60개 기계 추출 → 병렬 다운로드(xargs -P 12) → python 파싱(문자수는 C 로케일 함정 회피 위해 `len()` 사용).

| 신호 | 결과 |
| --- | --- |
| HTTP 상태 | **60/60 = 200** |
| canonical | 60/60 정확히 1개, self 일치, 불일치 0 |
| `<title>` | 60/60 고유, 중복 0, 빈 값 0, **>60자 0건**(max 59 / min 12) ← 07-24 회귀 복구 확인 |
| meta description | 60/60 존재·고유, 빈 값 0 |
| `<h1>` | 60/60 정확히 1개 |
| robots noindex | 색인대상 0건(실수 deindex 없음) |
| JSON-LD | 60/60 존재 |
| viewport meta | 60/60 정확히 1개, 값 전부 `width=device-width, initial-scale=1` |
| `<html lang>` | 60/60 = `ko` |
| img alt | 이미지 5개 전부 alt 보유, 누락 0 |
| robots.txt | 200, `Allow: /` + sitemap 지시 정상 |
| `og:` 태그 | 60/60 = **0개**(기 등재 티켓 #2) |
| twitter card / hreflang | 0개 (hreflang은 단일 언어라 불필요, 정상) |
| desc 길이 편차 | **>160자 8개 / <70자 16개**(티켓 #3) |

**07-24 대비 desc >160 카운트 9→8**: 코드 변경이 없었으므로 회귀·개선이 아니라, 07-24 집계가 34편 desc 수정(164→149) **반영 전 HTML** 기준이었던 것으로 판단(당시 로그는 "수정 후 기준"이라 적었으나 실제로는 배포 전 스냅샷). 오늘 수치가 정본이다.

## 3. ★모바일 실측 검증 (10일 묵은 "미확인" 항목 부분 종결)
GSC 모바일 사용성 리포트를 못 보는 대신, **실제 375×812 뷰포트**에서 라이브 페이지를 직접 측정했다(추론 아님).

**측정 대상 3종** — 블로그 상세(`/blog/electricity-usage-marginal-cost-2026`, 어제 수정한 페이지), 계산기 상세(`/calculator/salary-net-calculator`), 홈(`/`).

| 항목 | 블로그 상세 | 계산기 상세 | 홈 |
| --- | --- | --- | --- |
| `scrollWidth` vs `clientWidth` | 375 = 375 | 375 = 375 | 375 = 375 |
| **가로 스크롤** | **없음** | **없음** | **없음** |
| 뷰포트 밖으로 넘치는 요소 | **0개** | **0개** | **0개** |
| 24px 미만 탭타겟 | 3개(브레드크럼 인라인 링크 16~18px) | 0 | 0 |
| 폼 요소 | — | input/select/button **전부 높이 48px**, **font-size 16px** | — |

- **계산기 실제 동작 검증(모바일)**: 연봉 `50,000,000` 입력 → 계산 실행 → **월 실수령액 3,553,983원** 정상 렌더. 내부 정합성 재검산 통과(월 급여총액 4,166,666 − 4대보험 385,145 − 소득세·지방세 227,538 = **3,553,983** ✓). **결과 렌더 후에도 오버플로 0**(결과 카드가 레이아웃을 깨지 않음).
- **`font-size: 16px` 입력창은 iOS Safari 자동 확대(zoom-on-focus)를 막는 값**이라 모바일 폼 품질이 의도적으로 잘 잡혀 있다. 48px 높이는 구글 권장 탭타겟(≥48px) 충족.
- **전 60페이지 구조 스캔**: `<table>` **0개**(모바일 오버플로 최대 원인이 구조적으로 부재 — 표 대신 리스트로 서술하는 편집 방침의 부수 효과), `whitespace-nowrap` 121곳에 대해 `overflow-x-auto` 컨테이너 **120곳**으로 가드됨. 360px 초과 고정폭은 홈 히어로 이미지의 `xl:w-[380px]` 2건뿐인데 **xl(≥1280px) 한정**이고 모바일 기본값은 `w-[320px]`라 375px에서 안전.
- **남은 미확인**: 픽셀 스크린샷(시각적 육안 검증)은 오늘도 **미수행**. 사유가 이전과 다르다 — 브라우저 도구는 붙었으나 무인 실행이라 **Browser 패널이 표시되지 않아 프레임 합성이 안 되어 스크린샷이 타임아웃**(도구 응답 원문: "the Browser pane is not displayed"). 즉 **기하·동작은 실측 완료, 픽셀 렌더링 육안 검증만 잔여**.
- 소형 폰트: 사이드 위젯 "관련 서비스"(형제 사이트 링크)에 9~11px 텍스트 6개. 본문이 아닌 보조 내비 라벨이라 가독성 리스크는 낮으나 9px는 다소 작음 → **티켓 #4(낮음)로 신규 등재**.

## 4. 신규 발행 예정 블로그 35편 사전 린트 (게시 전 차단, 타 세션 산출물)
워킹트리에 **미커밋** 상태의 신규 글이 있다(`lib/blog.ts` +199줄, `planning/blog-post-35-spec.md`). 07-24 로그의 인계사항 #4("발행 전 title 린트를 항목으로 추가 권장")를 **이번에 실제로 집행**했다.

- slug `savings-maturity-by-monthly-amount-2026` / category `loan` / publishedDate `2026-07-25`
- **title 40자 → 렌더 49자(≤60) PASS** ← 34편이 깼던 바로 그 규칙(post.title ≤51자)을 준수
- **description 139자(≤160) PASS**
- 35편 전체 **slug 중복 0 / title 중복 0**
- `relatedCalculatorSlugs` 3개 전부 `lib/calculators.ts`에 실존 PASS

→ **결함 없음**. 이 파일들은 **타 세션 작업물이라 커밋·수정하지 않았다**(07-24 선례 준수). 소유 세션이 그대로 발행해도 SEO 회귀는 없다.

## 5. 팀 핑퐁
**없음 — 발생 사유가 없었다.** 색인 오류·노출 문제·모바일 사용성 문제 모두 미검출이고 코드 변경이 0이므로, planning/marketing 진단도 dev 수정도 qa 검증도 호출하지 않았다. (없는 문제를 만들어 팀을 호출하지 않는다.)

## 6. 배포(push)
- **커밋 대상: 이 감사 로그 1개 파일뿐.** 소스 변경 없음.
- **타 세션 산출물 회피**: `lib/blog.ts`(35편), `planning/blog-post-35-spec.md`, `.claude/launch.json`, `.claude/settings.local.json`, `.claude/settings.json`, `marketing/promo-drafts.md` **전부 미터치**.
- `git add -A` 미사용, **경로 명시 스테이징**.

## 7. 미확인 / 다음 실행 인계
1. **[★사용자 확인 필요 — 유지, 10일 연속]** GSC API 자격증명 부재. 서비스계정 `.json` 또는 OAuth 토큰 env 없으면 Coverage/노출·클릭/모바일사용성 3종 **영구 미확인**. 매일 감사의 최대 구조적 공백이며, 10일째 동일 문구를 반복 중이다 — **사용자 조치 없이는 자동 해소 불가**.
2. **[오검출 주의 — 신규]** env grep으로 `google|oauth|gsc`를 찾으면 Claude Code 자체 OAuth 변수 4개가 잡힌다. 이걸 "자격증명 발견"으로 오판하지 말 것. `.env.local`도 Resend/Meta 키뿐이다.
3. **[티켓 #2 — 유지]** OG/metadataBase 전면 부재(60/60 `og:` 0개). 첫 SNS 게시 승인 전 처리 권장. `.env.local`에 인스타 토큰이 생긴 것으로 보아 **게시 파이프라인이 준비되는 중**일 수 있으므로 우선순위 상향 검토 필요(og:image 에셋은 design + 사용자 승인 사안).
4. **[티켓 #3 — 유지, 낮음]** desc 길이 편차. >160자 8건(최장 196자 `/blog/loan-prepayment-break-even-2026`, 185자 `/blog/dsr-by-income-rate-2026`), <70자 16건(최단 26자 `/calculator/bmi-calculator`·`/calculator/dday-calculator`). 짧은 쪽 CTR 개선 여지.
5. **[티켓 #4 — 신규, 낮음]** 형제사이트 위젯 "관련 서비스"의 9px 텍스트(`text-[9px]` "현재" 라벨). 11px로 상향 검토.
6. **[운영 표준 — 확립]** ★신규 블로그 발행 전 린트 4종(post.title ≤51자 / desc ≤160자 / slug·title 중복 / relatedCalculatorSlugs 실존)을 감사에서 집행한다. 오늘 35편에 적용해 전부 PASS. 34편 같은 사후 회귀를 사전에 차단하는 경로가 작동함을 확인.
7. **[운영 표준 — 유지]** C 로케일 Git Bash에서 `wc -m`·`${#}`·`awk length`는 바이트를 센다. 한글 문자수는 python `len()` 필수. 오늘도 준수.
8. **[운영 표준 — 유지]** URL은 sitemap `<loc>` 단일 출처에서 기계 추출, 병렬 다운로드로 타임아웃 회피.
9. **[백로그 — 유지]** canonical 생성 2종 공존 드리프트 / `coming-soon` 스텁 신호충돌 / 프리뷰 `x-robots-tag` 미확인 / 프로덕션 Linux 대소문자 slug 미확인 / **픽셀 스크린샷 시각 검증(무인 실행은 패널 미표시로 불가 — 사용자 동석 실행에서만 가능)** / `.claude/settings.local.json` `bypassPermissions`(사용자 확인) / AdSense 심사 대기 / 마케팅 게시 승인 대기 / 로드맵 #16 취득세 보류.

## 제약 준수
확정 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. `.env.local` 값 미열람(키 이름만 확인), 시크릿 미포함. `git add -A` 미사용. 타 세션 워킹트리 변경 미터치.
