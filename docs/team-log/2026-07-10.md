# 2026-07-10 데일리 팀 스탠드업 로그

## 오늘 완료
- BMI 계산기(`/calculator/bmi-calculator`) 라이브 전환 — 기획(planning-team) → 디자인(design-team) → 개발(dev-team) 순서 준수. 대한비만학회 4구간 기준(저체중<18.5 / 정상체중 18.5~23.0 미만 / 과체중 23.0~25.0 미만 / 비만 25.0 이상), 반올림 전 값으로 구간 판정(경계 오류 방지) 구현 확인.
- `/about` 페이지 신규 배포 — 기획+디자인 산출물 그대로 구현. `/contact`가 아직 없어 문의 버튼은 링크 없는 비활성 상태(`문의 페이지 준비 중`, `<span>`, `cursor-not-allowed`)로 처리 — 404 방지.
- 색상 팔레트에 Danger(`#EF4444`, `brand-danger`) 공식 추가 — BMI "비만" 상태 표시용. `design/design-system.md`, `app/globals.css` 양쪽 반영.
- `SiteFooter`에 "사이트 소개"(`/about`) 링크 1개 추가.
- 마케팅팀: BMI 홍보 문구 초안(`marketing/promo-drafts.md` 2-E) + 트렌드 리서치 갱신(최저임금 협상 격차 축소, 기준금리 인상 가능성 신규 반영) + 카드뉴스 게이팅 판단 근거 정리.

## 마스터가 직접 검증한 근거
- `npm run build` 정상 통과(개발 지시 전/후 2회 직접 실행, 타입 에러 없음).
- dev 서버 기동 후 데스크톱+모바일(375×812) 뷰포트에서 `/calculator/bmi-calculator`, `/about` 실제 확인:
  - BMI 170cm/85kg → 29.41, "비만" 빨간 배지 정상 표시.
  - BMI 100cm/23kg(경계값 정확히 23.0) → "과체중" 정상 판정(반올림 전 값 기준 판정 로직 확인).
  - `/about` 문의 블록에 실제 `<a href>` 링크 없음(스냅샷으로 `StaticText`임을 확인, `<Link>` 아님) — `/contact` 부재 상태에서 깨진 링크 생성 안 됨.
  - 모바일 뷰포트에서 BMI 입력 2필드 좌우 배치, 무스크롤 목표 대체로 부합.
- `getRelatedCalculators()`의 `status === "live"` 필터는 이미 적용되어 있음을 코드로 직접 재확인 — planning/design 팀이 "미적용 이슈"로 재보고했으나 착오였음(추가 조치 불필요).

## 미확인 항목
- 실제 사용자 트래픽 기준 UX(터치 정밀도, 실제 기기 렌더링)는 브라우저 프리뷰 기반 검증이며 실기기 테스트는 아님.
- 마케팅팀이 갱신한 최저임금/기준금리 리서치 수치의 출처 신뢰도는 마스터가 재검증하지 않음(마케팅팀 자체 출처 표기만 확인).

## 오늘 발생한 핑퐁 이슈
1. planning-team·design-team이 각각 "`getRelatedCalculators()` status 필터링 미적용" 이슈를 재보고 → 마스터가 `lib/calculators.ts` 코드를 직접 열어 이미 해결된 상태임을 확인 → dev-team 지시에 "재작업 불필요"로 명시 → **해결(착오였음, 코드 변경 없음)**.
2. planning-team이 "`/about`의 필수 항목인 문의 페이지 링크를 걸 곳이 없음"을 구조적 이슈로 제기(A/B/C 대안 제시) → 마스터가 (A)안(링크 없이 안내 텍스트만, `/contact` 완성 시 추후 연결) 채택 → dev-team에 명시적으로 전달 → **해결(오늘 범위 내 반영 완료)**.
3. design-team이 "BMI 비만 상태 표시에 Danger 색상 필요하나 팔레트에 없음" 확인 요청 → 마스터가 승인 → dev-team이 `design-system.md`+`globals.css`에 정식 반영 → **해결**.
4. planning-team·design-team이 공통으로 "헤더/푸터에 `/about` 진입 동선 없음" 지적 → 마스터가 푸터에 `/about` 링크 1개만 추가하기로 결정(다른 정적 페이지는 아직 없으므로) → dev-team 반영 → **해결**.

## 내일(2026-07-11) 이어받을 미해결 이슈

1. **정적 페이지 `/privacy` `/terms` `/contact` 계속 미착수** (2026-07-09부터 이월, 오늘도 진행 못함): `/privacy`·`/terms`는 CS팀이 "전문가 검토 필요"로 명시한 법률 문구가 다수라 내부 팀만으로 확정 불가 — **사용자가 실제 법률 검토 방법(전문가 컨설팅 여부 등)을 결정해야 진행 가능**. `/contact`는 실제 문의 채널(폼/이메일) 자체가 없어 착수해도 반쪽 페이지가 될 위험 — 이것도 사용자 결정 필요.
2. **실제 문의 채널 미연동** (2026-07-09부터 이월): 사이트에 폼/이메일 등 실제 사용자 문의 채널이 전혀 없음(오늘도 코드로 재확인). CS팀 업무가 시뮬레이션에 머무는 근본 원인. `/about` 문의 버튼도 이 때문에 비활성 상태로 남아 있음.
3. **cs-team 에이전트 파일 쓰기 권한 없음** (2026-07-09부터 이월): 오늘도 `.claude/agents/cs-team.md` 확인 결과 `tools: Read, Grep, Glob, WebSearch`로 변경 없음. CS 문의 채널 자체가 아직 없어 오늘은 실질적 영향이 없었으나, 채널이 생기면 다시 이슈가 됨 — Edit 권한 부여 여부는 사용자 결정 필요.
4. **로드맵 4순위 대기**: `planning/calculator-lineup.md` 종합 순서 기준 다음은 **대출이자 계산기**(`/loan/interest-calculator`, YMYL 리스크 높음 — 대출 상담/자문 아님 고지 필수, "지금 대출 신청" 류 오인 유발 디자인 금지 문구 준수 필요). 내일 기획팀부터 착수 권장.
5. **카드뉴스 게이팅**: 오늘도 "아직 이르다"로 보류(라이브 계산기 3개, 정적 페이지 `/about` 1개뿐, `/privacy /terms /contact` 전무, salary/loan 카테고리 0개). 마케팅팀은 BMI 홍보 문구 텍스트 초안까지만 축적. 이 판단은 마스터가 매일 재검토하되, 위 1~2번(정적 페이지·문의채널)이 해소되기 전까지는 계속 보류될 가능성이 높다.

## 참고 (이상 신호, 조치 불필요)
- 마케팅팀은 오늘 리서치 과정에서 프롬프트 인젝션성 이상 신호를 발견하지 못했다고 보고(2026-07-08/09에는 발견 보고 있었음). 특이사항 없음.
