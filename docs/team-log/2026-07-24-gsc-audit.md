# GSC 일일 점검 - 2026-07-24 08:00

## 결론
GSC **API** 점검은 여전히 미진행(자격증명 부재, **9일 연속 동일 블로커**). 대체 점검으로 **라이브 60개 URL 전수 온페이지 감사**를 수행한 결과 **신규 회귀 1건 검출·수정·검증·배포 완료**: 07-23 신규 발행된 블로그 34편(`electricity-usage-marginal-cost-2026`)의 렌더 `<title>`이 **69자**로 SERP 잘림 임계(~60자)를 초과(전 페이지 ≤59자 baseline 파괴). marketing→dev→qa→master 핑퐁 1라운드로 **69→54자** 교정, description도 164→149자 동반 정정. **1개 파일 2줄(+2/-2) 커밋·푸시.** 그 외 신호 전부 통과, 회귀 0건.

## 0. 사전 조건 (재검증 근거)
- **배포/저장소 정상**: `git fetch` 후 `HEAD == origin/main == 838b15b`(0 ahead/0 behind). 라이브 `sitemap.xml` 200(10,231 B, **60 URL** — 07-23의 59에서 34편 추가로 +1).
- **GSC API 자격증명 여전히 없음(9일째)**:
  - 루트 `.json` = package/package-lock/tsconfig뿐, 서비스계정 키 부재. env에 GSC/google/oauth 관련 0개(grep 0히트).
  - → Coverage / 노출·클릭 / 모바일사용성 리포트 3종 **미확인**.
- ⚠️ `layout.tsx`의 `google-site-verification`은 소유권 확인용이지 API 접근 권한이 아니다(9일째 유지).
- ⚠️ 무인 안전수칙 준수: git 조회 전부 `git -C` 형태, `cd`+`git` 연결 없음.

## 1. GSC API 점검 결과
| 항목 | 결과 |
| --- | --- |
| 색인 현황(Coverage) | **미확인** — API 자격증명 부재 |
| 모바일 사용성(GSC 리포트) | **미확인** — API 자격증명 부재 |
| 노출/클릭 | **미확인** — API 자격증명 부재 |

## 2. 라이브 전수 온페이지 감사 (60개 URL)
sitemap `<loc>`에서 60개 기계 추출 → 병렬 다운로드(xargs -P 12) → python 파싱(문자수는 C 로케일 함정 회피 위해 `len()` 사용).

| 신호 | 결과 |
| --- | --- |
| HTTP 상태 | **60/60 = 200** |
| canonical | 60/60 정확히 1개, self 일치, 불일치 0 |
| `<title>` | 60/60 고유, 중복 0 · **>60자 1건 ← 신규 회귀(34편), 아래 처리** |
| meta description | 60/60 존재·고유, 빈 값 0 |
| `<h1>` | 60/60 정확히 1개 |
| robots noindex | 색인대상 0건(실수 deindex 없음) |
| JSON-LD | 60/60 존재 |
| viewport meta | 60/60 정확히 1개 |
| `og:` 태그 | 60/60 = **0개**(기 등재 티켓 #2) |
| desc 길이 편차 | >160자 9개 / <70자 16개(티켓 #3, 34편 수정 후 재집계 기준) |

## 3. ★신규 회귀 검출·수정 (티켓 #1, 이번 실행 종결)
**검출**: 34편 `electricity-usage-marginal-cost-2026`의 렌더 `<title>` = **69자**. 원인: `app/blog/[slug]/page.tsx:36`가 `${post.title} | 계산기 허브`로 조립 → post.title(60자) + 접미사(9자) = 69. 기존 33편은 post.title이 짧아 +9자 후에도 ≤60을 지켰으나 이 글이 baseline(전 페이지 ≤59, >60자 0개)을 깼다. 부수적으로 description 164자(>160).

**핑퐁 1라운드 (한 방향 진전, 되돌림 없이 수렴)**:
1. **marketing-team**(카피 소관): post.title ≤51자 + desc ≤160자 후보 제안. 최종 title 45자 / desc 149자, 핵심 키워드·수치 보존.
2. **master 자체 검산**: python `len()`으로 title 45(렌더 54)·desc 149 재확인, 수치 10개 보존 확인 후 dev에 전달.
3. **dev-team**: `lib/blog.ts` L5460·L5462 두 문자열만 1:1 교체. `tsc --noEmit` EXIT 0, `git diff --stat` +2/-2.
4. **qa-team 독립 검증**: 5/5 PASS — 파일 값 exact 일치, title 45(렌더 54≤60)/desc 149≤160, 수치 7종 본문 대조 무모순, 변경 범위 +2/-2, tsc 0에러.
5. **master 직접 검증**: 소스 L5459~5462 육안 확인 + **`npx next build` 실행** → 생성된 `.next/server/app/blog/electricity-usage-marginal-cost-2026.html`의 실제 `<title>` = **54자(≤60) ✓**, desc = 149자(≤160) ✓.

**수정 값**:
- title(69→54 렌더): `여름 전기요금 100kWh 더 쓰면? 구간별 한계단가와 경계 점프 (주택용 저압)` (45자) + ` | 계산기 허브`
- description(164→149): `여름 에어컨으로 전기요금이 얼마나 오를까요? 2026년 주택용 저압 하계 요금표 기준 100kWh 추가 시 21,210~42,640원, 구간별 한계단가(151.0·257.6·362.1원), 450kWh에서 1kWh만 넘겨도 6,790원 뛰는 경계 점프를 정리했습니다.`

## 4. 모바일
- post.title은 이 글의 H1로도 재사용되는데, 이번 수정으로 **H1이 더 짧아졌다**(오버플로 위험은 증가가 아니라 감소). 메타데이터+헤딩 축소만 있고 레이아웃 코드 무변경이라 375px 회귀 가능성 없음 → 별도 뷰포트 재측정 생략(추론 근거 명시).
- **미확인**: 픽셀 스크린샷 시각 검증은 이전 실행들과 동일하게 미수행.

## 5. 배포(push)
- **커밋 대상**: `lib/blog.ts`(2줄) + 이 감사 로그. **경로 명시 스테이징**(`git add -A` 미사용).
- **타 세션 산출물 회피**: 워킹트리의 `.claude/launch.json`·`.claude/settings.local.json`·`.claude/settings.json`·`marketing/promo-drafts.md`는 손대지 않음.
- build+rendered HTML+qa PASS+master 직접 검증 모두 통과 → 자동 push 승인 범위(2026-07-13) 내.

## 6. 팀 핑퐁 요약
color: 색인 오류·노출·모바일 사용성 문제는 미검출. 유일 이슈(title 길이)는 **카피 소관**이라 master 단독 재작성 대신 marketing→dev→qa 정규 채널로 처리(07-23 로그의 "copy-length는 planning/marketing 소관" 선례 준수). 되돌림 없이 1라운드 수렴.

## 7. 미확인 / 다음 실행 인계
1. **[★사용자 확인 필요 — 유지, 9일 연속]** GSC API 자격증명 부재. 서비스계정 `.json` 또는 OAuth 토큰 env 없으면 Coverage/노출·클릭/모바일사용성 3종 영구 미확인. 매일 감사의 최대 구조적 공백.
2. **[티켓 #2 — 유지]** OG/metadataBase 전면 부재(60/60 `og:` 0개). 첫 SNS 게시 승인 전 처리 권장. og:image 에셋 필요(design + 이미지 사용자 승인).
3. **[티켓 #3 — 유지, 낮음]** meta description 길이 편차(>160자 9개 / <70자 16개). 카피 품질 이슈로 planning/marketing 정규 슬롯. 특히 짧은 쪽(계산기 상세 26~35자) CTR 개선 여지.
4. **[운영 표준 — 재확인]** ★신규 블로그 발행 시 `<title>` = post.title + 9자 접미사 규칙상 **post.title은 ≤51자로 유지**해야 SERP 잘림 없음. 34편이 이 규칙 없이 발행돼 회귀 발생 → 발행 전 lint 항목으로 추가 권장(planning/marketing).
5. **[운영 표준 — 유지]** C 로케일 Git Bash에서 `wc -m`·`${#}`·`awk length`는 바이트를 센다. 한글 문자수는 python `len()` 필수. 오늘도 준수.
6. **[운영 표준 — 유지 ⓕ]** URL은 sitemap `<loc>` 단일 출처에서 기계 추출, 병렬 다운로드로 타임아웃 회피.
7. **[백로그 — 유지]** canonical 생성 2종 공존 드리프트 / `coming-soon` 스텁 신호충돌 / 프리뷰 `x-robots-tag` 미확인 / 프로덕션 Linux 대소문자 slug 미확인 / 픽셀 시각 검증 미확인 / `.claude/settings.local.json` `bypassPermissions`(사용자 확인) / AdSense 심사 대기 / 마케팅 게시 승인 대기 / 로드맵 #16 취득세 보류.

## 제약 준수
확정 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. `.env*`·시크릿 미포함. `git add -A` 미사용, 경로 명시 스테이징. 타 세션 워킹트리 변경 미터치.
