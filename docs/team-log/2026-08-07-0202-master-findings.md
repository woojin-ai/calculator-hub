# 마스터 실측 노트 — 2026-08-07 02:02 회차

> ## 🔴 티켓번호 충돌 정정 (2026-08-07 02:45 추가 — **인용 전 반드시 읽을 것**)
>
> 이 회차는 **01:50 회차 세션과 동시 실행**됐고(티켓 #51 참조), 양쪽이 `#47`을 **각각 다른 뜻으로** 발행했다. 커밋 순서상 **상대 세션이 선착**(`c064a36` → 내 `4866957`)이므로 **그쪽 번호가 정본**이다.
>
> | 정본 | 내용 | 발행 |
> | --- | --- | --- |
> | **#47** | 기준일 명시 글의 전역 스윕 보호 목록화 | 01:50 세션 (**선착 — 유효**) |
> | **#48** | 2027 외부 고시 추적 대장 | 이 회차 (~~#47~~ 에서 이동) |
> | **#49** | 방치 worktree 감사 grep 오염 | 이 회차 (~~#48~~) |
> | **#50** | 렌더 출력 헤드리스 계측 + 2배 환산 | 이 회차 (~~#49~~) |
> | **#51** | 동시 세션 충돌 재발 | 이 회차 (~~#50~~) |
> | **#52** | 🔴 자동차세 연납 할인율 3% 정본 불일치 의심 | 이 회차 (~~#51~~) |
> | **#53** | `CAR_TAX_BASE_YEAR` 2027 시한 + 하드코딩 감사축 | 이 회차 (~~#52~~) |
>
> ⚠️ **커밋 `4866957`의 메시지는 이동 전 번호(#47~#52)로 적혀 있다.** 이미 push돼 재작성 불가하니 **커밋 메시지를 인용할 때 이 표로 환산**할 것. 아래 본문은 정본 번호로 갱신했다.
>
> **티켓번호 #53까지 소진**(배정 전 grep 필수).
> 📌 이 충돌 자체가 티켓 #51(동시 세션)의 실피해 사례다 — **번호 발행이 원자적이지 않다.** 다음 회차는 발행 전 `git log origin/main`까지 확인할 것(로컬 grep만으로는 상대 세션의 미push 발행을 못 본다).

> 이 파일은 팀 로그 본문(`2026-08-07.md`)의 부속 자료다. 회차 보고는 본문을 볼 것.
> **모든 수치는 `5f05109` 기준 마스터 직접 측정값이다.** 전달받아 검증하지 않은 값은 이 파일에 넣지 않았다(표준 ㋥).

---

## 1. 게이트 실측

| 게이트 | 명령 | 결과 |
| --- | --- | --- |
| 회귀 테스트 | `npm test` | **454 assertions · ALL SUITES PASS** (「최저시급 리터럴 금지」 34 포함 — 티켓 #41 탐지기 생존 확인) |
| 타입 | `npx tsc --noEmit` | 무출력 = 통과 |
| 빌드 | `npm run build` | exit 0. 블로그 36 + 계산기 16 경로 확인 |
| 원격 동기 | `git rev-list --left-right --count origin/main...HEAD` | **0 0** = 완전 동기 (01:06 회차 §6 「미확인」 해소) |

⚠️ **`npx vitest run`은 이 저장소의 게이트가 아니다.** `package.json`의 `test`는 `npx tsx scripts/regression/engines.test.ts`이고 vitest 설정 파일이 없다. vitest로 돌리면 경로 별칭 `@/lib/salary`를 못 풀어 **0개 테스트 실행에 3 FAIL**이 나온다 — 이건 코드 결함이 아니라 **잘못된 명령**이다. 이 오해가 재발하지 않도록 기록한다.

---

## 2. 🔴 티켓 #49 — 방치 worktree 2개가 감사 grep을 오염시키고 있었다

### 발견 경위
위 vitest 오실행이 우연히 노출시켰다. 실패 로그에 `.claude/worktrees/` 경로가 찍혔다.

### 실측

```
git worktree list
  C:/claude code webside                                  5f05109 [main]
  .claude/worktrees/cranky-feistel-94e116  97fa96d (detached)  2026-07-18 00:30
  .claude/worktrees/jolly-leavitt-3b98ee   3eda8ed (detached)  2026-07-18 01:23
```

20일간 방치. 각각 `lib/` 전체 사본을 들고 있었다.

### 왜 위험한가 — 도구에 따라 답이 갈린다

| 질문 | bash `grep -rn` | Grep 도구(ripgrep) |
| --- | --- | --- |
| `MINIMUM_WAGE` 출현 | **47** (그중 6 = 유령 사본, 12.8%) | 6건 제외 |

`.claude/worktrees/`는 `.git/info/exclude:18`에 있다. **ripgrep은 존중하고 bash grep은 무시한다.** 이 저장소는 건수 측정에 크게 의존하는데(전이폐포 41건, grep 포착률 34%/43%/47% 등), **같은 질문에 도구별로 다른 답이 나오고 있었다.**

### 더 위험했던 것

티켓 #30에서 **라이브 오류로 판정돼 제거된 날조 상수가 사본 안에 살아있었다**:

```
.claude/worktrees/cranky-feistel-94e116/lib/salary.ts:33:
  export const LONG_TERM_CARE_MULTIPLIER = 0.129457;
.claude/worktrees/jolly-leavitt-3b98ee/lib/salary.ts:33:  (동일)
```

정정 여부를 bash grep으로 확인하는 감사는 **"아직 안 고쳐졌다"는 오판**을 하게 된다.

### 조치

두 worktree 모두 **(a) 미커밋 변경 0건 (b) 커밋이 main의 ancestor** 확인 후 제거(`git worktree remove` + `prune`). 고유 작업 손실 없음 — 두 커밋은 main에 이미 들어있다(각각 크로스사이트 내비, DSR 조견표 블로그).

**제거 후 재측정**: `MINIMUM_WAGE` 47 → **41**. 게이트 454 PASS 유지. tracked 변경 **0건**(커밋 대상 아님 — worktree는 exclude 대상).

### 잔존 `0.129457` 9건은 전부 정상

전건 육안 확인함 — 정정 이력 서술(주석 4)·회귀 테스트 앵커(5). 그중 하나는 **재도입 금지 가드**다:

```
scripts/regression/engines.test.ts:368:
  t.ok(LONG_TERM_CARE_MULTIPLIER !== 0.129457, "구 오기 0.129457 재도입 금지")
```

라이브 상수에는 **0건**. 오탐 아님.

---

## 3. 티켓 #50 — 렌더 출력 헤드리스 계측 경로 확립

인계 §8 item 8(`preview_start` 300초 타임아웃 ×2, 2일 이월)의 **부분 해소**.

### 확립된 것

`npm run build` 후 `.next/server/app/blog/<slug>.html`을 직접 grep하면 **dev 서버 없이 실제 렌더 출력을 계측**할 수 있다. 소스가 아니라 **독자가 보는 것**을 재는 축이다.

### 🔴 신규 계측 함정 — 렌더 카운트 = 소스 카운트 **× 2**

RSC 페이로드(`self.__next_f.push` **21개**)가 본문 텍스트를 통째로 재방출한다. 실증:

| 토큰 | 소스 `lib/blog.ts` L4395 | 렌더 HTML | 비 |
| --- | --- | --- | --- |
| `만 18세` | 1 | 2 | ×2 |
| `만 19세` | 4 | 8 | ×2 |

두 토큰 모두 정확히 2배. **환산 없이 비교하면 유령 불일치를 보고하게 된다.** 이는 §5 보호 목록의 「만 19세 ×4 + 만 18세 ×1」이 렌더 실측으로도 옳음을 확인해 준다.

### 기준선 — 12월 #44 실행자는 이것과 diff하면 된다

`age-by-birth-year-2026.html` @ `5f05109` — **86,999 bytes**

| 토큰 | 렌더 | ÷2 = 소스 |
| --- | --- | --- |
| `2026년` | 54 | 27 |
| `2000년` | 18 | 9 |
| `2005년` | 10 | 5 |
| `2023년` | 8 | 4 |
| `2006년` / `2007년` | 4 / 4 | 2 / 2 |
| `만 19세` / `만 18세` | 8 / 2 | 4 / 1 |

형제 글 `age-birthday-boundary-2026.html` — **86,899 bytes**, `2026년` 38, **`2026년 7월 21일` 16회(=소스 8)**. 기준일이 8곳에 명시돼 있어 영구히 참 → §5 제5계열 보호 근거가 렌더 실측으로 뒷받침된다.

### ⚠️ 해소되지 **않은** 것 (과장 금지)

- **모바일/데스크톱 시각 검수는 여전히 불가.** `scripts/html-snapshot.mjs`는 바이트 동일성 도구이지 시각 검수 도구가 아니며, 스스로 커버리지 한계를 명시한다(동적 라우트는 HTML 산출물 자체가 없음, 청크 *내용*은 안 봄).
- 이 경로는 **텍스트·구조**만 잡는다. 레이아웃 깨짐·오버플로는 못 잡는다.

---

## 4. 티켓 #46 — 마스터 독립 실측 (⚠️ **작성 시점엔 미결재였다. 아래 4-0 참조.**)

### 4-0. 🔴 동시 세션이 이미 결재했다 — 이 절은 그 결재의 **독립 교차검증**으로 읽을 것

이 절을 쓰는 도중(02:11:54) `planning/ticket-44-age-table-2027-workorder.md`가 **다른 세션에 의해 워킹트리에서 수정**됐다. 그 세션은 「2026-08-07 **01:50 회차 결재**」로 **행 신설(15→16행)을 철회**했고, 자체 QA R3(F2·F3·F6·F13)를 돌렸다. 내 회차(02:02 슬롯)와 겹친 것이다 — **티켓 #11 재발**(세션 소요 60~91분 ≫ 30분 간격).

**그래서 이 절의 가치는 결재가 아니라 교차검증이다.** 나는 그 세션의 산출물을 보지 않은 상태에서, **렌더 HTML 역산**이라는 전혀 다른 경로로 아래 4-1·4-2에 도달했고, 결론이 일치한다:

| 항목 | 동시 세션(01:50 결재) | 내 독립 경로(렌더 HTML) | 일치 |
| --- | --- | --- | --- |
| 조견표 행수 | 15행 (16행 판정 철회) | 15개 서로 다른 출생연도 | ✅ |
| 행 신설의 문제 | 매년 무한 증식 + 2030년부터 기존 15행 전부 성년화 | (독립 도출 **아님** — 아래 주 참조) | — |

> ⚠️ **정정(자기적발).** 이 표의 초안에서 나는 「2030년부터 기존 15행 전부 성년화」를 **내가 추가한 근거**인 것처럼 적었다. **아니다 — 그 세션의 diff에서 읽은 것이다.** 내가 한 것은 산술 재확인뿐이다(2030 − 2010 = 연 20, 만 19~20 → 성년. 표의 최연소 행이 2010년생이므로 2030-01-01부로 15행 전원 성년. **성립**).
> 내가 독립적으로 도출한 것은 **4-1(15행)과 4-2(11+4 분해)뿐**이다. 표준 ㋥ — **relay한 값과 검증한 값을 구분해서 말하라.** 이 파일 자체가 그 표준을 어길 뻔했고, 어긴 방향이 하필 **내 기여를 크게 보이게 하는 쪽**이었다.

⚠️ 다만 **결재 (ii) 앵커 귀속에서는 갈린다** — 그 세션은 앵커 귀속을 「설계 규칙으로 쓰지 말 것」으로 정리했고, 나는 4-2에서 11+4 분해가 자기정합적으로 성립함을 실측했다. **내 4-2를 설계 규칙으로 승격하자는 뜻이 아니다**(4-3 참조). 두 판단은 양립한다: 성립한다는 사실과, 그것을 규칙으로 삼아도 되는가는 별개다.



§10-4의 결재 3건 중 **1·2번의 사실 기반**을 렌더 HTML에서 독립 추출했다. 기획팀 산출물을 보지 않고 별도 경로로 뽑은 값이다.

### 4-1. 조견표는 **15행**이 맞다 (16행 아님)

렌더 HTML의 출생연도 토큰(÷2):

```
1960·1966·1970·1975·1980·1985·1990·1995·1996·2000·2004·2005·2006·2007·2010
```

= **15개 서로 다른 출생연도.** 기획팀의 15행 주장과 일치하고, 과거 마스터 relay였던 「16행」은 **틀렸다**(메모리에 이미 정정 기록됨 — 이번에 제3 경로로 재확인).

### 4-2. 11 + 4 분해가 **자기정합적으로 성립한다**

내가 뒤에서 맞춰본 것이 아니라, 15개를 놓고 분류하면 자연히 갈린다:

- **고정(출생연도 앵커) 11개** — 5년 격자 `1960·1970·1975·1980·1985·1990·1995·2000·2005·2010` + `2004`
- **이동(연 나이 앵커) 4개** — `1966`(연 60) · `1996`(연 30) · `2006`(연 20) · `2007`(성년 경계 19)

2027년 적용 시 이동분만 +1 → `1967·1997·2007·2008`. **충돌 없음**(2006이 비운 2007 자리를 성년 앵커가 아니라 연-20 앵커가 채운다), **행 증식 없음**(15행 유지), 2006 퇴출·2008 진입.

→ **§4-1의 「2008년생 행 신설(15→16행)」은 철회가 타당해 보인다.** 신설 없이도 §4-1이 우려한 `L4395` 거짓 문제가 해소된다(2008이 이동 앵커로 자동 진입).

### 4-3. 그러나 결재하지 않는다 — 결재 2번(앵커 귀속)이 여전히 미해결

`1960`의 귀속이 갈린다:
- **내 판정**: 고정(5년 격자의 시작점). 이동으로 보면 2027년에 `1961`이 되어 격자가 `1961·1970·1975…`로 깨진다.
- **§4-1 기재**: 「`1960`=연 나이 66」 → 이동 취급.

`1996`도 마찬가지로 「연 나이 30」이라는 해석이 깔끔하지만, 5년 격자 이웃 `1995`(키워드 `95년생` 앵커, 소스 2회)와 붙어 있어 단정할 수 없다.

🔴 **이건 출력이 같아도 의도가 다르면 2027년부터 조용히 갈리는 종류의 문제다.** 그리고 내 4-2는 **HEAD 출력에서 역산한 추론이지 문서화된 의도가 아니다** — 기획팀 분해와 같은 성격의 한계를 갖는다. 두 경로가 일치한다는 사실은 **정황 보강이지 증명이 아니다.**

**따라서 나는 이 축으로 결재하지 않는다.** 결재 (i)(행 신설 철회)는 동시 세션이 01:50에 내렸고 내 실측이 이를 뒷받침한다(4-0). 결재 (ii)(앵커 귀속)는 **여전히 열려 있다고 본다** — 다만 그 세션이 「설계 규칙으로 쓰지 말 것」으로 봉인했다면 실무상 닫힌 것과 같다. **다음 회차가 두 문서를 병합할 때 이 차이를 명시적으로 처리할 것.**

---

## 4-9. 🔴 이번 회차 QA R3의 기준선은 `5f05109`(커밋본)이다

내가 02:02에 배정한 QA R3는 **커밋된 `5f05109`**를 읽었다. 동시 세션은 그 이후 워킹트리를 수정했다(02:11). 따라서:

- 내 R3 보고의 줄번호·건수는 **커밋본 기준으로만 유효**하다. 워킹트리에 그대로 대입하면 어긋난다.
- 두 R3는 **중복이 아니라 다른 baseline**이다 — 내 것은 「main에 들어간 문서가 옳은가」, 그쪽은 「내가 방금 고친 게 옳은가」.
- ⚠️ **이 구분을 지우고 합치면 표준 ㋢(건수 오차)의 새 사례가 된다.** 인용 시 **반드시 baseline을 병기**할 것.

---

## 4-10. QA R3 결과 — **FAIL 4건** (baseline `5f05109` 커밋본)

QA는 내 결론을 모른 채 호출됐다. PASS 범위가 넓고 근거가 두텁다 — 특히 **미열거 0건을 양방향으로 증명**했다(범위 내 숫자 보유 34줄을 전수 추출해 A14 + B1 + 함정2 + 보호2 + 표15로 완전 분해, 잔여 0). §4-1 전략의 산술 5건, §10-2 형제 글 산술 3건, §10-1 축자 대조(`diff` 무출력 147/147)도 독립 재검산으로 성립.

**FAIL 4건 중 3건을 내가 직접 재측정해 확인했다.** (표준 ㋥ — 아래는 relay가 아니라 검증값이다.)

### 🔴 FAIL-1 — 재오염원 정정 범위가 **또** 미달 (측정축 전환, 3회차 재발) — **마스터 확인 ✅**

§10-1은 오염을 "30/30줄"로 정정하고 처방을 **「BlogPost 리터럴 블록(L15~L161) 삭제」**로 적었다. 그런데 `planning/age-by-birth-year-blog-content.md`는 **207행**이고, 블록 **밖**에 2026 기준 값이 살아 있다. 내가 `sed`로 직접 뽑은 7줄 — **QA가 지목한 줄번호와 7/7 일치**:

| 줄 | 실제 내용(발췌) | 2027년 참값 |
| --- | --- | --- |
| L1 | `# … 2026년 출생연도별 나이 조견표` | 2027 |
| L6 | `기준연도: 2026년` | 2027 |
| L170 | `성년(만 19세) 여부가 2006/2007년생 경계에서 갈림` | 2007/2008 |
| L193 | `warning callout 1개(2026 기준 참고용…)` | 2027 |
| L197 | `세는나이 = 연 나이 +1(2000년생 27 등)` | **28** |
| L198 | `2007년생 생일 후 만 19세/생일 전 만 18세, 2006년생 이미 성년` | 2008 / 2007 |
| L199 | `2000년생 만 25~26세, 1995년생 세는 32세` | 만 26~27세 / 세는 33세 |

L197·L198·L199는 §3이 작업대상으로 지정한 값을 **축자 보유**한다. 지시대로 L15~L161만 지우면 §10-1이 경고한 「미래 세션이 여기서 다시 복사해 온다」가 **나머지 46행에서 그대로 성립**한다.

🔴 **원인이 날카롭다**: §10-1의 「30/30, MISS 0」은 *"blog의 30줄이 plan에 있는가"*를 잰 값이지 *"plan의 오염이 30줄뿐인가"*가 아니다. **측정축이 뒤집혔다.** 같은 절이 「마스터 초판 53%는 §3이 경계한 47% 실패 모드」라고 자백해 놓고, 바로 그 절의 처방에서 다시 과소 스코프를 만들었다. **표준 ㋎의 3회차.**

### 🔴 FAIL-2 — 보호 목록 불완전 2건 — **마스터 확인 ✅ (워킹트리에서도 미수정)**

**2-a. 산술 불변식이 6줄 중 3줄만 덮인다.** 내 실측:

```
L4339 "…연 나이에서 1을 뺍니다"      ← §5 누락
L4340 "…항상 연 나이보다 한 살 많습니다"   ✓ 등재
L4387 "…만 나이가 연 나이보다 한 살…"     ✓ 등재
L4391 "…언제나 연 나이보다 한 살 많습니다"  ✓ 등재
L4403 "…한 살 차이 날 수 있습니다"      ← §5 누락
L4423 "…1을 빼면"/"1을 더한 값"        ← §5 누락
```

누락 3줄은 **전부 A(작업대상)**이고 L4423은 `2026`을 2개소 보유해 치환 사정권 한복판이다. ⓘ 보충 — 이 계열은 「줄 전체 동결」이 아니라 **줄 안의 규칙 문구만 보존**하는 구절 단위 보호다(이미 등재된 L4387도 `2026`을 갖고 있다). 그 기준이면 누락 3줄은 동등하게 해당한다.

**2-b. `slug` L4298이 §5에 없다.** §3은 함정을 2건(`slug` L4298 · `publishedDate` L4310)으로 명시하는데 §5 표에는 `publishedDate`만 있다. 워킹트리 §5 실측 `grep -c 4298` → **0**. §8-1 item 9 / §8-2 item 6이 `slug 무변경`을 별도로 갖고 있어 실무상 이중으로 덮이지만, §5를 보호 정본으로 읽는 경로에서는 **색인된 URL을 깨뜨리는 줄이 보호 목록에 없다.**

### 🔴 FAIL-3 — §0-3 결정표에 **2027-01-01 행이 없다** — **마스터 확인 ✅**

워킹트리 표 실측: `2026-08-07 / 12-01 / 12-26 / 12-29~31 / **2027-01-02 이후**` — **01-01이 통째로 빠졌다.**

| 배포 시점 | 총 거짓 노출 |
| --- | --- |
| 12-29 (권장 하단) | 72시간 |
| 12-31 (권장 상단) | 24시간 |
| **2027-01-01 01:00 (표에 없음)** | **약 1시간** |
| 2027-01-01 07:30 (당일 최종 슬롯) | 7.5시간 |

문서는 이 문제를 「**최소화 문제**이지 제거 문제가 아니다」라고 선언해 놓고 **최소해를 표에서 뺐다.** 권장안이 누락된 선택지보다 24~72배 나쁘다.

⚖️ **다만 QA에 전면 동의하지는 않는다.** 01-01을 배제할 실질 근거는 문서에 이미 있다 — 실패 시 되돌릴 버퍼가 0이고, **2026-08-01 좀비 세션으로 이틀 연속 실행 0회가 실제로 있었다.** 기대 노출로 보면 01-01 단일 시도는 꼬리가 길다. **그래서 고쳐야 할 것은 권장일이 아니라 근거 라벨이다** — 「노출 최소화」가 아니라 **「최악 노출 상한 관리」**로 다시 쓰고, 01-01 행을 표에 넣어 트레이드오프를 드러낼 것.

### 🟠 FAIL-4 — `1966`·`1960` 앵커가 지시 없이 깨진다

§4-1이 선언한 4개 앵커 중 성년 경계 2개만 처리되고 `1966=연 나이 60`·`1960=연 나이 66`은 아무 지시가 없다(2027년 61·67로 밀려 앵커 소멸). 산문에 이 값을 참조하는 문장이 없어 **거짓 문장은 생기지 않는다**(QA 전수 확인) — 그래서 🟠다.

🔁 **이건 내 §4-3과 같은 지점이다.** 나는 렌더 HTML 역산으로, QA는 문서 내부 정합으로, **서로 모르는 채 같은 구멍에 도달했다.** 동시 세션도 §10-4 결재 (ii)에서 같은 2건(`1960`·`1996`)을 미결로 남겼다. **세 경로가 독립적으로 같은 곳을 가리키면 그건 우연이 아니다.**

### 이번 R3에 대한 마스터 유보 1건

QA가 **§0-1의 「75개 = 16행×4 + 11」을 PASS**로 판정했다. 계산 자체는 맞지만 **16행 전제는 동시 세션이 01:50에 철회했다**(→ 60+11=71). QA는 커밋본을 봤으니 그 baseline에서는 옳다. **두 판정이 충돌하는 게 아니라 baseline이 다르다** — §4-9의 경고가 즉시 실례를 얻었다. 인용 시 반드시 baseline 병기.

---

## 5. 소규모 수정 1건 (커밋 보류 — QA 대기)

`scripts/regression/engines.test.ts` L272~273 주석이 **닫힌 항목을 열린 것으로 서술**하고 있었다:

> (구) `planning/four-insurance-calculator-content.md` … 「아직 구식 = 문서 정정 필요(마스터 보고)」

**실측 결과 그 문서는 이미 정정돼 있다**(티켓 #32 스윕, 08-05):

| 확인 | 결과 |
| --- | --- |
| 구 승수 `0.129457` | **0건** |
| 구 금액 9종(194,207 / 393,414 / 13,961 / 291,311 / 485,519 / 983,538 / 660,252 / 1,338,004 / 14,582) | **전건 0회** |
| 신 금액 8종(194,348 / 393,696 / 14,172 / 291,522 / 485,870 / 984,240 / 660,743 / 1,338,986) | 전건 존재 |

> `14,801`은 문서에 0건이지만 이건 결함이 아니다 — A~D 앵커 세트가 아닌 **다른 테스트 케이스**의 값이다(engines.test.ts L160). 9종 중 8종만 대응되는 이유.

주석을 정정 완료 사실로 교체(6줄). **계산 로직·단정문 무변경**, `npm test` 454 PASS·`tsc` 무출력으로 확인.

**커밋 안 함** — 규정상 QA 검증 없이 push하지 않는다. 다음 회차 QA 배정 대상.

---

## 5-2. 🔴 티켓 #48 산출물에서 **라이브 오류 의심 1건 + #44 동형 시한 1건** — 코드 무수정, 다음 회차 최우선

기획팀이 `planning/external-notice-register-2027.md`(신규)를 작성했다. 6종 대장을 만들라는 지시였는데, 조사 과정에서 **현재 코드 결함 후보**가 나왔다. **코드 쪽 사실은 내가 직접 실측했다**(법령 해석은 미검증 — 아래 구분).

### (A) 🔴 `PREPAY_DISCOUNT_RATE = 0.03` — 정본 불일치 **의심**

| 항목 | 값 | 출처 |
| --- | --- | --- |
| 코드 | `lib/car-tax.ts:33` `= 0.03` (주석 「1월 연납 할인율(2026)」), 소비처 L132 | **마스터 실측 ✅** |
| 기획팀 인용 | 지방세법 시행령 제125조제6항 = 「100분의 **5**」 (law.go.kr OpenAPI 직독) | **마스터 미검증** ⚠️ |

🔴 **회귀 테스트가 이 값을 잠그고 있다** — `engines.test.ts:1151` `t.eq(a.amounts.prepayDiscount, 9_600, "A 연납 할인액(본세 3%)")`. **탐지기가 아니라 인증기로 작동한다.** 상수가 틀렸다면 테스트는 그 오류를 **고정**한다 — 티켓 #30(`0.129457`)에서 「내부정합 감사는 상수오류를 원리적으로 못 잡는다(계산결과와 본문이 똑같이 틀려서 정합함)」고 배운 것과 **완전히 같은 구조**다.

⚠️ **그럼에도 오늘 고치지 않는다.** 기획팀 스스로 **두 겹의 불확실성**을 보고했다 — (i) 상위 조문 제128조제3항의 산식은 `연세액 × (잔여일수/365) × 이자율`인 기간비례인데 코드는 정액 `round(baseTax × 3%)`이고, (ii) 분모가 「연세액(본세+지방교육세)」인지 코드의 `baseTax`(본세)인지 다를 수 있다. **요율만 5%로 바꾸고 종결하면 표준 ㋎의 교과서적 재현**이다. 부칙도 미취득(§7 M2).
👉 방향은 **사용자에게 불리**(납부액 과대)라 제보 가능 유형이고, 실제로 틀렸다면 애드센스 재심사 창(8월말~9월초)과 겹친다. **다음 회차 최우선, 단 QA 독립 정본 확인 선행.**

### (B) 🔴 `CAR_TAX_BASE_YEAR = 2026` — 티켓 #44와 **동형**인데 어느 티켓에도 없다

`lib/car-tax.ts:22`, 소비처 **L96**(`registerYear > CAR_TAX_BASE_YEAR` → 검증) · **L121**(`carAge = CAR_TAX_BASE_YEAR - registerYear`) — **마스터 실측 ✅**.

2027-01-01부로 (i) 전 차량 차령이 1년 과소 → 경감률 5%p 과소 → **세액 과대**, (ii) 2027년식 등록연도 입력이 `invalid-year`로 **거부**된다. **외부 고시 대기가 불필요한 확정 항목** = 오늘 티켓을 열 수 있다.

🔁 **이로써 「기준연도 하드코딩」 유형이 2건이 됐다**(#44 `age-by-birth-year-2026`, 여기 `car-tax`). 개별 대응이 아니라 **전 저장소 감사축**으로 세워야 한다 — 기획팀도 같은 제안을 했다.

### (C) 시점 축의 구조적 결론 (기획팀 보고 — 마스터 미검증)

- 대장 항목 **대부분이 하한 = 상한(교체 창 0일)**이고 **2027-01-01에 8개 이상 동시 만기**. 이대로면 실패한다.
- **2026-08 하순 건정심**(약 3주 뒤)에서 2027년 건보료율이 나오면 **정답을 알면서 약 4개월간 틀린 값을 띄워야 하는 기간**이 생긴다 — 선의의 조기 반영이 가장 현실적인 사고 시나리오.
- 국민연금 기준소득월액은 2027-03경 고시·**하한 2027-07-01** → 즉시 반영하면 약 3개월 오류. **147일 사고와 같은 형태.**
- **DSR은 위험이 아니라 모범 사례**로 분류됐다 — `lib/`에 상수가 없고 본문이 스트레스 금리 수치를 의도적으로 회피한다. 「가장 빨리 썩는 값은 본문에 넣지 않는다」.
- **이중 리터럴 2건**(건강보험 요율 `lib/salary.ts:46`↔`:60`, 전기 단가 `lib/electricity.ts:31`↔`:38`) — 한쪽만 고치면 조용히 어긋난다(#30 동형). **교체 티켓 발행 전에 파생화하면 티켓 자체가 안전해진다.**
- ⚠️ 「세금우대 9.5%」의 정본이 **존재하지 않는다**(조특법 제89조의3은 2026년 가입분 5%, 2027-01-01 이후 9%). `lib/blog.ts:3248` 라벨 문제 — **하한 없음(지금 이미 부정확)과 하한 2027-01-01이 한 항목에 섞여 있어 분리 필수.**
- 기획팀 미확인 **13건을 §7에 명시**했고, **law.go.kr 부칙 취득 경로가 없다는 공백**을 이번 조사가 처음 드러냈다(메모리의 직독 표준 경로에 부칙 항목 부재).

---

## 6. 운영 표준 추가 제안

- **[★신규 ㋫] 건수에는 「어느 도구로 셌는가」를 병기하라.** 표준 ㋢가 정의부·헬퍼·줄바꿈·줄/출현 4종 오차를 걷어내라고 했는데, **제5 오차원이 있었다: 측정 도구.** bash `grep -r`은 `.git/info/exclude`를 무시하고 ripgrep은 존중한다 — 같은 질문에 47 vs 41. 20일간 아무도 몰랐다.
- **[★신규 ㋬] 렌더 카운트와 소스 카운트를 같은 표에 놓지 마라.** RSC 재방출로 **정확히 2배**다. 환산 규칙을 명시하거나 축을 분리할 것.
- **[재확인 ㋥] 「16행」이 또 틀렸다.** 이번엔 제3 경로(렌더 HTML)로 15행을 확인했다. 이 수치는 과거 마스터 relay 오류로 이미 한 번 정정된 이력이 있는데, 지시서 §4-1에 **아직 16행 판정이 살아있다**(§10-4가 철회 후보로 올려둔 상태). 정정된 값이 문서 한 곳에서만 고쳐지고 다른 곳에 남는 전형적 패턴.
