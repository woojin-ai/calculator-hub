# 팀 로그 2026-07-19

## 01:00대 실행분 (블로그 신규 1편: 입사일별 근속 만근일 조견표 — service-period 2번째 전용 블로그, date 밸런싱 4→5 · planning→마스터검토→dev→**QA FAIL 1건→핑퐁 수정→QA 재검증 PASS**→마스터§7 완주 + auto-push)

### 세션 시작 상태
- 오늘 **첫 실행**(`2026-07-19.md` 부재). `git fetch` 후 local==origin==`6f7ce3a`(대환 손익분기 카드뉴스), 0 ahead/behind. 동시세션 추가 push 없음.
- working tree: `.claude/settings.local.json`·`.claude/settings.json`(권한 캐시)·미추적 gsc-audit 3건(07-15·16·18, daily-gsc-audit 소관)만 — 무관·제외. 미완료 작업 잔존 없음.
- 07-18 후속 이월: #2(마케팅 카드뉴스 계속) vs #4(블로그 2편째 심화) 중 **#4 블로그 선택**. 판단 근거: 카드뉴스는 이미 4편이 **전부 사용자 승인 대기(미게시)** 상태라 5편째 초안 추가의 한계효용이 낮은 반면, 블로그는 즉시 라이브 배포되어 SEO 자산이 된다. 카테고리 밸런스도 **date 4편 최저**로 블로그 쪽 갭이 명확.

### 주제 선정 + 카니벌 사전 차단 (착수 전 grep — 운영 표준 준수)
- date 클러스터: age 2편·dday 1편·service-period 1편. **service-period 2편째** 선정.
- **카니벌 사전 확인**: 형제글 `service-period-guide-2026`을 직접 Read → 이미 **3값 구분·+1 원리·윤년 원리·"소수 근속연수 1년 ≠ 만 1년" 함정**을 깊게 커버. → **윤년 각도는 카니벌 高로 기각.**
- **선정 각도 = 만근일 값 lookup**(개념글 vs 조견표글 분리, `age-korean-age-law` vs `age-by-birth-year`에서 검증된 패턴). 형제글에 만근일 조견표 **0건** 확인 → 카니벌 0.

### 마스터 사전 재검산 (`lib/service-period.ts` `calculateServicePeriod()` `npx tsx` 직접 실행)
- 규칙: **만근일(근속기간이 정확히 "N년 0개월 0일"로 표시되는 종료일) = 입사일 + N년 − 1일**. 재직일수 양 끝 포함(+1).
- **앵커 12입사일 × 4마일스톤 = 48값** 전량 엔진 산출(브루트포스 탐색으로 만근일 유일성까지 확인). 경계값 5종 추가 확보.
- **비자명 인사이트 4종(전부 감산·비교 파생)**: ①만근일 = 응당일 −1일(응당일 당일은 이미 "N년 0개월 1일") ②**2024-02-29 윤일 입사의 1년 만근일 = 2025-02-28**(존재하지 않는 2/29 대신 예외 없이 떨어짐 — 최강 소재) ③같은 만 1년인데 재직일수 365 vs 366, 10년은 3652 vs 3653 ④만근일 하루 전은 "0년 11개월 27~30일"로 뚝 떨어짐.

### 파이프라인
- **티켓**: TaskCreate #1(메인, owner planning→dev→qa→master 순환), **#2(QA FAIL 핑퐁, owner planning→dev→qa)**.
- **planning-team**: `planning/service-period-milestone-blog-content.md` 신규 Write. slug `service-period-milestone-2026`, category date, related3(service-period 주·dday·age). **severance/annual-leave 계산기는 카니벌·YMYL 회피 목적으로 related·본문 링크 양쪽에서 의도적 제외.** 조견표 분할축을 연차가 아닌 **입사연도**로 잡아(한 줄에 4마일스톤 전부) 사용자가 섹션 하나만 보면 끝나게 설계. blocker 0.
- **마스터 검토(Read 직접)**: 48값 전부 사전 재검산과 1:1 일치·경계값 5종 일치·slug grep 0(유일)·형제글 실재 1·related3 전부 `status:"live"`(age 51·dday 82·service-period 113)·BlogSection 유니온 정합 → append 승인.
- **dev-team**: `lib/blog.ts` 순수 append(170/0). md 펜스 블록을 **스크립트로 splice**(손 전사 배제 → 전사 오타 원천 차단). tsc exit0·build 59→60 정적.

### QA 검증 1차 (qa-team 독립) — **FAIL 1건** (①②③④⑤⑦ PASS)
- ③ **수치 독립 재현 53/53**(조견표 48 + 경계 5), 엔진 직접 실행·만근일 유일성 검증까지. 창작 수치 0.
- ⑥ **★팬텀 UI FAIL**: 본문 사용절차가 **"종료일 칸"을 조작 지시**하나, 라이브 `components/ServicePeriodCalculator.tsx`의 실제 렌더 라벨은 **`입사일`(71행)·`퇴사일 (미입력 시 오늘)`(99행)** 2개뿐. "종료일"은 **93·176행 코드 주석에만** 존재. 재직 중 독자에게 미래 만근일을 넣게 하는 맥락이라 "나 아직 퇴사 안 했는데?"로 이탈 위험 — 단순 표기 문제 아님.
- **마스터 재확인**: grep으로 라벨 2개·주석 전용 직접 확인 → **FAIL 타당 인정.**

### 핑퐁 루프 (QA FAIL → planning → dev → QA 재검증)
- **planning-team 되돌림**(문구 소유팀): §1의 **4줄만** 수정. ①"근속 연차를 세다"→"근속 햇수를 세다"(연차 이중 의미 해소) ②"입사일과 종료일 두 날짜"→"입사일과 퇴사일(종료일)"(첫 등장 병기) ③2단계 "종료일 칸에"→"'퇴사일 (미입력 시 오늘)' 칸에" **+ 재직 중 미래 날짜 가능 안심 문구 추가** ④4단계 "종료일을"→"퇴사일 칸의 날짜를".
- **★마스터 드리프트 검증**: planning이 Edit 불가로 **파일 전체를 Write 재작성**했으므로, 수정본 §1과 기존 삽입분을 **직접 diff** → **정확히 4줄만 차이, 170/170줄 유지, 수치·메타·구조 무변경** 확인(의도치 않은 드리프트 0).
- **★마스터 근거 검증**: planning이 안심 문구의 근거로 든 컴포넌트 111~114행 도움말("비워 두면 오늘 날짜까지 계산합니다. **퇴사 예정일 등 미래 날짜도 입력할 수 있습니다.**")이 실재하는지 직접 확인 → 실재·렌더됨. **안심 문구가 그 자체로 또 다른 팬텀이 되지 않음을 확인.**
- **dev-team 재적용**: 4388~4557행을 수정본으로 splice 교체(differing lines 4, 나머지 166줄 바이트 동일). `종료일 칸에` grep 0.
- **dev 지적 수용(마스터 지시 정정)**: 마스터가 제시한 numstat 기대치 174/4는 **최초 append가 미커밋 상태라 성립 불가**한 값이었고 실제 170/0이 정상 — dev 판단이 옳아 수용. 단일 커밋으로 확정.
- **QA 재검증 — PASS(FAIL 0)**: ①diff 0(170/170 바이트 동일) ⑥-a 팬텀 조작 지시 0건·잔존 "종료일" 5건 전부 개념어 용법(라이브 계산기 `interpretation`도 동일 병기 방식 사용)·안심 문구 **3중 확인**(소스·실제 렌더 HTML·엔진이 미래 종료일 허용) ⑥-b 카니벌 신규 침범 0 ⑦ 회귀 정상.
- **1라운드 만에 해소**(FAIL 제기 → 수정 → 재검증 PASS). 동일 항목 반복 FAIL 0.

### 마스터 §7 최종 직접 검증 (근거)
- **build**: 성공·**60/60 정적**. **git**: `.env` 스테이징 0(NO-ENV-OK).
- **라이브 렌더**(`.next/server/app/blog/service-period-milestone-2026.html`, 74,739B): 앵커 grep — `2025년 2월 28일`(4)·`366일`(22)·`3652일`(16)·`3653일`(16)·`0년 11개월 27일`(2)·`만근일`(76) 전존.
- **팬텀 부정검사**: `종료일 칸`·`자동 생성`·`자동으로 나옵니다`·`마일스톤 탭`·`입사일만 넣으면` = **전부 0**. 실제 라벨 안내 `'퇴사일 (미입력 시 오늘)' 칸에`(1)·`퇴사일 칸의 날짜`(2) 존재.
- **양방향 내부링크**: `.next/server/app/calculator/service-period-calculator.html`에 역링크 — slug(3)·"만근일 조견표"(2) 노출(RelatedBlogPosts 자동). **sitemap 포함 1건 확인**.
- **★375px 모바일 실측**(dev 서버 + 브라우저, 이번엔 실제 실시): `innerW==scrollW==375`, **문서 가로 오버플로 0px**. **조견표 12행(한 줄에 4마일스톤이 들어가는 최장 문자열) 중 넘치는 행 0건** — 이 글 고유의 실질 리스크였으므로 가정하지 않고 직접 측정. 진짜 초과 요소는 `<nav overflow-x:auto>` 1건뿐(사이트 공통 의도된 가로 스크롤 행, 07-18 gsc-audit과 동일 판정). **콘솔 에러 0건.**
- **브라우저 텍스트 실측**: 2단계·4단계 문구가 수정본대로 렌더·`종료일 칸` 0건·윤일 행("2024-02-29 입사 → 1년 2025-02-28(366일) / … / 10년 2034-02-28(3653일)") 정확 렌더 확인.
- **미확인 항목**: 없음(파일·엔진재계산·콘텐츠정합·링크·빌드·라이브렌더·양방향링크·sitemap·팬텀/카니벌정합·375px 실측 전부 직접확인). 픽셀 스크린샷만 렌더러 타임아웃으로 미취득(기존 이력과 동일) → DOM 정량 측정으로 대체.

### 마일스톤
- **service-period 2번째 전용 블로그 — date 카테고리 밸런싱(4→5).** 블로그 총 **28편(salary 8·loan 8·date 5·life 7)**. date 클러스터: age 2편·service-period 2편·dday 1편. 개념글+조견표글 2편이 동일 계산기를 양방향 링크로 지지하는 구조가 age에 이어 service-period에도 완성.
- **팬텀 가드레일이 실제로 FAIL을 잡아낸 첫 사례.** 지난 3슬롯은 사전 전달로 사고 0이었으나, 이번엔 **가드레일이 커버하지 못한 축(출력 포맷은 맞았으나 입력 라벨명은 미대조)**에서 결함이 발생 → QA가 포착. **교훈: 팬텀 점검은 출력 문구뿐 아니라 입력 필드 라벨까지 실제 컴포넌트와 대조해야 한다.**

### 마케팅 카드뉴스 게이팅
- **이번 실행 마케팅 미배정**(블로그 파이프라인 + QA 핑퐁 1라운드로 30분 슬롯 소진). START 상태 유지. 만근일 강소재("만근일은 입사 응당일이 아니라 하루 전날"·"2월 29일 입사도 만근일은 2/28로 예외 없이 떨어진다") 백로그 추가.
- 판단: 카드뉴스 4편이 **전부 이미지화·게시 사용자 승인 대기** 중이라, 초안만 더 쌓기보다 라이브 자산(블로그) 확장이 우선이라고 봄.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 인바운드 큐 부재). 조회할 실제 문의 없음.

### 핑퐁 이슈 요약
| 제기 | → 이관 | 결과 |
| --- | --- | --- |
| **qa-team ⑥ 팬텀 라벨 FAIL** | → planning(문구 수정) → dev(재적용) → qa(재검증) | **해결**(1라운드, QA 재검증 PASS) |
| planning §6 참고(dday 2편째 여지) | → 마스터 백로그 | 이월(후속 #2) |
| dev: 마스터 numstat 기대치 오류 지적 | → 마스터 | **수용**(dev 판단이 옳음, 단일 커밋 확정) |
| QA 비차단 권고 3건 | → 판단 보류/백로그 | 아래 후속 참고 |
- blocking 0. planning blocker 0.

### 배포(push)
- 대상: `lib/blog.ts`(신규 글 1편 append 170/0), `planning/service-period-milestone-blog-content.md`(신규 기획안), 이 팀로그. **QA 재검증 PASS(FAIL 0) + 마스터 §7 전부 통과(미확인 0)** → §8 auto-push 충족.
- 제외: `.claude/settings.local.json`·`.claude/settings.json`(권한 캐시), `docs/team-log/2026-07-15·16·18-gsc-audit.md`(기존 미추적, daily-gsc-audit 소관). `.env*` 미포함·시크릿 0.

### 다음 실행(01:30)으로 넘기는 미해결/후속
1. **[동시세션 주의 — 유지]** 시작 시 `git fetch`+`git log` 필수, append 전 slug 중복 grep 필수.
2. **[블로그 확장 — 계속]** 오늘 1편. 카테고리 salary 8·loan 8·**date 5**·**life 7**. 다음 후보: **dday 2편째**(date 클러스터에서 유일하게 전용글 1편 — planning·QA 양쪽이 지목) 또는 life 3편째(bmi/electricity, 카니벌 grep 필수). 착수 전 형제글 Read로 카니벌 경계 확인 필수.
3. **[★신규 운영 표준 — 팬텀 점검 강화]** 블로그가 계산기 조작 절차를 안내할 때는 **실제 컴포넌트의 `<label>` 텍스트를 grep으로 대조**할 것(출력 포맷만 맞추면 놓친다 — 이번 FAIL의 원인). planning 프롬프트에 **실제 라벨 리터럴을 사전 삽입**하는 방식이 유효.
4. **[QA 비차단 권고 — 미반영, 다음 사이클 판단]** ⓐ`service-period-milestone-2026` 4423행 정의문에서 "종료일" 첫 등장 시 병기(현재 병기는 88줄 뒤 4511행) ⓑ잔여 "연차" 이중 의미 2건(4482·4538, 햇수 뜻) → "근속 햇수"로 통일 검토(리스크 낮음, 차단 아님) ⓒ조견표 재직일수 콤마 미표기(계산기 UI는 `1,096일`, 표는 `1096일`) — 표 가독성상 현행 유지도 타당.
5. **[구조 이슈 — 신규 발견]** **blog↔blog 내부링크 부재**: 본문이 형제글을 이름으로 defer해도 실제 이동 링크가 없다(`RelatedBlogPosts`는 카테고리 페이지에만 부착). 28편 규모에서 defer 효과가 반감되므로 **기획+개발 과제로 검토 권장**(이 글만의 회귀 아님, 사이트 전역 구조).
6. **[마케팅]** 카드뉴스 4편 전부 이미지화·게시 **사용자 승인 대기**. 남은 강소재: 만근일·나이 조견표·평↔㎡·DSR·D-Day. 카드뉴스 링크 slug는 반드시 `calculators.ts` 실제 slug grep 확인(`-calculator` 접미사 주의).
7. **[개선 — 비차단]** `CarTaxCalculator.tsx` 하한 1900 리터럴 → lib export 정합(저비용).
8. **[로드맵 #16]** 취득세 — 마스터 카테고리결정+多회차·高YMYL, 자율창 보류 유지.
9. **[유지 백로그]** 07-15 핸드오프(salary 가시 내부링크 승인·마케팅 게시 승인·GSC API 미연동) 이월. AdSense 심사 대기(광고 스크립트=승인+pub-id 후).
10. **[운영 표준]** 블로그 데이터배열 append·YMYL 면책·마스터 사전 재검산(엔진 직접실행)·heading+list 표 표준·앵커 감산/파생만 허용·**팬텀 가드레일(라벨까지 대조)**·**2편째 심화 시 카니벌 경계 착수 전 형제글 Read**·**팀이 파일 전체 재작성 시 마스터 diff로 드리프트 검증** 유지.

## 01:40 실행분 (구조 개선: 블로그 상세 blog↔blog 내부링크 신설 — 28편 전역 · planning+design 병렬 → 마스터 충돌조정 → dev → QA PASS → 마스터 실측 §7 완주 + auto-push)

### 세션 시작 상태
- 오늘 **2번째 실행**. `git fetch` 후 local==origin==`3d06a03`(만근일 조견표), 0 ahead/behind. 동시세션 push 없음.
- 직전 실행 후속 목록에서 **#5(blog↔blog 내부링크 부재)** 선택 — #2(블로그 1편 추가) 대비 판단 근거: 블로그 1편은 자산 +1편이지만, #5는 **기존 28편 전부의 내부링크 구조를 한 번에 개선**(28×4=112개 신규 내부링크)하는 전역 레버리지. 직전 실행에서 planning·QA가 **양쪽 독립으로 지목**한 구조 이슈이기도 하다.

### 사전 스코프 확인 (마스터 직접)
- `RelatedBlogPosts`·`toBlogListItems`·`BlogPostCard` 등 **재사용 자산이 이미 전부 존재**, 갭은 상세 페이지가 형제글을 렌더하지 않는 것뿐 → 30분 슬롯에 적합한 범위로 판단.
- 카테고리 실측 `salary 8·loan 8·life 7·date 5`(합 28) — 뒤의 편수 결정 근거로 사용.

### 기획+디자인 병렬 배정 → **마스터 충돌 조정 2건**
- **planning-team**: `planning/blog-internal-linking-spec.md` 신규. **4티어 알고리즘**(T1 같은 primary → T2a overlap≥2 → T3 같은 카테고리 → T2b overlap=1+primary앵커 → T4 태그). 핵심 발견: `relatedCalculatorSlugs[0]`이 주력 계산기이고 뒤 슬롯은 장식이라 **단순 교집합은 노이즈**(life 글들이 3번 슬롯에 dday를 관성적으로 달고 있어 만나이 글이 자동차세 글을 끌어옴) → 카테고리(T3)를 약한 겹침(T2b)보다 **위**에 배치. age쌍·service-period쌍이 T1 대칭으로 상호노출 보장됨을 실증.
- **design-team**: `design/blog-related-posts-section-spec.md` 신규. **★브리핑 정정 1건 제출** — `RelatedBlogPosts`는 "카테고리 전용"이 아니라 **호출부 2곳**(CategoryPage `max-w-5xl` + 계산기상세 `max-w-3xl`). 마스터 grep 재확인 → **정정이 옳음, 수용**(하위호환 대상이 1곳이 아닌 2곳으로 확대).
- **★충돌 ①: 노출 편수 planning 6편 vs design 4편(2×2)** → **마스터 4편 확정**. 근거: 최소 카테고리 date 5편 → 자기 제외 4편이라 **28편 전부 T3만으로도 4편 보장** → 유일하게 모든 페이지가 깨진 마지막 줄 없이 2×2로 채워지는 값.
- **★충돌 ②: 3열 grid 재사용 가부** → design 기각안 채택(`columns` prop 파라미터화, 블로그 상세만 2열). design 계산: max-w-3xl(768)−px(48)=720 → 3열 시 카드 229.3px·제목 2줄 ≈21자인데 실제 제목은 30~45자 → **모바일 375px(≈39자)가 데스크톱 3열보다 제목을 더 보여주는 역전**. (이 수치는 아래 마스터 실측에서 그대로 재현됨.)
- design 에스컬레이션 **[중] 계산기 상세도 같은 3열 문제 보유(라이브 16페이지)** → **범위 밖 백로그로 분리**, dev에 "이번 커밋 금지" 명시.

### dev-team 구현 (지시: 마스터 확정사항이 문서와 충돌 시 우선)
- `lib/blog.ts` `getRelatedBlogPosts` + `POST_RELATED_BLOG_LIMIT=4` 신규 export(기존 시그니처 무변경) / `RelatedBlogPosts.tsx` optional prop 2개(`columns?`·`headingText?`, 기본값이 현행과 동일) / `app/blog/[slug]/page.tsx` `<hr>`+섹션.
- **Tailwind purge 사고 방지**: 템플릿 리터럴 금지, **정적 룩업 맵**만 사용(지시 준수 확인).
- `border-brand-border` 토큰 실재 여부를 **임의 생성 말고 확인하라**는 지시 → `app/globals.css:22` 실재 확인 후 그대로 사용(대체 0).
- tsc exit0 · lint 0 · test 188 assertions PASS · build **60 유지**(신규 라우트 없으니 증가가 이상신호라고 사전 고지한 값과 일치).

### QA 검증 (qa-team 독립) — **기능 9/9 PASS**
- **하위호환**: `git stash`로 baseline 빌드 후 전수 비교 → **55개 중 blog 28개만 차이, 계산기상세 16·카테고리 4·기타 7은 바이트 동일**. dev 주장 재현 성공. 작업물 유실 0(백업 후 진행, stash 복원 확인).
- **알고리즘 3자 대조**: 기획안 텍스트만 보고 **QA 독립 재구현** 작성 → (기획안 손계산 · 구현 · QA 재산출) **28/28 불일치 0**.
- 자기 제외 28편 전수 0건 · 카드 정확히 4장(count!=4: 0) · **연속 2회 빌드 55/55 동일(SSG 결정성)**.
- **팬텀 점검(직전 실행 FAIL 재발 방지)**: "함께 읽으면 좋은 글" **블로그 28편에만** 존재·계산기/카테고리 **누출 0**, 기존 "관련 글"은 2곳에 잔존, 계산기 3열 유지(=범위 밖 미실시 확인).
- 헤딩 위계 28편 전수 건너뜀 0 · 섹션 순서 28편 전수 위반 0 · 회귀 4종 PASS · 토큰 실재 확인.
- **QA 미확인 1건**: 이 환경에 playwright/puppeteer/jsdom 전무 + QA 세션에 브라우저 도구 없음 → **375px 런타임 실측 불가**, 정적 분석으로 대체함을 명시(창작 없이 한계를 정확히 보고 — 양호).

### ★마스터 §7 최종 직접 검증 (QA 미확인분을 마스터가 실측으로 마감)
- **build 재실행**: 성공 · **60/60 정적**. 렌더 HTML 자기링크 **0**.
- **375px 실측**(dev 서버+브라우저): `innerW==scrollW==375`, **가로 오버플로 0px**, 카드 4장 1열(343px), `<hr>` 1개, h2 순서 `관련 계산기` → `함께 읽으면 좋은 글` 확정 순서대로. 초과 요소는 `<nav>` 1건뿐(사이트 공통 의도된 가로 스크롤, 07-18 gsc-audit·직전 실행과 동일 판정). **콘솔 에러 0**.
- **★디자인 근거 실측 재현**(QA도 못 한 1023↔1024 경계): 1280px에서 **신규 블로그 섹션 = 352px×2열, 45자 제목 `titleClipped:false`(안 잘림)** / 같은 페이지 **관련 계산기 섹션 = 229.328px×3열** — design이 손계산한 229.3px와 **소수점까지 일치**. 또한 계산기 섹션은 1023px에서 352px×2 → 1280px에서 229px×3으로 **넓어질 때 카드가 123px 쪼그라드는 역방향 반응형이 라이브에서 실재함을 실측 확인**(신규 2열 섹션은 이 병리를 회피). → 백로그 항목의 근거가 추정에서 **실측**으로 승격.
- **미확인 항목: 없음**(빌드·렌더·알고리즘·하위호환·375px·데스크톱·경계폭·콘솔 전부 직접확인). 픽셀 스크린샷만 기존 이력대로 미취득 → DOM 정량 측정으로 대체.

### ★범위 밖 발견 — 사용자 확인 필요 (커밋 제외)
- QA가 `.claude/settings.local.json`(+63줄, **`"defaultMode": "bypassPermissions"` 포함**)과 `.claude/settings.json`(신규)을 **dev-team의 범위 위반**으로 보고.
- **마스터 검증 결과 QA의 귀속 판단은 오류**: 세션 시작 시점 `git status`에 이미 두 파일이 변경/미추적으로 존재했고, `.claude/settings.json` mtime은 **07-18**이다 → 이번 실행 팀들의 소행 아님. dev-team 무혐의.
- 다만 **지적 자체는 타당**: `defaultMode: bypassPermissions`는 **추적 파일**에 실재하며(116행) 권한 확인 절차를 무력화하는 설정이다. 누가 언제 넣었는지 이 실행에서는 규명 불가.
- 조치: 기존 모든 실행과 동일하게 **커밋에서 제외**하고, **사용자 확인 필요 항목으로 상신**(무인 실행에서 권한 설정을 임의 판단·수정하지 않음).

### 핑퐁 이슈 요약
| 제기 | → 이관 | 결과 |
| --- | --- | --- |
| design: 마스터 브리핑의 "카테고리 전용" 오류 정정(호출부 2곳) | → 마스터 | **수용**(grep 재확인, 하위호환 범위 확대) |
| planning 6편 vs design 4편 충돌 | → 마스터 재정 | **해결**(4편 확정, 데이터 기반 근거) |
| design [중] 계산기상세 3열 문제(라이브 16p) | → 마스터 | **범위 분리**(백로그, 이번 실측으로 근거 승격) |
| QA: settings 권한변경 범위위반 | → 마스터 검증 | **귀속 오류 정정**(팀 무혐의) + **사용자 상신** |
| QA 375px 실측 불가 | → 마스터 실측 | **해결**(마스터가 브라우저로 마감) |
- QA FAIL **0건**(직전 실행의 팬텀 교훈을 사전 지시에 반영한 효과). blocking 0.

### 마일스톤
- **사이트 전역 내부링크 구조 완성**: 이제 계산기↔블로그(양방향)에 더해 **블로그↔블로그**까지 연결. 28편×4장 = **112개 신규 내부링크**, QA 감사 결과 **고립글(in-degree 0) 0편**.
- 직전 실행 후속 #5를 1슬롯에 해소. `columns` prop 도입으로 계산기 상세 후속 작업은 **호출부 2줄**이면 끝나는 상태로 정리됨.

### 마케팅 카드뉴스 게이팅
- **이번 실행 미배정**(구조 개선 파이프라인으로 슬롯 소진). START 상태 유지. 카드뉴스 4편 전부 **사용자 승인 대기** 상태 변동 없음 — 초안 누적보다 라이브 자산 우선 기조 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `lib/blog.ts`·`components/RelatedBlogPosts.tsx`·`app/blog/[slug]/page.tsx`(구현 3), `planning/blog-internal-linking-spec.md`·`design/blog-related-posts-section-spec.md`(기준 문서 2), 이 팀로그. **QA PASS(FAIL 0) + 마스터 §7 미확인 0** → §8 auto-push 충족.
- 제외: `.claude/settings.local.json`·`.claude/settings.json`(**권한 설정 — 사용자 확인 필요, 위 참고**), `docs/team-log/2026-07-1{5,6,8}-gsc-audit.md`(daily-gsc-audit 소관 기존 미추적). `.env*` 미포함·시크릿 0.

### 다음 실행(02:00 이후)으로 넘기는 미해결/후속
1. **[★사용자 확인 필요 — 신규]** `.claude/settings.local.json`의 `"defaultMode": "bypassPermissions"`(116행) + 광범위 와일드카드 허용. 추적 파일이며 이번 실행 소행 아님(07-18 이전 유입 추정). **무인 실행에서 임의 되돌리지 않았음** — 사용자 판단 필요.
2. **[백로그 — 근거 실측 완료]** 계산기 상세(`app/calculator/[slug]/page.tsx:146`) 관련글/관련계산기 3열 → 2열. 이번에 **229.328px 및 역방향 반응형(1023→1280에서 352→229px)을 실측 확인**. `columns` prop이 이미 있어 **호출부 2줄** 수정이면 끝. 단 라이브 16페이지 하단이 바뀌므로 design 승인선에서 진행 권장.
3. **[블로그 확장 — 계속]** salary 8·loan 8·**date 5**·life 7. 다음 후보: **dday 2편째**(date 클러스터 유일 1편) 또는 life 3편째. 착수 전 형제글 Read로 카니벌 경계 확인 필수.
4. **[기획 후속]** planning이 스스로 남긴 in-degree 감사 → **QA가 이미 산출**(고립글 0, 최소 in-degree 1이 6편: dsr-loan-limit·summer-electricity·weekly-holiday-allowance·savings-interest·bmi·savings-interest-tax). **이 6편을 다음 글의 링크 대상으로 우선 지목**하면 자연스럽게 균형이 잡힌다.
5. **[구현 참고]** T2b·T4 티어는 현재 데이터에서 **0장 기여**(편수를 6→4로 줄인 결과 도달 불가 분기). 방어 코드로 무해하나, 글이 늘어 T1/T2a가 얇아지면 활성화된다 — 제거하지 말 것.
6. **[운영 표준 — 유지+추가]** 직전 실행의 팬텀 교훈(라벨까지 대조)을 **QA 프롬프트에 사전 주입**해 이번 FAIL 0 달성 → 계속 유지. 신규: **팀이 마스터 브리핑의 사실 오류를 정정해 오면 grep으로 재확인 후 수용**(이번 design 사례), **팀 간 수치 충돌은 마스터가 데이터로 재정**(6 vs 4편).
7. **[유지 백로그]** 07-19 01:00대 후속 ⓐ~ⓒ(문구 다듬기, 비차단), `CarTaxCalculator.tsx` 하한 1900 리터럴, 로드맵 #16 취득세(高YMYL·자율창 보류), AdSense 심사 대기(광고 스크립트=승인 후), GSC API 미연동, 마케팅 게시 승인 대기.

### ⚠️ 배포 경위 정정 (커밋 후 확인된 사실 — 다음 실행 필독)
- 마스터가 스테이징하려던 시점에 소스 5개가 이미 사라져 조사한 결과, **동시 실행 중이던 `daily-gsc-audit` 세션이 마스터의 미커밋 작업물을 `1000fb3`으로 선(先)커밋·푸시**했다("블로그: 상세 페이지 "함께 읽으면 좋은 글" 내부링크 신규 (28편 × 4링크)", 자기 gsc-audit 로그 4개와 함께).
- **결과물은 정상**: 포함 파일이 의도한 5개와 정확히 일치(`lib/blog.ts` 106 · `RelatedBlogPosts.tsx` 20 · `page.tsx` 11 = dev 보고치와 동일, spec 2건), **`settings*` 유출 0 · `.env` 0**. 마스터가 `git diff HEAD`로 **작업트리 == HEAD 드리프트 0**을 확인 → **QA PASS + 마스터 §7 실측을 거친 바로 그 내용이 푸시됨**이 증명됨.
- 팀로그는 별도 커밋 `c51c84c`로 푸시.
- **교훈/표준 추가**: 후속 #1(동시세션 주의)이 실제로 발생했다. 지금까지는 "시작 시 fetch"만 표준이었으나, **작업 종료·커밋 직전에도 `git log`/`git status`를 재확인**해야 한다(다른 세션이 내 미커밋 변경을 가져가 커밋할 수 있음). 특히 **settings 유출 여부는 내가 스테이징하지 않았더라도 반드시 검사**할 것 — 이번엔 무사했으나 동시세션이 `git add -A`를 쓰면 `bypassPermissions`가 들어간 `.claude/settings.local.json`이 그대로 올라갈 수 있다(후속 #1이 더 시급해진 이유).
