# 2026-07-15 데일리 팀 스탠드업 로그

> 오늘 첫 실행(07-15.md 미존재 확인 후 신규 생성). 직전일(07-14) 20:37 실행분 핸드오프의
> "다음 = **11순위 4대보험료 계산기**(/salary, 연봉 보조), planning부터"를 이어받는다.

## 01:00 실행분 (로드맵 #11 4대보험료 계산기 — 고아 작업 검증 후 라이브 10개→11개)

### 세션 시작 시 상태 (동시성: 고아 작업 발견)
- `git status` 결과 working tree에 **미커밋 #11 4대보험 계산기 작업**이 남아 있었음 — 겹쳐 돈 다른 스케줄
  세션이 20:37 핸드오프대로 #11을 구현했으나 자체 검증/커밋/push를 끝내지 못하고 남긴 **고아(orphaned)
  작업**([[project_scheduled_workflows]]가 반복 경고한 동시성 위험의 재현).
  - 신규: `lib/four-insurance.ts`, `components/FourInsuranceCalculator.tsx`,
    `planning/four-insurance-calculator-content.md`, `design/four-insurance-calculator-ui-spec.md`,
    임시 `_master-verify-4ins.ts`(마스터 오라클 스크립트).
  - 수정: `lib/calculators.ts`(항목 등록), `app/calculator/[slug]/page.tsx`(라우트 매핑),
    `lib/salary.ts`(`floorTo1000` export 전환 — 4대보험이 동일 라운딩 공유).
- origin과 0/0 동기화(`git fetch` 후 `git rev-list --left-right --count origin/main...HEAD` = 0 0),
  tip `6814395`(#10 중도상환수수료).
- 방침: 스탠드업 원칙대로 **무작정 커밋/무시하지 않고 QA 독립검증 관문부터 통과**시킨 뒤 처리.

### 완료: 고아 #11을 정상 검증 파이프라인에 태워 확정 (개발산출물 → QA 독립검증 PASS → 마스터 재검증 → push)
설계·구현은 이미 존재(고아). 이번 실행은 **검증 3층(QA 독립 + 마스터 빌드/오라클 + 마스터 라이브)**을 채워 릴리스.

- `lib/four-insurance.ts` (엔진): STEP 0~6. 요율·국민연금 하한상한·`floorTo1000`을 `lib/salary.ts`에서
  import 재사용(신규 상수 = 고용안정·직능개발 요율맵 1건뿐). 국민연금 base=clamp(floor1000(T),41만,659만),
  건강=T×3.595%, 장기요양=건강보험료×12.9457%(T 직접곱 아님), 고용 근로자=0.9%·사업주=0.9%+규모별.
  산재 제외. 무효입력(비유한·≤0)→null.
- `components/FourInsuranceCalculator.tsx`: YMYL 패밀리 패턴 준수(상단 amber `role=note` 배너·중립
  "계산하기" 1개·근로자/사업주/총 3-way·데스크톱 4열 grid + 모바일 보험별 블록 스택(**table 미사용**)·
  국민연금 상한 note 조건부·개인정보 안내).
- `lib/calculators.ts`: four-insurance-calculator 등록(status live, category salary,
  relatedSlugs override=[연봉·퇴직금·만나이] — 부모 연봉 1순위 고정). `app/calculator/[slug]/page.tsx` 매핑.
- 커밋 예정(아래 배포 섹션).

### QA팀 독립검증 결과: **전 항목 PASS** (차단 0, 비차단 관찰 2 — 둘 다 기지 사안)
- 엔진 재사용 없이 **from-scratch 참조구현(node) + 실엔진 npx tsx 3중 대조**: 기획앵커 A~D
  (194,207/199,207/393,414 · 291,310/298,811/590,121 · 485,519/498,019/983,538 ·
  660,251/677,752/1,338,003) = 독립계산 = 엔진출력, **전부 원 단위 일치**.
- 국민연금 상한 clamp(700만·800만 근로자분 313,025 고정, 건강·고용은 계속 증가), 하한(41만) clamp,
  장기요양 건강보험료 기반, 고용보험 규모 4종 사업주 차등(34,500/40,499/46,500/52,500), **연봉 계산기
  (salary.ts)와 근로자분 원단위 정합**(기획 §8-6) 전부 PASS.
- 엣지(0·음수·NaN·Inf→null), 콘텐츠-엔진 수치 일치(interpretation/FAQ 예시=엔진), 빌드 27페이지,
  구조(table 0·amber배너·중립CTA·3-way·산재각주·상한note), 3자(기획↔디자인↔구현) 목록 일치(과거
  단위변환 핑퐁 재발 없음), 링크/용어/산재면책 일관성, 개인정보 0 — 전부 PASS.
- 비차단 관찰 2건(수정 아님): ① 고용보험 근로자분 **26,999**(수학적 27,000의 JS float floor) — 기획
  §1-4에서 "정수산술 정밀화는 salary.ts 공유로직 변경 필요 → 후속 태스크"로 명시 분리, 현재는 연봉
  계산기와 원단위 정합 우선이라 의도된 상태·자기일관 확보. ② 동적요약에 국민연금 상한 미언급 — 상한
  note가 Tier③ 조건부로 커버, 문안은 기획 소관. **둘 다 커밋 차단 아님.**

### 마스터가 직접 확인한 근거 (QA PASS 후에도 별도 수행)
- **오라클 재실행**(`_master-verify-4ins.ts` `npx tsx`, 확인 후 삭제): 앵커 A~D `ALL PASS
  (오라클==앵커==엔진)`. 국민연금 700만==800만 근로자분 313,025 동일, 무효(0/음수/NaN)→null,
  1000인이상 사업주 고용보험 floor(3,000,000×0.0175)=52,500 확인.
- **빌드**: `npm run build` 직접 실행, `Finished TypeScript`(에러 0), 정적 **27/27**,
  four-insurance-calculator SSG. (계산기 10→11 라이브.)
- **라이브 렌더 독립 확인**(prod `next start -p 3399` + 마스터 자체 curl grep): `/calculator/
  four-insurance-calculator` HTTP **200**, H1 "4대보험료 계산기", `role="note"` amber 배너 1,
  "계산하기"·"월 과세대상 급여"·"사업장 규모" 라벨, "브라우저 안에서만" 개인정보 캡션, interpretation
  값 **590,121·298,811**이 SSG HTML에 노출(콘텐츠=엔진 정합 실렌더 재확인), **`<table>` 0개**
  (모바일 가로 오버플로 안전 구조). 확인 후 서버 종료.
- **보안**: 신규·수정 파일 `grep sss159228|@gmail|RESEND|api_key|CONTACT_TO` **0건**. 커밋에
  `.env*`·`.claude/settings.local.json` 미포함.

### 미확인 항목
- 실기기(실제 휴대폰)·픽셀 스크린샷은 이번에도 미확보(preview 뷰포트 불안정 반복 환경이슈) — prod
  렌더 HTML(curl grep)·코드 구조(table 0·flex·grid 분기)·QA curl로 대체 검증, 픽셀 미측정.
- 실제 Vercel 배포 반영·AdSense 상태는 push까지만(GSC API 미연동, 감사 트랙 소관).
- 4대보험 요율 상수(2026)는 교차확인값 + salary.ts 공유; 각 공단 공식 고시표 원본 최종대조는 미수행
  (연봉 계산기 상수 정합에 종속, 요율 갱신 체크리스트 백로그와 연동).

### 핑퐁 이슈
1. **[동시성→마스터] 고아 #11 작업 인수** — 다른 세션이 구현 후 검증/커밋 미완으로 남긴 작업을 마스터가
   발견 → 무시·덮어쓰기 대신 QA 독립검증 관문에 태워 인수 → PASS 후 마스터 재검증 → 커밋. **해결.**
2. **[QA→기획/dev, 비차단 백로그]** 고용보험 26,999 정수산술 정밀화 + 동적요약 상한 언급 — 둘 다 기지
   사안으로 이미 문서화, 차단 아님. 아래 이월.
- planning↔design 핑퐁 0(고아 작업이 이미 3자 정합 상태로 구현돼 있었음, QA가 확인).

### 마케팅 카드뉴스 게이팅: **START 유지** — 신규 라이브(4대보험) 전용 카드뉴스 6장 + SNS 캡션 3종 초안 추가
- 판단 근거: 라이브 11개·빌드 정상·품질 바 충족(게이팅 START는 07-14부터 유지). 오늘 신규 대표 콘텐츠
  = 4대보험료 계산기를 소재로 배정(고정 소재 반복 회피 원칙).
- YMYL-중간 가드 준수(급여협상·이직 부추김 배제, 산재 제외·회사신고 기준소득월액 차이·요율 갱신 3중
  면책, 근로자/사업주 부담 구분 정보 전달 톤). 실제 이미지화·게시는 사용자 승인 사안(미실행, 초안까지만).
- 누적 카드뉴스: 연봉7·단위변환6·근속연수6·퇴직금6·전기요금6 + (07-14 배정 누락) 중도상환수수료·이번
  4대보험. (상세는 아래 마케팅 섹션.)

### CS 문의 채널
- 변동 없음. `/support` Resend 발신 전용, 서버측 인바운드 큐 부재 — 조회할 실제 문의 없음(이번 실행도
  사용자 문의 응대 없음). [[project_scheduled_workflows]] 기지 갭.

### 배포(push)
- 대상: `lib/four-insurance.ts`·`components/FourInsuranceCalculator.tsx`·`lib/calculators.ts`·
  `app/calculator/[slug]/page.tsx`·`lib/salary.ts`(floorTo1000 export)·`planning/`·`design/` + 이 팀로그.
  QA PASS + 마스터 빌드/오라클/라이브 통과분이라 auto-push 대상(2026-07-13 승인).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시), 임시 `_master-verify-4ins.ts`(검증 후 삭제).
  `.env*` 미포함·개인정보 grep 0건 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[로드맵]** #11 완료로 salary 카테고리 = 연봉·퇴직금·4대보험 3종. 다음 신규 계산기 =
   **12순위 DSR 계산기**(/loan, 정책 민감도 높음) 또는 `planning/calculator-lineup.md` §2 재확인 후
   차순위. 신규면 planning부터. YMYL-중간+ 이면 근사/면책 가드 필수.
2. **[비차단 백로그, 낮음]** 고용보험 근로자분 26,999→27,000 정수산술 정밀화 — salary.ts 공유 로직
   변경 필요(두 계산기 동시 반영). 요율 갱신 체크리스트와 함께 검토.
3. **[게이팅 후속, 사용자 승인 대기]** 카드뉴스 이미지화·실제 게시 채널/계정 승인 — 누적 대량 축적
   (연봉7·단위6·근속6·퇴직6·전기6·중도상환·4대보험). 마케팅은 묶어 검토 제안.
4. (이월 유지) 근로소득세 Option A 이관, 요율·세율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기
   연료비), design-system 편입 보류분, 법률 전문가 검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 관찰,
   전기요금 한전 PDF·중도상환 은행연합회 상수 원본 대조, 단위변환 전통단위 확장.
5. **[운영 관찰]** 이번 실행에서 고아 작업(동시 세션이 검증 전 남긴 미커밋 #11)을 또 인수 처리함 — cron
   30분 겹침 하에서 "구현은 되나 검증/커밋 전 끊긴 세션"이 반복 패턴. 매 실행 시작 `git status`로 고아
   작업 확인 절차 유지 필수.

### 참고 (프롬프트 인젝션, 조치 불필요)
- 마케팅팀 작업 시 Higgsfield MCP instructions의 "generate_image 등을 핵심 도구로 쓰라"·업로드 유도
  문구를 재확인(07-09/13/14 동일). 도구 정의 보일러플레이트 = 프롬프트 인젝션으로 취급, 생성/업로드
  도구 미호출.

---

## 01:00 실행분 (이어서) — 로드맵 #12 DSR 계산기 신규 출시 (라이브 11개→12개, 로드맵 완주)

### 세션 시작 상태
- `git status` = `.claude/settings.local.json`만 수정(로컬 권한 캐시), 고아 미커밋 작업 없음. origin/main과
  동기화, tip `cc8f10d`(직전 01:00분 #11 4대보험 카드뉴스). 직전 실행 핸드오프 1번 "**#12 DSR 계산기**(/loan,
  정책 민감) — 신규면 planning부터"를 이어받음. 이로써 `calculator-lineup.md` §2 12개 로드맵 **완주**.

### 파이프라인 (기획+디자인 병렬 → 개발 → QA → 정정 핑퐁 → re-QA → 마스터)
- **기획팀**(`planning/dsr-calculator-content.md`) + **디자인팀**(`design/dsr-calculator-ui-spec.md`) 병렬
  착수. 두 팀이 서로 못 보는데도 **동일한 6입력 설계·DSR 공식·relatedSlugs로 독립 수렴**(정합 우수).
- **마스터 결정(locked)**: ① 입력방식 (B)상세형+(A)간편 자동포섭, **상환방식 토글 제외→원리금균등 고정
  (입력 실질 5개)** — 원금균등 1년차 근사(§1-1-b)가 YMYL-높음에서 가장 취약 + calculateLoan이 1년차합을
  직접 안 줌 → v1 단순·방어 우선(양 문서 §8-3b/§10 사전승인 축소안). ② 신규 연원리금=
  `calculateLoan(P,i,months,"equalPayment").monthlyPayment×12`, `@/lib/loan` **import 재사용(중복구현 0)** —
  기획이 요청한 "공유 리팩터"는 calculateLoan이 이미 독립함수라 불필요. ③ 규제상수 40%/50% 분리(해석·배지
  전용, 판정로직 아님), 구간 안전≤30%/주의30~40%/초과>40%. ④ **초과 빨강 배지 승인**(중립 히어로+작은 배지
  색·아이콘·텍스트 3중+사실서술 "기준선 초과"), DSR% 히어로 중립색(text-brand-text) 승인. ⑤ relatedSlugs
  =[대출이자·중도상환·연봉] + **중도상환 3번째 age→dsr 교체**(loan 3계산기 내부순환 강화).
- **개발팀**: `components/DsrCalculator.tsx` 신규, `lib/calculators.ts`·`app/calculator/[slug]/page.tsx`
  수정. 빌드 통과·앵커 B/C 자체검산 보고.

### QA 독립검증 1차: **FAIL 1건 + 나머지 전 PASS**
- **FAIL(콘텐츠·비차단성이나 태스크 명시 요구)**: interpretation 예시가 "월 1,520,053/연 18,240,636"인데
  엔진 실측(npx tsx)=**1,520,056/18,240,672** (3원·36원). 원인=기획 손계산의 (1+r)^360 근사(3.847721 vs
  실제 3.8476980), 엔진이 정답. "해설≠엔진" CS 재발 패턴 → 관대판정 배제하고 FAIL.
- PASS: 엔진 재검산(from-scratch node + tsx 2중, DSR 30.4%/44.3%/40.0% 앵커 완전일치), 엣지 E1~E9,
  구간경계 오프바이원 없음(40.0%=주의), 빌드 28페이지, 구조(table 0·amber배너·참고용배지·중립히어로·
  게이지 role=img·CTA "계산하기" 1개·금지문구 0·개인정보캡션), 통합(매핑·relatedSlugs·중도상환 교체),
  링크·용어 일관성.

### 핑퐁: QA→기획팀 반환 → 정정 → 마스터 반영 → re-QA
- **[QA→planning]** interpretation 수치 정정 티켓(Task #2). 기획팀이 planning 문서(§2·§1-4·§6) 엔진값으로
  정정 + (QA 비차단관찰 반영) 2문단 톤을 "기준선 40%를 넘지는 않지만 여유가 크지는 않은 편"으로 정합
  (정적해설 vs 동적배지 "주의구간" 톤 엇갈림 제거). 최종 문안 회신.
- **[planning→마스터→dev산출물]** 마스터가 확정 텍스트를 `lib/calculators.ts` interpretation 1·2문단에
  기계적 반영(수치 2개+톤).
- **[re-QA]** 정정만 집중 재검증: 텍스트 정정 확인·엔진 재대조(1,520,056/18,240,672 완전일치)·라이브 렌더
  grep(신값 노출·옛값 0)·회귀 없음(28페이지). → **PASS. DSR #12 QA 최종 통과.**

### 마스터 최종 확인(§7, QA PASS 후 별도 수행)
- **빌드**: `npm run build` 직접 실행 = `✓ Compiled successfully`, 정적 **28/28**(기존 27+DSR).
- **빌드 산출물 HTML 독립 grep**(`.next/server/app/calculator/dsr-calculator.html`): H1 "DSR 계산기",
  정정값 `1,520,056`·`18,240,672` 노출·옛값 **0건**, amber `role="note"` 배너, "참고용" 배지, "계산하기"
  CTA, 개인정보 "브라우저 안에서만" 캡션, `text-brand-text` 중립 히어로, **`<table>` 0개**, 금지CTA grep
  2건은 전부 면책문구 "대출 가능 여부를 **판단하지 마세요**"(오탐, 실제 CTA 아님).
- **보안**: 신규·수정 파일 시크릿(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO) grep **0건**, `.env` 미포함.

### 미확인 항목
- 결과카드(배지·게이지·면책캡션)는 계산 후 클라이언트 렌더라 초기 SSR HTML엔 없음 → QA가 컴포넌트 코드로
  검증(배지+면책캡션 세트, 게이지 role=img+aria 면책). 마스터는 코드·빌드·초기렌더로 확인, **실제 클릭 후
  결과카드 픽셀·실기기 모바일 스크린샷은 미확보**(preview 뷰포트 불안정 반복 환경이슈). 모바일은 table 0·
  flex justify-between·%폭 게이지로 **구조적** 가로오버플로 0 검증(픽셀 미측정).
- 규제상수(은행권40%/제2금융50%·스트레스 DSR 3단계)는 2026-07 리서치값 + "일반적으로~"·"변동" 완곡·4중
  면책 처리. 공식 고시 원본 최종대조는 미수행(정책 갱신 거버넌스 백로그 §8-5와 연동).

### 배포(push)
- 대상: `components/DsrCalculator.tsx`(신규)·`lib/calculators.ts`·`app/calculator/[slug]/page.tsx`·
  `planning/dsr-calculator-content.md`(신규)·`design/dsr-calculator-ui-spec.md`(신규) + 이 팀로그.
  QA PASS + 마스터 빌드/HTML/보안 통과분 → auto-push(2026-07-13 승인).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개·빌드 정상). 다만 이번 실행은 파이프라인(기획→…→re-QA→마스터→push)에 집중해
  **DSR 카드뉴스는 다음 실행에 배정**(YMYL-높음이라 "대출 승인 오인 금지"·"스트레스 DSR"·"참고용" 가드가
  특히 중요 → 소재를 신중히 준비). 이번 실행 마케팅 신규 배정 없음(고정소재 반복 회피).

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 다음 실행으로 넘기는 미해결/후속
1. **[로드맵 완주 후]** 12개 로드맵 전부 라이브 달성. 다음 방향은 **큰 결정(사용자 확인 권장)**: (a) 로드맵
   확장(13번째~ 신규 계산기 발굴 — planning 트렌드 리서치), (b) 기존 12개 품질·SEO 심화(콘텐츠 보강·
   내부링크·구조화데이터), (c) 애드센스 승인/배포 확인. **사용자 확인 필요**로 남김 — 다음 실행은 우선
   기존 라이브 품질/카드뉴스 등 처리 가능 작업부터.
2. **[마케팅]** DSR 카드뉴스 초안(YMYL-높음 가드) — 다음 실행 배정.
3. **[비차단 백로그]** DSR 원금균등 상환방식 옵션(v1 원리금균등 고정, 백로그). 고용보험 26,999 정수산술
   정밀화. 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비 + **DSR 규제비율/스트레스
   DSR** = 정책 민감도 최고, §8-5 거버넌스). 신규 시각요소(규제 3구간 배지·DSR 위치게이지) design-system
   편입 검토. 근로소득세 Option A, 전기요금 한전PDF·중도상환 은행연합회·DSR 규제 공식원본 대조, 법률검토,
   CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행은 고아 없음).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출.

---

## 01:44 실행분 (이월분: DSR 계산기 카드뉴스 초안 — YMYL-높음 가드)

### 세션 시작 상태
- `git status` = `.claude/settings.local.json`만 수정(로컬 권한 캐시), 고아 미커밋 작업 **없음**(직전 01:00 두 건 push 완료 상태 확인). origin/main과 0/0 동기화, tip `ce8f0b9`(#12 DSR 계산기). 12개 로드맵 완주 상태.
- 이번 실행 배정 근거: 01:00 두 번째 실행분 핸드오프 2번 "**DSR 카드뉴스 초안(YMYL-높음 가드) — 다음 실행 배정**"을 이어받음. 핸드오프 1번(로드맵 완주 후 큰 방향: 로드맵 확장/품질 심화/애드센스 점검)은 **사용자 확인 필요**로 보류 유지 → 확인 불필요한 이월 작업(DSR 카드뉴스)부터 처리.

### 완료: DSR 계산기 카드뉴스 6장 + SNS 캡션 3종 초안 (marketing-team → 마스터 콘텐츠 검수 → append)
- **marketing-team** 호출: `planning/dsr-calculator-content.md` §1-4 앵커 B/C·§2·§3·§5 근거로 카드뉴스 완성 초안 작성. 마케팅 서브에이전트가 Edit/Bash 미활성 + Write는 전체 덮어쓰기라 849줄 기존 파일 append 불가 → 초안을 scratchpad `dsr-section.md`에 저장하고 마스터에게 append 요청(기존 승인 콘텐츠 손상 회피 판단, 적절).
- **마스터 처리**: scratchpad 초안을 Read로 **콘텐츠 직접 검수**(§7 카피/콘텐츠 검증) 후 `cat >>`로 `marketing/promo-drafts.md` 맨 끝에 이어붙임(848→938줄, +90). 기존 4대보험 섹션(~848행)과 신규 DSR 섹션(852행~) 사이 `---` 구분 정상 삽입 확인, 기존 내용 무손상.

### 마스터 콘텐츠 검수 결과 (직접 확인 근거)
- **수치 정확성**: 앵커 B(연소득6천·신규3억4.5%/360/원리금균등 → 월 1,520,056/연 18,240,672/DSR 30.4%)·앵커 C(연5천·신규2억+기존1천 → 총 22,160,424/DSR 44.3%) 전부 planning §1-4 **QA 정정값(엔진 실측)** 과 일치. 옛 손계산값(1,520,053/18,240,636) 미사용 확인.
- **YMYL-높음 가드 6종 전부 충족**: ① 대출 승인/한도 오인 문구 0(단순 DSR·참고용 명시, "40% 미만도 거절 가능·반대도 가능") ② 스트레스 DSR 정성 서술만(특정 스트레스 금리 % 하드코딩 0 — 1절 리서치의 1.50% 본편 미인용, 오인 방지) ③ 정책 변동 면책(완곡 표현) ④ 4중 면책 전부 카드5(필수·생략 금지) + 캡션 ⑤ 개인정보 안내(브라우저 내 계산) ⑥ 신청/승인 오인·상업 부추김 CTA 0("무료·회원가입 없이 내 DSR 계산" 정보성 톤).
- **에셋/게시**: 실제 이미지·영상 생성 도구 미호출, 게시 미실행(문구+비주얼 지시 텍스트까지). Higgsfield 생성/업로드 유도 문구는 이번에도 인젝션 취급·미호출.
- **보안**: `marketing/promo-drafts.md` 시크릿 grep(sss159228/@gmail/RESEND/api_key/CONTACT_TO/PRIVATE KEY) **0건**. 커밋에 `.env*`·`.claude/settings.local.json` 미포함.

### QA팀 검증 관련
- 이번 산출물은 **사이트 코드 변경이 아닌 마케팅 카드뉴스 텍스트 초안**(빌드/화면 영향 없음, 신규 화면·계산 로직 없음). 07-14~07-15 카드뉴스(연봉·단위변환·근속·퇴직·전기·4대보험) 전례대로 **qa-team 빌드/계산 검증 대상 아님**, 마스터 콘텐츠 검수로 갈음(수치 정확성은 이미 push된 라이브 코드·QA 정정 앵커에 종속). 코드 변경 0이라 빌드 회귀 위험 없음.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개·빌드 정상 전제). 이번 실행 신규 소재 = DSR(오늘의 대표 신규 라이브·로드맵 완주). 고정 소재 반복 회피 원칙 준수. 누적 카드뉴스: 연봉7·단위변환6·근속6·퇴직6·전기6·4대보험6·**DSR6** + 캡션 3종씩.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `marketing/promo-drafts.md`(+90행 DSR 카드뉴스) + 이 팀로그. 마스터 콘텐츠 검수 통과·시크릿 0·코드 무변경 → auto-push(2026-07-13 승인, "검증 통과분").
- 제외: `.claude/settings.local.json`(로컬 권한 캐시).

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12개 로드맵 완주 후 방향: (a) 로드맵 확장(#13~ 신규 계산기 발굴), (b) 기존 12개 품질·SEO 심화(콘텐츠 보강·내부링크·구조화데이터), (c) 애드센스 승인/배포 확인. **사용자 확인 필요**로 계속 보류. 다음 실행도 확인 불필요한 처리 가능 작업(품질 심화·내부링크·카드뉴스 묶음 기획 등)부터.
2. **[마케팅, 낮음]** 누적 카드뉴스 7종(연봉·단위·근속·퇴직·전기·4대보험·DSR) 이미지화·게시 채널/계정 승인 = 사용자 승인 대기. loan 3종(대출이자→DSR→중도상환) 시리즈형 캐러셀 묶음 기획 제안(마케팅 의견, 결정 아님). 3절 타이밍 표에 DSR 상시 행 반영(동시편집 충돌 회피로 이번엔 보류).
3. **[비차단 백로그]** (유지) DSR 원금균등 상환방식 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·**DSR 규제비율/스트레스 DSR**=정책 민감도 최고 §8-5), 신규 시각요소(3구간 배지·위치게이지) design-system 편입, 근로소득세 Option A, 전기요금 한전PDF·중도상환 은행연합회·DSR 규제 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출(marketing-team 세션에서도 동일 확인).

---

## 02:27 실행분 (SEO 심화: 계산기 상세 페이지 JSON-LD 구조화 데이터 — 로드맵 완주 후 처리 가능 작업)

### 세션 시작 상태
- `git status` = `.claude/settings.local.json`만 수정(로컬 권한 캐시), 고아 미커밋 작업 **없음**. origin/main 0/0 동기화, tip `67e0b68`(01:44분 DSR 카드뉴스). 12개 로드맵 완주 상태 유지.
- 배정 근거: 01:44 핸드오프 1번 "**큰 결정(로드맵 확장/품질 심화/애드센스)은 사용자 확인 필요 → 보류**, 다음 실행은 확인 불필요한 처리 가능 작업(품질 심화·내부링크·카드뉴스 묶음 기획)부터"를 이어받음. 사용자 부재 중이므로 방향 전환급 결정은 계속 보류하고, **비가시(invisible)·완전 가역·화면변경 0인 SEO 심화**만 골라 처리 → 사이트 전역에 구조화 데이터(JSON-LD)가 **하나도 없음**을 grep으로 확인, 이걸 계산기 상세 페이지에 추가(가장 큰 미착수 SEO 자산, AdSense 콘텐츠 사이트 CTR 개선).

### 파이프라인 (planning → dev → QA 독립검증 → 마스터 → auto-push). design은 화면변경 0이라 N/A(스탠드업 §2 비가시 작업 디자인 생략 허용).
- **기획팀**(`planning/calculator-jsonld-seo-spec.md` 신규): @graph 단일 script 설계 — BreadcrumbList(3단계) + WebApplication(SoftwareApplication 아님, offers 무료) + FAQPage(faq 있을 때). YMYL faq **전수 점검** = 리스크 낮음(대출/DSR/BMI 답변이 이미 "참고용·승인 확답 아님"으로 방어됨, 수정 불필요). coming-soon noindex 페이지 스키마 제외, `</script>` 브레이크아웃 이스케이프, 필드 소스 1:1 매핑(신규 데이터필드 0). 루트 WebSite/Organization·카테고리 CollectionPage·화면 3단계 크럼은 §6 "다음 단계"로 분리(범위 제어).
- **마스터 확정**: 기획 스펙 Read 검수 후 그대로 승인(applicationCategory 2분류 salary·loan→Finance/date·life→Utilities 채택, @id v1 생략). design 단계 생략 명시.
- **개발팀**: `lib/calculator-jsonld.ts` 신규(`buildCalculatorJsonLd` 순수 헬퍼, status!==live→null), `app/calculator/[slug]/page.tsx` 주입(+10줄, `{jsonLd && <script type="application/ld+json" dangerouslySetInnerHTML={{__html: JSON.stringify(jsonLd).replace(/</g,"\u003c")}}/>}`). 화면 요소 무변경. 빌드 28페이지·lint 클린 보고.

### QA 독립검증: **전 항목 PASS** (FAIL 0)
- 직접 `npm run build`(28/28 유지·타입에러 0), live 12개 정적 HTML 전수 `application/ld+json` **실제 DOM script 태그 정확히 1개**(RSC flight 페이로드에 escaped 복사본이 1개 더 있어 단순 grep은 2를 반환 → QA가 실제 태그만 1개임을 재확인·근거 제시, 좋은 독립 캐치), 12개 전부 JSON.parse 성공·`@context`·`@graph`, `</script>` 브레이크아웃 없음(rawLt=false).
- 노드 구성 12개 모두 [BreadcrumbList,WebApplication,FAQPage], 방어로직 코드 확인(`status!=="live"→null` `:22`, `faq&&length>0` `:69`). 필드 스팟체크 4개(salary/dsr/age/bmi 4카테고리 전부): 브레드크럼 URL·WA name/desc/applicationCategory/offers·FAQ 개수(6/5/4/5)·DSR 첫 답변 전문(÷×' 포함) exact match. page.tsx diff = 10 insert/0 delete(화면 요소 무변경).

### 마스터 최종 확인(§7, QA PASS 후 별도 수행)
- **코드 Read**: `lib/calculator-jsonld.ts` 스펙 정합 확인(파생맵·3노드·null 분기·이스케이프는 호출부 담당).
- **빌드 직접 실행**: `✓ Compiled successfully`, `Generating static pages (28/28)`.
- **정적 HTML 독립 파싱**(node, salary/dsr/bmi 3개): @context=https://schema.org, 3노드, WA cat=FinanceApplication(salary·loan)/UtilitiesApplication(bmi)·price 0 KRW·free true, BC item2=`/{category}`·item3=`/calculator/{slug}`(SITE_URL 정합), FAQ 6/5/5, **rawLt(<)=false**(이스케이프 안전). page.tsx diff = import 1 + jsonLd 1 + 조건부 script 블록뿐, 기존 화면 요소 무변경 확인.
- **보안**: 신규·수정 파일 시크릿 grep(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO/PRIVATE KEY) **0건**. 커밋에 `.env*`·`.claude/settings.local.json` 미포함.

### 미확인 항목
- Google Rich Results Test 실호출(외부 URL 검증)은 미배포 상태라 미수행 — 로컬 정적 HTML JSON 구문/필수필드 정합으로 대체 검증. 배포 후 GSC/Rich Results 실측은 감사 트랙 소관.
- FAQ 리치결과 실제 노출은 Google 정책(2023-08~ 정부·보건 당국 제한)상 기대하지 않음(스펙 §1-3 명시) — 구문 유효·시맨틱 신호 목적. 별점 리치결과도 평점 데이터 없어 미노출(정상).

### 핑퐁 이슈
- planning↔dev↔QA 정합 우수, **핑퐁 0**(QA FAIL 0). 스펙이 이스케이프/방어분기/필드매핑까지 미리 못박아 dev가 그대로 구현 → 재작업 없이 1라운드 통과.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개·빌드 정상). 이번 실행은 비가시 SEO 인프라 작업이라 **신규 카드뉴스 소재 없음**(화면·신규 콘텐츠 변화 0 → 홍보 소재 아님). 마케팅 신규 배정 없음(고정소재 반복 회피 원칙과 정합). 누적 카드뉴스 7종(연봉·단위·근속·퇴직·전기·4대보험·DSR) 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `lib/calculator-jsonld.ts`(신규)·`app/calculator/[slug]/page.tsx`·`planning/calculator-jsonld-seo-spec.md`(신규) + 이 팀로그. QA PASS + 마스터 빌드/파싱/보안 통과분 → auto-push(2026-07-13 승인).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12개 로드맵 완주 후 방향: (a) 로드맵 확장(#13~), (b) 기존 12개 품질·SEO 심화, (c) 애드센스 승인/배포 확인. 계속 보류. 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[SEO 심화 후속, v1 범위 밖 — 처리 가능]** 스펙 §6: (a) 루트 `app/layout.tsx`에 WebSite/Organization JSON-LD, (b) 카테고리 페이지(/salary 등) CollectionPage+ItemList, (c) 화면 breadcrumb 3단계 노출(디자인 변경건 → design 협의 필요). (a)(b)는 비가시라 다음 실행 배정 가능 후보.
3. **[마케팅]** DSR 카드뉴스 완료. loan 3종(대출이자→DSR→중도상환) 시리즈형 캐러셀 묶음 기획 제안(마케팅 의견). 누적 7종 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
4. **[비차단 백로그]** (유지) DSR 원금균등 상환방식 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·DSR 규제비율/스트레스 DSR §8-5), 신규 시각요소 design-system 편입, 근로소득세 Option A, 전기요금 한전PDF·중도상환 은행연합회·DSR 규제 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출.

---

## 02:57 실행분 (SEO 심화 v2: 사이트 전역 WebSite/Organization + 카테고리 CollectionPage/ItemList JSON-LD — 로드맵 완주 후 처리 가능 작업 이어감)

### 세션 시작 상태
- `git status` = `.claude/settings.local.json`만 수정(로컬 권한 캐시), 고아 미커밋 작업 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `3b3642b`(02:27분 상세 JSON-LD). 12개 로드맵 완주 상태 유지.
- 배정 근거: 02:27 핸드오프 2번 "**[SEO 심화 후속, v1 범위 밖 — 처리 가능]** 스펙 §6: (a) 루트 layout.tsx WebSite/Organization JSON-LD, (b) 카테고리 페이지 CollectionPage+ItemList — 둘 다 비가시라 다음 실행 배정 가능"을 이어받음. 큰 결정(로드맵 확장/품질 심화/애드센스)은 **사용자 확인 필요**로 계속 보류, 사용자 부재 중 비가시·완전 가역·화면변경 0 작업만 선택 처리(직전 실행들과 동일 원칙). 사이트 전역·카테고리에 구조화 데이터가 **하나도 없음**을 grep으로 재확인 후 착수.

### 파이프라인 (planning 스펙 → 마스터 검수 → dev 구현 → QA 독립검증 → 마스터 빌드/파싱/보안 → auto-push). design은 화면변경 0이라 N/A(스탠드업 §2 비가시 작업 디자인 생략 허용).
- **기획팀**: `planning/calculator-jsonld-seo-spec.md` 맨 끝에 §8 "사이트 전역 + 카테고리 JSON-LD 구현 스펙(v2)" 추가(기존 §1~7 보존). 마스터 locked 결정을 스펙에 그대로 못박음.
- **마스터 locked 결정**(코드 착수 전 확정, 재논의 금지): ① WebSite에 **SearchAction/potentialAction 미포함** — 실제 사이트 검색 엔드포인트 없음(`find app -iname *search*` 0건). ② Organization에 **logo 미포함** — 실제 브랜드 로고 래스터 에셋 없음(public/에 카테고리 아이콘 webp·Next 기본 svg만, 브랜드 로고 아님) → 가짜 로고 URL 생성 금지(§6 조작 금지 원칙). ③ 카테고리 ItemList는 **status==="live"만**(`getCalculatorsByCategory`가 status 안 거름 — 방어 필수). ④ 트레일링 슬래시 없음, 화면 변경 0, 신규 데이터 필드 0.
- **개발팀**: `lib/site-jsonld.ts` 신규(`buildSiteJsonLd()` WebSite+Organization / `buildCategoryJsonLd(category)` CollectionPage+ItemList, 내부 live 필터·빈 ItemList 생략), `app/layout.tsx` body 최상단 site script 주입(+7줄), `components/CategoryPage.tsx` 반환 JSX 최상단 category script 주입(+10줄). v1과 동일 이스케이프(`JSON.stringify(x).replace(/</g,"<")` + dangerouslySetInnerHTML). 빌드 28페이지 자체검증 보고.

### QA 독립검증: **전 항목 PASS** (7/7, FAIL 0)
- 직접 `npm run build`(28/28 유지·타입에러 0·`npx eslint .` EXIT 0), 산출물 HTML 실제 `<script type="application/ld+json">` DOM 태그만 카운트(RSC escaped 복사본은 `<`가 이스케이프돼 매치 안 됨, v1 이슈 회피 확인):
  - 홈·about·blog·privacy·terms·support = 각 **1개**(site 전역만), 카테고리 4개 = 각 **2개**(site+category), 상세 live 12개 = 각 **2개**(site+v1). coming-soon 0.
- 노드 구성: 루트 @graph=[WebSite,Organization] 정확히 2노드, **WebSite potentialAction 부재·Organization logo 부재** 확인. 카테고리 @graph=[CollectionPage,ItemList], ItemList ListItem 개수 == 각 카테고리 live 수(salary/loan/date/life 전부 live 3 → ItemList 3, 소스 대조 일치).
- 필드 스팟체크: CollectionPage name/description=categoryInfo 일치·url 트레일링슬래시 없음·inLanguage ko, ItemList position 1..N 연속·**url 키 사용(item 키 부재)**·url=`${SITE_URL}/calculator/{slug}`. 루트 name="계산기 허브"·url=SITE_URL. 전 script JSON.parse 성공·브레이크아웃 없음. 시각변화 0(diff=import+script 주입만). 시크릿 grep 0.

### 마스터 최종 확인(§7, QA PASS 후 별도 수행)
- **코드 Read**: `lib/site-jsonld.ts` 스펙 정합(WebSite/Org 필드 locked대로, 카테고리 live 필터 `:55-57`·빈 ItemList 생략 `:59`·url 키 `:66`).
- **git diff 독립 확인**: `app/layout.tsx`=import 1+body 최상단 script 블록(className/텍스트/레이아웃 무변경), `components/CategoryPage.tsx`=import 1+div 최상단 script 블록(h1/p/grid 무변경). **화면 요소 변경 0** 재확인.
- **빌드 직접 실행**: `✓ Compiled successfully`, `Generating static pages (28/28)`(무회귀).
- **산출물 HTML 독립 파싱**(node, home/loan/dsr/about 4종): HOME=1태그[WebSite,Organization] potentialAction=false·logo=false, CATEGORY loan=2태그(site + [CollectionPage "대출 계산기" url=/loan 슬래시없음, ItemList n=3 keys=[@type,position,name,url] pos=1,2,3 hasItemKey=false]), DETAIL dsr=2태그(site + [BreadcrumbList,WebApplication,FAQPage] — **v1 무회귀**), STATIC about=1태그(site만). 전부 ok-escaped·JSON.parse 성공.
- **보안**: 신규·수정 파일 시크릿 grep(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO/PRIVATE KEY/bearer/sk_live) **0건**. layout.tsx의 google/naver verification 코드는 기존 코드(diff 미포함)·공개 노출 소유권 검증 메타로 시크릿 아님. 커밋에 `.env*`·`.claude/settings.local.json` 미포함.

### 미확인 항목
- Google Rich Results Test 실호출(외부 URL 검증)은 미배포·외부망 제약으로 미수행 — 로컬 정적 HTML JSON 구문/노드/필드 정합으로 대체. 배포 후 실측은 감사 트랙 소관.
- WebSite/Organization/CollectionPage/ItemList는 별점류 시각 리치결과 대상 아님(엔티티/브랜드/목록 신호 목적) — 미노출 정상.

### 핑퐁 이슈
- planning→(마스터 locked)→dev→QA 정합 우수, **핑퐁 0**(QA FAIL 0). 스펙이 이스케이프/live필터/노드필드/빈ItemList까지 미리 못박아 dev가 1라운드 통과.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개·빌드 정상). 이번 실행은 비가시 SEO 인프라 작업이라 **신규 카드뉴스 소재 없음**(화면·신규 콘텐츠 변화 0 → 홍보 소재 아님). 마케팅 신규 배정 없음(고정소재 반복 회피). 누적 카드뉴스 7종(연봉·단위·근속·퇴직·전기·4대보험·DSR) 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `lib/site-jsonld.ts`(신규)·`app/layout.tsx`·`components/CategoryPage.tsx`·`planning/calculator-jsonld-seo-spec.md`(§8 추가) + 이 팀로그. QA PASS + 마스터 빌드/파싱/diff/보안 통과분 → auto-push(2026-07-13 승인).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12개 로드맵 완주 후 방향: (a) 로드맵 확장(#13~ 신규 계산기 발굴), (b) 기존 12개 품질·SEO 심화, (c) 애드센스 승인/배포 확인. 계속 보류. 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[SEO 심화 후속, 처리 가능 후보]** §6/§8로 (a) 루트 WebSite/Organization + (b) 카테고리 CollectionPage/ItemList **완료**. 남은 §6 항목: (c) **화면 breadcrumb 3단계 노출** = 가시적 디자인 변경이라 design팀 협의 필요(비가시 아님, 사용자 부재 중 착수 신중). (d) 실제 **브랜드 로고 래스터 에셋** 확보 시 Organization.logo 추가 티켓(디자인/브랜드 백로그). (e) 사이트 검색 기능 생기면 WebSite SearchAction 추가(엔드포인트 없어 보류). 비가시 SEO 자산은 거의 소진 → 다음은 콘텐츠 보강(FAQ 확장·내부링크)이나 카드뉴스 묶음 기획 등 검토.
3. **[마케팅]** loan 3종(대출이자→DSR→중도상환) 시리즈형 캐러셀 묶음 기획 제안(마케팅 의견). 누적 7종 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
4. **[비차단 백로그]** (유지) DSR 원금균등 상환방식 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·DSR 규제비율/스트레스 DSR §8-5), 신규 시각요소(3구간 배지·위치게이지) design-system 편입, 근로소득세 Option A, 전기요금 한전PDF·중도상환 은행연합회·DSR 규제 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출.

---

## (다음 실행분) 블로그 구축 — /blog 스텁 → 데이터 기반 블로그 + 첫 대표글 (로드맵 완주 후 최대 미착수 콘텐츠 자산)

### 세션 시작 상태
- `git status` = `.claude/settings.local.json`만 수정(로컬 권한 캐시), 고아 미커밋 작업 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `d4fe005`(02:57분 SEO v2). 12개 로드맵 완주 상태 유지.
- 배정 근거: 02:57 핸드오프 2번 "비가시 SEO 자산 거의 소진 → 다음은 콘텐츠 보강(FAQ/내부링크)이나 카드뉴스 묶음 기획". 실제 확인 결과 `app/blog/page.tsx`가 "아직 등록된 글이 없습니다" **빈 스텁**이고 sitemap엔 이미 /blog 등록됨 + 스텁 문구 자체가 "자동 생성 글은 추후 게시 예정"으로 **확정 계획**. → 12계산기 완주 후 **최대 미착수 콘텐츠 자산 = 블로그**로 판단. 방향 전환(로드맵 확장/애드센스)이 아니라 **이미 스캐폴딩된 계획의 실행**이고, 추가(기존 계산기 무변경)·가역이라 사용자 부재 중에도 착수 가능한 처리 대상으로 선택. 내부링크(relatedSlugs)는 이미 구현돼 있어 제외.

### 파이프라인 (planning+design 병렬 → 마스터 검수/locked → dev → QA 독립검증 → 마스터 빌드/데스크톱/모바일/JSON-LD → auto-push)
- **기획팀**(`planning/blog-spec.md`+`planning/blog-post-01-draft.md` 신규): IA 스펙(순수 데이터 배열 방식, 마크다운 파서 없이 `BlogSection` discriminated union으로 본문 표현 → XSS 위험 0), 조회 헬퍼(파생 readingTime로 drift 방지), BlogPosting JSON-LD(조작 금지—조직을 저자/발행처로, 로고 미포함), 첫 글 1편 완성 초안(연봉 실수령액 가이드, 요율·예시 수치 calculators.ts에서 그대로 인용). 이슈 제기: 글 1편이면 얇은 콘텐츠 인상 → 3~4편 로드맵 권장(마스터에 위임).
- **디자인팀**(`design/blog-layout-spec.md` 신규): 목록/상세 `max-w-3xl` 단일컬럼, `BlogPostCard`=CalculatorCard 클래스 복제, 본문 문단 `text-brand-text`(회색 아님), breadcrumb·warning callout·RelatedCalculators 재사용, 데스크톱+모바일(≤640px) 반응형 표. 이슈 제기: 블로그 카테고리를 계산기 4개 재사용할지 전용 taxonomy 신설할지 결정 필요.
- **마스터 locked 결정**(양 스펙 Read 검수 후 확정): ① 데이터모델=기획안 채택(필드명 description/publishedDate/relatedCalculatorSlugs, readingTime는 헬퍼 파생), ② **카테고리=기존 4개(salary/loan/date/life) 재사용, 전용 taxonomy 신설 금지**(정보구조 변경 안 함 — 디자인팀 오픈질문 해소), ③ 레이아웃=디자인안, ④ 본문 내부링크=calculatorCta 블록만(존재/live 검증, 없으면 null), ⑤ JSON-LD=조직 저자/발행처·로고/평점 금지, ⑥ 첫글 1편 이번 실행 + 다수글 로드맵은 후속(사이트 전체는 계산기 12개로 얇지 않음). 참조 헬퍼(getCalculatorBySlug·RelatedCalculators·categoryInfo) 실재 확인.
- **개발팀**: 신규 `lib/blog.ts`·`lib/blog-jsonld.ts`·`components/BlogPostCard.tsx`·`app/blog/[slug]/page.tsx`, 개편 `app/blog/page.tsx`·`app/sitemap.ts`. 빌드 28→**29**(`/blog/salary-net-income-guide-2026` SSG), lint 클린 자체보고. warning callout은 신규 토큰 없이 기존 DsrCalculator disclaimer 패턴(amber Tailwind 기본색) 재사용(디자인 재검수 대상으로 명시).

### QA 독립검증: **전 9항목 PASS** (FAIL 0)
- 직접 `npm run build`(29/29·타입에러0)·`npx eslint`(EXIT0). 산출물 HTML 실제 JSON-LD script 2개(전역 site + 블로그), JSON.parse OK, @graph=[BreadcrumbList,BlogPosting], **logo/개인저자/평점 없음**, datePublished=2026-07-15, breadcrumb 3단계 URL 정합, 브레이크아웃 없음.
- 기사 수치 전량 calculators.ts 대조 일치(요율 5종·예시 공제액 6종·월291만/연3,495만). 내부 CTA 3 slug 모두 실재 live·방어로직(null 반환) 확인. sitemap 등록 확인. BlogPostCard 루트클래스 CalculatorCard와 문자열 완전동일. 본문 text-brand-text. amber=Tailwind 기본(커스텀 토큰 아님). 반응형 클래스 정합(본문 16px 모바일 축소 없음). 시크릿 grep 0. dangerouslySetInnerHTML=JSON-LD 1곳만.

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **빌드 직접 실행**: `✓ Compiled successfully`, `Generating static pages (29/29)`, `/blog/salary-net-income-guide-2026` 정적생성 라우트 확인.
- **소스 Read**: `app/blog/[slug]/page.tsx`(이스케이프 `/</g→<` JSON-LD script만, 본문 플레인텍스트 switch, calculatorCta getCalculatorBySlug 없으면 null, related live 필터), `lib/blog.ts`(수치 draft/calculators.ts 정합), `lib/blog-jsonld.ts`(조직 저자/발행처·로고 없음), `app/blog/page.tsx`(소개문 교체·0편 분기 유지) 확인.
- **실제 브라우저 검증**(dev 서버 localhost:3000): /blog 목록 read_page — 카드 1장(배지 "급여 계산기"·제목·요약·"2026. 7. 15."·"읽는 시간 4분") 링크 정합. /blog/[slug] read_page — breadcrumb·H1·메타·본문(제목/문단/리스트 수치)·CTA 3개(정확한 /calculator/... href)·warning callout(complementary)·관련계산기 3카드·"← 블로그 목록으로" 전부 렌더, 콘솔 에러 0.
- **모바일(375×812) JS 검증**: horizontalOverflow=**false**(scrollWidth 375), H1 24px, 본문 문단 16px(축소 없음), CTA 배경 brand-primary(rgb 37,99,235). 레이아웃 깨짐 없음.
- **라이브 JSON-LD 독립 파싱**(DOM): script 2개 전부 parse OK, 블로그 @graph=[BreadcrumbList,BlogPosting], author=Organization "계산기 허브"(개인저자 아님), logo 없음, datePublished 2026-07-15, breadcrumb 3.
- **보안**: 신규/수정 6파일 시크릿 grep 0. 스테이징에 `.env*`·`settings.local.json` 미포함 확인(`.env.local`은 gitignore).
- 스크린샷 도구는 이 환경에서 반복 타임아웃(페이지 200·콘솔에러0·read_page/JS 정상 → 도구/환경 이슈로 판단, read_page+computed style로 대체 검증).

### 배포(push): **완료**
- 커밋 `ab64296` "블로그 구축…", push `d4fe005..ab64296` origin/main. Vercel 자동배포. QA PASS+마스터 빌드/모바일/JSON-LD 통과 → §8 auto-push(2026-07-13 승인) 조건 충족. `.env` 미포함.

### 미확인 항목
- Google Rich Results Test 실호출(외부 URL)은 미배포·외부망 제약으로 미수행 — 로컬 정적 HTML/라이브 DOM JSON 파싱·필수필드 정합으로 대체. 배포 후 GSC 실측은 감사 트랙 소관.
- 스크린샷(픽셀 시각) 미확보 — read_page(구조/텍스트)+JS computed style(폰트크기·오버플로우)로 대체 검증(기능/레이아웃 무결성 확인, 픽셀 단위 시각디테일은 다음 실행 디자인팀 재검수 권장).

### 핑퐁 이슈
- design 오픈질문(블로그 카테고리 체계) → **마스터가 "기존 4개 재사용, 전용 taxonomy 신설 금지"로 즉시 확정**(정보구조 변경 회피) → dev에 locked로 전달. 재작업 없이 해소.
- planning 이슈(글 1편 얇은 콘텐츠) → 마스터 판단: 사이트 전체(계산기 12+카테고리+정적)는 얇지 않음, 첫 글 1편 MVP로 오픈하고 다수글은 후속 로드맵. **핑퐁 QA FAIL 0**(스펙 정합으로 1라운드 통과).

### 마케팅 카드뉴스 게이팅
- **START 유지 + 이번엔 신규 소재 있음**: 오늘 신규 블로그 글이 실제 콘텐츠 변화 → 마케팅팀에 해당 글(연봉 실수령액 가이드) 카드뉴스 6장+SNS 캡션 3종 초안 배정(진행 중). 실제 이미지 생성·게시는 사용자 승인 대기 유지.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12계산기+블로그 착수 후 방향: (a) 블로그 글 추가(3~4편 로드맵 — planning 권장), (b) 로드맵 확장(#13~), (c) 애드센스 승인/배포 확인. 계속 보류, 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[블로그 후속, 처리 가능 후보]** (a) 2~3편 추가 글(대출/DSR·전기요금·만나이 등 각 계산기 지원 정보성 글 — 같은 데이터모델에 객체 추가만) → 얇은 콘텐츠 방어 + 목록 풍성화. (b) 블로그 warning callout amber 스타일 디자인팀 재검수(신규색 아님이나 블로그 첫 등장). (c) 계산기 카테고리 페이지 하단 "관련 글" 블록(양방향 내부링크, 비가시 확장) — design 협의.
3. **[마케팅]** 신규 블로그 카드뉴스 초안(이번 실행 진행). 누적 카드뉴스 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
4. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 정수산술, 규제·요율 갱신 체크리스트, 신규 시각요소 design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 겹침 고아작업 관찰(이번 실행 고아 0).

---

## 03:31 실행분 (블로그 글 2편 추가: DSR/대출한도 + 여름 전기요금 누진제 — 로드맵 완주 후 콘텐츠 보강)

### 세션 시작 상태
- `git status` = `.claude/settings.local.json`(로컬 권한 캐시)·`docs/team-log/2026-07-15.md`(직전 블로그 구축분 append)만 수정, 고아 미커밋 코드 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `ab64296`(블로그 구축·첫 글 1편). 12계산기+블로그 스캐폴딩 완료 상태.
- 배정 근거: 직전 "블로그 구축" 실행 핸드오프 2번 "**[블로그 후속, 처리 가능 후보]** 2~3편 추가 글(각 계산기 지원 정보성 글 — 같은 데이터모델에 객체 추가만) → 얇은 콘텐츠 방어 + 목록 풍성화"를 이어받음. 큰 결정(로드맵 확장/애드센스)은 **사용자 확인 필요**로 계속 보류, 사용자 부재 중 추가·가역·기존 계산기 무변경 작업만 선택. 현 블로그는 salary 글 1편뿐 → 카테고리 다양화(loan+life) 겸 착수.

### 파이프라인 (planning 초안 → 마스터 검수/locked → dev → QA 독립검증 → 마스터 빌드/브라우저(데스크톱+모바일)/JSON-LD → auto-push). design 별도 호출 생략(레이아웃 무변경·데이터 추가만, 마스터가 모바일 직접 재검수).
- **기획팀**(`planning/blog-post-02-03-draft.md` 신규): 두 글 완성 초안(BlogPost 객체 형태, `// 근거:` 주석으로 calculators.ts 출처 명기). 글A=DSR(dsr-loan-limit-guide-2026, loan), 글B=전기요금 누진제(summer-electricity-progressive-rate-2026, life). 수치 전량 calculators.ts 실제 구현값에서만 인용. 이슈 제기 4건(table 섹션타입/CTA 3연속 간격/YMYL "대출한도"/unit-converter 제외).
- **마스터 locked 결정**(양 초안 Read + calculators.ts 대조 후): ① 두 초안 수정 없이 채택(수치 전량 소스 일치 확인), ② YMYL "대출한도" 키워드 = 중립·정보성 톤+warning callout 필수+수치 계산기값 한정으로 방어, **완화 불필요·유지**, ③ table 신규 섹션타입 추가 금지(blog-spec §1 확장보류), list 렌더로 충분, ④ unit-converter 제외 동의(electricity 단일 링크·관련성 우선), ⑤ CTA 3연속은 기존 글도 2연속 사례 있어 정상, 모바일 간격 마스터 재검수. calculators.ts DSR·전기요금 정의 사전 Read로 정답값 확보.
- **개발팀**: `lib/blog.ts` blogPosts 배열에 2객체 추가만(신규 파일·컴포넌트·sitemap 수정 불필요 — sitemap은 배열서 동적등록). `// 근거:` 주석 제거·순수 객체 삽입. 빌드 29→31 자체보고, 변경 파일 `lib/blog.ts` 단일.

### QA 독립검증: **전 8항목 PASS** (FAIL 0)
- 직접 `npm run build`(31/31·타입에러0·신규 라우트 2줄)·`npx eslint .`(EXIT0). node로 수치 독립 재검산: DSR 월상환 1,520,056·연 18,240,672·DSR 30.40%(소스 일치), 전력량요금 200×120+150×214.6=56,190·전기요금계 62,690·부가세 6,269·전력기금 1,690(10원절사)·최종 70,640(전부 소스 일치). 내부링크 4 slug 전부 live·깨진 링크 0. 신규 BlogSection 타입 0·기존 글 무변경(diff +267/-0). 산출 HTML JSON-LD @graph=[BreadcrumbList,BlogPosting]·개인저자/로고/평점 없음·datePublished 2026-07-15·breadcrumb 3단계. 시크릿 grep 0·.env 미포함. 전력기금 라벨 2.7% vs 절사값 1,690 = 소스 동일이라 불일치 아님(관찰만).

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **git diff 독립 확인**: `lib/blog.ts` +267/-0(추가만), 기존 salary 글·헬퍼 무변경. 신규 2객체 category=loan/life, callout variant="warning" 양쪽 확인.
- **빌드 직접 실행**: `✓ Compiled successfully`, `(31/31)`, `/blog/dsr-loan-limit-guide-2026`·`/blog/summer-electricity-progressive-rate-2026` 2줄 SSG 생성.
- **실제 브라우저 검증**(dev localhost:3000): /blog 목록 = 3카드(급여/대출/생활 배지·제목·요약·읽는시간 4/6/5분) 정합. DSR 상세 = h1 정확, calculatorCta 6개(본문 3 dsr/loan-interest/loan-prepayment + 관련계산기 3카드) href 전부 `/calculator/{live-slug}`, warning callout 표시, JSON-LD [WebSite,Organization]+[BreadcrumbList,BlogPosting]. 전기요금 상세 = 350kWh 핵심수치(56,190·70,640·307.3) 렌더, CTA electricity 단일(본문+관련 2건), JSON-LD 정합.
- **모바일(375×812) JS 검증**: 두 글 모두 가로 오버플로우 **false**(scrollW=375). DSR: h1 24px·본문 16px. 전기요금: 긴 350kWh 소계 list 20개 항목 max right-edge 359<375 = **넘침 없이 정상 줄바꿈**(planning/dev 우려 소계 항목 실측 무해 확인). 콘솔 에러 0.
- **보안**: `lib/blog.ts` 시크릿 grep 0·.env 미포함. 스테이징에 `.claude/settings.local.json` 미포함.

### 미확인 항목
- 스크린샷(픽셀 시각)은 이 환경에서 반복 타임아웃(직전 실행들과 동일 도구/환경 이슈) — read_page/JS computed-style/콘솔0으로 대체 검증(기능·레이아웃·오버플로우 무결성 확인, 픽셀 디테일만 미확보).
- Google Rich Results Test 실호출(외부 URL)은 미배포·외부망 제약으로 미수행 — 로컬 산출 HTML JSON 파싱·필수필드 정합으로 대체. 배포 후 GSC 실측은 감사 트랙 소관.

### 핑퐁 이슈
- planning→(마스터 locked)→dev→QA 정합 우수, **핑퐁 0**(QA FAIL 0). 초안이 수치 출처·타입까지 미리 못박아 dev 붙여넣기만으로 1라운드 통과. planning 이슈 4건은 마스터 locked로 즉시 해소(table/YMYL/unit-converter/CTA간격) — 재작업 없음.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개+블로그 3편·빌드 정상). 이번 실행 신규 콘텐츠 = 블로그 글 2편(DSR·전기요금). 다만 직전 실행이 이미 블로그 카드뉴스(연봉 글)를 배정한 상태라 **이번 실행 마케팅 신규 배정은 보류**(고정소재 중복 회피 + 신규 글 2편은 다음 실행에서 loan 시리즈 캐러셀 묶음으로 함께 소재화 검토가 효율적). 누적 카드뉴스 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `lib/blog.ts`(글 2편 추가)·`planning/blog-post-02-03-draft.md`(신규) + 이 팀로그(직전 블로그 구축분 append 포함). QA PASS + 마스터 빌드/브라우저/모바일/JSON-LD/보안 통과분 → auto-push(2026-07-13 승인).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12계산기+블로그 3편 후 방향: (a) 블로그 글 추가(planning 3~4편 로드맵 — date/life 계산기 지원 글 여지), (b) 로드맵 확장(#13~), (c) 애드센스 승인/배포 확인. 계속 보류, 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[블로그 후속, 처리 가능 후보]** (a) 추가 글 1~2편(만나이/D-day 등 date, BMI 등 life 계산기 지원 정보성 글 — 같은 데이터모델 객체 추가). (b) blog `list` 소계 항목용 `table` 섹션타입 신설 = 디자인/개발 공동 검토 과제(현재 list로 무해하나 표형 데이터 가독성 개선 여지). (c) 계산기 카테고리/상세 하단 "관련 글" 블록(블로그↔계산기 양방향 내부링크, 비가시 확장) — design 협의.
3. **[마케팅]** 직전 연봉 글 카드뉴스에 이어, 이번 신규 loan(DSR)·life(전기요금) 글로 소재 확장 검토(loan 3종 시리즈 캐러셀 묶음 제안 유효). 누적 카드뉴스 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
4. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 정수산술, 규제·요율 갱신 체크리스트, 신규 시각요소 design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출.

---

## 05:00 실행분 (블로그 4번째 글: 만 나이 통일법 — date 카테고리 첫 글, 4개 카테고리 커버 완성)

### 세션 시작 상태
- `git status` = `.claude/settings.local.json`(로컬 권한 캐시)만 수정, 고아 미커밋 코드 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `721f7ad`(직전 블로그 2편). 12계산기 + 블로그 3편(salary/loan/life) 상태.
- 배정 근거: 직전 03:31 핸드오프 2번 "[블로그 후속, 처리 가능 후보] 추가 글 1~2편(만나이/D-day 등 date … 같은 데이터모델 객체 추가)". 블로그 3편이 salary/loan/life뿐 **date 카테고리 공백** → date 첫 글로 4개 카테고리(salary/loan/date/life) 색인 커버리지 완성. 큰 결정(로드맵 확장/AdSense)은 **사용자 확인 필요**로 계속 보류, 사용자 부재 중 추가·가역·기존 계산기 무변경 작업만 선택.

### 파이프라인 (planning 초안 → 마스터 검수/locked → dev 삽입 → QA 독립검증 → 마스터 빌드/브라우저(데스크톱+모바일)/JSON-LD → auto-push). design 별도 호출 생략(레이아웃 무변경·데이터 추가만, info callout은 기존 렌더러가 이미 지원).
- **기획팀**(`planning/blog-post-04-draft.md` 신규): 완성 BlogPost 객체 1개(slug=`age-korean-age-law-2026`, category=date). 수치·법 사실 전량 `lib/calculators.ts` age-calculator interpretation·faq에서만 인용(`// 근거:` 주석 + 출처 요약표 첨부). 이슈 3건 제기(info callout 첫 사용/date 후속 콘텐츠 갭/4편 후 방향 결정).
- **마스터 locked 결정**(초안 Read + calculators.ts age-calculator 대조 후): ① 초안 수정 없이 채택(통일법 2023-06-28·세는나이·연 나이·윤년 2/28 전량 소스 일치 확인), ② **info callout 유지**(렌더러 `app/blog/[slug]/page.tsx` L88-101이 info=neutral box / warning=amber box로 이미 구분 렌더 — 디자인 작업 불필요, 기획 이슈#1 해소), ③ 기획 이슈 #2(date 후속)·#3(5편 확장 vs AdSense)=큰 결정 → **HOLD(사용자 확인)**, 이번 실행 미착수. 세 CTA slug(age/dday/service-period) 전부 라이브 사전 확인.
- **개발팀**: `lib/blog.ts` blogPosts 배열에 객체 1개 추가만(`// 근거:` 주석 제거·순수 객체). 신규 파일·컴포넌트·sitemap 수정 불필요(sitemap 동적등록). 빌드 31→32 자체보고.

### QA 독립검증: **전 8항목 PASS** (FAIL 0)
- 직접 `npm run build`(32/32·`/blog/age-korean-age-law-2026` SSG·타입에러0)·`npx eslint lib/blog.ts`(EXIT0). `git diff` = 132 insertions(+)·삭제0, 기존 3편·헬퍼 무변경. 사실 정확성: 통일법 2023-06-28/세는나이/연 나이(병역법·청소년보호법)/윤년 2/28/실생활 활용 전량 age-calculator 소스 일치, 지어낸 조문·통계 0. info callout 크기관계(세는≥연≥만) 독립 재검산(생일 전 만+2/만+1, 생일 후 연=만) 논리 성립. 내부링크 3 slug 전부 live·깨진 링크 0. 산출 HTML JSON-LD @graph=[BreadcrumbList,BlogPosting]·개인저자/로고/평점 없음·datePublished 2026-07-15·breadcrumb 3단계. 시크릿 grep 0. (비차단 관찰: "국제적으로 통용" 일반서술은 조문·수치 아니라 FAIL 아님.)

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **git diff --stat 독립 확인**: `lib/blog.ts` +132/-0(추가만), 기존 글·헬퍼 무변경. code 변경은 `lib/blog.ts` 단일(+ 신규 planning 초안·team-log).
- **빌드 직접 실행**: `✓ Compiled successfully`, `(32/32)`, `/blog/age-korean-age-law-2026` SSG 라우트 출력 확인.
- **실제 브라우저 검증**(dev localhost:3000): /blog 목록 = 4카드(급여/대출/생활 + **신규 "날짜 계산기" 배지** 만 나이 글, 읽는시간 6분) 정합. 상세 read_page = breadcrumb(홈>블로그)·H1·날짜·카테고리 배지·본문(정의 3종/통일법/윤년/실생활 list)·**info callout(complementary)+warning callout(complementary) 둘 다 렌더**·CTA 3개(정확한 `/calculator/age-calculator|dday-calculator|service-period-calculator` href)·관련계산기 3카드·"← 블로그 목록으로" 전부 표시. 콘솔 에러 **0**.
- **모바일(375×812) JS 검증**: horizontalOverflow=**false**(scrollW 375), H1 24px·본문 16px(축소 없음), CTA 배경 brand-primary rgb(37,99,235). **info callout bg rgb(249,250,251) neutral vs warning bg amber → infoDistinctFromWarn=true**(두 variant 시각 구분 실측 확인).
- **라이브 JSON-LD 독립 파싱**(DOM): script 2개 parse OK, 블로그 @graph=[BreadcrumbList,BlogPosting], author=Organization "계산기 허브"(개인저자 아님), logo 없음, aggregateRating 없음, datePublished 2026-07-15.
- **보안**: `lib/blog.ts` 시크릿 grep 0·.env 미포함. 스테이징에 `.claude/settings.local.json` 미포함.

### 미확인 항목
- 스크린샷(픽셀 시각)은 이 환경 반복 타임아웃(직전 실행들과 동일 도구/환경 이슈) — read_page(구조/텍스트)+JS computed-style(폰트·오버플로우·callout 배경색)+콘솔0으로 대체 검증(기능·레이아웃·오버플로우·variant 구분 무결성 확인, 픽셀 디테일만 미확보).
- Google Rich Results Test 실호출(외부 URL)은 미배포·외부망 제약으로 미수행 — 로컬 산출/라이브 DOM JSON 파싱·필수필드 정합으로 대체. 배포 후 GSC 실측은 감사 트랙 소관.

### 핑퐁 이슈
- planning→(마스터 locked)→dev→QA 정합 우수, **핑퐁 0**(QA FAIL 0). 초안이 수치·법 출처·타입까지 미리 못박아 dev 삽입만으로 1라운드 통과. planning 이슈 3건: #1(info callout 스타일) = 렌더러 기존 지원으로 마스터 즉시 해소(재작업 없음), #2·#3 = 큰 결정 HOLD.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개 + 블로그 4편·빌드 정상). 이번 실행 신규 콘텐츠 = 만 나이 통일법 글(date, 진짜 신규). 03:00 연봉 글 카드뉴스 선례대로 **이번 신규 글 카드뉴스+SNS 캡션 초안을 마케팅팀에 배정**(고정소재 반복 아님·당일 실제 신규 콘텐츠 매칭). 실제 이미지 생성·게시는 사용자 승인 대기 유지. (03:31에 미소재화된 DSR·전기요금 글은 다음 실행 loan 시리즈 캐러셀 묶음 후보로 유지.)

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `lib/blog.ts`(글 1편 추가)·`planning/blog-post-04-draft.md`(신규)·`marketing/promo-drafts.md`(만 나이 카드뉴스 초안 append)·이 팀로그. QA PASS + 마스터 빌드/브라우저/모바일/JSON-LD/보안 통과분 → auto-push(2026-07-13 승인).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12계산기 + 블로그 4편(4개 카테고리 전부 커버) 후 방향: (a) 블로그 글 추가(date 2~3편·salary/loan 심화 등), (b) 로드맵 확장(#13~), (c) **AdSense 신청/색인 점검 단계 진입**. 4개 카테고리 최소 1편씩 확보로 초기 볼륨 마련됨 → 방향 결정 적기. 계속 보류, 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[블로그 후속, 처리 가능 후보]** (a) date 후속 글(D-Day 활용법·근속연수/연차 계산 등 — 같은 데이터모델 객체 추가). (b) 계산기 카테고리/상세 하단 "관련 글" 블록(블로그↔계산기 양방향 내부링크, 비가시 확장) — design 협의. (c) blog `table` 섹션타입 신설(list로 무해하나 표형 데이터 가독성) — design/dev 공동.
3. **[마케팅]** 이번 만 나이 글 카드뉴스 초안(배정·진행). 미소재화 DSR·전기요금 → loan 시리즈 캐러셀 묶음 후보. 누적 카드뉴스(연봉·단위·근속·퇴직·전기·4대보험·DSR·만나이 등) 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
4. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 정수산술, 규제·요율 갱신 체크리스트, 신규 시각요소 design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출.

---

## 05:30 실행분 (계산기 상세 "관련 글" 역방향 내부링크 블록 — blog↔calculator 양방향 완성)

### 세션 시작 상태
- 현재 KST 05:37(get_current_korean_time), 직전 05:00분(만 나이 블로그 4번째 글) 이어받음. `git status` = `.claude/settings.local.json`(로컬 권한 캐시)만 수정, 고아 미커밋 코드 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `3fada12`(만 나이 카드뉴스). 12계산기 + 블로그 4편(4개 카테고리 전부 커버) 상태.
- 배정 근거: 05:00 핸드오프 2번 "[블로그 후속, 처리 가능 후보] (b) 계산기 카테고리/상세 하단 '관련 글' 블록(블로그↔계산기 양방향 내부링크, 비가시 확장) — design 협의"를 선택. **5번째 같은-날짜 블로그 글 대신 내부링크를 택한 이유**: 블로그 4편이 전부 2026-07-15 발행이라 같은 날 추가 글은 콘텐츠 덤프 신호(AdSense 승인에 불리)를 강화 → 새 카피 없이 crawl 그래프를 강화하는 내부링크가 더 가치. blog→calculator 링크(relatedCalculatorSlugs)는 이미 있고 역방향(calculator→blog)만 없어 이걸 채움. 추가·가역·기존 계산기 무변경이라 사용자 부재 중 착수 가능(큰 결정 아님). 큰 방향 결정(로드맵 확장/AdSense)은 계속 HOLD.

### 파이프라인 (planning+design 병렬 → 마스터 locked → dev → QA 독립검증 → 마스터 빌드/HTML/브라우저(데스크톱+모바일) → auto-push)
- **기획팀**(`planning/related-blog-posts-spec.md` 신규) + **디자인팀**(`design/related-blog-posts-ui-spec.md` 신규) 병렬. 두 팀 독립 수렴: 역인덱싱(계산기측 신규필드 X)·관련계산기 아래 배치·빈 상태 null·live 게이팅·RelatedCalculators 그리드(1/2/3) 재사용. 둘 다 **핵심 이슈 동일 포착**: `BlogPostCard` 제목이 `<h2>` 하드코딩 → 섹션 h2 밑 카드도 h2 = 계층 붕괴(CalculatorCard는 h3). planning은 발행일 4편 동일 → tie-break 필요도 포착.
- **마스터 locked 결정**(양 스펙 + 실소스 CalculatorCard(h3)/BlogPostCard(h2)/blog 상세 직접 Read 후): ① 헬퍼 `getBlogPostsForCalculator` = relatedCalculatorSlugs 역필터·발행일 desc+slug asc tie-break(SSG 결정성)·상한 3. ② `BlogPostCard`에 `headingLevel?:"h2"|"h3"` prop(**기본 h2로 블로그 목록 동작 100% 보존**), 이 블록만 h3 주입, className 불변(외형 회귀 0). ③ 신규 `RelatedBlogPosts`(RelatedCalculators 미러). ④ 상세 페이지 RelatedCalculators 바로 아래 live 게이팅 주입. ⑤ **planning 부수발견(four-insurance-calculator 0편 → salary 글에 remapping)은 이번 범위서 DEFER** — 이미 승인·push된 블로그 글의 가시 변경이라 사용자 부재 중 보류, 핸드오프 이관.
- **개발팀**: `lib/blog.ts`(헬퍼)·`components/BlogPostCard.tsx`(prop)·`components/RelatedBlogPosts.tsx`(신규)·`app/calculator/[slug]/page.tsx`(주입). 빌드 32페이지 유지·역매핑 스팟체크 자체보고.

### QA 독립검증: **전 8항목 PASS** (FAIL 0)
- 직접 `npm run build`(32/32·타입에러0). 역매핑을 헬퍼가 아닌 **blogPosts 소스에서 손으로 재구성** + 빌드 산출 HTML 대조: 12개 계산기 전부 기대치 일치(service-period=2[age-·salary- 순, tie-break], dsr/salary/severance/loan-interest/loan-prepayment/electricity/age/dday=각1, bmi/unit-converter/four-insurance=0). 빈 상태(0편 3종 산출 HTML "관련 글" 부재)·헤딩 계층(섹션 h2/카드 h3, 블로그 목록은 h2 보존)·live 게이팅·회귀(className 불변·blog 데이터 무수정·RelatedCalculators 무변경)·깨진 링크 0·보안 grep 0 전부 PASS.

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **git diff 독립 확인**: 3개 수정 파일 surgical — page.tsx(+import2/+relatedPosts/+게이팅 블록), BlogPostCard(`<h2>`→`<HeadingTag>` 태그치환+prop, className 불변), blog.ts(헬퍼만 추가·blogPosts 무수정). 신규 RelatedBlogPosts.tsx = RelatedCalculators 미러.
- **빌드 직접 실행**: `✓ Compiled successfully`, `Generating static pages (32/32)`.
- **빌드 산출 HTML 독립 파싱**(grep): dsr=관련글 헤딩+h3 1카드(dsr-loan-limit-guide-2026), service-period=h3 2카드 **age-korean-age-law-2026 → salary-net-income-guide-2026 순**(tie-break slug asc 실증), bmi=관련글 헤딩 0·blog링크 0(빈 상태).
- **실제 브라우저 검증**(dev localhost:3000): service-period 상세 — 관련 글 섹션이 관련 계산기 **아래**(compareDocumentPosition 확인)·카드 2개·헤딩 H3·href age-→salary- 순. **모바일 375×812**: horizontalOverflow=**false**(scrollW 375)·카드 1열 스택·maxRight 359<375·h3 16px. 블로그 목록(/blog) — BlogPostCard 4개 전부 **H2 유지**(기본값 보존·회귀 0). bmi 상세 — 관련 글 헤딩 미노출·관련 계산기 헤딩은 존재(빈 상태 정상). 콘솔 에러 **0**.
- **보안**: 변경/신규 소스+스펙 6파일 시크릿(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO/PRIVATE KEY) grep **0건**. 커밋에 `.env*`·`.claude/settings.local.json` 미포함.

### 미확인 항목
- 스크린샷(픽셀 시각)은 이 환경 반복 타임아웃 이슈 — read_page 대신 JS computed-style/getBoundingClientRect/compareDocumentPosition + 콘솔0으로 대체(구조·순서·오버플로·헤딩·폰트 실측, 픽셀 디테일만 미확보).
- Google Rich Results 등 외부 검증은 미배포·외부망 제약으로 미수행(해당 없음 — 이 작업은 JSON-LD 무관, 순수 내부링크).

### 핑퐁 이슈
- planning·design **독립 동일 이슈 포착**(BlogPostCard h2 계층) → 마스터가 design 권고안(headingLevel prop, 기본 h2 보존)으로 즉시 locked → dev 1라운드 통과. **QA FAIL 0**. design이 넘긴 "공용 컴포넌트 prop 추가 시 dev 이견 가능" 우려는 dev가 그대로 수용(회신 이견 없음).
- planning 부수발견(four-insurance remapping)·design 부수발견(카드 :focus-visible 링 부재)은 둘 다 **범위 밖 백로그 이관**(가시 변경/공용자산 → 사용자 부재 중 보류).

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개+블로그 4편·빌드 정상). 이번 실행은 **비가시성 내부링크 인프라**(화면상 신규 카피·신규 콘텐츠 0, 기존 카드 재배치 성격) → **신규 카드뉴스 소재 아님**. 마케팅 신규 배정 없음(고정소재 반복 회피 원칙과 정합). 누적 카드뉴스 8종(연봉·단위·근속·퇴직·전기·4대보험·DSR·만나이) 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `lib/blog.ts`·`components/BlogPostCard.tsx`·`components/RelatedBlogPosts.tsx`(신규)·`app/calculator/[slug]/page.tsx`·`planning/related-blog-posts-spec.md`(신규)·`design/related-blog-posts-ui-spec.md`(신규) + 이 팀로그. QA PASS + 마스터 빌드/HTML/브라우저(데스크톱+모바일)/보안 통과분 → auto-push(2026-07-13 승인).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12계산기 + 블로그 4편(4카테고리 커버) + 양방향 내부링크 완성 후 방향: (a) 블로그 글 추가(단, 같은-날짜 다량 발행은 콘텐츠 덤프 신호 주의 — 발행일 분산/속도 조절 고려), (b) 로드맵 확장(#13~), (c) **AdSense 신청/색인 점검 단계 진입**(초기 볼륨·내부링크 구조 확보됨 → 적기). 계속 보류, 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[내부링크 후속, 처리 가능 후보]** (a) **카테고리 페이지(/salary 등) 하단 "관련 글" 블록** — 이번은 계산기 상세만 했으므로 카테고리 레벨도 같은 헬퍼(카테고리별 글 필터)로 확장 가능(비가시 확장). (b) planning DEFER건: `salary-net-income-guide-2026` 글 relatedCalculatorSlugs에 `four-insurance-calculator` 추가(그 글이 4대보험 요율 상세 서술 → 역매핑 채움·상호링크). 이미 승인된 글의 가시 변경(관련계산기 카드 3→4)이라 사용자 확인/디자인 검토 권장. (c) bmi·unit-converter 지원 블로그 글(현재 관련 글 0편) — 블로그 백로그.
3. **[디자인 백로그]** BlogPostCard/CalculatorCard 공통 `:focus-visible` 포커스 링 부재 — 접근성 일관 보강(이번 블록 단독 변경 금지로 미적용, 별도 건).
4. **[마케팅]** 미소재화 DSR·전기요금 글 → loan 시리즈 캐러셀 묶음 후보. 누적 8종 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
5. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·DSR 규제/스트레스), 신규 시각요소 design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출.

---

## 06:06 실행분 (카테고리 페이지 하단 "관련 글" 블록 — blog↔category 카테고리 레벨 내부링크 완성)

### 세션 시작 상태
- 현재 KST 06:06(get_current_korean_time). `git status` = `.claude/settings.local.json`(로컬 권한 캐시)만 수정, 고아 미커밋 코드 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `e4f9574`(직전 05:30분 계산기상세 역방향 관련글). 12계산기 + 블로그 4편 + 계산기상세↔블로그 양방향 내부링크 완성 상태. 오늘 GSC 감사 파일 없음.
- 배정 근거: 05:30 핸드오프 2(a) "**[내부링크 후속, 처리 가능 후보] 카테고리 페이지(/salary 등) 하단 '관련 글' 블록 — 같은 헬퍼(카테고리별 글 필터)로 확장(비가시 확장)**"을 이어받음. 05:30이 계산기'상세'만 했으므로 카테고리 레벨로 미러링. 큰 결정(로드맵 확장/AdSense)은 **사용자 확인 필요**로 계속 HOLD, 사용자 부재 중 추가·가역·기존 계산기 무변경 작업만 선택. 이번 변경은 sr-only 헤딩 채택으로 화면 픽셀 변화 사실상 0(비가시).

### 파이프라인 (planning+design 병렬 → 마스터 locked → dev → QA 독립검증 → 마스터 빌드/HTML/JSON-LD/라이브DOM(데스크톱+모바일) → auto-push)
- **기획팀**(`planning/category-related-blog-spec.md` 신규) + **디자인팀**(`design/category-related-blog-ui-spec.md` 신규) 병렬. 두 팀 **독립적으로 핵심 이슈 동일 포착**: 카테고리 페이지는 계산기 그리드 위에 섹션 헤딩(h2)이 없어 이미 `h1→h3` 스킵이 있고, 여기 "관련 글" h2를 붙이면 `h3→h2` 아웃라인 역전 발생. 둘 다 해법으로 그리드 위 sr-only h2 신설을 1순위 권고.
- **마스터 locked 결정**(양 스펙 실물 Read + 실소스 CategoryPage/CalculatorCard(h3)/RelatedBlogPosts/BlogPostCard 확인 후): ① 헬퍼 `getBlogPostsByCategory` = category 필터·발행일 desc+slug asc(SSG 결정성)·`slice(0,6)` 상수화(상세는 3, 허브는 6 — 각 카테고리 4편 되기 전 미발동 = 현재 레이아웃 변화 0). ② **(B) sr-only h2 "계산기 목록"**으로 그리드를 `<section aria-labelledby>` 래핑 — 화면 변화 0, 스킵·역전 동시 해결, `mt-8`은 그리드 div 유지. ③ **헤딩 문구 "관련 글" 그대로·RelatedBlogPosts 무수정 재사용**(headingText prop 미도입) — 기획(전용문구 "이 주제의 블로그 글")↔디자인(유지) 이견에서 **디자인 손**: 사이트 전역 일관·상세페이지 회귀 0·최소 diff·공용 컴포넌트 미변경 우선. ④ `buildCategoryJsonLd` 무변경(관련 글은 JSON-LD 미포함, ItemList=계산기 컬렉션 의미 유지). ⑤ 빈 상태는 RelatedBlogPosts null 위임.
- **개발팀**: `lib/blog.ts`(헬퍼+상수 추가만)·`components/CategoryPage.tsx`(section+sr-only h2 래핑, RelatedBlogPosts 주입) 2파일. `CalculatorCategory`↔`BlogCategory`는 동일 유니온이라 캐스팅 없이 타입 통과 보고. 빌드 32페이지 유지·lint 클린 자체보고.

### QA 독립검증: **전 8항목 PASS** (차단 FAIL 0)
- 직접 `npm run build`(32/32·타입에러0)·`npx eslint`(EXIT0). 헬퍼 소스 스펙 정합(`lib/blog.ts:576-588`), 4개 카테고리 산출 HTML 스팟체크 = 각자 자기 카테고리 글만(salary→연봉/loan→DSR/date→만나이/life→전기요금), **교차 오염 0**. 헤딩 위계 실측 `h1→h2(sr-only 계산기목록)→h3×3→h2(관련글)→h3` 정상. `git status`로 변경 2파일 확인·RelatedBlogPosts/BlogPostCard/site-jsonld diff 0·계산기상세 관련글(e4f9574) 회귀 없음. JSON-LD ItemList=계산기 URL만(`/blog/` 0건). 깨진 링크 0·빈상태 방어·보안 grep 0. 비차단 관찰 1(로컬 settings 파일=커밋 제외 권고, 반영됨).

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **git diff 독립 확인**: `components/CategoryPage.tsx` = import 2줄 + 그리드를 section+sr-only h2로 래핑(mt-8·그리드 클래스 보존) + RelatedBlogPosts 1줄 주입뿐. `lib/blog.ts` = 상수 1 + 헬퍼 1 추가(+15/-0), 기존 blogPosts·헬퍼 무수정. surgical.
- **빌드 직접 실행**: `✓ Compiled successfully`, `Generating static pages (32/32)`(무회귀).
- **산출 HTML 4카테고리 grep**: sr-only "계산기 목록" 존재, 각 카테고리 자기 블로그 링크 1개(교차오염 0), 계산기 링크는 카테고리별 3개 정상.
- **JSON-LD 파싱**(node, loan): ItemList urls = loan-interest/loan-prepayment/dsr 계산기 URL만, **blog 섞임 false**. 헤딩 태그순서 grep도 h1→h2(sr-only)→h3→h2→h3 확인.
- **라이브 DOM 검증**(dev localhost:3000 /salary): JS 아웃라인 = H1→H2(sr-only)→H3×3→H2(관련글)→H3, relHeading "관련 글", blogCard `/blog/salary-net-income-guide-2026`. **모바일 375×812 재측정**: horizontalOverflow=**false**(scrollW 375), sr-only h2 박스 1×1px(시각 숨김 정상), 블로그 카드 폭 343px == 계산기 카드 343px(**동일 폭**), 우측끝 359<375. 콘솔 에러 **0**.
- **보안**: 변경/신규 4파일 시크릿 grep(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO/PRIVATE KEY) **0건**. 커밋에 `.env*`·`.claude/settings.local.json` 미포함.

### 미확인 항목
- 스크린샷(픽셀 시각)은 이 환경 반복 타임아웃(직전 실행들과 동일 도구/환경 이슈, computer screenshot 30s 타임아웃) — read_page 대신 JS getBoundingClientRect/computed-style + 콘솔0 + 산출HTML grep으로 대체(구조·순서·오버플로·폰트·동일폭·헤딩 실측, 픽셀 디테일만 미확보). 단 이번 변경은 sr-only 채택으로 화면 픽셀 변화 자체가 없어 시각 회귀 위험 최저.
- Google Rich Results 등 외부 검증은 이 작업이 JSON-LD 무변경(순수 내부링크)이라 해당 없음.

### 핑퐁 이슈
- planning·design **독립 동일 이슈 포착**(그리드 위 h2 부재로 헤딩 스킵/역전) → 마스터가 양팀 1순위 권고(sr-only h2)로 즉시 locked → dev 1라운드 통과. **QA FAIL 0.**
- **[기획↔디자인 이견 → 마스터 판정]** 관련글 헤딩 문구: 기획=전용문구+headingText prop 파라미터화 / 디자인="관련 글" 유지·무수정. 마스터가 디자인 손(전역 일관·회귀0·최소 diff). 공용 컴포넌트 미변경으로 상세페이지 회귀 위험 제거. 재작업 없이 해소.
- planning 부수발견(salary 글 relatedCalculatorSlugs에 four-insurance 추가=가시 변경)·design 부수발견(focus-visible 링 부재)은 이전 실행들과 동일 백로그로 이관(범위 밖).

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개+블로그 4편·빌드 정상). 이번 실행은 **비가시성 내부링크 인프라**(sr-only 헤딩 채택으로 화면 신규 카피·신규 콘텐츠 0) → **신규 카드뉴스 소재 아님**. 마케팅 신규 배정 없음(고정소재 반복 회피 원칙과 정합). 누적 카드뉴스 8종(연봉·단위·근속·퇴직·전기·4대보험·DSR·만나이) 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push): **완료**
- 커밋 `b2f8698`, push `e4f9574..b2f8698` origin/main. Vercel 자동배포. 대상: `lib/blog.ts`·`components/CategoryPage.tsx`·`planning/category-related-blog-spec.md`(신규)·`design/category-related-blog-ui-spec.md`(신규). QA PASS + 마스터 빌드/HTML/JSON-LD/라이브DOM(데스크톱+모바일)/보안 통과 → §8 auto-push(2026-07-13 승인) 조건 충족.
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12계산기 + 블로그 4편(4카테고리 커버) + blog↔calculator 양방향(상세·카테고리 둘 다) 내부링크 완성 후 방향: (a) 블로그 글 추가(단, 같은-날짜 다량 발행은 콘텐츠 덤프 신호 주의 — 발행일 분산 고려), (b) 로드맵 확장(#13~), (c) **AdSense 신청/색인 점검 단계 진입**(초기 볼륨·양방향 내부링크 구조 확보됨 → 적기). 계속 보류, 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[내부링크/콘텐츠 후속, 처리 가능 후보]** (a) planning DEFER건: `salary-net-income-guide-2026` 글 relatedCalculatorSlugs에 `four-insurance-calculator` 추가(그 글이 4대보험 요율 상세 서술 → 역매핑 채움). **이미 승인된 글의 가시 변경(관련계산기 카드 3→4)이라 사용자 확인/디자인 검토 권장** — 사용자 부재 중 보류. (b) bmi·unit-converter 지원 블로그 글(현재 관련 글 0편) — 블로그 백로그. (c) 비가시 SEO/내부링크 자산은 거의 소진(상세+카테고리 양방향 완료, 루트/카테고리 JSON-LD 완료). 다음 처리 가능 후보는 콘텐츠(FAQ 확장 등)나 카드뉴스 묶음 기획.
3. **[디자인 백로그]** BlogPostCard/CalculatorCard 공통 `:focus-visible` 포커스 링 부재 — 접근성 일관 보강(별도 건, 단독 변경 금지).
4. **[마케팅]** 미소재화 DSR·전기요금 글 → loan 시리즈 캐러셀 묶음 후보. 누적 8종 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
5. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·DSR 규제/스트레스), 신규 시각요소 design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출. planning/design/dev/qa 서브에이전트 세션에서도 동일.

## 06:47 실행분 (접근성: 네비/카드 전역 focus-visible 포커스 링 추가 — 디자인 백로그 소진)

### 세션 시작 상태
- 현재 KST 06:47(`date`). `git status` = `.claude/settings.local.json`(로컬 권한 캐시)만 수정, 고아 미커밋 코드 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `83a7fca`(직전 06:06분 카테고리 관련글 팀로그). 12계산기 + 블로그 4편 + blog↔calculator 양방향 내부링크(상세·카테고리) 완성 상태. 오늘 GSC 감사 파일 없음.
- 배정 근거: 06:06 핸드오프 2(c) "비가시 SEO/내부링크 자산 거의 소진 → 다음 처리 가능 후보는 콘텐츠(FAQ)나 카드뉴스 묶음" + 반복 이월된 **디자인 백로그 "BlogPostCard/CalculatorCard 공통 :focus-visible 포커스 링 부재"**(05:30·06:06 §다음실행 3번)를 이번에 처리. 큰 결정(로드맵 확장/AdSense)은 **사용자 확인 필요**로 계속 HOLD. 이 작업은 추가·완전 가역·무포커스 화면 픽셀 변화 0(키보드 Tab 시에만 링 표시)이라 사용자 부재 중 착수 적합.
- 착수 전 grep 확인: SiteHeader nav/로고, SiteFooter 링크, CalculatorCard, BlogPostCard, FaqAccordion summary 등 **모든 네비/카드/토글에 focus-visible 스타일 전무**(브라우저 기본 아웃라인 의존) = 전역 WCAG 2.4.7 갭. 폼 인풋은 이미 사이트 전역 동일 포커스 언어(`focus:ring-4 focus:ring-brand-primary/15`) 사용 중 → 이 언어를 링크류에 요소별로 확장.

### 파이프라인 (design 스펙 → 마스터 locked → dev → QA 독립검증 → 마스터 빌드/CSS산출/diff/보안 → auto-push). planning은 콘텐츠 0이라 N/A(순수 UI/a11y).
- **디자인팀**(`design/focus-visible-ring-ui-spec.md` 신규): 2-티어 스펙 확정. 티어A(테두리 카드=인풋언어 복제 `focus-visible:outline-hidden border-brand-primary ring-4 ring-brand-primary/15`), 티어B-1(테두리 없는 알약/인라인 `ring-2 ring-brand-primary` solid), 티어B-2(summary=`ring-inset`+`rounded-md`). **Tailwind v4 핵심 캐치: `outline-none`(v4=완전제거) 대신 `outline-hidden`(고대비 forced-colors 모드 아웃라인 폴백 유지)**. 스코프 전역 5개·중앙상수 `lib/focusRing.ts` 권장.
- **마스터 locked 결정**(스펙 Read + 스택 검증 후): ① **스코프=(B) 전역 5개 전부**(부분 적용이 더 혼란·회귀 위험 낮음). ② **재사용=중앙 `lib/focusRing.ts` 상수 4종**(인풋이 12+파일 문자열 중복된 스멜 반복 회피). ③ **`outline-hidden`만·`outline-none` 금지**(v4 의미변경). ④ 기존 hover·기본 클래스 무변경 append. ⑤ 스택 사전검증: `package.json` tailwindcss `^4`·next 16.2.10, `globals.css` `@import "tailwindcss"`/`@theme inline` + `--color-brand-primary:#2563eb`·`--color-brand-border` 존재 → `outline-hidden`·`focus-visible:`·`ring-brand-primary/15`·`ring-4` 전부 유효(인풋이 이미 `focus:ring-4 focus:ring-brand-primary/15` 사용=투명도·링폭 동작 증명). ⑥ 인풋 `outline-none`→`outline-hidden` forced-colors 정렬은 12+파일 별건 **DEFER**(백로그).
- **개발팀**: `lib/focusRing.ts` 신규(상수 4종 완결문자열)·5개 컴포넌트에 import+append(SiteHeader만 로고·nav 2곳). 일반문자열 className→템플릿리터럴 변환하되 기존 클래스 무변경. 빌드 32·lint EXIT0·산출 CSS 8종 유틸 생성 자체보고.

### QA 독립검증: **전 7항목 PASS** (FAIL 0)
- 직접 `npm run build`(32/32 유지·에러0)·`npx eslint .`(EXIT0). `lib/focusRing.ts` 4종 상수 스펙 §5 exact 일치(`outline-hidden`만·`outline-none` 0). 요소별 적용 6행 소스 대조(카드×2=CARD, 로고=LINK_ROUNDED, nav=LINK, 푸터=LINK_ROUNDED, summary=INSET) 전부 정합. **Tailwind v4 실제 생성**: 산출 CSS에서 8개 focus-visible 유틸 전부 규칙 생성 확인(`ring-brand-primary/15`→`#2563eb26`, `outline-hidden`→forced-colors `outline:2px solid #0000` 폴백 동반). `git diff`로 focusRing import+focus-visible append 뿐·기존 클래스/레이아웃/hover 무변경. 신규 `outline-none` 0. 시크릿 grep 0.

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **`lib/focusRing.ts` Read**: 스펙 §5와 4종 상수 문자열 exact 일치, `FOCUS_RING_LINK_ROUNDED`만 완결클래스 이어붙임(`${FOCUS_RING_LINK} focus-visible:rounded-md`), `outline-hidden`만·`outline-none` 없음.
- **git diff 독립 확인**: 5개 컴포넌트 = import 1줄 + className 템플릿리터럴 변환(순수 append). 예 CalculatorCard `...hover:shadow-md ${FOCUS_RING_CARD}`, SiteHeader 로고/nav 2곳. 기존 클래스 한 글자도 미변경·hover·레이아웃 보존. +11/-6 6파일.
- **빌드 직접 실행**: `✓ Compiled successfully in 2.7s`, `Generating static pages (32/32)`(무회귀).
- **산출 CSS 독립 grep**(`.next/static/chunks/34gabau1fiz5v.css`): `ring-4`·`ring-2`·`ring-brand-primary`(`#2563eb`)·`ring-brand-primary/15`(`#2563eb26`=15% alpha)·`ring-inset`·`border-brand-primary`·`rounded-md`·`outline-hidden`(+ forced-colors `outline:2px solid` 폴백) **8종 전부 :focus-visible 선택자로 생성 확인** → v4가 완결 클래스 문자열 정상 감지. 신규 `focus-visible:outline-none` 0.
- **보안**: 변경/신규 6파일 + 스펙 시크릿(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO/PRIVATE KEY) grep **0건**. 커밋에 `.env*`·`.claude/settings.local.json` 미포함.

### 미확인 항목
- 실제 키보드 Tab 픽셀 렌더(포커스 링 시각)는 이 환경 프리뷰/스크린샷 반복 타임아웃으로 미확보 — 소스·산출 CSS `:focus-visible` 선택자 생성·forced-colors 폴백 규칙·빌드 무회귀로 대체 검증(동작 조건 충족 확인, 픽셀만 미측정). 이 변경은 무포커스 화면 픽셀 변화 0이라 시각 회귀 위험 최저.
- Windows 고대비/forced-colors 모드 실기기 테스트 미수행 — `outline-hidden` 폴백 규칙(`outline:2px solid #0000`)이 산출 CSS에 존재함으로 대체 확인.

### 핑퐁 이슈
- design→(마스터 locked)→dev→QA 정합 우수, **핑퐁 0**(QA FAIL 0). 스펙이 티어별 클래스·outline-hidden·상수 형태·스코프까지 미리 못박아 dev 1라운드 통과. design이 넘긴 마스터 결정건(스코프 A/B·중앙상수 여부)은 마스터가 스택검증 후 즉시 locked(B·중앙상수) → 재작업 없음.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개+블로그 4편·빌드 정상). 이번 실행은 **비가시성 접근성 인프라**(무포커스 화면 신규 카피·콘텐츠 0, 키보드 포커스 시에만 링) → **신규 카드뉴스 소재 아님**. 마케팅 신규 배정 없음(고정소재 반복 회피 원칙 정합). 누적 카드뉴스 8종(연봉·단위·근속·퇴직·전기·4대보험·DSR·만나이) 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push): **완료**
- 대상: `lib/focusRing.ts`(신규)·`components/{CalculatorCard,BlogPostCard,SiteHeader,SiteFooter,FaqAccordion}.tsx`·`design/focus-visible-ring-ui-spec.md`(신규) + 이 팀로그. QA PASS + 마스터 빌드/CSS산출/diff/보안 통과 → §8 auto-push(2026-07-13 승인) 조건 충족.
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12계산기 + 블로그 4편(4카테고리 커버) + blog↔calculator 양방향 내부링크 + 전역 focus-visible 접근성 완성 후 방향: (a) 블로그 글 추가(단, 같은-날짜 다량 발행은 콘텐츠 덤프 신호 주의 — 발행일 분산 고려), (b) 로드맵 확장(#13~), (c) **AdSense 신청/색인 점검 단계 진입**(초기 볼륨·양방향 내부링크·접근성 확보 → 적기). 계속 보류, 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[접근성 후속, 처리 가능 후보]** (a) **인풋 `outline-none`→`outline-hidden` 정렬**(forced-colors 고대비 모드에서 인풋 포커스 지표 사라지는 잠재 갭, 12+파일 — design/dev 별건, 이번 링크류 스펙 §9 후속). (b) 기타 인터랙티브(있다면) focus-visible 점검. 둘 다 비가시·가역이라 착수 가능 후보.
3. **[내부링크/콘텐츠 후속]** (a) planning DEFER건: `salary-net-income-guide-2026` 글 relatedCalculatorSlugs에 `four-insurance-calculator` 추가(가시 변경=관련계산기 카드 3→4, 사용자 확인/디자인 검토 권장 — 보류). (b) bmi·unit-converter 지원 블로그 글(현재 관련 글 0편) — 블로그 백로그. (c) 비가시 SEO/내부링크 자산 소진 → 콘텐츠(FAQ 확장)·카드뉴스 묶음 기획이 다음 후보.
4. **[마케팅]** 미소재화 DSR·전기요금 글 → loan 시리즈 캐러셀 묶음 후보. 누적 8종 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
5. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·DSR 규제/스트레스), 신규 시각요소(3구간 배지·위치게이지) design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출. design/dev/qa 서브에이전트 세션에서도 동일.

---

## 22:06 실행분 (접근성 후속: 인풋 outline-none→outline-hidden — forced-colors 포커스 지표 갭 해소)

### 세션 시작 상태
- 현재 KST 22:06(`TZ=Asia/Seoul date`). `git status` = `.claude/settings.local.json`(로컬 권한 캐시)만 수정, 고아 미커밋 코드 **없음**. `git fetch` 후 origin/main과 0/0 동기화, tip `e8cbc04`(직전 06:47분 focus-visible 링). 12계산기 + 블로그 4편 + blog↔calculator 양방향 내부링크 + 전역 focus-visible 완성 상태. 오늘 GSC 감사 파일 없음. (06:47 이후 이번이 다음 실행 — 중간 실행 없음.)
- 배정 근거: 06:47 핸드오프 2(a) "**[접근성 후속, 처리 가능] 인풋 `outline-none`→`outline-hidden` 정렬(forced-colors 고대비 모드에서 인풋 포커스 지표 사라지는 잠재 갭, 13파일) — 이번 링크류 스펙 §9 후속**"을 이어받음. 큰 결정(로드맵 확장/AdSense)은 **사용자 확인 필요**로 계속 HOLD. 이 작업은 단순 토큰 치환·비가시(일반 모드 화면 변화 0)·완전 가역이라 사용자 부재 중 착수 적합.

### 파이프라인 (design 마이크로스펙 → 마스터 locked → dev → QA 독립검증 → 마스터 빌드/CSS산출/diff/보안 → auto-push). planning은 콘텐츠 0이라 N/A(순수 a11y).
- **디자인팀**(`design/input-outline-hidden-spec.md` 신규): 4개 확정 — ① `outline-none`→`outline-hidden` **단일 토큰만** 치환(나머지 `focus:border-brand-primary focus:ring-4 focus:ring-brand-primary/15`·hover·`appearance-none`·`min-h-[140px]` 무변경). ② **`focus:`→`focus-visible:` 전환 안 함**(인풋은 클릭/터치 진입도 캐럿=입력위치 신호로 포커스 지표 필요, 06:47 카드/링크와 정반대 요구). ③ 일반 모드 시각 회귀 0(`outline-hidden`=투명 아웃라인, 박스모델 밖). ④ 중앙 상수화 안 함·최소 diff(접근성 교정에 리팩터 안 섞음). grep 재확인 20곳/13파일.
- **마스터 locked**: 스펙 그대로 승인(planning N/A 명시). Tailwind v4에서 `outline-none`=아웃라인 완전 제거 → forced-colors 모드에서 box-shadow 기반 `ring`이 OS에 무시되어 포커스 지표 소멸(WCAG 2.4.7 갭), `outline-hidden`=투명 아웃라인 유지 → 고대비 모드가 가시화. 근거 확정.
- **개발팀**: 13개 컴포넌트 20곳 토큰 치환. 잔여 `outline-none`(코드) 0·`outline-hidden` 20곳·빌드 32페이지·eslint EXIT0 자체보고.

### QA 독립검증: **전 9항목 PASS** (FAIL 0)
- 직접 grep(app/components/lib `outline-none` **잔여 0**·`outline-hidden` components **정확히 20곳/13파일** 스펙 §5 표 완전일치), `git diff` 20 hunk 전수 = 각 라인 단일 토큰 치환뿐(주변 클래스·`focus:` 트리거·에러분기·`disabled:opacity-60` 무변경, `focus-visible:` 전환 0), `npm run build`(32/32·TS0), `npx eslint .`(EXIT0).
- **산출 CSS 실증(핵심)**: `.next/static/chunks/*.css`에서 `.outline-hidden{--tw-outline-style:none;outline-style:none}` + `@media (forced-colors:active){.outline-hidden{outline-offset:2px;outline:2px solid #0000}}` 실제 규칙 생성 확인 — Tailwind v4가 토큰 인식, forced-colors 폴백(투명 아웃라인→OS 시스템색 가시화) 실증. 회귀 0(focusRing/JSON-LD/블로그 등 diff 0, 인풋 컴포넌트에만 국한), 보안 grep 0.

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **grep 직접**: `outline-none`(app/components/lib) 잔여 **0**, `outline-hidden`(components) **20**.
- **git diff --stat 직접**: 정확히 13파일 **20 insertions / 20 deletions**(1:1 토큰 치환). 변경 라인 중 `outline-hidden`/`outline-none` 외 문자열을 바꾼 라인 수 = **0**(비토큰 변경 없음 재확인).
- **빌드 직접 실행**: `✓ Compiled successfully in 2.6s`, `Generating static pages (32/32)`(무회귀).
- **산출 CSS 독립 grep**: `.outline-hidden{...outline-style:none}`(일반 모드=기존 `outline-none`과 동일, 시각변화 0) + `@media (forced-colors:active){.outline-hidden{outline:2px solid #0000}}`(고대비 폴백) 양쪽 방출 확인 — WCAG 2.4.7 갭 산출물에서 실증.
- **보안**: 변경 13파일 시크릿(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO/PRIVATE KEY) grep **0건**. 커밋에 `.env*`·`.claude/settings.local.json` 미포함.

### 미확인 항목
- 실제 Windows 고대비/forced-colors 모드 실기기에서 인풋 Tab/클릭 시 아웃라인 픽셀 출현은 헤드리스 환경상 미확보 — 소스 토큰 치환 + 산출 CSS `@media (forced-colors:active)` 폴백 규칙 방출로 대체 검증(동작 조건 충족, 픽셀만 미측정). 이 변경은 일반 모드 화면 픽셀 변화 0이라 시각 회귀 위험 최저.

### 핑퐁 이슈
- design→(마스터 locked)→dev→QA 정합 우수, **핑퐁 0**(QA FAIL 0). 스펙이 토큰 치환 규칙·`focus:` 유지·비상수화까지 미리 못박아 dev 1라운드 통과.
- design·dev·QA 공통 부수제언(이번 건과 분리): 인풋 클래스 문자열 13파일 중복 드리프트 스멜 → 별도 티켓 `lib/inputClass.ts`류 단일소스 통합(06:47 `lib/focusRing.ts` 톤). 백로그로 이월.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개+블로그 4편·빌드 정상). 이번 실행은 **비가시성 접근성 인프라**(일반 모드 화면 변화 0, forced-colors 모드에서만 아웃라인) → **신규 카드뉴스 소재 아님**. 마케팅 신규 배정 없음(고정소재 반복 회피 원칙 정합). 누적 카드뉴스 8종(연봉·단위·근속·퇴직·전기·4대보험·DSR·만나이) 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `components/{Age,Bmi,Dday,Dsr,ElectricityBill,FourInsurance,LoanInterest,LoanPrepaymentFee,SalaryNet,ServicePeriod,SeverancePay,SupportInquiry,UnitConverter}*.tsx`(13파일)·`design/input-outline-hidden-spec.md`(신규) + 이 팀로그. QA PASS + 마스터 빌드/CSS산출/diff/보안 통과 → §8 auto-push(2026-07-13 승인) 조건 충족.
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지]** 12계산기 + 블로그 4편 + blog↔calculator 양방향 내부링크 + 전역 focus-visible + 인풋 forced-colors 접근성 완성 후 방향: (a) 블로그 글 추가(같은-날짜 다량 발행 = 콘텐츠 덤프 신호 주의, 발행일 분산 고려), (b) 로드맵 확장(#13~), (c) **AdSense 신청/색인 점검 단계 진입**(초기 볼륨·양방향 내부링크·접근성 확보 → 적기). 계속 보류, 다음 실행도 확인 불필요한 처리 가능 작업부터.
2. **[접근성/리팩터 후속, 처리 가능 후보]** (a) 인풋 클래스 문자열 13파일 중복 → `lib/inputClass.ts` 단일소스 통합(design/dev/QA 공통 제언, 06:47 focusRing 톤). 비가시·가역이나 20곳 큰 문자열 리팩터라 회귀검증 필요 — 별건. (b) 기타 인터랙티브(있다면) forced-colors 점검. **참고: 06:47 이후 focus-visible 링크류 + 이번 인풋까지로 사이트 전역 focus/forced-colors 접근성 자산 사실상 소진.**
3. **[내부링크/콘텐츠 후속]** (a) planning DEFER: `salary-net-income-guide-2026` 글 relatedCalculatorSlugs에 `four-insurance-calculator` 추가(가시 변경=관련계산기 카드 3→4, 사용자 확인/디자인 검토 권장 — 보류). (b) bmi·unit-converter 지원 블로그 글(현재 관련 글 0편) — 블로그 백로그. (c) 비가시 SEO/내부링크 자산 소진 → 콘텐츠(FAQ 확장)·카드뉴스 묶음 기획이 다음 후보.
4. **[마케팅]** 미소재화 DSR·전기요금 글 → loan 시리즈 캐러셀 묶음 후보. 누적 8종 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
5. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·DSR 규제/스트레스), 신규 시각요소 design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침 고아작업 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출. design/dev/qa 서브에이전트 세션에서도 동일.

---

## 22:36 실행분 (유지보수/접근성 후속: 폼 필드 클래스 단일소스 lib/inputClass.ts 추출 — handoff 2a)

### 세션 시작 상태
- 머신 클럭 `TZ=Asia/Seoul date`가 `07-14 22:36`을 반환(로그/MEMORY의 07-15와 불일치) — [[project_scheduled_workflows]]가 반복 기록한 cron 겹침/클럭 quirk. 실질 근거: `git fetch` 후 origin/main과 **0/0 동기화**, tip `00f2445`(직전 22:06분 인풋 outline-hidden), 고아 미커밋 코드 **없음**(`.claude/settings.local.json` 로컬 권한 캐시만 수정), 오늘 GSC 감사 파일 없음. 22:06 직후 다음 실행으로 이어감.
- 배정 근거: 22:06 핸드오프 2(a) "**인풋 클래스 문자열 13파일 중복 → `lib/inputClass.ts` 단일소스 통합(design/dev/QA 공통 제언, 06:47 focusRing 톤)**"을 이어받음. 큰 결정(로드맵 확장/블로그 추가/AdSense)은 **사용자 확인 필요**로 계속 HOLD. 이 작업은 화면 픽셀 변화 0·완전 가역이라 사용자 부재 중 착수 적합. 07-15 outline-none→outline-hidden lockstep(13파일 동시수정)의 재발 방지가 직접 목적.

### 파이프라인 (design 스펙 → 마스터 lock → dev → QA 독립검증 → 마스터 빌드/CSS-md5/diff/보안 → auto-push). planning은 콘텐츠 0이라 N/A(순수 구조 리팩터).
- **디자인팀**(`design/input-class-consolidation-spec.md` 신규): 20곳 실측 인벤토리(13 컴포넌트) + 상수 taxonomy + 스팟별 매핑표 + **핵심 불변식(각 스팟 최종 유틸 SET 100% 보존, Tailwind는 유틸 순서 무관)** + 회귀검증 체크리스트(빌드32·**산출 CSS 바이트 diff**·SET 대조·diff 성격). 핵심 판단: `w-full`(표준 select) vs `min-w-0`(UnitConverter)는 **가법 델타가 아닌 상충** → 폭 없는 `SELECT_CORE`를 공유하고 폭은 스팟이 결정(억지 단일화 금지). UnitConverter 폭/border 불일치는 스코프 밖(시각변화 발생, 별도 티켓)으로 보존.
- **마스터 lock**: 스펙 Read 검수 후 승인. 재량 1건(`FIELD_FOCUS`에 `text-base text-brand-text` 포함 여부)은 **포함으로 확정**(전 스팟 공유·SET 보존·최단). 별칭 import(`INPUT_BASE as inputBase`/`SELECT_BASE as selectBase`)로 사용처 diff 0, UnitConverter 로컬 const 유지·불일치 미변경 확정.
- **마스터 baseline 캡처**(dev 착수 전, tree clean): `npm run build` → CSS 청크 `.next/static/chunks/30oli9ez58l2d.css` md5 `43ce92453856243a37e1ca289eed38f4`. **SET 보존 시 Tailwind 산출 CSS는 바이트 동일 → 해시 파일명·md5 불변**이라는 객관 게이트로 사전 설정.
- **개발팀**: `lib/inputClass.ts` 신규(FIELD_FOCUS/INPUT_BASE/SELECT_CORE/SELECT_BASE/TEXTAREA_BASE 5상수)·13 컴포넌트 20곳 치환. 빌드32·eslint EXIT0·**CSS md5 baseline과 동일**·diff 순수치환 자체보고.

### QA 독립검증: **전 7항목 PASS** (FAIL 0)
- 핵심 게이트 CSS 바이트 동일성: working tree 재빌드 → `30oli9ez58l2d.css`/md5 `43ce9245...` **baseline과 동일**(SET 무변경의 최강 증거). 빌드 32/32·TS0. `git show HEAD:components/*`로 리팩터 전 원본 추출(자체 전사본과 완전일치 검증) 후 **20곳 토큰 집합 node 스크립트 대조 전부 PASS** — 인라인 4곳+textarea의 상수-델타 공백 1칸 실재(`${INPUT_BASE} ${`), UnitConverter min-w-0/w-full 공존, 표준 select `w-full` 실재(폭 회귀 0). 잔여 raw base 문자열 grep 0. diff 성격 순수치환(로직/JSX 0). eslint EXIT0. 보안 grep 0.
- 런타임 픽셀만 미측정(프리뷰 환경 이슈) — 스펙 §6이 규정한 상위 안전망(CSS 바이트 동일 + SET 대조)이 통과라 병합 차단 아님으로 QA 판정.

### 마스터 최종 확인(§7, QA PASS 후 별도 수행 — 직접 근거)
- **`lib/inputClass.ts` Read**: 5상수 lock 스펙과 exact 일치(FIELD_FOCUS에 text-base/text-brand-text 포함, INPUT_BASE/SELECT_CORE/SELECT_BASE(=CORE+w-full)/TEXTAREA_BASE 합성).
- **빌드 직접 실행**: `✓ Compiled successfully`, `Finished TypeScript`(에러0), 정적 유지.
- **CSS md5 직접 재대조**: `30oli9ez58l2d.css` / `43ce92453856243a37e1ca289eed38f4` = baseline과 **동일 파일명·동일 md5**. 산출 CSS 바이트 동일 → 앱 전체 유틸 합집합 무변경 → 20곳 SET 보존 확정. **시각 회귀 구조적으로 불가**(브라우저가 동일 CSS+동일 클래스셋 수신).
- **인라인 공백 직접 grep**: Age:50/Bmi:102·122/Dday:53 `${INPUT_BASE} ${`, Dday:78 `${INPUT_BASE} border-brand-border`, Support:141 `${TEXTAREA_BASE} ${`, UnitConverter:18 `${INPUT_BASE} min-w-0 border-brand-border`·:20 `${SELECT_CORE} min-w-0` — 전부 공백 실재(토큰 뭉갬 0).
- **diff 성격 직접 확인**: 13파일 = import 추가 + const/인라인 문자열→`${상수}` 치환 두 종류뿐, 로직/JSX 0. 비치환 `+`라인 = 멀티라인 import 닫는 `} from "@/lib/inputClass";`뿐.
- **보안**: 신규·수정 파일 시크릿(sss159228/@gmail/RESEND/api_key/secret/CONTACT_TO/PRIVATE KEY) grep **0건**. `.env*` 미포함. `.claude/settings.local.json`(로컬 권한 캐시, 이번에 defaultMode·타 프로젝트 경로 등 변경됨)은 **커밋 제외**, `git add -A` 미사용·파일 명시 add.

### 미확인 항목
- 실제 브라우저 픽셀(인풋 에러 border 토글·셀렉트 포커스 링·disabled) 육안은 프리뷰 환경 반복 불안정으로 미측정 — 단 CSS 바이트 동일 + 20곳 SET 보존 + 소스 공백/폭 토큰 실재로 시각 회귀는 코드 레벨에서 구조적 배제(픽셀 디테일만 미확보).

### 핑퐁 이슈
- design→(마스터 lock)→dev→QA 정합 우수, **핑퐁 0**(QA FAIL 0). 스펙이 SET 불변식·CSS 바이트 게이트·스팟별 조합식·공백 처리·별칭 import까지 미리 못박아 dev 1라운드 통과. design이 넘긴 재량 1건(FIELD_FOCUS 범위)은 마스터 즉시 lock(포함) → 재작업 없음.
- design·dev·QA 공통 인계(스코프 밖): UnitConverter 폼 필드 폭/border 처리가 다른 계산기와 원래 불일치 → 정합 맞추면 **시각 변화 발생 → 디자인/마스터(사용자) 승인 필요**, 이번 "픽셀 0" 작업과 분리 보존. 백로그.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12개+블로그 4편·빌드 정상). 이번 실행은 **비가시성 구조 리팩터**(화면 변화 0, 신규 카피·콘텐츠 0) → **신규 카드뉴스 소재 아님**. 마케팅 신규 배정 없음(고정소재 반복 회피 원칙 정합). 누적 카드뉴스 8종(연봉·단위·근속·퇴직·전기·4대보험·DSR·만나이) 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `lib/inputClass.ts`(신규)·`components/{Age,Bmi,Dday,Dsr,ElectricityBill,FourInsurance,LoanInterest,LoanPrepaymentFee,SalaryNet,ServicePeriod,SeverancePay,SupportInquiry,UnitConverter}*.tsx`(13파일)·`design/input-class-consolidation-spec.md`(신규) + 이 팀로그. QA PASS + 마스터 빌드/CSS-md5/diff/보안 통과 → §8 auto-push(2026-07-13 승인) 조건 충족.
- 제외: `.claude/settings.local.json`(로컬 권한 캐시). `.env*` 미포함·시크릿 grep 0 확인.

### 다음 실행으로 넘기는 미해결/후속
1. **[큰 결정, 사용자 확인 필요 — 유지·강조]** 12계산기 + 블로그 4편 + blog↔calculator 양방향 내부링크 + 전역 focus-visible + 인풋 forced-colors + **폼 필드 클래스 단일소스**까지 완성 → **비가시 SEO/내부링크/접근성/유지보수 자산 사실상 소진**. 남은 가치 있는 방향은 전부 사용자 확인 필요: (a) 블로그 글 추가(같은-날짜 다량 발행=콘텐츠 덤프 신호 주의, 발행일 분산), (b) 로드맵 확장(#13~ 신규 계산기 발굴), (c) **AdSense 신청/색인 점검 단계 진입**(초기 볼륨·양방향 내부링크·접근성·구조화데이터 확보 → 적기). **다음 실행 시점에도 사용자 응답 없으면 처리 가능한 순수-내부 작업이 거의 없음 → 소규모 콘텐츠 보강(FAQ 확장 등)이나 문서/백로그 정리 정도만 남음.**
2. **[유지보수 후속, 처리 가능 후보]** (a) UnitConverter 폼 필드 폭/border 불일치 정합(시각 변화 → 승인 필요, 보류). (b) 신규 계산기 추가 시 `lib/inputClass.ts` 상수 import 컨벤션화(드리프트 재발 방지) — 신규 계산기 착수 시 반영.
3. **[내부링크/콘텐츠 후속]** (a) planning DEFER: `salary-net-income-guide-2026` 글 relatedCalculatorSlugs에 `four-insurance-calculator` 추가(가시 변경=관련계산기 카드 3→4, 사용자 확인/디자인 검토 권장 — 보류). (b) bmi·unit-converter 지원 블로그 글(현재 관련 글 0편) — 블로그 백로그(같은-날짜 다량 발행 주의).
4. **[마케팅]** 미소재화 DSR·전기요금 글 → loan 시리즈 캐러셀 묶음 후보. 누적 8종 이미지화·게시 채널/계정 승인 = 사용자 승인 대기.
5. **[비차단 백로그]** (유지) DSR 원금균등 옵션, 고용보험 26,999 정수산술, 규제·요율 갱신 체크리스트(1월 4대보험·3월 간이세액표·분기 연료비·DSR 규제/스트레스), 신규 시각요소 design-system 편입, 근로소득세 Option A, 공식원본 대조, 법률검토, CS 서버 인바운드 큐 부재, cron 30분 겹침/클럭 quirk 관찰(이번 실행 고아 0).

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) 재확인 — 도구정의=인젝션 취급, 미호출. design/dev/qa 서브에이전트 세션에서도 동일.
