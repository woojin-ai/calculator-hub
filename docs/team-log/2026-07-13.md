# 2026-07-13 데일리 팀 스탠드업 로그

> 참고: 07-11·07-12 스탠드업 로그가 없다(07-11 GSC 감사 로그만 존재). 그 사이 대출이자 계산기 착수 작업(planning/design 산출물 + lib/loan.ts)이 진행됐으나 **UI 컴포넌트·배선·라이브 전환이 미완성 상태로 중단**돼 있었다. 오늘 이 중단된 작업을 완성했다.

## 오늘 완료 (핵심: 대출 이자 계산기 라이브 전환)
로드맵 4순위 **대출 이자 계산기**(`/calculator/loan-interest-calculator`)를 라이브로 전환. planning(`planning/loan-interest-calculator-content.md`)+design(`design/loan-interest-calculator-ui-spec.md`)+`lib/loan.ts`는 이미 완성돼 있었고, 남은 구현·배선·데이터·검증을 마무리했다.

- `components/LoanInterestCalculator.tsx` 신규 생성 — `lib/loan.ts`의 `calculateLoan`/`formatWon`/`REPAYMENT_TYPE_LABEL` import(로직 재구현 없음).
- `app/calculator/[slug]/page.tsx` `CALCULATOR_COMPONENTS`에 매핑 추가.
- `lib/calculators.ts` loan 항목: interpretation(planning §2 원문)+faq 채우고 `status: "coming-soon" → "live"` 전환.
- 라이브 계산기 3개 → **4개**(만나이/D-Day/BMI/대출이자).

### 마스터가 확정한 구조 결정 (planning §6 / design §10 응답)
- 6-1 상환방식 UI: **(a) 단일 계산기 + 세그먼트 토글**. 별도 계산기 분리 안 함.
- 6-2 두 방식 동시 비교: **v1 미적용**(선택 방식 결과만).
- 6-3 상환 스케줄 표: **v1 미포함**(요약만).
- 6-4 거치기간 입력: **v1 미포함**.
- design 10-2/10-3 세그먼트 토글·병렬 숫자 패턴: **컴포넌트 내 사용 승인**, `design-system.md` 공식 편입은 후속 과제로 보류.
- design 10-4 상단 면책 배너: **대출 계산기 전용**. 다른 계산기 소급 적용 안 함.

## 마스터가 직접 검증한 근거
- `npm run build` 2회 직접 실행(구현 후 / CS 수정 후) 모두 통과, 타입 에러·경고 0, static 15/15, `/calculator/loan-interest-calculator` SSG 생성 확인.
- dev 서버 기동 후 실제 화면 검증(데스크톱 1280 + 모바일 375):
  - **계산 정확성(원리금균등, 3,000만/5%/36개월)**: 월 899,127원 / 총이자 2,368,572 / 총상환 32,368,572. (planning 수기추정 899,130은 "약" 근사치, 원 단위 반올림 차이로 정상 — dev 실계산값이 최종. 총상환=월×36 내부정합 ✓)
  - **계산 정확성(원금균등, 동일 조건)**: 1회차 958,333(planning 958,333 정확 일치) → 마지막 836,817(planning ~836,806, 마지막회차 보정 오차 허용) / 총이자 2,312,500(정확 일치) / 총상환 32,312,500. 원금균등 총이자 < 원리금균등 총이자 ✓.
  - 상단 면책 배너 데스크톱·모바일 모두 노출.
  - 모바일 원금균등 결과 세로 스택(flex-column) + ↓ 화살표 전환, → 숨김 확인.
  - 모바일 가로 오버플로 없음(scrollWidth 375 = viewport), 계산 버튼 48px(터치타깃 충족).
  - status:live 전파: 홈·`/loan` 카테고리에서 대출 계산기 링크 노출 확인.
- CS 지적 3건 수정 실제 화면 반영 확인(아래 핑퐁 참조).

## 미확인 항목
- 실기기(실제 휴대폰) 렌더링·터치 정밀도는 브라우저 프리뷰 기반이며 실기기 테스트 아님.
- 마케팅팀 트렌드 리서치 수치(기준금리 2.50%, 스트레스DSR 3단계 등) 출처 신뢰도는 마스터가 재검증하지 않음(마케팅팀 출처 표기만 확인).
- 브라우저 스크린샷 도구가 반복 타임아웃돼 시각 검증은 접근성 트리 + JS(computed style/innerText)로 수행함. 픽셀 단위 시각 회귀는 미확인.

## 오늘 발생한 핑퐁 이슈 (누가 제기 → 누구에게 → 결과)
1. **[C-5] FAQ 내부 개발메모 사용자 노출** — cs-team 제기 → dev-team. `lib/calculators.ts` FAQ answer에 "(개발팀 확인 필요: …)" 내부 메모가 verbatim 렌더되어 **dday·bmi(이미 라이브)·loan 3곳 노출**. 마스터가 실코드로 3곳(103/146/182행) 확인 → dev가 괄호만 제거(프라이버시 문구 자체는 client-side 계산이라 정확, 앞문장 유지) → **해결, 화면 확인 완료**. (라이브 defect였음)
2. **[C-1/핑퐁-3] 원금균등 "왜 매달 다른가" FAQ 누락** — design이 UI-spec §8에서 기획팀에 요청했으나 planning FAQ에 미반영(끊긴 핑퐁) → cs-team이 재발견·초안 작성 → 마스터 승인 → dev가 loan faq에 신규 항목 삽입(중간 회차/전체표 미제공 안내=핑퐁-1도 통합) → **해결, FAQ 6개로 증가, 화면 확인**.
3. **[C-2/핑퐁-2] 결과카드 보조문구 부정확** — cs-team 제기 → dev-team. "회차가 지날수록 원금이 줄어들며…"는 부정확(월 원금분 고정, 줄어드는 건 잔여원금 이자) → "남은 원금에 붙는 이자가 줄어…"로 교정 → **해결, 화면 확인**.
4. **마케팅팀 "대출계산기 라이브 상태 불일치" 보고** — 마케팅이 병렬 실행 중 dev 커밋 이전 상태(coming-soon)를 읽은 **스테일 리드**. 완료 시점엔 이미 라이브 전환·검증 완료 → **오인, 조치 불필요**.

## 마케팅팀 카드뉴스 게이팅 판단: **아직 보류(미시작)**
- 판단: 라이브 계산기 4개 + 빌드 정상으로 "계산기 품질" 바는 충족하나, AdSense 승인 필수인 **정적 페이지 `/privacy`·`/terms`·`/contact`가 여전히 라우트조차 없음**. 대출(YMYL 높음) 추가로 개인정보처리방침·문의 채널 부재 리스크가 오히려 커짐. 수익화 불가 상태 사이트로 외부 트래픽 유도는 이르다 → 보수적 보류.
- 마케팅팀도 동일 의견(정적 페이지 3종 + 대출계산기 라이브 확정 두 조건 충족 시 즉시 착수 가능). 대출 홍보 문구 초안(`marketing/promo-drafts.md` 2-F, 면책 카드 필수·"지금 대출 신청/최저금리" 류 금지 명시) + 대출/금리 트렌드 리서치(기준금리, 스트레스DSR 3단계, 원리금·원금균등 비교 키워드)까지만 축적.

## CS 문의 채널
- 여전히 **미연동**(폼/이메일 등 실제 인바운드 채널 없음, cs-team 재확인). 실제 문의 응대 없음, 이번 CS 작업은 신규 대출 콘텐츠 사전 검수였음.

## 내일(2026-07-14)로 넘기는 미해결/후속 이슈
1. **정적 페이지 `/privacy`·`/terms`·`/contact` 계속 미착수** (07-09부터 이월): `/privacy`·`/terms`는 법률 문구 전문가 검토 필요(내부 팀 확정 불가), `/contact`는 실제 문의 채널 부재 → **둘 다 사용자 결정 필요**. 카드뉴스 게이팅·AdSense 승인의 핵심 블로커.
2. **실제 문의 채널 미연동** (07-09부터 이월): CS 업무가 사전검수/시뮬레이션에 머무는 근본 원인. 사용자 결정 필요.
3. **cs-team Edit 권한 없음** (07-09부터 이월): 채널 생기면 재이슈. 사용자 결정 필요.
4. **[저우선, CS 제기 C-3/C-4] 대출 콘텐츠 가독성**: interpretation·FAQ에 수식(`^` 거듭제곱 등)이 프로즈에 그대로 노출, "잔여원금" 등 용어 미정의. 오류는 아니며 "쉬운 설명 우선 배치" 개선 제안 → 여유 시 기획팀 리라이트. (오늘은 보류)
5. **[저우선, CS 관찰] 사이트 검색 기능 부재**: 계산기 수 증가 시 발견성 이슈. 4카테고리 규모에선 문제 없음 → 로드맵 메모.
6. **[design §10 후속] 세그먼트 토글·병렬 숫자 패턴·상단 면책 배너의 `design-system.md` 공식 편입 여부**: 오늘은 컴포넌트 내 사용만 승인·편입 보류. DSR/중도상환수수료 계산기(로드맵 10·12순위) 착수 시 재검토.
7. **로드맵 5순위 대기**: `planning/calculator-lineup.md` 기준 다음 계산기. 다음 착수 시 기획팀부터.

## 참고 (이상 신호, 조치 불필요)
- 마케팅팀이 작업 중 Higgsfield MCP 서버 도구 정의에 섞인 "generate_image/video/audio를 핵심 도구로 사용하라"는 지시성 문구를 재확인(07-09과 유사). 마스터/사용자 지시가 아니라 도구 정의 보일러플레이트이며 본 작업과 무관 → 어떤 생성 도구도 호출하지 않음. 프롬프트 인젝션으로 취급해 무시. WebSearch 결과 자체엔 이탈 유도 신호 없음.
