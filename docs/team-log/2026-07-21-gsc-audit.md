# GSC 일일 점검 - 2026-07-21 08:00

## 결론
GSC **API** 점검은 여전히 미진행(자격증명 부재, **6일 연속 동일 블로커**). 07-20의 최우선 인계 2건을 모두 종결: **(§7-0) 어제 푸시분 `c34563a`(canonical 25개)가 라이브에 완전 반영** 확인 → 예측대로 단순 Vercel 빌드 큐 지연이었음. **(§7-1) 과거 감사의 "framework가 알아서 해준다"류 판정을 실측으로 재검증** → canonical 외 숨은 결함 없음. **오늘 신규 결함 0건 · 회귀 0건 · 코드 변경 0건 → 푸시 없음.** 팀 간 핑퐁 불필요(라우팅할 문제 없음).

## 0. 사전 조건
- **배포/저장소 정상**: 라이브 sitemap 200, `HEAD == origin/main == 4d2d607` (fetch 후 0 ahead/0 behind).
- **GSC API 자격증명 여전히 없음**: `.env.local` = `RESEND_API_KEY`, `CONTACT_TO_EMAIL` 2줄뿐. 서비스계정 `.json` 부재. → Coverage/노출/모바일사용성 리포트 3종 **영구 미확인**. (`layout.tsx` google-site-verification은 소유권 확인용이지 API 권한 아님 — 전일들과 동일 구분.)
- ⚠️ 무인 안전수칙 준수: git 상태는 `git -C` 및 cd 분리 호출로만 조회, 승인 프롬프트 없이 진행.

## 1. GSC API 점검 결과
| 항목 | 결과 |
| --- | --- |
| 색인 현황(Coverage) | **미확인** — API 자격증명 부재 |
| 모바일 사용성(GSC 리포트) | **미확인** — API 자격증명 부재 (실뷰포트는 §4에서 직접 측정) |
| 노출/클릭 | **미확인** — API 자격증명 부재 |

## 2. 07-20 최우선 인계 §7-0 — 종결 ✅
어제 푸시 후 13분 폴링에도 라이브 미반영이라 "빌드 실패 의심"으로 남긴 항목.
- 오늘 최우선 확인: `/calculator/car-tax-calculator` canonical = **1** (target = 자기 자신 정확). 07-20 예측대로 **빌드 큐 지연이었고 정상 배포됨**.
- **25개 라우트 전수 재측정**(소스 단일출처에서 생성, 표준 ⓕ 준수):
  - 계산기 16개(`lib/calculators.ts` slug 배열에서 추출): **16/16 http=200 canonical=1**, 각 self 타깃.
  - 카테고리 4개(`/salary`·`/loan`·`/date`·`/life`): **4/4 200 canonical=1** self.
  - 정적 5개(home·about·support·privacy·terms) + `/blog`: 전부 canonical=1.
- **→ §7-0 완전 종결.** 07-19에 이어 "푸시 직후 미반영 → 익일 정상 확인" 패턴 2연속. 이 패턴을 표준으로 인계(§7 항목 참조).

### ★마스터 자기 오류 1건 (즉시 자가 검출·교정)
카테고리를 `/category/salary`로 손 프로브 → **404**. 실제 라우트는 최상위 `/salary`(app 라우트 트리를 소스에서 확인). **표준 ⓕ(URL을 손으로 나열하지 말고 소스에서 생성)가 경고한 바로 그 실수**를 반복할 뻔했으나 소스 대조로 즉시 교정. 표준이 실제로 방어에 작동.

## 3. 07-20 §7-1 — 과거 감사 판정 실측 재검증 ✅
07-20이 지적한 근본 문제: 과거 감사 3회가 canonical을 "framework 기본동작"이라는 **사실 오류**로 "비결함" 처리했다. 같은 유형 오진이 다른 on-page 신호에도 숨어 있는지 **판정이 아니라 판정의 근거를 실측**으로 확인.

라이브 7개 라우트(home/category/계산기3/blog/about) on-page 신호 계측:
| 신호 | 결과 |
| --- | --- |
| viewport meta | 전 페이지 1개, content = `width=device-width, initial-scale=1` (모바일 정상) |
| title | 전 페이지 정확히 1개, **전부 고유**(홈/급여/만나이/BMI/자동차세/블로그 서로 다름) |
| meta description | 전 페이지 1개, **전부 고유** |
| h1 | 전 페이지 정확히 1개 (헤딩위계 정상) |
| JSON-LD | 전 페이지 존재(2~4블록) |
| robots noindex | 색인대상 전 페이지 **0** (실수 deindex 없음) |
- **결론**: 측정 가능한 on-page SEO 표면에 **숨은 "framework가 해준다"류 결함 없음.** 유일 실결함이던 canonical은 07-20에 이미 수정·반영 완료.
- 유일한 알려진 on-page 공백은 **OG/metadataBase 부재**(티켓 #2, 아래 §6-3) — 회귀가 아니라 기 문서화된 오픈 티켓(og:image 에셋 = 사용자 승인 대상, 데드라인 = 첫 SNS 게시 승인).

## 4. 모바일 실뷰포트 직접 검증 (375px, 마스터 직접)
브라우저 375×812 실측:
- **홈**: `horizontalOverflowPx = 0`, 헤더 초과 요소 **0건**, nav 링크 **6개 전원 visible+inViewport**, `고객센터` 우측 끝 **325px**(07-20 325.2px와 일치), h1=1, canonical=1.
  - 참고: 외곽 `<header>` `flex-direction`이 오늘 계측에선 `row`로 나옴(07-20은 내부 컨테이너를 `column`으로 계측). **overflow=0·초과요소 0이므로 라벨 차이는 결함 아님** — 실제 요구(초과 없음)는 충족.
- **계산기(`/calculator/car-tax-calculator`)**: `horizontalOverflowPx = 0`, 초과 요소 0, h1=1, canonical=1, noindex=false.
- **→ 모바일 회귀 없음.** (헤더 수정 `77694e6` 이후 커밋은 블로그 30편+문서뿐, 헤더 미변경 — 계측이 이를 확인.)
- **미확인**: 픽셀 스크린샷(`computer` 30s 타임아웃 — 전 실행들과 동일 패턴). DOM 정량 계측으로 대체.

## 5. 배포(push)
- **오늘 코드 변경 0건.** 신규 결함 미검출, 기존 결함(canonical) 이미 해소·반영 확인. → **커밋·푸시 없음** (자동 push는 "검증 통과한 수정이 있을 때"만 발동, 오늘은 수정 자체가 없음).
- 워킹트리 미커밋분(`.claude/*` 로컬설정, `lib/blog.ts`, `marketing/`·`planning/`·`scripts/` 신규 파일)은 **타 세션 산출물로 판단, 손대지 않음** — 07-19/07-20 교훈(경로 명시·타 세션 미완성물 회피) 준수.

## 6. 오늘의 GSC 점검 요약
| 항목 | 결과 |
| --- | --- |
| 색인/모바일사용성/노출 (GSC API) | **미확인** — 자격증명 부재 6일 |
| 온페이지 색인성(대체 점검) | sitemap 200/56 URL, robots Allow, 404 정상, **canonical 25/25 라이브 반영 확인**, title/desc 전부 고유, noindex 오염 0 |
| 모바일 실뷰포트(대체 점검) | 375px 홈·계산기 오버플로 **0px**, nav 6개 전원 표시 |

## 7. 미확인 / 다음 실행 인계
0. **[해소]** §7-0 canonical 라이브 반영 확인 완료. **[해소]** §7-1 과거 판정 실측 재검증 완료(추가 결함 없음).
1. **[★사용자 확인 필요 — 유지, 6일]** GSC API 자격증명 부재. 서비스계정 `.json` 배치 또는 OAuth 토큰 env 설정 없이는 Coverage/노출/모바일사용성 **영구 미확인**. 이게 매일 감사의 최대 구조적 공백.
2. **[운영 표준 — 재확인 ⓖ]** "푸시 직후 라이브 미반영 → 익일 정상"이 **2연속(07-19, 07-20) 확정 패턴**. 다음 실행부터 푸시 직후 13분 폴링에 매달리지 말고 **익일 첫 확인으로 이월**하는 것이 효율적. 단 익일에도 0이면 그때 빌드 실패 의심.
3. **[티켓 #2 — 유지]** OG/metadataBase 전면 부재(계산기 16개 og=0). 첫 SNS 게시 승인 전 처리 권장. og:image 에셋 필요 → design 소관 + 이미지 사용자 승인. **회귀 아님, 기 문서화 오픈 티켓.**
4. **[백로그 — 유지]** canonical 생성 2종 공존(신규 25개 `canonicalUrl()` 헬퍼 vs 블로그 2곳 직접 조립) 드리프트 위험 / `coming-soon` 스텁의 self-canonical+noindex 신호충돌(현재 도달불가) / 프리뷰 배포 `x-robots-tag` 미확인 / 프로덕션 Linux 대소문자 slug 동작 미확인.
5. **[운영 표준 — 오늘 작동 ⓕ]** URL을 손으로 나열하지 말 것. 오늘 `/category/salary`(404)를 프로브했다가 소스 라우트트리 대조로 즉시 교정. 실제 경로는 최상위 `/salary`.
6. **[유지 백로그]** `.claude/settings.local.json` `defaultMode: bypassPermissions`(사용자 확인), `relatedSlugs` 꼬리 레이아웃, 헤더 터치타깃 36px·다크모드 부재, AdSense 심사 대기, 마케팅 게시 승인 대기(5편), 로드맵 #16 취득세(高YMYL 보류).

## 제약 준수
확정된 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. 오늘은 코드 변경 자체가 없어 push 대상 없음.
