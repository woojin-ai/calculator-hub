# GSC 일일 점검 - 2026-07-15 08:00

## 결론: GSC **API** 점검은 여전히 미진행(연동 정보 없음). 대신 07-14 대비 **크게 성장한 라이브 사이트**(sitemap 15→26 URL)의 색인 인프라를 마스터가 직접 회귀 검증 → **결함 0**. 추가로 이번 실행은 **실제 375px 모바일 뷰포트 렌더까지** 확인(HTTP/메타 레벨을 넘어섬).

## 0. 사전 조건 (재검증 근거)
- **배포/저장소 정상**: `origin = github.com/woojin-ai/calculator-hub.git`, 라이브 `https://calculator-hub-delta.vercel.app` 응답 정상.
- **GSC API 인증정보 여전히 없음**: `.env.local` = `RESEND_API_KEY`, `CONTACT_TO_EMAIL` 2줄뿐(89B). 서비스계정 키(.json/.pem) 없음, `env`에 Google/GSC/OAuth/service_account 값 없음. → GSC API 조회 불가.
- ⚠️ `layout.tsx`의 `google-site-verification`은 소유권 확인용이며 API 접근 권한 아님(07-14와 동일 구분).

## 1. GSC API 점검 결과
- 색인 현황(Coverage): **미확인** (API 접근 불가)
- 모바일 사용성 리포트: **미확인** (API 접근 불가) — 단 온페이지 요소는 2·3번에서 직접 확인
- 노출/클릭 데이터: **미확인** (API 접근 불가)

## 2. 마스터 직접 검증 — 라이브 색인 인프라 (전부 통과)
기준: `https://calculator-hub-delta.vercel.app`
- **홈** `GET /` → 200, 55KB. viewport 메타(`width=device-width`) 서빙 확인, google/naver site-verification 태그 서빙 확인.
- **sitemap.xml** → 200, **URL 26개**(07-14는 15개). 정적 10 + 라이브 계산기 12 + 블로그 4. `app/sitemap.ts` 소스 및 실제 라우트 트리(`find app -name page.tsx`)와 **정확히 일치** — 누락/고아 라우트 0. `api/contact/route.ts`는 비색인 대상이라 정상 제외. coming-soon 스텁 0개.
- **robots.txt** → 200, `User-Agent:* / Allow:/` + `Sitemap:` 지정. 크롤 차단 없음.
- **신규 페이지 타입 색인성**(07-14 이후 추가분):
  - `/calculator/dsr-calculator`(로드맵 #12 최종), `/calculator/four-insurance-calculator` → 200 + `robots: index, follow` 명시.
  - `/blog`, `/blog/dsr-loan-limit-guide-2026`(canonical 있음), `/salary` 카테고리 → 200, 색인 가능(robots 메타 없음 = 기본 색인).
- **404 가드**: `/calculator/nonexistent-xyz`, `/blog/nonexistent-post` → **404 + noindex**. 소프트-404/thin content 색인 위험 없음.

## 2b. 모바일 375px 실제 렌더 검증 (이번 실행 신규, HTTP/메타 레벨 초과)
브라우저 뷰포트 375×812로 실측:
- **홈**: `docWidth==innerWidth==375`, **가로 오버플로 없음**.
- **DSR 계산기**(최신 페이지): `docWidth==innerWidth==375`, 가로 오버플로 없음, h1="DSR 계산기", input 5개 정상 렌더.
- ※ 스크린샷 캡처는 렌더러 타임아웃으로 실패했으나, DOM 폭 측정(정량)으로 오버플로 없음 확정.

## 3. 발견된 문제 / 핑퐁 루프
- **SEO/색인 결함 0건.** 라우팅할 이슈 없음 → planning/marketing/dev 핑퐁·TaskCreate·qa-team 검증 미발동(정상 — 대응할 문제 없음).
- (비결함 메모) `/blog` 목록·카테고리 랜딩엔 명시적 canonical 없음(블로그 상세엔 있음). GSC 문제 아님(자기 canonical 처리됨) → 미조치.

## 4. 배포(push)
- 코드 수정 없음(점검·검증만) → 커밋/푸시 대상 없음. 자동 push 미실행.
- working tree: `.claude/settings.local.json`(M)만 존재 — GSC 감사 소관 아님, 미터치.

## 5. 미확인 / 다음 실행 인계
- **GSC API 텔레메트리 3종(Coverage·노출/클릭·Mobile Usability)은 계속 미확인.** 실조회하려면 사용자가 GSC 서비스계정 키(.json)를 프로젝트에 두거나 OAuth 토큰을 env에 설정해야 함.
- 온페이지 인프라 + 모바일 렌더는 오늘 정상. 다음 실행: (a) GSC 크리덴셜 생겼는지 먼저 확인 → 있으면 실 API 점검 시작, (b) 없으면 오늘처럼 라이브 인프라 회귀(sitemap 개수/robots/viewport/신규 라이브 페이지 색인성/404 가드) + 375px 오버플로 재확인.
- 실기기 픽셀 단위 회귀·스크린샷 시각 검증은 렌더러 타임아웃으로 미수집(DOM 정량 측정으로 대체).
