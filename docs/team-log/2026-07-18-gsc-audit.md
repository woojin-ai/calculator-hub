# GSC 일일 점검 - 2026-07-18 08:00

## 결론: GSC **API** 점검은 여전히 미진행(연동 정보 없음). 대신 07-16 대비 **다시 성장한 라이브 사이트**(sitemap 35→53 URL, 블로그 9→27편)의 색인 인프라를 마스터가 직접 회귀 검증 → **결함 0**. 최신 블로그·계산기를 실제 375px 뷰포트에서 렌더 실측(문서 가로 오버플로 0). 코드 수정 없음 → push 없음. (07-17 로그 없음 — 이번 실행이 07-16을 직접 이어받음)

## 0. 사전 조건 (재검증 근거)
- **배포/저장소 정상**: 라이브 `https://calculator-hub-delta.vercel.app` sitemap `GET` → 200.
- **GSC API 인증정보 여전히 없음**: `.env.local` = `RESEND_API_KEY`, `CONTACT_TO_EMAIL` 2줄뿐. 프로젝트 루트 `.json`은 package/tsconfig뿐(서비스계정 키 아님). `GSC_/SEARCH_CONSOLE/service_account/GOOGLE_APPLICATION/searchconsole` grep(node_modules 제외) → 히트 0. → GSC API 조회 불가.
- ⚠️ `layout.tsx`의 `google-site-verification`은 소유권 확인용이며 API 접근 권한 아님(전일과 동일 구분).

## 1. GSC API 점검 결과
- 색인 현황(Coverage): **미확인** (API 접근 불가)
- 모바일 사용성 리포트: **미확인** (API 접근 불가) — 단 온페이지/실뷰포트 요소는 2·2b에서 직접 확인
- 노출/클릭 데이터: **미확인** (API 접근 불가)

## 2. 마스터 직접 검증 — 라이브 색인 인프라 (전부 통과)
기준: `https://calculator-hub-delta.vercel.app`
- **sitemap.xml** → 200, **URL 53개**(07-16는 35개, 07-15는 26개). 내역: 홈 1 + 정적 9 + 라이브 계산기 **16** + 블로그 **27**. `app/sitemap.ts`가 `lib/calculators`(status==="live")·`lib/blog` 데이터 기반 생성 → 라이브 URL 유형별 집계와 **일치**(고아/coming-soon 스텁 0). **가장 큰 변화는 블로그 9→27편**(최근 커밋 age-by-birth-year·loan-prepayment-break-even·area-pyeong-sqm 등 대량 콘텐츠 확장).
- **robots.txt** → 200, `User-Agent:* / Allow:/` + `Sitemap:` 지정. 크롤 차단 없음.
- **신규 페이지 색인성**(07-16 이후 추가분 샘플):
  - `/blog/age-by-birth-year-2026`, `/blog/loan-prepayment-break-even-2026`, `/blog/area-pyeong-sqm-conversion-2026` → 200 + 자기참조 `canonical` 있음 + viewport 정상. (blog 상세는 robots 메타 없음 = 기본 index, 07-16과 동일 설계)
  - `/calculator/unit-converter` → 200 + `robots: index, follow` 명시 + viewport 정상.
- **404 가드**: `/blog/nonexistent-xyz`, `/calculator/nonexistent-xyz` → **404 + noindex**. 소프트-404/thin content 색인 위험 없음.

## 2b. 모바일 375px 실제 렌더 검증
브라우저 뷰포트 375×812 실측:
- **최신 블로그** `/blog/age-by-birth-year-2026`: `scrollW==innerW==375`, **문서 가로 오버플로 0px**, h1 정상. 초과폭 요소 1건은 `<nav overflow-x:auto>` 안의 "고객센터" 링크(sw=309>cw=251) — **의도된 가로 스크롤 링크 행**(사이트 공통 nav 패턴)이라 문서 본문은 넘치지 않음. Google '콘텐츠가 화면보다 넓음' 사용성 결함 아님.
- **최신 계산기** `/calculator/unit-converter`: `scrollW==innerW==375`, **문서 가로 오버플로 0px**, 스크롤 컨테이너 내부 요소 제외 시 **진짜 초과폭 요소 0건**, h1="단위 변환 계산기".
- **콘솔 에러 0건**(onlyErrors 조회).
- ※ 스크린샷 캡처는 이전 실행들처럼 안 시도(렌더러 타임아웃 이력) → DOM 정량 측정(overflowPx/trueOverflowElems)으로 오버플로 없음 확정.

## 3. 발견된 문제 / 핑퐁 루프
- **SEO/색인 결함 0건.** 라우팅할 이슈 없음 → planning/marketing/dev 핑퐁·TaskCreate·qa-team 검증 미발동(정상 — 대응할 문제 없음).
- (비결함 메모) 계산기 상세엔 명시적 `canonical` 없음(자기참조 canonical 기본동작, 신·구 전부 동일) — 기존 설계, GSC 위험 아님. 블로그 상세엔 canonical 있음.

## 4. 배포(push)
- 코드 수정 없음(점검·검증만) → 커밋/푸시 대상 없음. 자동 push 미실행.
- working tree: `.claude/settings.local.json`(M) + `.claude/settings.json`(신규) + team-log만 — GSC 감사 소관 코드 아님.

## 5. 미확인 / 다음 실행 인계
- **GSC API 텔레메트리 3종(Coverage·노출/클릭·Mobile Usability)은 계속 미확인.** 실조회하려면 사용자가 GSC 서비스계정 키(.json)를 프로젝트에 두거나 OAuth 토큰을 env에 설정해야 함. (3일+ 연속 동일 블로커)
- 온페이지 인프라 + 375px 모바일 렌더는 오늘 정상. 다음 실행: (a) GSC 크리덴셜 생겼는지 먼저 확인 → 있으면 실 API 점검 시작, (b) 없으면 오늘처럼 라이브 인프라 회귀(sitemap 개수·데이터 일치/robots/viewport/신규 라이브 페이지 색인성/404 가드) + 375px 오버플로 재확인.
- **블로그 27편으로 급증** — 향후 GSC 연동되면 신규 블로그들의 실제 색인/노출 여부를 최우선 확인할 것(대량 신규 URL은 색인 지연·thin content 판정 리스크 구간).
