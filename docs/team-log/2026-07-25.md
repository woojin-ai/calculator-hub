# 2026-07-25 데일리 팀 스탠드업 로그

## 01:0x 실행분 (오늘 첫 실행)

### 컨텍스트 파악
- **07-24에는 daily-team-standup이 실행되지 않았다** — `docs/team-log/2026-07-24.md` 부재, `2026-07-24-gsc-audit.md`만 존재. 즉 이번 실행의 인계 원본은 **07-23 로그의 "다음 실행 인계"** + 07-24 GSC 감사 로그다.
- git status: `.claude/launch.json`·`.claude/settings.local.json`·`.claude/settings.json`(untracked)·`marketing/promo-drafts.md` 미커밋 — 07-23과 동일한 타 세션 산출물이라 이번에도 **미터치**. `.env*` 없음.
- 라이브 상태(실측): 블로그 34편(salary 9 · life 9 · date 8 · loan 8), 계산기 16개, static pages 67, sitemap 60 URL.
- 07-24 GSC 감사가 34편 `<title>` 69→54자 회귀를 이미 정정·배포 완료(`d6f577e`). 중복 지시 없음.

### ★ 07-24 감사에서 승격된 신규 발행 표준 (이번 회차부터 적용)
`app/blog/[slug]/page.tsx`가 `${post.title} | 계산기 허브`(접미사 9자)로 조립하므로 **post.title ≤51자**여야 렌더 `<title>` ≤60자(SERP 잘림 회피)를 지킨다. 34편이 이 규칙 없이 발행돼 회귀가 났다. **이번 회차부터 기획 지시서에 명시적 lint 항목으로 포함**했고, QA 항목으로도 배정했다.

### 배정: 블로그 35편 (축1 = 카테고리 균형 + 검색수요)
- **planning-team** → `planning/blog-post-35-spec.md` 작성.
  - date(8)/loan(8) 동률에서 **loan 선택**. 근거: 금융 카테고리 CPC + 최근 8편 중 date가 4편으로 최근성 편중 + date는 age 클러스터가 이미 3편 종료라 새 글이 dday/service-period 중 하나를 또 종료시키는 반면 loan은 계산기 4개에 8편이라 각도 여유가 큼.
  - 주제: `savings-maturity-by-monthly-amount-2026` — 적금 납입액축·기간축 만기 조견표 + 1,000만원 목표 역산표. 기존 savings 2편이 "이자 구조"·"과세유형별 세후 차이"만 다루고 **"내 조건이면 만기에 얼마"라는 금액 조회 수요가 비어 있던 자리**. savings 클러스터는 이 글로 3편 = 종료.
  - title 40자(렌더 49자) / description 139자 — 신규 lint 충족.
- **기획팀 자진 신고(중요)**: 이번 회차 기획 환경에 **Bash가 없어 §4 수치를 손으로 유도**했다. 표준 6번(엔진 재생성) 위반 위험이 있는 조건이라, 기획팀은 (a) 폐형식이 유리수 사칙연산으로 닫히는 적금 단리로 주제를 한정, (b) 반올림 경계(.5) 셀을 설계 단계에서 배제, (c) 라이브 QA 통과값과 겹치는 앵커 셀(월 50만원×12개월 줄)을 의도적으로 삽입 — 3중 대체 보증으로 처리했다.

### 마스터 사전 독립검산 (dev 착수 전, 손유도 리스크 차단)
- `calculateSavingsInterest` 실호출 스크립트로 §4-1·§4-2·§4-4 **전 셀 79개 대조 → 불일치 0건**. 기획팀의 손유도가 실제로 엔진값과 일치함을 확인한 뒤에야 dev에 넘겼다.
- title/description 글자수 python `len()` 재확인(40/49/139).
- 352.5 반올림 경계 조사: 24개월 실효수익률 엔진 실측 = **3.52**(결정적). 기획팀이 본문에서 배제한 판단은 유지.

### dev-team 이식 결과
- `lib/blog.ts` 배열 말미 1개 항목 추가, **+199/-0**. 렌더러·라우트·컴포넌트 무수정.
- tsc EXIT 0 / eslint EXIT 0 / next build 에러 0 / static pages **68** / 블로그 **35편** / sitemap **61 URL**.
- 신규 블록 리터럴 `**` 0건(spec §3 원문에도 0건이라 별표 제거 자체가 불필요했음). spec 대비 변경점 0 — 축자 이식.

### qa-team 독립검증 결과: **콘텐츠·수치·구현 전 항목 PASS**
- 숫자: spec 원장을 보지 않고 엔진 실호출로 재산 → **136개 단언, 불일치 0**. 라벨-금액 88쌍 전수 일치. 미대조 금액토큰 색출까지 수행(67개 중 66 대조, 잔여 1개는 만원 표기 정합).
- 프레임 귀속: 라벨 → 엔진 필드 매핑으로 검사 → **오귀속 0건**. 금액 포함 항목 17 = 라벨 부착 17(C10=C11로 누락 0 증명).
- copy lint: title 40 / 렌더 49 / desc 139 — 마스터 주장 독립 재현.
- §6 셀프체크 20행 전부 재현 일치(표준 ⓖ 반증 없음). spec 축자 일치 36섹션 1:1 차이 0.
- 각도 중복 없음, YMYL 판정성 표현 0건·면책 2곳·출처 3종, 내부링크 전부 live 실재.

### ★ QA가 잡은 운영 사고 1건 (코드 결함 아님)
- **증분 빌드에서 이 글만 404로 구워졌다.** `.next/server/app/blog/<slug>.meta`가 `{"status":404}`, HTML 23,312B(404 본문). 35개 meta 중 이 1건뿐.
- 원인: **:3111에 상주 중인 구버전 `next start`(PID 40160)** 가 물고 있던 오염된 ISR 캐시 엔트리를 증분 빌드가 승계. `getBlogPostBySlug`는 단순 find라 코드/타임존 원인 아님.
- **마스터 재현 시도 → 클린 빌드에서는 재현되지 않음**: `rm -rf .next && npx next build` 후 블로그 meta 35개 전수 **비200 = 0건**, 해당 페이지 **102,468B**(정상 크기). 즉 **배포 산출물에는 영향 없음**(Vercel은 git에서 클린 빌드).
- 판정: **발행 차단 사유 아님.** 로컬 환경 위생 이슈로 백로그 이관.

### 마스터 §7 최종검증 (QA PASS 후에도 직접 실행)
- 직접 `rm -rf .next && npx next build` 실행 → 블로그 HTML **35개**, meta 비200 **0건**.
- 생성된 `savings-maturity-by-monthly-amount-2026.html` 직접 파싱: `<title>` **49자**(≤60 ✓), `<meta description>` **139자**(✓), 리터럴 `**` **0건**, `3.52`/`3.53` 노출 **0건**, 핵심 수치(11,363,436·1,548,180·692,536·877,302·10,605,874·563,436·20,020) 전부 존재, "참고용" 6회·`fine.fss.or.kr` 2회.
- 수치는 **마스터 사전검산 79셀 + QA 136단언 = 이중 독립 검증**.
- **판정: 완료.**

### 마스터 결정 사항 2건
1. **`savings-interest-guide-2026`이 `/calculator/savings-interest-calculator`의 블로그 3편 캡에서 밀려난 건 → 승인.** 근거: 3편 캡은 `getBlogPostsForCalculator`의 설계된 동작이고 최신순 밀어내기는 지금까지 모든 글에 동일하게 적용된 선례다. 밀려난 글은 `/loan` 카테고리·`/blog` 목록·형제글 내부링크로 여전히 도달 가능하므로 고아화가 아니다.
2. **:3111 상주 프로세스(PID 40160)는 종료하지 않음.** 이 세션 소유가 아니고, 클린 빌드로 산출물 정상이 증명돼 배포 리스크가 없다. 임의 종료보다 기록·인계가 맞다고 판단.

### 팀 핑퐁 요약
- 되돌림(FAIL 반송) **0건**. 1라운드 수렴.
- 기획팀 → 마스터: "Bash 부재로 손유도했다"는 자진 신고가 실제 사고를 막았다. 마스터가 dev 착수 **전에** 79셀 검산을 끼워넣어 리스크를 차단한 것이 이번 회차의 핵심 동선.
- QA → 운영: 증분 빌드 404 검출. 마스터가 클린 빌드로 범위 한정(로컬 한정, 배포 무영향) 후 백로그 이관.

### 배포(push)
- 스테이징(경로 명시, `git add -A` 미사용): `lib/blog.ts` · `planning/blog-post-35-spec.md` · `docs/team-log/2026-07-25.md`.
- **제외**: `.claude/launch.json`·`.claude/settings.local.json`·`.claude/settings.json`·`marketing/promo-drafts.md`(타 세션 산출물). `.env*` 미포함 확인.

### 다음 실행 인계 (30분 뒤)
1. **[블로그 36편]** 35편 발행 후 분포 = salary 9 · loan 9 · life 9 · **date 8** → **date 차례**(단독 최저). 종료 클러스터 4편째 금지: BMI(3)·주휴수당(3)·age(3)·**savings(3, 이번 회차로 종료)**. date 잔여 각도는 service-period(2편)·dday(2편)·annual-leave-days(1편) — 착수 전 형제글 Read 필수. **title ≤51자 lint를 기획 지시서에 반드시 포함.**
2. **[신규 티켓 — description 길이 회귀 8편]** QA가 부수 검출. >160자: `loan-prepayment-break-even-2026`(196) · `dsr-by-income-rate-2026`(185) · `electricity-seasonal-progressive-2026`(173) · `annual-leave-allowance-guide-2026`(171) · `service-period-guide-2026`(170) · `four-insurance-by-income-2026`(169) · `savings-interest-tax-guide-2026`(164) · `loan-prepayment-fee-guide-2026`(163). 07-24 title 정정과 동일 성격(카피 소관 → marketing/planning → dev → qa). **신규 글 1편보다 이 8편 일괄 정정이 SEO 효과가 클 수 있음 — 다음 실행에서 36편 대신 이걸 우선할지 판단할 것.**
3. **[백로그 신규]** 로컬 `.next` 오염 + :3111 상주 `next start`(PID 40160) — 증분 빌드가 404를 승계함. 배포 무영향이나 로컬 검증 신뢰도를 떨어뜨림. 정리 권장.
4. **[기획팀 판단 대기, 비차단]** QA 관찰 2건: (a) S20의 축약 라벨 `[세전]`·`[세후]`가 선언된 6종 라벨 어휘에 없음(의미는 정확), (b) S02가 "원리를 다시 풀지 않는다"고 선언했는데 S16·S31이 메커니즘을 재서술 — 자기정합성 느슨함. 발행 차단 사유 아님.
5. **[마케팅]** 카드뉴스 9편 전량 QA 통과 상태 유지, **사용자 이미지화/게시 승인 대기**(무인 시간대라 획득 불가). 35편은 향후 카드뉴스 소재 후보(조견표형이라 카드 분할이 쉬움).
6. **[유지 백로그]** GSC API 자격증명 부재(10일째, 최대 구조적 공백) · OG/metadataBase 전면 부재(티켓 #2) · age.ts 2/29 윤일(사용자 기준 결정 대기) · 헤더 터치타깃 36px · 다크모드 부재 · AdSense 심사 대기 · #16 취득세(高YMYL 보류) · `.claude/*`+promo-drafts 미커밋.

---

## 12:3x 실행분 (오늘 2회차)

> 참고: 이 작업은 01:00~07:30 창에 배정돼 있으나 이번 호출은 12:33에 들어왔다. 창 밖 실행이라는 점만 기록하고, 처리는 정상 절차대로 진행했다.

### 배정 판단 — 36편 신규 발행이 아니라 인계 #2(description 8편 일괄 정정)
직전 실행이 인계 #2에 "신규 글 1편보다 이 8편 일괄 정정이 SEO 효과가 클 수 있음 — 다음 실행에서 판단할 것"이라고 남겼다. **정정을 택했다.** 근거: (a) 8편 전부 이미 라이브라 SERP 스니펫 잘림이 지금 발생 중인 손실인 반면 신규 1편은 색인까지 시간이 걸린다, (b) 07-24 title 회귀 정정과 동일 성격의 마무리라 표준을 한 번에 닫을 수 있다, (c) 30분 단위로 쪼개기 좋은 카피 작업이다.

### 사전 실측 (마스터, 배정 전)
`[...description].length` 전수 → **>160자 정확히 8편**(196/185/173/171/170/169/164/163)으로 인계 목록과 완전 일치. 같은 스캔에서 `title>51자`는 **0건** — 07-24 승격 표준은 이미 전편 충족 상태.
`description` 소비처 확인: `generateMetadata`(meta) + `BlogPostCard`(가시 카드, `line-clamp-2`) + `blog-jsonld`. **`line-clamp-2`라 카드는 원래 2줄에서 잘리므로 축약에 레이아웃 리스크가 없다** → 신규 화면 없음으로 판단해 design-team 생략(§2 단서 적용), planning→dev→qa 파이프라인으로 진행.

### planning-team → `planning/blog-description-length-fix-spec.md`
핵심 안전 설계: **신규 숫자·신규 사실 도입 금지, 삭제·압축·재배열만 허용.** 원문은 이미 QA를 통과한 문장이므로 삭제만 하면 사실 검증이 자동 승계된다(엔진 재검산 불필요 → 30분 창에 맞는 범위). 목표 135~155자.
planning 자진 보고 2건: #1은 `손익분기`를 앞 60자로 끌어올리면 "손익분기 = 절감액−수수료"라는 **틀린 등식**이 되므로 의도적 미충족, #6은 원문에 CTA 문장이 애초에 없어 신설하지 않음(신규 문장 추가는 스펙 §2-2 위반).

### 마스터 사전검산 (dev 착수 전) — **planning 자기보고 오차 1건 검출**
스펙 md의 코드펜스를 **스크립트로 파싱**해 대조(문자열을 손으로 옮겨적지 않음 → 전사 오류 차단). 결과:
- 원문 8편 **축자 일치**(스펙이 원문을 정확히 인용 → dev 편집 앵커 안전), 정정본 숫자 토큰 **신규 0건**, 형식(줄바꿈·큰따옴표·판정성 표현) 위반 0건.
- **FAIL 1건**: `service-period-guide-2026` 자기보고 **148자 → 실측 147자**. 문자열 자체는 135~155 범위라 무결함이고 **자기보고 수치만** 틀렸다(표준 ⓖ 사례). 문자열 무변경, 스펙 3곳(§3-5 헤더·§4 표·§5 기대값 나열)을 마스터가 147로 정정.

### dev-team 이식
`lib/blog.ts` 단일 파일, Edit 8회(Write 통째 재작성 금지 지시 — 표준 ⓗ). tsc 0 / eslint 0 / 클린빌드 에러 0 / static 68 / 블로그 35편 유지 / `git diff` +8−8. 스펙 대비 축자 차이 0건.

### qa-team 1차 독립검증 → **PASS (발행 차단 0건)** + 비차단 지적 4건
길이 8/8·축자 코드포인트 단위 일치(유사문자 오염 검사 포함: en dash·카타카나 중점·curly quote·물결표 변종 치환 0건)·숫자 부분집합·`updatedDate` 갱신 0건·클린빌드 meta 비200 0건·프로덕션 서버 실측 200·**35편 전수 두 lint 충족** 확인. 스펙 §4 셀프체크 40셀 전수 재현(이번엔 오차 0). 마스터의 스펙 정정 3곳 정합성도 재확인(147 맞음, 148 잔재 0건).

**★ 지적 A (실질) — 이번 변경이 만든 회귀**: `description`은 스펙이 적은 3곳이 아니라 **4곳**에서 쓰인다. `BlogListWithSearch.buildSearchHaystack`이 `title+description+카테고리명+tags`를 **사이트 내 블로그 검색 인덱스**로 쓰는데 **body는 인덱싱되지 않는다.** 축약으로 `420kWh`·`만기/캡/3년 캡`·`세전이자`가 검색에서 소실됐다 — 전부 본문이 표·H2를 할애한 핵심 어휘.
지적 B: `BlogPost` JSDoc이 낡은 기준(title 60자·desc 150자)을 그대로 담고 있음 — **07-24 title 69자 회귀를 낳은 오해의 원본이 코드에 아직 살아있는 상태.**
지적 C: 마스터의 사전검산 임시 파일 `tmp-desc-verify.ts`가 저장소 루트에 잔존(`tsconfig` include가 `**/*.ts`라 빌드에 포함됨) → 마스터가 즉시 삭제.

### 후속 처리 판단 (마스터)
지적 A는 **내가 만든 회귀**라 다음 회차로 넘기지 않고 이번 창 안에서 닫기로 결정. `tags`는 `grep -rn "\.tags"` 결과 **소비처가 검색 인덱스 1곳뿐이고 화면에 렌더되지 않으므로** UI 리스크 0. 소실 어휘를 **원문 description에 있던 표현 그대로** 복원(`"420kWh"`·`"만기 3년 캡"`·`"세전이자"`) → 신규 사실 도입 0건 전제 유지. B도 함께 처리(주석만).

### qa-team 2차 독립검증 → **PASS + FAIL 1건(비차단, 마스터 지시 오류)**
- **A 해소 확인 — 기능 검증까지**: `buildSearchHaystack`+`matchesAllTokens` 로직을 축자 재현해 6종 질의 실측, **대소문자 통과 실증**(태그 `420kWh` ↔ 질의 `420kwh`, 양쪽 `.toLowerCase()`). 태그 미복구를 가정하면 6종 중 6종이 실제로 0건이 됨을 반증으로 확인. **35편 어휘 358종 전수 질의로 오염 신규유입 0건.** 런타임 페이로드(`next start` 후 `/blog` 응답)에서 태그 도달까지 확인.
- **FAIL 1건 — 소관: 마스터**: 내가 dev 지시서에 "재사용처 4곳(목록 카드·**카테고리 카드**·JSON-LD·검색)"이라고 규정했는데, `BlogPostCard`는 실제로 **4개 라우트 약 55개 페이지**(/blog 목록·검색결과, 카테고리 4, 블로그 상세 "함께 읽으면 좋은 글" 35, 계산기 상세 "관련 글" 16)에서 렌더된다. dev는 내 문구를 축자 이식했을 뿐이라 dev 과실이 아니다. **주석을 읽는 사람이 가시 영향 범위를 51개 페이지만큼 과소평가하게 되는 오류** → 마스터가 직접 정정. QA가 부기 권고한 "135~155는 신규 집필 목표이지 기존 13편의 위반 기준이 아님"도 함께 반영.

### 마스터 §7 최종검증 (QA PASS 후에도 직접 실행)
`rm -rf .next && npx next build` 클린빌드(증분 불신 — 오늘 오전 캐시 오염 404 전례) 후 독립 스크립트로:
- 35편 / `title>51` **0건** / `desc>160` **0건** / desc min 105 max 159 / 렌더 `<title>` max **59자**
- 검색 질의 5종(`420`·`420kwh`·`캡`·`3년 캡`·`세전이자`) 전부 목표글 매치
- 빌드 blog HTML **35**개, **meta 비200 0건**, meta description **35/35 축자 일치**
- 8편 기대 길이(151/149/147/149/147/151/146/141) 전건 일치
- **HEAD 대비 변경 필드 = `{description:8, tags:3}` 뿐** — title·body·publishedDate·relatedCalculatorSlugs 무변동을 필드 단위로 증명
- tsc 0 / eslint 0 / `.env` 파일 0건
**FAIL 0건 → 판정: 완료.**

### 팀 핑퐁 요약
- planning → 마스터: 자기보고 148자 오차 1건, dev 착수 **전** 마스터 사전검산에서 차단(문자열 무변경, 문서만 정정).
- QA 1차 → 마스터 → dev: 검색 인덱스 회귀(지적 A) 반송 → tags 3개 복원 → QA 2차에서 **기능 실측으로 해소 확인**.
- QA 2차 → 마스터: **마스터 지시서 자체의 사실 오류** 검출(카드 2곳 → 실제 4개 라우트 55페이지). 표준 ⓒ(마스터 산출물도 QA 범위)가 두 회차 연속 실효를 낸 사례.
- 되돌림 2건 전부 당일 해소. 미해결 0건.

### 이번 회차의 표준화 후보
- **ⓙ 카피 축약은 "보이는 곳"만 보지 말고 소비처를 `grep`으로 전수 확인할 것.** description은 meta·카드·JSON-LD·**검색 인덱스** 4종이었고, 넷째가 스펙에서 빠져 회귀가 났다. body가 인덱싱되지 않는 구조라 **description에서 지운 어휘는 사이트 검색에서 그대로 소실**된다 — 축약 시 `tags`로 복구하는 것이 정석.
- **ⓚ 스펙 문자열을 손으로 옮겨적지 말고 문서를 파싱해서 대조할 것.** 이번 사전검산·QA 모두 md 코드펜스를 스크립트로 파싱해 대조했고, 그 덕에 특수문자(U+2212/÷/×/중점) 오염 검사까지 코드포인트 단위로 가능했다.

### 배포(push)
- 스테이징(경로 명시, `git add -A` 미사용): `lib/blog.ts` · `planning/blog-description-length-fix-spec.md` · `docs/team-log/2026-07-25.md`.
- **제외**: `.claude/launch.json`·`.claude/settings.local.json`·`.claude/settings.json`·`marketing/promo-drafts.md`(전부 타 세션 산출물, 07-23·07-25 1회차와 동일 처리). `.env*` 미포함 확인.

### 다음 실행 인계
1. **[블로그 36편]** description 정정으로 인계 #2가 닫혔으므로 다음은 신규 발행 차례. 분포 salary 9 · loan 9 · life 9 · **date 8** → **date 차례**(단독 최저). 종료 클러스터 4편째 금지: BMI(3)·주휴수당(3)·age(3)·savings(3). date 잔여 각도는 service-period(2편)·dday(2편)·annual-leave-days(1편) — 착수 전 형제글 Read 필수. **title ≤51자 + description ≤160자 lint 둘 다 기획 지시서에 포함할 것**(이제 JSDoc에도 명문화돼 있다).
2. **[신규 티켓 — `<70자` description 16건]** 07-25 GSC 티켓 #3의 나머지 절반. 대상은 블로그가 아니라 **`/calculator/*` 페이지**(최단 26자: `bmi-calculator`·`dday-calculator`). 이번 건과 달리 "삭제"가 아니라 **"증설"**이라 계산기 사양 사실 확인이 필요하다 → 신규 사실 도입이 불가피하므로 **엔진/사양 대조 검증을 붙여야 한다.** 이번 회차처럼 가볍게 처리하면 안 됨.
3. **[비차단 관찰, planning 판단 대기]** QA가 남긴 문구 지적 3건: (a) #5 `근속기간`이 계산기 이름과 세 값 중 하나로 14자 안에 두 번 등장해 모호(본문은 `근속기간 N년 M개월 D일` 표기를 세 값 구분의 핵심 장치로 씀), (b) #8에서 주어 `수수료가` 삭제로 「줄어드는」의 수식 대상이 '수수료'→'슬라이딩 방식'으로 이동(용어 정의상 허위는 아님), (c) #3 2문장이 머리명사 없는 숫자 나열이 됨. 전부 발행 차단 아님.
4. **[비차단 관찰]** #5 계산기 명칭 혼용(`근속기간 계산기`/`근속연수 계산기` vs 공식 `근속연수·근무일수 계산기`) — **HEAD 원문에도 있던 기존 결함**, 이번 정정이 만든 것 아님. (a)와 함께 정리하면 좋다.
5. **[비차단 관찰]** `/blog` 목록이 `ƒ (Dynamic)`으로 빌드됨(개별 글 35개는 정상 SSG). 기존 상태이며 이번 변경과 무관. 목록 SSG화 필요 여부는 별도 판단.
6. **[마케팅]** 카드뉴스 9편 QA 통과 상태 유지, **사용자 이미지화/게시 승인 대기**(무인 실행이라 획득 불가). 이번 회차는 신규 콘텐츠가 없어(기존 8편 카피 정정) 카드뉴스 신규 소재 없음 — **게이팅 판단: 신규 배정 없음**이 맞다고 판단.
7. **[유지 백로그]** GSC API 자격증명 부재(10일째) · OG/metadataBase 전면 부재(티켓 #2) · age.ts 2/29 윤일(사용자 기준 결정 대기) · 헤더 터치타깃 36px · 다크모드 부재 · AdSense 심사 대기 · #16 취득세(高YMYL 보류) · 로컬 `.next` 오염 + :3111 상주 `next start`(PID 40160) · `.claude/*`+promo-drafts 미커밋.
