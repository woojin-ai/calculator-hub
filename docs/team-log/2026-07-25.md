# 2026-07-25 데일리 팀 스탠드업 로그

## 01:0x 실행분 (오늘 첫 실행)

### 컨텍스트 파악
- **07-24에는 daily-team-standup이 실행되지 않았다** — `docs/team-log/2026-07-24.md` 부재, `2026-07-24-gsc-audit.md`만 존재. 즉 이번 실행의 인계 원본은 **07-23 로그의 "다음 실행 인계"** + 07-24 GSC 감사 로그다.
- git status: `.claude/launch.json`·`.claude/settings.local.json`·`.claude/settings.json`(untracked)·`marketing/promo-drafts.md` 미커밋 — 07-23과 동일한 타 세션 산출물이라 이번에도 **미터치**. `.env*` 없음.
- 라이브 상태(실측): 블로그 34편(salary 9 · life 9 · date 8 · loan 8), 계산기 16개, static pages 67, sitemap 60 URL.
- 07-24 GSC 감사가 34편 `<title>` 69→54자 회귀를 이미 정정·배포 완료(`d6f577e`). 중복 지시 없음.

### ★ 07-24 감사에서 승격된 신규 발행 표준 (이번 회차부터 적용)
`app/blog/[slug]/page.tsx`가 `${post.title} | 계산기 허브`(접미사 9자)로 조립하므로 **post.title ≤51자**여야 렌더 `<title>` ≤60자(SERP 잘림 회피)를 지킨다. 34편이 이 규칙 없이 발행돼 회귀가 났다. **이번 회차부터 기획 지시서에 명시적 lint 항목으로 포함**했고, QA 항목으로도 배정했다.

### 배정: 블로그 35편 (축1 = 카테고리 균형 + 검색수요)
- **planning-team** → `planning/blog-post-35-spec.md` 작성.
  - date(8)/loan(8) 동률에서 **loan 선택**. 근거: 금융 카테고리 CPC + 최근 8편 중 date가 4편으로 최근성 편중 + date는 age 클러스터가 이미 3편 종료라 새 글이 dday/service-period 중 하나를 또 종료시키는 반면 loan은 계산기 4개에 8편이라 각도 여유가 큼.
  - 주제: `savings-maturity-by-monthly-amount-2026` — 적금 납입액축·기간축 만기 조견표 + 1,000만원 목표 역산표. 기존 savings 2편이 "이자 구조"·"과세유형별 세후 차이"만 다루고 **"내 조건이면 만기에 얼마"라는 금액 조회 수요가 비어 있던 자리**. savings 클러스터는 이 글로 3편 = 종료.
  - title 40자(렌더 49자) / description 139자 — 신규 lint 충족.
- **기획팀 자진 신고(중요)**: 이번 회차 기획 환경에 **Bash가 없어 §4 수치를 손으로 유도**했다. 표준 6번(엔진 재생성) 위반 위험이 있는 조건이라, 기획팀은 (a) 폐형식이 유리수 사칙연산으로 닫히는 적금 단리로 주제를 한정, (b) 반올림 경계(.5) 셀을 설계 단계에서 배제, (c) 라이브 QA 통과값과 겹치는 앵커 셀(월 50만원×12개월 줄)을 의도적으로 삽입 — 3중 대체 보증으로 처리했다.

### 마스터 사전 독립검산 (dev 착수 전, 손유도 리스크 차단)
- `calculateSavingsInterest` 실호출 스크립트로 §4-1·§4-2·§4-4 **전 셀 79개 대조 → 불일치 0건**. 기획팀의 손유도가 실제로 엔진값과 일치함을 확인한 뒤에야 dev에 넘겼다.
- title/description 글자수 python `len()` 재확인(40/49/139).
- 352.5 반올림 경계 조사: 24개월 실효수익률 엔진 실측 = **3.52**(결정적). 기획팀이 본문에서 배제한 판단은 유지.

### dev-team 이식 결과
- `lib/blog.ts` 배열 말미 1개 항목 추가, **+199/-0**. 렌더러·라우트·컴포넌트 무수정.
- tsc EXIT 0 / eslint EXIT 0 / next build 에러 0 / static pages **68** / 블로그 **35편** / sitemap **61 URL**.
- 신규 블록 리터럴 `**` 0건(spec §3 원문에도 0건이라 별표 제거 자체가 불필요했음). spec 대비 변경점 0 — 축자 이식.

### qa-team 독립검증 결과: **콘텐츠·수치·구현 전 항목 PASS**
- 숫자: spec 원장을 보지 않고 엔진 실호출로 재산 → **136개 단언, 불일치 0**. 라벨-금액 88쌍 전수 일치. 미대조 금액토큰 색출까지 수행(67개 중 66 대조, 잔여 1개는 만원 표기 정합).
- 프레임 귀속: 라벨 → 엔진 필드 매핑으로 검사 → **오귀속 0건**. 금액 포함 항목 17 = 라벨 부착 17(C10=C11로 누락 0 증명).
- copy lint: title 40 / 렌더 49 / desc 139 — 마스터 주장 독립 재현.
- §6 셀프체크 20행 전부 재현 일치(표준 ⓖ 반증 없음). spec 축자 일치 36섹션 1:1 차이 0.
- 각도 중복 없음, YMYL 판정성 표현 0건·면책 2곳·출처 3종, 내부링크 전부 live 실재.

### ★ QA가 잡은 운영 사고 1건 (코드 결함 아님)
- **증분 빌드에서 이 글만 404로 구워졌다.** `.next/server/app/blog/<slug>.meta`가 `{"status":404}`, HTML 23,312B(404 본문). 35개 meta 중 이 1건뿐.
- 원인: **:3111에 상주 중인 구버전 `next start`(PID 40160)** 가 물고 있던 오염된 ISR 캐시 엔트리를 증분 빌드가 승계. `getBlogPostBySlug`는 단순 find라 코드/타임존 원인 아님.
- **마스터 재현 시도 → 클린 빌드에서는 재현되지 않음**: `rm -rf .next && npx next build` 후 블로그 meta 35개 전수 **비200 = 0건**, 해당 페이지 **102,468B**(정상 크기). 즉 **배포 산출물에는 영향 없음**(Vercel은 git에서 클린 빌드).
- 판정: **발행 차단 사유 아님.** 로컬 환경 위생 이슈로 백로그 이관.

### 마스터 §7 최종검증 (QA PASS 후에도 직접 실행)
- 직접 `rm -rf .next && npx next build` 실행 → 블로그 HTML **35개**, meta 비200 **0건**.
- 생성된 `savings-maturity-by-monthly-amount-2026.html` 직접 파싱: `<title>` **49자**(≤60 ✓), `<meta description>` **139자**(✓), 리터럴 `**` **0건**, `3.52`/`3.53` 노출 **0건**, 핵심 수치(11,363,436·1,548,180·692,536·877,302·10,605,874·563,436·20,020) 전부 존재, "참고용" 6회·`fine.fss.or.kr` 2회.
- 수치는 **마스터 사전검산 79셀 + QA 136단언 = 이중 독립 검증**.
- **판정: 완료.**

### 마스터 결정 사항 2건
1. **`savings-interest-guide-2026`이 `/calculator/savings-interest-calculator`의 블로그 3편 캡에서 밀려난 건 → 승인.** 근거: 3편 캡은 `getBlogPostsForCalculator`의 설계된 동작이고 최신순 밀어내기는 지금까지 모든 글에 동일하게 적용된 선례다. 밀려난 글은 `/loan` 카테고리·`/blog` 목록·형제글 내부링크로 여전히 도달 가능하므로 고아화가 아니다.
2. **:3111 상주 프로세스(PID 40160)는 종료하지 않음.** 이 세션 소유가 아니고, 클린 빌드로 산출물 정상이 증명돼 배포 리스크가 없다. 임의 종료보다 기록·인계가 맞다고 판단.

### 팀 핑퐁 요약
- 되돌림(FAIL 반송) **0건**. 1라운드 수렴.
- 기획팀 → 마스터: "Bash 부재로 손유도했다"는 자진 신고가 실제 사고를 막았다. 마스터가 dev 착수 **전에** 79셀 검산을 끼워넣어 리스크를 차단한 것이 이번 회차의 핵심 동선.
- QA → 운영: 증분 빌드 404 검출. 마스터가 클린 빌드로 범위 한정(로컬 한정, 배포 무영향) 후 백로그 이관.

### 배포(push)
- 스테이징(경로 명시, `git add -A` 미사용): `lib/blog.ts` · `planning/blog-post-35-spec.md` · `docs/team-log/2026-07-25.md`.
- **제외**: `.claude/launch.json`·`.claude/settings.local.json`·`.claude/settings.json`·`marketing/promo-drafts.md`(타 세션 산출물). `.env*` 미포함 확인.

### 다음 실행 인계 (30분 뒤)
1. **[블로그 36편]** 35편 발행 후 분포 = salary 9 · loan 9 · life 9 · **date 8** → **date 차례**(단독 최저). 종료 클러스터 4편째 금지: BMI(3)·주휴수당(3)·age(3)·**savings(3, 이번 회차로 종료)**. date 잔여 각도는 service-period(2편)·dday(2편)·annual-leave-days(1편) — 착수 전 형제글 Read 필수. **title ≤51자 lint를 기획 지시서에 반드시 포함.**
2. **[신규 티켓 — description 길이 회귀 8편]** QA가 부수 검출. >160자: `loan-prepayment-break-even-2026`(196) · `dsr-by-income-rate-2026`(185) · `electricity-seasonal-progressive-2026`(173) · `annual-leave-allowance-guide-2026`(171) · `service-period-guide-2026`(170) · `four-insurance-by-income-2026`(169) · `savings-interest-tax-guide-2026`(164) · `loan-prepayment-fee-guide-2026`(163). 07-24 title 정정과 동일 성격(카피 소관 → marketing/planning → dev → qa). **신규 글 1편보다 이 8편 일괄 정정이 SEO 효과가 클 수 있음 — 다음 실행에서 36편 대신 이걸 우선할지 판단할 것.**
3. **[백로그 신규]** 로컬 `.next` 오염 + :3111 상주 `next start`(PID 40160) — 증분 빌드가 404를 승계함. 배포 무영향이나 로컬 검증 신뢰도를 떨어뜨림. 정리 권장.
4. **[기획팀 판단 대기, 비차단]** QA 관찰 2건: (a) S20의 축약 라벨 `[세전]`·`[세후]`가 선언된 6종 라벨 어휘에 없음(의미는 정확), (b) S02가 "원리를 다시 풀지 않는다"고 선언했는데 S16·S31이 메커니즘을 재서술 — 자기정합성 느슨함. 발행 차단 사유 아님.
5. **[마케팅]** 카드뉴스 9편 전량 QA 통과 상태 유지, **사용자 이미지화/게시 승인 대기**(무인 시간대라 획득 불가). 35편은 향후 카드뉴스 소재 후보(조견표형이라 카드 분할이 쉬움).
6. **[유지 백로그]** GSC API 자격증명 부재(10일째, 최대 구조적 공백) · OG/metadataBase 전면 부재(티켓 #2) · age.ts 2/29 윤일(사용자 기준 결정 대기) · 헤더 터치타깃 36px · 다크모드 부재 · AdSense 심사 대기 · #16 취득세(高YMYL 보류) · `.claude/*`+promo-drafts 미커밋.
