# 팀 로그 — 2026-08-07

## 01:06 실행분 (오늘 첫 실행)

### 결론

인계 §9-2가 **"티켓 #44 착수 가능 — 개발팀에 바로 배정 가능하다"**로 넘겨준 작업을, 배정하지 않고 **정지시켰다.**

지시서대로 오늘 실행했으면 **라이브가 147일간 틀렸다.** QA 독립 검증으로 확인했고, 원인은 담당 팀 역량이 아니라 **어제 티켓을 분리할 때 시점 축이 탈락하면서 문장이 반전된 것**이다.

이번 회차 산출물은 코드가 아니라 **그 지시서의 정정**이다. 라이브 코드 **무수정**.

---

## 0. 사전 조건

- **HEAD** = `a098191`. `git fetch` 미실행(네트워크 미접근) — 원격 동기 상태 **미확인**.
- **`lib/blog.ts`는 `58632df`↔`a098191` 바이트 동일**(`git diff --stat` 무출력, 마스터 직접 실행) → 지시서 줄번호 전량 유효. §8 item 1이 요구한 재확인을 실제로 수행한 것이다.
- 워킹트리 기존 미커밋: `.claude/launch.json`, `.claude/settings.local.json`(🔴 **평문 크론 시크릿**), `.claude/settings.json`, `.codex/`, `ebook/`, `trading/`, `planning/blog-year-slug-policy.md`(QA FAIL 상태 유지), `planning/simplified-tax-table-byl2-raw.txt` — **전부 미접촉.**
- 무인 안전수칙: `cd`+`git` 연결 호출 **0건**, Bash heredoc **0건**.

---

## 1. 🔴 적발 — 티켓 #44를 오늘 실행하면 라이브가 147일간 틀린다

### 1-1. 무엇이 틀리는가 (QA 실측, 마스터 재확인)

| 지점 | 오늘 배포 시 화면 | 2026-08-07 참값 |
| --- | --- | --- |
| title | 「**2027년** 출생연도별 나이 조견표」 | 2026년 |
| 조견표 2000년생 | 연 **27**세, 만 **26~27**세, 세는 **28**세 | 연 26 / 만 25~26 / 세는 27 |
| FAQ L4419 | "**오늘 기준** 만 나이 … 생일 전 만 26세" | 만 25세 |

- 틀리는 값: 조견표 **64개**(16행×4) + 산문 **11개** = **75개**
- 기간: **2026-08-07 ~ 12-31 = 147일**
- 🔴 **자사 계산기가 이 글을 즉시 반증한다** — 같은 페이지 CTA가 가리키는 만 나이 계산기는 `lib/age.ts` **L16 기본 인자 `referenceDate: Date = new Date()`**로 실시간 계산한다. 글이 독자에게 권한 검증 절차가 그대로 반박 절차가 된다.
- `publishedDate`는 보호 대상이라 **"2026. 7. 18. 발행" 아래 "2027년" 제목**이 렌더된다.

### 1-2. 🔴 원인 — 티켓 분리 때 문구가 **반전**됐다

원본 `planning/blog-year-slug-policy.md`에는 시점 통제가 **있었다**(마스터 직접 확인):

| 원본 정책문서 | 분리된 티켓 초판 |
| --- | --- |
| L384~L388 실행순서표에 **`시점` 열 존재** | 승계 **안 됨** |
| L427 「#27 — **12월 내 필착.** 외부 고시 대기 불필요, **지연 사유 없음**」 | §1 「12월을 **기다릴 이유가 전혀 없고, 지금 착수할 수 있다**」 |

**같은 사실(외부 고시 독립성)이 "늦을 핑계가 없다" → "기다릴 이유가 없다"로 뒤집혔다.** 단어는 겹치는데 운영 의미가 정반대다.

또한 원본 커밋 A행은 이 저장소가 **8월 배포 허용 기준을 이미 정해 뒀음**을 보여준다 — 라이브 표시 변화 **「없음(바이트 동일)」**. §4-1의 변경은 75개 숫자가 바뀌므로 이 기준을 **충족하지 못한다.**

### 1-3. 탐지기가 없었다

§8 체크리스트(초판 **11개** — 내가 QA에 "10개"라고 잘못 relay했고 QA가 재측정해 정정)에 **시점을 통제하는 항목 0개.** 게이트 4종(`tsc`/`eslint`/`test`/`build`)은 전부 **정적 검사**라 조견표가 미래 연도로 채워져도 전부 통과한다.

---

## 2. 팀 핑퐁

### ① 마스터 → qa-team + planning-team 동시 호출

QA에는 **내 결론을 알려주지 않았다**(㉹ — 목록을 주면 읽는 순간 독립성이 사라진다). "오늘 실행하면 어떤 상태가 되는지 판정하라"만 지시.

### ② qa-team R1 → **FAIL**, 그리고 내 가설보다 나은 근거

나는 "지시서가 배포 시점을 다루지 않는다"까지 봤는데, QA는 **원본 문서에 시점 축이 있었고 분리 때 탈락했다**는 이력까지 찾아냈다(§1-2). `배포`·`릴리스`·`푸시` 전 문서 **0회** 실측 포함.

### ③ planning-team → 파라미터화 설계, **조건부 채택 권고**

`AGE_TABLE_YEAR` 단일 상수화. 핵심 발견 — **조견표 15행 = 출생연도 앵커 11개 + 연 나이 앵커 4개의 합집합**으로 HEAD와 행·순서 완전 일치. 이러면 §4의 "정정 방향이 서로 반대" 난제가 **구조적으로 소멸**한다(각 줄이 자기 앵커를 선언하므로 충돌할 전역 방향이 없다).

**미채택.** 마스터 결재 3건 선행 필요 → **티켓 #46**.

### ④ 마스터 문서 정정 → **qa-team R1 재검증 → FAIL 7건**

내 정정이 원래 결함과 **같은 유형의 새 결함 3건**을 만들었다(㋎). 압권은 **FAIL-4**:

> §10-1에 오염 범위를 16줄로 적었는데 실제는 **30줄 전부**. 내 포착률 **53%** — 같은 문서 §3이 "grep 포착률 47%"로 경계한 **바로 그 실패 모드**. 결함을 지적하는 문장이 같은 결함을 저질렀다.

**FAIL-5**도 같은 유형 — `L5230~`을 **종료점 없이** 적었다. 문면대로면 26편이 통째로 동결된다. 이 문서 §3이 자기 성과로 내세운 **`L4444` 함정과 완전히 동일한 실수**다.

### ⑤ 7건 수정 → **qa-team R2 → 6건 종결 / 신규 4건**

또 내 정정이 오류를 반복했다:
- **F-1**: "§8은 8월에도 전량 통과한다"고 썼는데 **거짓** — item 1·10은 8월에 통과 불가.
- **F-2**: 하한을 12-26으로 옮기고 **잔여 오차는 12-29~31 기준(1~3일)으로 남겼다.** 12-26 집행 시 실제는 **6일**.

R2 지적 5건 전건 수정 완료. 수정 중 **표 구조 파손 1건**(§5 행이 표 밖으로 이탈)을 마스터가 자체 검출·수정.

---

## 3. 마스터 직접 검증 (팀 보고 복창 안 함 — 표준 ㋥)

| 항목 | 팀 주장 | 내 실측 | 판정 |
| --- | --- | --- | --- |
| §8 체크리스트 항목수 | QA **11** | `grep -c "^- \[ \]"` → **11** | **QA 옳음, 내 relay(10) 틀림** |
| `배포`/`릴리스`/`푸시` | 0건 | `grep -cE` → **0** | QA 옳음 |
| 정책문서 시점 제약 | L384~388 `시점` 열, L427 "12월 내 필착" | 직접 출력 확인 | **QA 옳음** |
| 재오염원 | plan 문서 = BlogPost 전문 | plan **L12**가 스스로 "완성된 BlogPost 객체 리터럴" 명시, 대표 5줄 전건 HIT | **QA 옳음, 내 초판(16줄) 틀림** |
| 형제글 경계 | L5353 종료 | `awk` → L5353 `},` / **L5354 = 다음 글 여는 `{`** | **QA 옳음** |
| `lib/age.ts` | L16 기본 인자 | L16 `referenceDate: Date = new Date()`, L31·L41은 `getFullYear()` | **QA 옳음, 내 초판 틀림** |
| U+2212 | 비ASCII | `od -c` → `342 210 222`, ASCII 하이픈 **0건** | 확인 |
| A 줄/개소 | 14줄/17개소 | 원시 **16줄/19개소** − 보호 2줄 = **14/17** | 일치 |

**게이트 미실행 사유**: 이번 회차는 **코드 무수정**(변경 = `planning/` 1개 + `docs/team-log/` 1개). `tsc`/`test`/`build`를 돌릴 대상이 없다. "게이트 통과"가 아니라 **해당 없음**이다.

---

## 4. 산출물

`planning/ticket-44-age-table-2027-workorder.md` 정정(+약 130행):

1. **§0 배포 시점 게이트 신설** — 147일 실측, 반전 이력, 「착수 가능 ≠ 배포 가능」
2. **배포 하한 2026-12-26**(권장 12-29~31) + **근거표**(거짓 0일 선택지는 없음을 명시) + 잔여 6일 정직 인정
3. **§8을 8-1(착수 전 12항목) / 8-2(배포 전 11항목)로 분리** — 초판은 착수 목록만 있어 배포가 통제되지 않았다
4. **§5 제5 보호계열**(형제 글 `L5230~L5353`) + 제3계열에 `만 18세` 추가(5개 중 4개만 덮고 있었음)
5. **§10-1 재오염원** — plan 문서 **30/30줄** 대응표
6. **§10-4 파라미터화 제안** — 미결정 명시, 결재 3건
7. U+2212·U+2014 잘라붙이기 원칙, `lib/age.ts` L16 정정

---

## 5. 티켓

| # | 내용 | owner | 상태 |
| --- | --- | --- | --- |
| **#45** | 지시서 #44 배포 시점 게이트 부재 | qa→마스터 | **종결**(정정 완료) |
| **#46** | `AGE_TABLE_YEAR` 파라미터화 | 마스터 결재 대기 | **신규·미착수** |
| #44 | 나이 조견표 2027 갱신 | — | **12-26까지 배포 금지로 변경** |

**티켓번호 #46까지 소진**(배정 전 grep 확인).

---

## 6. 미확인 (명시)

- **QA R3 미실시** — R2 지적 5건을 수정했으나 그 수정에 대한 독립 재검증은 못 했다. 이번 회차에 정정문이 2라운드 연속 새 오류를 만들었으므로 **R3에서 또 나올 가능성이 있다.** "정정 완료"는 마스터 자체 검증(표 구조·잔존 참조 스윕)까지만 뒷받침된다.
- **`git fetch` 미실행** — 원격 동기 상태 미확인.
- **브라우저/모바일 실측 미수행** — 코드 무수정이라 대상 없음. 08-06의 `preview_start` 300초 타임아웃 ×2 문제는 **미해결 이월**.
- **GSC 색인/노출 — 23일째 측정 수단 없음.**
- 기획팀 앵커 분해(§10-4)는 **추론이지 문서화된 의도가 아니다.** `1960`·`1996` 2건이 §4-1과 어긋난다.

---

## 7. 사용자 확인 필요 블로커 — **오늘 신규 없음**

| # | 사안 | 알림 |
| --- | --- | --- |
| GSC | API 자격증명 부재(23일째) | 07-26 발송 — **재발송 금지** |
| #12 | 커스텀 도메인 | 08-01·08-02 — **재발송 금지** |
| #11 | 스케줄 세션 동시 실행 | 07-29 — **재발송 금지** |
| #6 | 공개 저장소 평문 크론 시크릿 | 07-27 — **재발송 금지** |

티켓 #45·#46은 팀/마스터 선에서 판정 가능 → **알림 대상 아님.**

---

## 8. 다음 실행 인계

1. **[🔴 최우선] QA R3** — 오늘 R2 지적 5건 수정분 독립 재검증. 정정문이 2라운드 연속 새 오류를 만들었다.
2. **[🔴 티켓 #44 — 배정 금지]** 개발팀에 **배정하지 말 것.** 12-26 이전 main push 금지. 8월에 할 수 있는 건 로컬 브랜치 작업까지이며, **그마저 #46 결재 전에는 헛수고가 될 수 있다**(파라미터화 채택 시 작업 방식이 통째로 바뀐다).
3. **[🔴 티켓 #46 결재 3건]** — 다음 회차의 실질 작업 후보. (i) §4-1 "2008년생 행 신설(15→16행)" 철회 여부 (ii) 앵커 귀속(`1960`·`1996`) (iii) 탐지기 강도.
4. **[티켓 #42 R3 — 여전히 미착수]** `blog-year-slug-policy.md`. 여전히 **untracked**. 단 오늘 이 문서가 **시점 제약의 원출처**로 확인됐으므로(§1-2) 폐기 대상이 아니다.
5. **[티켓 #34 승격 유지]** `updatedDate` 정책 — #44의 전제.
6. **[⏰ 시한]** `MINIMUM_WAGE` 교체(12-31) — **#44와 커밋 분리.**
7. **[신설 필요 — 유지]** 2027 고시 추적 주체 부재 6종(4대보험·이자소득세·한전·자동차세·DSR·중도상환수수료).
8. **[도구]** `preview_start` 타임아웃 — 헤드리스 대체 검토(08-06 §9-7 이월).

---

## 9. 운영 표준

- **[★신규 ㋩] 시한 항목은 "언제까지"와 "언제부터"를 **둘 다** 적어야 한다.** 초판은 마감(상한)만 있고 하한이 없어, 규정을 100% 준수하면서 147일 오배포가 가능했다. **상한만 있는 시한은 게이트가 아니다.**
- **[★신규 ㋪] 문서를 분리하면 제약이 탈락한다.** 어제 티켓 분리는 옳은 결정이었고 그 자체가 QA 3라운드 실패의 처방이었다. 그런데 분리 과정에서 원본의 `시점` 열이 승계되지 않았고, 남은 문장은 **의미가 반전됐다**("지연 사유 없음" → "기다릴 이유 없음"). **분리 시에는 원본의 제약 조항을 열 단위로 대조할 것.**
- **[★재확인 ㋎ — 이번 회차만 5건]** 정정문이 같은 오류를 반복했다: 오염 범위 53%만 지목(§3이 경계한 47% 실패 모드), 범위 종료점 누락(§3이 자기 성과로 내세운 `L4444` 함정), "8월에 전량 통과" 거짓 단정, 잔여일수를 하한 아닌 권장일 기준으로 기술, 표 구조 파손. **정정 직후가 가장 위험한 순간이다.**
- **[재확인 ㉹ — 13연속] 하류가 상류를 또 반증했다.** 이번엔 대상이 **어제의 나 자신**이었고, 반증자는 내가 결론을 숨긴 채 호출한 QA였다. 내가 relay한 "체크리스트 10개"도 QA가 11개로 정정했다.
- **[재확인 ㋦] 보호 목록은 작업 목록과 같은 크기의 산출물이다.** 제5 계열(형제 글)과 `만 18세`가 이번에 추가됐다. 보호 목록은 **두 회차 연속 불완전했다.**

---

## 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. `.env*` 미열람. **`git add -A` 미사용**(경로 명시 스테이징). 라이브 코드 **무수정**. 타 세션 워킹트리 무접촉. 이미지/영상 생성 도구 호출 0. 결제 행위 없음. 모든 줄번호·건수는 `a098191` 기준 직접 측정값이며, 검증하지 않은 전달값은 §6에 격리 표시했다.
