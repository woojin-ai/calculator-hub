# 팀 로그 — 2026-08-07

## 01:06 실행분 (오늘 첫 실행)

### 결론

인계 §9-2가 **"티켓 #44 착수 가능 — 개발팀에 바로 배정 가능하다"**로 넘겨준 작업을, 배정하지 않고 **정지시켰다.**

지시서대로 오늘 실행했으면 **라이브가 147일간 틀렸다.** QA 독립 검증으로 확인했고, 원인은 담당 팀 역량이 아니라 **어제 티켓을 분리할 때 시점 축이 탈락하면서 문장이 반전된 것**이다.

이번 회차 산출물은 코드가 아니라 **그 지시서의 정정**이다. 라이브 코드 **무수정**.

---

## 0. 사전 조건

- **HEAD** = `a098191`. `git fetch` 미실행(네트워크 미접근) — 원격 동기 상태 **미확인**.
- **`lib/blog.ts`는 `58632df`↔`a098191` 바이트 동일**(`git diff --stat` 무출력, 마스터 직접 실행) → 지시서 줄번호 전량 유효. §8 item 1이 요구한 재확인을 실제로 수행한 것이다.
- 워킹트리 기존 미커밋: `.claude/launch.json`, `.claude/settings.local.json`(🔴 **평문 크론 시크릿**), `.claude/settings.json`, `.codex/`, `ebook/`, `trading/`, `planning/blog-year-slug-policy.md`(QA FAIL 상태 유지), `planning/simplified-tax-table-byl2-raw.txt` — **전부 미접촉.**
- 무인 안전수칙: `cd`+`git` 연결 호출 **0건**, Bash heredoc **0건**.

---

## 1. 🔴 적발 — 티켓 #44를 오늘 실행하면 라이브가 147일간 틀린다

### 1-1. 무엇이 틀리는가 (QA 실측, 마스터 재확인)

| 지점 | 오늘 배포 시 화면 | 2026-08-07 참값 |
| --- | --- | --- |
| title | 「**2027년** 출생연도별 나이 조견표」 | 2026년 |
| 조견표 2000년생 | 연 **27**세, 만 **26~27**세, 세는 **28**세 | 연 26 / 만 25~26 / 세는 27 |
| FAQ L4419 | "**오늘 기준** 만 나이 … 생일 전 만 26세" | 만 25세 |

- 틀리는 값: 조견표 **64개**(16행×4) + 산문 **11개** = **75개**
- 기간: **2026-08-07 ~ 12-31 = 147일**
- 🔴 **자사 계산기가 이 글을 즉시 반증한다** — 같은 페이지 CTA가 가리키는 만 나이 계산기는 `lib/age.ts` **L16 기본 인자 `referenceDate: Date = new Date()`**로 실시간 계산한다. 글이 독자에게 권한 검증 절차가 그대로 반박 절차가 된다.
- `publishedDate`는 보호 대상이라 **"2026. 7. 18. 발행" 아래 "2027년" 제목**이 렌더된다.

### 1-2. 🔴 원인 — 티켓 분리 때 문구가 **반전**됐다

원본 `planning/blog-year-slug-policy.md`에는 시점 통제가 **있었다**(마스터 직접 확인):

| 원본 정책문서 | 분리된 티켓 초판 |
| --- | --- |
| L384~L388 실행순서표에 **`시점` 열 존재** | 승계 **안 됨** |
| L427 「#27 — **12월 내 필착.** 외부 고시 대기 불필요, **지연 사유 없음**」 | §1 「12월을 **기다릴 이유가 전혀 없고, 지금 착수할 수 있다**」 |

**같은 사실(외부 고시 독립성)이 "늦을 핑계가 없다" → "기다릴 이유가 없다"로 뒤집혔다.** 단어는 겹치는데 운영 의미가 정반대다.

또한 원본 커밋 A행은 이 저장소가 **8월 배포 허용 기준을 이미 정해 뒀음**을 보여준다 — 라이브 표시 변화 **「없음(바이트 동일)」**. §4-1의 변경은 75개 숫자가 바뀌므로 이 기준을 **충족하지 못한다.**

### 1-3. 탐지기가 없었다

§8 체크리스트(초판 **11개** — 내가 QA에 "10개"라고 잘못 relay했고 QA가 재측정해 정정)에 **시점을 통제하는 항목 0개.** 게이트 4종(`tsc`/`eslint`/`test`/`build`)은 전부 **정적 검사**라 조견표가 미래 연도로 채워져도 전부 통과한다.

---

## 2. 팀 핑퐁

### ① 마스터 → qa-team + planning-team 동시 호출

QA에는 **내 결론을 알려주지 않았다**(㉹ — 목록을 주면 읽는 순간 독립성이 사라진다). "오늘 실행하면 어떤 상태가 되는지 판정하라"만 지시.

### ② qa-team R1 → **FAIL**, 그리고 내 가설보다 나은 근거

나는 "지시서가 배포 시점을 다루지 않는다"까지 봤는데, QA는 **원본 문서에 시점 축이 있었고 분리 때 탈락했다**는 이력까지 찾아냈다(§1-2). `배포`·`릴리스`·`푸시` 전 문서 **0회** 실측 포함.

### ③ planning-team → 파라미터화 설계, **조건부 채택 권고**

`AGE_TABLE_YEAR` 단일 상수화. 핵심 발견 — **조견표 15행 = 출생연도 앵커 11개 + 연 나이 앵커 4개의 합집합**으로 HEAD와 행·순서 완전 일치. 이러면 §4의 "정정 방향이 서로 반대" 난제가 **구조적으로 소멸**한다(각 줄이 자기 앵커를 선언하므로 충돌할 전역 방향이 없다).

**미채택.** 마스터 결재 3건 선행 필요 → **티켓 #46**.

### ④ 마스터 문서 정정 → **qa-team R1 재검증 → FAIL 7건**

내 정정이 원래 결함과 **같은 유형의 새 결함 3건**을 만들었다(㋎). 압권은 **FAIL-4**:

> §10-1에 오염 범위를 16줄로 적었는데 실제는 **30줄 전부**. 내 포착률 **53%** — 같은 문서 §3이 "grep 포착률 47%"로 경계한 **바로 그 실패 모드**. 결함을 지적하는 문장이 같은 결함을 저질렀다.

**FAIL-5**도 같은 유형 — `L5230~`을 **종료점 없이** 적었다. 문면대로면 26편이 통째로 동결된다. 이 문서 §3이 자기 성과로 내세운 **`L4444` 함정과 완전히 동일한 실수**다.

### ⑤ 7건 수정 → **qa-team R2 → 6건 종결 / 신규 4건**

또 내 정정이 오류를 반복했다:
- **F-1**: "§8은 8월에도 전량 통과한다"고 썼는데 **거짓** — item 1·10은 8월에 통과 불가.
- **F-2**: 하한을 12-26으로 옮기고 **잔여 오차는 12-29~31 기준(1~3일)으로 남겼다.** 12-26 집행 시 실제는 **6일**.

R2 지적 5건 전건 수정 완료. 수정 중 **표 구조 파손 1건**(§5 행이 표 밖으로 이탈)을 마스터가 자체 검출·수정.

---

## 3. 마스터 직접 검증 (팀 보고 복창 안 함 — 표준 ㋥)

| 항목 | 팀 주장 | 내 실측 | 판정 |
| --- | --- | --- | --- |
| §8 체크리스트 항목수 | QA **11** | `grep -c "^- \[ \]"` → **11** | **QA 옳음, 내 relay(10) 틀림** |
| `배포`/`릴리스`/`푸시` | 0건 | `grep -cE` → **0** | QA 옳음 |
| 정책문서 시점 제약 | L384~388 `시점` 열, L427 "12월 내 필착" | 직접 출력 확인 | **QA 옳음** |
| 재오염원 | plan 문서 = BlogPost 전문 | plan **L12**가 스스로 "완성된 BlogPost 객체 리터럴" 명시, 대표 5줄 전건 HIT | **QA 옳음, 내 초판(16줄) 틀림** |
| 형제글 경계 | L5353 종료 | `awk` → L5353 `},` / **L5354 = 다음 글 여는 `{`** | **QA 옳음** |
| `lib/age.ts` | L16 기본 인자 | L16 `referenceDate: Date = new Date()`, L31·L41은 `getFullYear()` | **QA 옳음, 내 초판 틀림** |
| U+2212 | 비ASCII | `od -c` → `342 210 222`, ASCII 하이픈 **0건** | 확인 |
| A 줄/개소 | 14줄/17개소 | 원시 **16줄/19개소** − 보호 2줄 = **14/17** | 일치 |

**게이트 미실행 사유**: 이번 회차는 **코드 무수정**(변경 = `planning/` 1개 + `docs/team-log/` 1개). `tsc`/`test`/`build`를 돌릴 대상이 없다. "게이트 통과"가 아니라 **해당 없음**이다.

---

## 4. 산출물

`planning/ticket-44-age-table-2027-workorder.md` 정정(+약 130행):

1. **§0 배포 시점 게이트 신설** — 147일 실측, 반전 이력, 「착수 가능 ≠ 배포 가능」
2. **배포 하한 2026-12-26**(권장 12-29~31) + **근거표**(거짓 0일 선택지는 없음을 명시) + 잔여 6일 정직 인정
3. **§8을 8-1(착수 전 12항목) / 8-2(배포 전 11항목)로 분리** — 초판은 착수 목록만 있어 배포가 통제되지 않았다
4. **§5 제5 보호계열**(형제 글 `L5230~L5353`) + 제3계열에 `만 18세` 추가(5개 중 4개만 덮고 있었음)
5. **§10-1 재오염원** — plan 문서 **30/30줄** 대응표
6. **§10-4 파라미터화 제안** — 미결정 명시, 결재 3건
7. U+2212·U+2014 잘라붙이기 원칙, `lib/age.ts` L16 정정

---

## 5. 티켓

| # | 내용 | owner | 상태 |
| --- | --- | --- | --- |
| **#45** | 지시서 #44 배포 시점 게이트 부재 | qa→마스터 | **종결**(정정 완료) |
| **#46** | `AGE_TABLE_YEAR` 파라미터화 | 마스터 결재 대기 | **신규·미착수** |
| #44 | 나이 조견표 2027 갱신 | — | **12-26까지 배포 금지로 변경** |

**티켓번호 #46까지 소진**(배정 전 grep 확인).

---

## 6. 미확인 (명시)

- **QA R3 미실시** — R2 지적 5건을 수정했으나 그 수정에 대한 독립 재검증은 못 했다. 이번 회차에 정정문이 2라운드 연속 새 오류를 만들었으므로 **R3에서 또 나올 가능성이 있다.** "정정 완료"는 마스터 자체 검증(표 구조·잔존 참조 스윕)까지만 뒷받침된다.
- **`git fetch` 미실행** — 원격 동기 상태 미확인.
- **브라우저/모바일 실측 미수행** — 코드 무수정이라 대상 없음. 08-06의 `preview_start` 300초 타임아웃 ×2 문제는 **미해결 이월**.
- **GSC 색인/노출 — 23일째 측정 수단 없음.**
- 기획팀 앵커 분해(§10-4)는 **추론이지 문서화된 의도가 아니다.** `1960`·`1996` 2건이 §4-1과 어긋난다.

---

## 7. 사용자 확인 필요 블로커 — **오늘 신규 없음**

| # | 사안 | 알림 |
| --- | --- | --- |
| GSC | API 자격증명 부재(23일째) | 07-26 발송 — **재발송 금지** |
| #12 | 커스텀 도메인 | 08-01·08-02 — **재발송 금지** |
| #11 | 스케줄 세션 동시 실행 | 07-29 — **재발송 금지** |
| #6 | 공개 저장소 평문 크론 시크릿 | 07-27 — **재발송 금지** |

티켓 #45·#46은 팀/마스터 선에서 판정 가능 → **알림 대상 아님.**

---

## 8. 다음 실행 인계

1. **[🔴 최우선] QA R3** — 오늘 R2 지적 5건 수정분 독립 재검증. 정정문이 2라운드 연속 새 오류를 만들었다.
2. **[🔴 티켓 #44 — 배정 금지]** 개발팀에 **배정하지 말 것.** 12-26 이전 main push 금지. 8월에 할 수 있는 건 로컬 브랜치 작업까지이며, **그마저 #46 결재 전에는 헛수고가 될 수 있다**(파라미터화 채택 시 작업 방식이 통째로 바뀐다).
3. **[🔴 티켓 #46 결재 3건]** — 다음 회차의 실질 작업 후보. (i) §4-1 "2008년생 행 신설(15→16행)" 철회 여부 (ii) 앵커 귀속(`1960`·`1996`) (iii) 탐지기 강도.
4. **[티켓 #42 R3 — 여전히 미착수]** `blog-year-slug-policy.md`. 여전히 **untracked**. 단 오늘 이 문서가 **시점 제약의 원출처**로 확인됐으므로(§1-2) 폐기 대상이 아니다.
5. **[티켓 #34 승격 유지]** `updatedDate` 정책 — #44의 전제.
6. **[⏰ 시한]** `MINIMUM_WAGE` 교체(12-31) — **#44와 커밋 분리.**
7. **[신설 필요 — 유지]** 2027 고시 추적 주체 부재 6종(4대보험·이자소득세·한전·자동차세·DSR·중도상환수수료).
8. **[도구]** `preview_start` 타임아웃 — 헤드리스 대체 검토(08-06 §9-7 이월).

---

## 9. 운영 표준

- **[★신규 ㋩] 시한 항목은 "언제까지"와 "언제부터"를 **둘 다** 적어야 한다.** 초판은 마감(상한)만 있고 하한이 없어, 규정을 100% 준수하면서 147일 오배포가 가능했다. **상한만 있는 시한은 게이트가 아니다.**
- **[★신규 ㋪] 문서를 분리하면 제약이 탈락한다.** 어제 티켓 분리는 옳은 결정이었고 그 자체가 QA 3라운드 실패의 처방이었다. 그런데 분리 과정에서 원본의 `시점` 열이 승계되지 않았고, 남은 문장은 **의미가 반전됐다**("지연 사유 없음" → "기다릴 이유 없음"). **분리 시에는 원본의 제약 조항을 열 단위로 대조할 것.**
- **[★재확인 ㋎ — 이번 회차만 5건]** 정정문이 같은 오류를 반복했다: 오염 범위 53%만 지목(§3이 경계한 47% 실패 모드), 범위 종료점 누락(§3이 자기 성과로 내세운 `L4444` 함정), "8월에 전량 통과" 거짓 단정, 잔여일수를 하한 아닌 권장일 기준으로 기술, 표 구조 파손. **정정 직후가 가장 위험한 순간이다.**
- **[재확인 ㉹ — 13연속] 하류가 상류를 또 반증했다.** 이번엔 대상이 **어제의 나 자신**이었고, 반증자는 내가 결론을 숨긴 채 호출한 QA였다. 내가 relay한 "체크리스트 10개"도 QA가 11개로 정정했다.
- **[재확인 ㋦] 보호 목록은 작업 목록과 같은 크기의 산출물이다.** 제5 계열(형제 글)과 `만 18세`가 이번에 추가됐다. 보호 목록은 **두 회차 연속 불완전했다.**

---

## 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. `.env*` 미열람. **`git add -A` 미사용**(경로 명시 스테이징). 라이브 코드 **무수정**. 타 세션 워킹트리 무접촉. 이미지/영상 생성 도구 호출 0. 결제 행위 없음. 모든 줄번호·건수는 `a098191` 기준 직접 측정값이며, 검증하지 않은 전달값은 §6에 격리 표시했다.

---

## 01:50 실행분

### 결론

직전 회차가 **최우선으로 인계한 QA R3를 실제로 실행했고, 결과는 또 FAIL**이다 — **치명 3 / 중 6 / 경 7**. 정정문이 **3라운드 연속** 새 결함을 낳았다(㋎).

동시에 티켓 **#46 결재 2건을 확정**했다. 결재 과정에서 **기획팀이 자기 이론을 스스로 반증**했고, 그 반증 보고에서 **마스터가 다시 2건을 반증**했다. 라이브 코드 **무수정**.

---

## 1. 사전 조건

- HEAD `5f05109`. **`git fetch` 실행 — `origin/main...HEAD` = `0 0`**(직전 회차 §6의 "원격 동기 미확인" **해소**).
- 무인 안전수칙: `cd`+`git` 연결 호출 **0건**, Bash heredoc **0건**.

### ⚠️ 동시 세션 인수사항 (내 작업 아님 — 커밋하지 않았다)

세션 시작 시 `git status`에 **없었던** `scripts/regression/engines.test.ts`(**6줄**)가 회차 도중 워킹트리에 나타났다. 08-06 커밋 A와 같은 동시 세션 패턴이다.

- 내용: **주석만 변경, 로직 무변경.** 티켓 #32 문서 스윕 완료 사실 반영 + "2026-08-07 마스터 재확인 — 구 승수 `0.129457` 0건, 구 금액 9종 전건 0회" 기재.
- **내 회차가 검증하지 않았다.** 저 주석이 주장하는 재확인은 **내가 한 것이 아니다.** 다음 회차가 검증 주체를 확인할 것.

---

## 2. QA R3 — **FAIL** (치명 3 전건 마스터 재측정 확인)

QA에 이전 라운드 지적 목록을 **주지 않았다**(㉹). 결과: 수치·줄번호 주장은 **PASS 25항목**으로 거의 전부 정확했고, FAIL은 "잘못 셌다"가 아니라 **범위·게이트·자기모순** 축에서 나왔다.

| # | 내용 | 마스터 실측 | 조치 |
| --- | --- | --- | --- |
| **F1** | plan 문서 **리터럴 블록 밖**(L162~207)에 2027년 거짓 문장 잔존 | L170·L197·L198·L199 확인, **4줄 전부 `2026` 문자열 없음**(grep 비가시) | **정정** §10-1-b 신설 |
| **F2** | 배포 게이트가 **「8~11월」**만 검사 → **12-01~12-25 25일 구멍** | 해당 표현 **4개소**(QA는 2개소 지목) | **정정** 4개소 전건 `2026-12-26 이전`으로 치환 |
| **F3** | 머리말 L6 「대상 파일 `lib/blog.ts` **단일**」 ↔ §10-1 「2파일 같은 커밋」 | L6 문면 확인 | **정정** 2파일 명시 |
| F6 | 「현재는 **L74**로 재작성」 | **L74는 빈 줄**, 실제 L77~L78 | **정정** |
| F13 | §0-1 「64개(**16행**×4)=75개」가 §4-1 전제를 안 밝힘 | 15행 정본 → **60+11=71개** | **정정** |
| **F9** | `dday-countdown-plan-2026`이 어떤 티켓·보호목록에도 없음 | L4614~4872, 기준일 `2026년 7월 20일` **본문 3회 명시**(L4636·4663·4691) | **티켓 #47 신설**, §10-2-b 신설 |
| F4·F5·F7·F8·F10~F12·F14~F16 | 중·경 9건 | **미정정** | 다음 회차 |

**F2가 이번 회차 최대 소득이다.** 직전 회차가 "상한만 있는 시한은 게이트가 아니다"(㋩)라며 하한 12-26을 신설했는데, **그 하한을 검사하는 조항 자체가 월 이름으로 적혀 있어 12-10 오배포가 체크리스트를 100% 통과**했다. **㋩를 세운 바로 그 회차가 ㋩를 자기 문서에서 위반한 것**이다.

**F9는 QA가 스스로 만든 기준을 다른 글에 적용해 찾아냈다.** 이 글은 L4691이 기준일 종속성을 독자에게 설명하므로 **2027년에도 참** — 갱신 대상이 아니라 **보호 대상**이고, 전역 스윕이 닿으면 참인 문장이 거짓이 된다.

---

## 3. 티켓 #46 결재 — 2건 확정 / 1건 보류

### ① 기획팀에 **자기 이론 반증**을 지시 → 스스로 뒤집었다

앞선 주장 "조견표 15행 = 출생연도 앵커 **11** + 연 나이 앵커 **4**"에 대해, 마스터가 반대 증거(`lib/blog.ts` **L4407** "이 조견표는 **대표 연도만 골라 담은 참고표**")를 제시하고 판정을 요구했다. 기획팀 결론: **"11+4는 틀렸다. 실측은 10+4+1이고, 11은 세어서 나온 값이 아니라 15−4였다."**

### ② 그 반증 보고에서 **마스터가 다시 2건을 반증** (표준 ㋥)

| 기획팀 주장 | 마스터 실측 | 판정 |
| --- | --- | --- |
| 5의 배수 출생연도 10행 / 총 15행 / 40·50 결번 | 직접 열거 일치 | **팀 옳음** |
| `2004` 저장소 전역 **3곳뿐** | **4곳**(`blog-year-slug-policy.md:549` 누락) | 🔴 **팀 틀림** |
| 선정 규칙 문서화 **0** | `docs/team-log/2026-07-18.md` **L285**가 2007년 행을 **성년 경계로 명시** | 🔴 **팀 과장** |

### ③ 결재

- **(i) 「2008년생 행 신설(15→16행)」 철회 — 승인.** 근거: 증식이 L4407을 언젠가 반드시 거짓으로 만든다 / **2030-01-01부터 기존 15행이 전부 성년**이 되어 목적 자체 소멸(2010년생 만 19~20세, 마스터 산술) / 문장 문제는 L4395 재작성으로 해결.
- **(ii) 「연 나이 앵커」 설계 규칙 폐기 — 승인**(산술 관찰로 강등). 조견표 = **출생연도 15개 고정 리터럴**.
- **(iii) 탐지기 강도 — 보류.** 파라미터화 채택 자체가 미결이라 순서상 후행.

🔴 **결재에 딸린 마스터 실측 1건**: `만 19세` **4회** + `만 18세` **1회** = **이 글의 법정 상수 5개소가 전부 L4395 한 줄에 몰려 있다**(글 전체 수치와 동일). 재작성 대상 문단이 곧 보호계열 제3의 전량 보유처 = **가장 위험한 한 줄**.

**모순 방지**: 철회를 §4-1 헤딩·본문·§8 체크리스트·§0-1 수치 **4개 지점에 동시 반영**했다. F3(머리말↔§10-1 불일치)와 같은 종류의 자기모순을 새로 만들지 않기 위함이다.

---

## 4. 마스터 직접 검증

| 항목 | 실측 | 결과 |
| --- | --- | --- |
| 조견표 행 수 | `L4355~L4369` = **15행** | 확인 |
| 5의 배수 출생연도 | 2010·2005·2000·1995·1990·1985·1980·1975·1970·1960 = **10** | 확인 |
| `만 19세`/`만 18세` | 글 전체 **4 / 1**, 전부 L4395 | 확인 |
| `2004` 전역 | **4곳**(범위: `lib app components planning docs`, `.ts/.tsx/.md`) | 팀 반증 |
| 07-18 L285 | 성년 경계 + **"표 밖 임의 나이/연도 금지 가드레일"** 원문 확인 | 확인 |
| 정정 후 잔존 스윕 | `8~11월` 잔존 1건 = **의도적 인용**(초판 오류 설명), `lib/blog.ts 단일` 1건 = **의도적 인용** | 확인 |
| 표 구조 | 파이프 행 열 수 3/4/5 분포, 파손 0 | 확인 |
| 원격 동기 | `origin/main...HEAD` = `0 0` | 확인 |

**게이트 미실행 사유**: 이번 회차 변경 = `planning/` 1개 + `docs/team-log/` 1개. **라이브 코드 무수정**이라 `tsc`/`test`/`build` 대상이 없다. "통과"가 아니라 **해당 없음**이다. 브라우저/모바일 실측도 같은 이유로 대상 없음.

---

## 5. 티켓

| # | 내용 | owner | 상태 |
| --- | --- | --- | --- |
| **#46** | `AGE_TABLE_YEAR` 파라미터화 | 마스터 | **(i)(ii) 결재 완료 / (iii) 보류** |
| **#47** | 기준일 명시 글의 전역 스윕 보호 목록화 | qa/기획 | **신규·미착수** |
| #44 | 나이 조견표 2027 갱신 | — | **12-26까지 배포 금지**(게이트 구멍 정정 완료), **배정 금지 유지** |
| #45 | 지시서 배포 시점 게이트 | — | 종결(R3에서 F2로 재적발 → 재정정) |

**티켓번호 #47까지 소진**(배정 전 grep 확인).

---

## 6. 미확인 (명시)

- **QA R4 미실시** — 이번 정정분(치명 3 + 경 2 + 결재 반영 4개소)에 대한 독립 재검증을 못 했다. **3라운드 연속 정정문이 새 결함을 만들었으므로 R4에서 또 나올 가능성이 높다.**
- **QA R3 중·경 9건 미정정**(F4·F5·F7·F8·F10·F11·F12·F14·F15·F16). 특히 **F8**(보호 목록에 없는 산술 불변식 3건이 전부 "고쳐야 하는 줄" 안에 있음)과 **F4**(§0-3 "8월 작업 가능" ↔ §10-4 미결로 헛수고 가능)는 실질 위험.
- **동시 세션의 `engines.test.ts` 주석 주장 미검증**(§1 참고).
- GSC 색인/노출 — **24일째 측정 수단 없음.**
- `preview_start` 타임아웃 미해결 이월.

---

## 7. 사용자 확인 필요 블로커 — **오늘 신규 없음**

GSC 자격증명(07-26) · #12 커스텀 도메인(08-01·02) · #11 세션 동시 실행(07-29) · #6 평문 크론 시크릿(07-27) — **전부 발송 완료, 재발송 금지.** 티켓 #46·#47은 팀/마스터 선에서 판정 가능 → 알림 대상 아님.

---

## 8. 다음 실행 인계

1. **[🔴 최우선] QA R4** — 이번 정정분 독립 재검증. 3연속 전례가 있다.
2. **[🔴 QA R3 잔여 9건 정정]** — **F8·F4 우선**(실질 위험), 나머지는 문면 정정.
3. **[티켓 #47]** 36편 전체를 "기준일 명시형" 기준으로 스윕해 보호 목록 **한 번에** 확정. 형제 글·dday 글 외에 더 있는지 **미확인** — 계열이 회차마다 하나씩 늘고 있다(제5 → 제6).
4. **[티켓 #46 (iii)]** 탐지기 강도 — 파라미터화 채택 여부와 함께 결재.
5. **[티켓 #44 배정 금지 유지]** 12-26 이전 main push 금지.
6. **[⏰ 시한]** `MINIMUM_WAGE` 교체(12-31) — #44와 커밋 분리.
7. **[신설 필요 — 유지]** 2027 고시 추적 주체 부재 6종.

---

## 9. 운영 표준

- **[★신규 ㋫] 게이트는 날짜로 적어라. 월 이름으로 적으면 경계에 구멍이 난다.** F2가 그 실례다 — 하한을 `12-26`으로 세운 **바로 그 회차**가 검사 조항은 「8~11월」로 적어, 12-01~12-25 **25일**이 사고 정의에서도 체크리스트에서도 빠졌다. **규칙과 그 규칙의 검사기는 같은 단위로 적혀야 한다.**
- **[★신규 ㋬] 자기 이론을 반증하라고 되돌리면 팀은 실제로 뒤집는다.** 기획팀에 방어가 아니라 반증을 지시하자 "11은 세어서 나온 값이 아니라 15−4였다"고 자백했다. **다만 반증 보고 자체도 검증 대상이다** — 그 안에서 다시 2건이 틀렸다(2004 전역 3곳→4곳, "문서화 0"→L285 존재). **반증은 새로운 진실이 아니라 새로운 주장이다.**
- **[재확인 ㋎ — 3라운드 연속]** 정정문이 또 같은 유형의 결함을 낳았다. 이번엔 **㋩(상한·하한 병기)를 세운 회차가 자기 검사 조항에서 ㋩를 위반**했다.
- **[재확인 ㉹ — 14연속] 하류가 상류를 또 반증했다.** 이번 회차는 **2단 반증**이다: QA가 마스터 정정문을 반증(F1~F16), 기획팀이 자기 전 회차 보고를 반증, 그리고 마스터가 그 반증 보고를 다시 반증.
- **[재확인 ㋦] 보호 목록은 세 회차 연속 불완전했다.** 제5 계열(형제 글) → 제6 계열(dday 글). 계열이 회차마다 하나씩 발견된다는 건 **아직 다 못 찾았다는 뜻**이다 → 티켓 #47.

---

## 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. `.env*` 미열람. **`git add -A` 미사용**(경로 명시 스테이징). 라이브 코드 **무수정**. **동시 세션의 `engines.test.ts` 무접촉·미커밋.** 이미지/영상 생성 도구 호출 0. 결제 행위 없음. 모든 줄번호·건수는 `5f05109` 기준 직접 측정값이며, 검증하지 않은 전달값은 §6에 격리 표시했다.

---

## 02:36 실행분

> HEAD `4bf1c49` 기준. **이 회차의 축은 티켓 #52(자동차세 연납) 단일.** `planning/ticket-44-*` 계열은 동시 세션 작업 중이라 무접촉(티켓 #51 재발 방지).

> ⚠️ **[04:06 회차 마스터 주석] 이 절은 미완성이다 — 02:36 세션이 본문을 쓰다가 중단됐다.**
> 그러나 **작업 자체는 완료됐고 산출물이 남아 있다**: `docs/team-log/2026-08-07-0236-ticket52.md` (123행, §0~§7 완결).
> 요지 — 티켓 #52를 「3%를 5%로 고쳐라」가 아니라고 프레이밍 정정하고, 정본 산식 구조·편차 4가설(최대 1.983배)·전이폐포 목록을 남긴 뒤 **라이브 코드 무수정**으로 판정을 다음 회차에 넘겼다. 그 판단은 옳았다 — 04:06 회차가 실제로 판정했고, 유보 없이 상수만 5%로 바꿨다면 **틀린 값이 됐을 것**이다(정답은 요율 단독 교체가 아니라 기간비례 인자 추가를 동반한다).
> 로그 미완성을 「작업 없음」으로 읽지 말 것.


---

## 04:06 실행분

> HEAD `4bf1c49` 기준. **축 = 티켓 #52(자동차세 연납) 판정·정정 + #54(재오염원) 신설·종결.**
> 상세 판정문: `docs/team-log/2026-08-07-0406-ticket52-verdict.md`
> `planning/ticket-44-*`·`blog-year-slug-policy.md`는 동시 세션 소유 → **무접촉 유지**.

### 0. 사전 조건
- 02:36 세션이 **로그를 쓰다 중단**됨(헤더만 4행). 산출물 `2026-08-07-0236-ticket52.md`(123행)는 완결 → 고아 헤더에 마스터 주석 추가해 「작업 없음」 오독 차단.
- `lib/`·`scripts/` 워킹트리 clean → car-tax 작업에 충돌 없음 확인 후 착수.

### 1. 🔴 티켓 #52 — 라이브 결함 확정·정정 완료

**정본**(마스터 원문 직독 + 기획팀 독립 검증 + QA 3차 재확인, 3경로 수렴):
`연세액 × 미경과일수/365 × 이자율 5%` (지방세법 제128조제3항 제1호 + 시행령 제125조제6항)
→ 1월 연납 실질 공제율 **약 4.58%** (334/365 × 5%)

**결함**: 코드는 `본세 × 3% 정액` = 정본의 **65.6%만** 지급 → 납부액 과대 → **사용자에게 불리**.
**A케이스 9,600원 → 14,641원(1.525배).**

**02:36 회차 프레이밍 2건 반증**:
- 「세 축이 전부 다르다」 → **틀린 축은 둘**. 기준액(`baseTax`)은 **원래 옳았다**(「연세액」에 지방교육세 불포함, 제127조제1항이 정의).
- 배수 1.983배 → **1.525배**.

**3% vs 5% 논쟁 규명**(기획팀 최대 성과): 「10→7→5→3」은 부칙이 아니라 **옛 본칙**이었고, **2025년 이후 3% 단계는 시행 전에 삭제**됐다(대통령령 제35177호). 2차 자료는 **폐기된 예정값**을 복사 유통 중 → **다수결로 판정 불가 유형**.
**부칙 특례 부존재 증명**(마스터): `curl`로 시행령 전문 1,176,532B 직독 → `100분의 3` **0건**, 부칙 제8조는 율이 아닌 **적용례**.

### 2. 팀 핑퐁 (5왕복)
| # | 제기 | → | 결과 |
|---|---|---|---|
| 1 | 마스터 지시서 | dev | ✅ 구현. **단, 지시서 누락 2파일을 마스터가 자력 발견해 중간 정정 송신**(`CarTaxCalculator.tsx` import 삭제 시 빌드 파손 + `calculators.ts` FAQ/JSON-LD) |
| 2 | **dev → 마스터** | 되돌림 | 🔴 **마스터 지시서의 전이폐포 목록이 불완전**(14지점 누락, grep 포착률 36%). 범위 임의 확대 않고 보고 → 마스터 승인 후 정정 |
| 3 | dev 통독 | 자체 | 신규 2건(「만 원대」 서술형 규모 표현 — 금액 토큰조차 없음) |
| 4 | **planning → 마스터** | 되돌림 | 🔴 범위 밖 재오염원 2건 발견 → 마스터가 직접 정정(§4) |
| 5 | **QA → 마스터/dev** | 되돌림 | 🔴 **FAIL-1**(연도 한정 표기 3곳 소실) → 마스터 직접 정정 후 재검증 |

### 3. QA 판정 — **FAIL 2건 → 전건 해소 후 PASS**
A 법령정본 PASS(4/4, 부칙 전수 독립 재확인) · B 재검산 PASS(앵커 9/9 + 엣지 23/23, **엔진 미import 독립 스크립트**) · C 전이폐포 PASS(219줄 육안 통독, 잔존 **0**) · D 화면 PASS(라벨 `약 4.58%`, `5%` 오출력 0, JSON-LD 정합) · E **사보타주 재현 PASS** · F 빌드·회귀 PASS · G 무접촉 PASS

**🔴 FAIL-1 (차단)** — 정정문이 「2026년 기준」 연도 한정을 **3곳에서 삭제**(`calculators.ts` L699·L714, `blog.ts` L1037). QA 논거: 사이트 확립 규약 위반 / 이 수치는 실제로 바뀐다는 걸 이 티켓이 증명 / `334`는 평년 전용(2028 윤년 335·366) / **JSON-LD는 페이지 문맥과 분리 유통**돼 "상단 배너에 2026 있으니 괜찮다"가 안 통함.
→ **마스터 직접 정정 + 윤년 단서 병기.** 재측정으로 해소 확인(`calculators.ts` 12=12, `blog.ts` 252=252).

**🟡 FAIL-2** — 2027 추적 대장 미갱신 → 마스터 직접 정정(§4).

### 4. 🔴 마스터 직접 조치 — 2027 추적 대장의 재발 유도 (기획팀 발견)
`planning/external-notice-register-2027.md`는 **미래 세션 필독 문서**인데 L392(`0.03`)+L405(「불일치 의심」)+L433(「3% vs 5%」)를 함께 읽으면 **「0.03을 0.05로 바꿔라」로 귀결** → A케이스 **16,000원**(정본 대비 **+9.3% 과대**) = 같은 유형의 **반대 방향 재발**.
→ 4개 지점 정정: 정액율 아님 명시 · `× 334/365` 필수 · 이자율(5%)≠실질공제율(4.58%) 단위 구분 · **§3-1 종결 처리** · 부칙 미확인 해소 · **`PREPAY_JAN_DAYS` 윤년 시한 신규 등재**.

> ⚠️ 이 대장 §3-1 「상위 조문」 칸은 **이미 정확한 산식을 담고 있었다.** 그런데 같은 문서 「오류 방향」이 「할인율 과소」로 요약해 붕괴시켰다 — **표준 ㋱가 한 문서 안에서 일어난 실례.**

### 5. 마스터 직접 검증 (팀 보고 복창 아님 — 표준 ㋥)
| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | **exit 0** (정정 전후 2회) |
| 회귀 466 assertions | **ALL PASS** (car-tax 32→**47**) |
| `npm run build` | **성공**, 69/69 SSG |
| 엔진 ↔ **마스터 독립 산출 10케이스** | **10/10 원 단위 일치** |
| 화면(모바일 375 / 데스크톱 1280) | 라벨 `약 4.58%`, 할인 14,641 / 납부 401,359, **가로 오버플로 0 · 콘솔 오류 0** |
| 블로그 2편 구값 18종 | **잔존 0**, `3%` **0건** |
| JSON-LD | 「본세 3%」 0건, 연도 한정 복원 확인 |
| `publishedDate`·`slug` | **무변경** |
| 커밋 직전 상수 재확인 | `0.05` / `334` / `365` ✅ |

### 6. 미확인 (명시)
- **334 vs 332일** — 2026-01-31이 **토요일**(마스터 실측). 기한특례 적용 시 332일(4.55%)이나, 서초구 공시 **4.58%**가 334를 지지해 채택. 차이 A케이스 **96원**. 위택스 실고지값 미취득.
- **3·6월 공시율 0.01%p 편차**(3.77 vs 3.76 / 2.50 vs 2.51) 원인 미확인 — 1월만 구현하므로 실害 없음.
- **3·6·9월 연납 미구현** — 단일 상수 구조로는 4개 기간 표현 불가(피승수가 연세액↔제2기분, 6월은 일수 인자 자체 없음).
- **지방교육세가 공제 전/후 기준인지** 미확인(현행=공제 전, 본문 서술과 정합).
- `lib/car-tax.ts:43` 주석이 한도를 「단서」로 지칭(실제는 본문) + 기준 다른 두 비율 비교 — **결론은 옳고 계산·화면 무영향**(QA 비차단 관찰).
- GSC 색인/노출 — **25일째 측정 수단 없음.**

### 7. 사용자 확인 필요 블로커 — **오늘 신규 없음**
GSC 자격증명 · #12 커스텀 도메인 · #11 세션 동시 실행 · #6 평문 크론 시크릿 — **전부 발송 완료, 재발송 금지.** #52·#54는 팀/마스터 선에서 판정 가능 → 알림 대상 아님.

### 8. 다음 실행 인계
1. **[⏰ 신설]** `PREPAY_JAN_DAYS`/`PREPAY_YEAR_DAYS` **2028 윤년 335/366** — 티켓 #53(`CAR_TAX_BASE_YEAR`)과 **동일 시한 계열로 병합**. 산문에도 `334`·`4.58%` 하드코딩됨.
2. **[티켓 #54 잔여]** `docs/team-log/2026-07-30-blog-audit.md` L197·L201이 구값 `9,600/406,400/11,994`를 **PASS로 인증**한 기록 → 팀로그는 append-only라 미수정. **미래 세션이 「이미 감사 PASS」로 읽고 재검증을 건너뛸 위험.** (#30에 이은 「내부정합 감사가 상수오류를 못 잡는다」 3번째 실례)
3. **[티켓 #52 잔여]** 3·6·9월 연납 미지원 — 화면이 그 범위를 명시하는지 확인 후 보강 여부 판단.
4. **[QA R4 미실시]** 이번 마스터 정정분(FAIL-1 3곳 + 대장 4곳)은 **독립 재검증을 못 받았다.** 3라운드 연속 전례 있음.
5. **[티켓 #44 배정 금지 유지]** 12-26 이전 main push 금지.
6. **[⏰ 시한]** `MINIMUM_WAGE` 교체(12-31) — #44와 커밋 분리.
7. **[#27]** 나이 조견표 2027 — 외부 고시 대기 불필요한 유일 항목.

### 9. 운영 표준 (상세는 판정문 §7)
- **[★신규 ㋲] 상수 파생 성질 검사는 상수 오염을 원리적으로 못 잡는다.** 사보타주 실증: 이자율을 `0.0328`로 오염 + 앵커 재생성 시 성질 검사 **전부 PASS**, 451 assertions 중 **외부 공시값 리터럴 `4.58` 1줄만 FAIL**. → **외부 리터럴은 「리터럴 금지」 대상이 아니라 유일한 탐지 수단.** 제거 금지 주석 삽입.
- **[★신규 ㋳] 사보타주 검증과 커밋 권한이 같은 워킹트리를 공유하면 안 된다.** 04:42 마스터 대조가 **10케이스 전부 FAIL** → QA 사보타주 진행 중이었다. 오늘 02:36 세션이 실제로 작업 중 죽은 전례가 있으므로, QA가 사보타주와 복원 사이에서 죽으면 **정정하러 온 회차가 더 나쁜 값을 라이브에 올린다.** → **커밋 직전 핵심 상수 재확인 의무화**(실제 수행, §5 마지막 행).
- **[★신규 ㋱] 하류 문서가 상류의 불확실성 표시를 삼킨다.** 발원지 `car-tax-design.md:30`이 상류 `calculator-lineup-expansion.md:69`의 **옳은 기간비례 표 + 「반드시 재확인」 경고**를 「3% (0.03)」 단일값으로 붕괴시켰다. **상류는 옳았고 아무도 구해주지 못했다.**
- **[★신규 ㋮] 두 수를 비교하기 전에 단위가 같은지 확인하라**(02:36 제안 → 이번 회차에 3회 재현: 최초 오진 / UI 라벨 함정 / 대장 요약).
- **[재확인 ㋊] grep 포착률 ≈ 1/3이 상수로 확정.** 세 독립 측정 일치 — dev `lib/blog.ts` **36%**, 기획 4개 문서 **34.4%**, 메모리 `MINIMUM_WAGE` **34%**. 기획팀 추가 관측: 미포착 42건 중 **11건은 `3%` 문자열이 실재하는데 목록화 단계에서 절삭**(도구 문제 아님) → **`grep -c` 총수와 열거 개수 대조 절차 필요.** 발원지 파일 포착률이 **17.6%로 최저**(코드블록·산식 표기 변형 탓).
- **[재확인 ㋎ — 11연속]** 정정문이 또 같은 유형 결함 생성. 이번엔 **산술이 아니라 표기 축**: 3%가 8개월 산 구조적 원인이 「연 단위 갱신 표지 부재」인데, 그 정정문이 연도 표지 3개를 지웠다.
- **[재확인 ㉹ — 15연속] 하류가 상류를 반증했다. 이번엔 3중**: dev가 마스터 지시서 반증(14지점) / QA가 dev+마스터 반증(FAIL-1) / 기획이 범위 밖 재오염원 반증.
- **[재확인 ㋣ — 마스터 자신의 위반]** 02:36 노트의 전이폐포 목록을 **검증 없이 완전한 것으로 취급**해 지시서에 실었다. 표준 ㋥가 요구하는 「relay와 검증의 구분」을 내가 어겼다.

### 제약 준수
확정 컨셉/스택/구조 임의 변경 없음. Vercel CLI 직접 배포 안 함. `.env*` 미열람. **`git add -A` 미사용**(경로 명시 스테이징 — 공개 저장소 + `.claude/settings.local.json` 평문 시크릿). 이미지/영상 생성 도구 호출 0. 결제 행위 없음. 동시 세션 소유 파일 무접촉.
