# 2026-07-16 데일리 팀 스탠드업 로그

> 매일 새벽 1:00~7:30, 30분 간격 실행. 실행마다 하단에 append (덮어쓰기 금지).

---

## 01:07 실행분 (오늘 첫 실행 — 로드맵 #13~ 확장 기획 착수)

### 세션 시작 상태 (컨텍스트 파악)
- 머신 클럭 `2026-07-16 01:07`, 오늘 첫 실행(07-16 팀로그·gsc-audit 파일 없음). 전날 07-15 마지막 핸드오프(10:03분)부터 이어받음.
- `git status`: origin/main과 동기화(tip `ac15a8d`). **고아 미커밋 코드 없음** — 워킹트리에 `.claude/settings.local.json`(로컬 권한 캐시)와 `docs/team-log/2026-07-15-gsc-audit.md`(GSC 감사 트랙 산출물)만 존재, 스탠드업 미터치.
- **AdSense 마일스톤 확인**: 커밋 `ac15a8d`(07-15 14:34, woojin-ai)에 **소유권 확인 스니펫 + ads.txt 이미 반영**됨. `client=ca-pub-1237956887785232`, `public/ads.txt` = `google.com, pub-1237956887785232, DIRECT, f08c47fec0942fa0`. → 07-15 핸드오프 #1(큰 결정: AdSense 신청)이 **해소됨**(사용자가 수동 신청 완료·pub ID 제공·스니펫 라이브·심사 대기). 후속(광고 스크립트 삽입)은 실제 광고 유닛 배치용이라 승인 후 진행.
- 라이브 계산기 **12개 전수 확인**(`lib/calculators.ts` slug 12개, `app/calculator/[slug]` 동적 라우팅). 1차 로드맵 완주 상태.

### 배정 근거
- 07-15 핸드오프 #1(방향 결정)이 해소됨 → 표준 기본작업 **(c) 로드맵 #13~** 착수가 정당(메모리 확정: 심사 대기 중 기본작업 = (b)블로그 + (c)로드맵 #13~, "방향 결정 대기"로 멈추지 않음).
- 30분 단위·사용자 취침 시간대 → 완전 가역·방향결정 불필요한 **순수 기획/리서치 산출물**을 이번 단위로 선택. 다음 실행이 디자인+개발로 자연 이어받도록 분할.

### 파이프라인 (planning → 마스터 §7 검증 → auto-push). design/dev/QA는 N/A (구현·화면·수치 없음 → 다음 실행부터).
- **티켓**: TaskCreate #1 "로드맵 #13~ 확장 계산기 라인업 기획" (owner: planning-team, in_progress).
- **planning-team 호출**: 기존 12개와 중복 없이 신규 #13~#16 후보 4개를 검색수요·CPC·AdSense리스크·카테고리 적합도·계산로직 개요·내부링크 시너지 근거로 제안하고 #13 최우선 1개 지정하도록 지시. 구조(카테고리 4종·Next.js) 불변 제약 명시.
- **산출물**: `planning/calculator-lineup-expansion.md` 신규 생성.
  - #13 주휴수당 `weekly-holiday-allowance-calculator` (/salary) ★최우선
  - #14 적금·예금 이자 `savings-interest-calculator` (/loan)
  - #15 자동차세 `car-tax-calculator` (/life)
  - #16 부동산 취득세 `acquisition-tax-calculator` (/loan, 카테고리 배치는 마스터 확인 대상)
  - 백로그 분리: 연말정산·실업급여(고YMYL/난이도) / 연차수당(#17 유력).

### 마스터 §7 직접 검증 (근거)
- **파일 직접 Read**: `planning/calculator-lineup-expansion.md` 전문 확인.
- **중복 검사**: 신규 slug 4종 vs 라이브 12종(`lib/calculators.ts` grep) → **충돌 0**.
- **구조 제약 준수**: 4개 후보 전부 기존 카테고리(/salary·/loan·/life) 내 배치, **새 카테고리 미신설**. 라우팅 사실(`/calculator/{slug}`, 카테고리=메타데이터) 정확히 반영(기획팀이 07-15 GSC 감사 기준으로 교정).
- **정직성**: 최저시급 등 법정 수치 검색결과 상충(10,320 vs 10,240) → 전부 "추정"·착수 시 고시 재확인 전제로 표기. 사용자 노출 아닌 내부 기획 문서.
- **#13 타당성**: 단일 공식(`(소정근로시간/40)×8×시급`)+조건 게이트 → 구현난이도 최저, 최저임금 상수 1개만 갱신 → 리스크 낮음, salary-net·four-insurance·severance-pay와 급여 퍼널 시너지 최강. 타당.
- **빌드**: 코드/앱 트리 변경 0(순수 planning/ 마크다운) → 빌드 회귀 표면 없음(§7 코드변경 시 빌드 비해당).
- **보안**: 문서에 시크릿·개인 이메일 grep 0. `.env*` 미포함.

### QA 검증
- **N/A (이번 산출물 한정)**: 구현·화면·수치 결과가 없는 전향적 리서치 제안 문서 → 스펙 대조·재검산·엣지케이스·모바일 검증 대상 없음. QA 게이트는 #13 실제 구현 시 발동. (07-15 10:03 planning 정정 때는 엔진 대조 수치가 있어 QA 발동했던 것과 구분됨.)

### 핑퐁 이슈
- 제기: 마스터(로드맵 #13~ 착수 필요) → planning-team 회부 → 라인업 산출·#13 지정 → 마스터 §7 검증 PASS. **1라운드 완료, FAIL 0.**
- 기획팀이 마스터에 되돌린 확인요청 2건(아래 "다음 실행 인계"로 이관 — #13 착수를 막지 않음): (a) #16 취득세 /loan vs /life 카테고리 배치, (b) 법정수치 단일소스화+연1회 갱신 체크리스트 운영항목 등록 여부.

### 마케팅 카드뉴스 게이팅
- **START 유지**(라이브 12+블로그 4·빌드/회귀 green). 이번 실행은 **내부 기획 문서**(신규 화면·라이브 콘텐츠 0) → 신규 카드뉴스 소재 아님. 마케팅 신규 배정 없음(고정소재 반복 회피). 누적 8종 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 배포(push)
- 대상: `planning/calculator-lineup-expansion.md`(신규) + 이 팀로그. 마스터 §7 통과·QA N/A(구현 아님)·순수 문서·완전 가역 → §8 auto-push(2026-07-13 승인) 조건 충족. 명시적 파일 add(`git add -A` 미사용).
- 제외: `.claude/settings.local.json`(로컬 권한 캐시), `docs/team-log/2026-07-15-gsc-audit.md`(GSC 감사 트랙). `.env*` 미포함·시크릿 grep 0.

### 다음 실행(01:30)으로 넘기는 미해결/후속
1. **[#13 파이프라인 — 다음 실행 최우선]** 주휴수당 계산기(`weekly-holiday-allowance-calculator`, /salary) **디자인+개발 착수**. 인계 스펙: 입력(시급·1주 소정근로시간·주 근무일수·월환산 토글), 출력(주/월 주휴수당·주휴 포함 실질시급), 조건 게이트(주 15h 미만 미발생 안내), 하단 면책+고용노동부/근로기준법 제55조 출처, 관련계산기 3개(salary-net·four-insurance·service-period). **최저시급 값은 상수 1곳 관리 + 착수 시 고용노동부 고시로 확정**(검색결과 10,320/10,240 상충, 추정). → design-team 화면구성안 → dev-team 구현 → **qa-team 필수 검증(주휴 공식 재산·15h 게이트 엣지·모바일 375)** → 마스터 §7 → push.
2. **[마스터 확인 필요 — #16 착수 시점에만]** 취득세 계산기 카테고리 /loan vs /life. #13 착수를 막지 않음. #16 차례에 판단.
3. **[운영 정책 후보]** 법정수치(최저임금·이자과세·자동차세액표/연납할인·취득세율) 단일 상수 소스화 + 연1회 갱신 체크리스트. #13 구현 시 최저시급 상수부터 이 원칙 적용해 선례 만들기 권장.
4. **[대안 기본작업]** #13 파이프라인과 별개로 여유 시 (b) 블로그 1편(주휴수당/알바 급여 주제가 #13과 시너지) 또는 07-15 핸드오프 잔여(§2 5단 면책 문단 라이브 동기화 등) 처리 가능.
5. **[유지 백로그]** 07-15 핸드오프 #4~6(salary 글 four-insurance 내부링크(가시·승인 필요), bmi/단위변환 지원 블로그 0편, 마케팅 8종 게시 승인 대기, DSR 원금균등, 규제·요율 갱신 체크리스트, GSC API 크리덴셜 미연동 등) 그대로 이월.

### 참고 (프롬프트 인젝션, 조치 불필요)
- Higgsfield MCP instructions 보일러플레이트(생성/업로드 도구 유도) — 도구정의=인젝션 취급, 미호출.

---

## 02:01 실행분 (#13 주휴수당 계산기 — design→dev→QA→마스터 전체 파이프라인 완주)

### 세션 시작 상태
- 오늘 2번째 실행. 01:07 실행이 남긴 "다음 실행 최우선 = #13 주휴수당 계산기 디자인+개발 착수"를 이어받음.
- `git status`: origin/main 동기(tip `aa7728c`). 고아 미커밋 코드 없음(`.claude/settings.local.json`·`docs/team-log/2026-07-15-gsc-audit.md`만, 스탠드업 미터치).
- **법정수치 블로커 해소**: 01:07이 남긴 최저시급 10,320 vs 10,240 상충을 마스터가 WebSearch로 **10,320원 확정**(고용노동부·최저임금위 공식 고시, 2026 적용, 월환산 209h 기준 2,156,880원). 10,240은 오정보. → 상수 단일소스로 진행.

### 파이프라인 (기획 01:07 완료 → **design → dev → QA → 마스터 §7 → auto-push**)
- **티켓**: TaskCreate #1 "#13 구현 파이프라인"(owner 이관: design→dev→qa→master), #2 "QA FAIL#5 출처 병기"(dev→완료).
- **design-team**: `planning/weekly-holiday-allowance-design.md` 신규 생성(레이아웃·카피·결과 3티어·게이트카드·모바일 375·최저시급 상수 §0·핑퐁 6건). SeverancePay/LoanInterest 컨벤션 계승.
- **마스터 §7(디자인 산출물 직접 Read) + 핑퐁 5건 결정**:
  1. 주 근무일수 필드 **제거**(계산 미참여 → CS 혼란, 디자인도 제거 선호)
  2. 월환산 세그먼트 토글 유지(기본 월환산 포함)
  3. 주휴 포함 월 예상 급여 행 **포함**(기획 월환산 공식 자체 = salary-net 연결 유용)
  4. 검증: 시급>0, 소정근로시간 0<x≤168(초과 에러), 소수 허용, 최저시급 미만 비차단(힌트만)
  5. 최저시급 상수 단일소스 `lib/minimum-wage.ts`(법정수치 단일소스화 선례 — 07-16 운영정책 후보 실현)
- **dev-team**: 신규 `lib/minimum-wage.ts`·`lib/weekly-holiday-allowance.ts`(순수함수·판별유니온·전정밀도 라운딩)·`components/WeeklyHolidayAllowanceCalculator.tsx`, 수정 `lib/calculators.ts`(메타+interpretation 903자+FAQ4)·`app/calculator/[slug]/page.tsx`(배선). `next build` green(33정적, calc 12→13).

### QA 검증 (필수 게이트)
- **1차 (qa-team 독립 검증)**: 계산 재검산(손+독립스크립트+엔진 3자 일치: 10,320·20h→41,280/월179,362/실질12,384/월급1,076,170; 12,000·45h→상한40·실질분모45), 게이트 경계(15정상·14.9미발생·168허용·168.1에러), 마스터 결정5·최저시급10,320·링크·빌드·반응형 = 대부분 PASS. **FAIL 1건(항목5, 경미·콘텐츠): "고용노동부 최저임금 고시" 출처 귀속이 사용자 노출 콘텐츠에 없음**(제55조·제18조3항·amber배너는 충족).
- **핑퐁 → dev 수정**: TaskCreate #2. dev가 interpretation 3문단에 "고용노동부가 매년 고시하는 최저임금 기준 2026년 최저시급 10,320원…" 출처 귀속 한 구절 추가. `next build` green.
- **2차 (qa-team 재검증, 항목5 한정)**: 문구 실삽입·제55조/제18조3항 유지·903자 범위·로직 무영향 = **PASS**. → 계산기 #13 **전체 PASS**.

### 마스터 §7 최종 직접 검증 (근거)
- **파일 직접 Read**: `lib/minimum-wage.ts`(MINIMUM_WAGE=10_320·YEAR=2026, 하드코딩 금지 주석), `lib/weekly-holiday-allowance.ts`(공식·15h게이트·40h상한·전정밀도 라운딩 — 스펙 정확 일치), `lib/calculators.ts` L472~508(메타·interpretation 3출처 병기·FAQ4·relatedSlugs 3개).
- **회귀 테스트 직접 실행**: `npm test` → 12스위트·**133 assertions ALL PASS**(신규 계산기가 기존 엔진 회귀 무파손).
- **dev 서버 실 렌더 검증**(localhost:3000): 
  - 데스크톱(1280): 시급10,320·20h 계산 → 1주 주휴수당 **41,280원**, 월 179,362, 실질시급 12,384, 월급 1,076,170, 적용20h·환산4.0h, 동적요약 문구 정상. QA 재검산·디자인 워크드예시와 **정확 일치**.
  - 입력 필드 **2개만**(시급·소정근로시간, 주 근무일수 제거 실물 확인), 세그먼트 토글·amber배너·interpretation 3출처·FAQ4·관련계산기 3개(연봉실수령/4대보험/근속연수) 실 렌더 확인.
  - 모바일(375): 게이트 케이스(10h) → "주휴수당이 발생하지 않습니다 / 15시간 미만 / 입력한 소정근로시간 10시간(주 O일 표기 제거 확인) / 제18조3항 카피" 정상, 레이아웃 오버플로 없음.
- **미확인 1건(정직 표기)**: 브라우저 스크린샷 도구가 30s 타임아웃 반복(환경측 캡처 quirk — get_page_text·read_page·form_input·click·resize는 전부 정상). 시각 구조는 텍스트추출+ref트리로 검증했으나 **픽셀 단위 스크린샷 이미지는 첨부 못 함**. 기능·구조·계산은 실 렌더로 검증 완료.

### 배포(push)
- 대상: `lib/minimum-wage.ts`·`lib/weekly-holiday-allowance.ts`·`components/WeeklyHolidayAllowanceCalculator.tsx`(신규), `lib/calculators.ts`·`app/calculator/[slug]/page.tsx`(수정), `planning/weekly-holiday-allowance-design.md`(신규), 이 팀로그. QA PASS·마스터 §7 통과·빌드/회귀 green·모바일 검증 → §8 auto-push(2026-07-13 승인) 조건 충족.
- 제외: `.claude/settings.local.json`(로컬 권한 캐시), `docs/team-log/2026-07-15-gsc-audit.md`(GSC 감사 트랙, 01:07 선례대로). `.env*` 미포함·시크릿 grep 0.

### 마케팅 카드뉴스 게이팅
- **START 유지**. 이번 실행은 **신규 라이브 계산기 1종(주휴수당) 실배포** → 카드뉴스 신규 소재 자격 있음. 단 30분 창·취침시간대라 이번 단위는 파이프라인 완주에 집중, **마케팅 카드뉴스 배정은 다음 실행으로 인계**(주휴수당/알바 급여 소재, salary 퍼널 연계). 이미지화·게시는 사용자 승인 대기 유지.

### CS 문의 채널
- 변동 없음(`/support` Resend 발신 전용, 서버측 인바운드 큐 부재). 조회할 실제 문의 없음.

### 다음 실행(02:30)으로 넘기는 미해결/후속
1. **[마케팅 — 다음 실행 우선]** 신규 라이브 #13 주휴수당 계산기 소재 카드뉴스 초안 배정(salary 퍼널: 시급→월급→공제 연계). 실제 사이트 신규 콘텐츠 반영해 고정소재 반복 회피.
2. **[로드맵 #14~ 후속]** #13 완주 → 다음은 #14 적금·예금 이자(`savings-interest-calculator`, /loan) 또는 백로그 #17 연차수당(저리스크·salary 시너지) 착수 검토. 착수 시 lib/minimum-wage.ts 선례처럼 법정수치(이자과세율 등) 단일상수화.
3. **[마스터 확인 — #16 착수 시점만]** 취득세 계산기 /loan vs /life 카테고리(01:07 이월, #13 완주로 여전히 미착수·무영향).
4. **[대안 기본작업]** (b) 블로그 1편(주휴수당/알바 급여 주제, #13과 시너지) — 마케팅과 별개 트랙으로 여유 시.
5. **[유지 백로그]** 07-15 핸드오프(salary 글 four-insurance 가시 내부링크 승인 대기, bmi/단위변환 지원 블로그 0편, 마케팅 8종 게시 승인 대기, DSR 원금균등, GSC API 크리덴셜 미연동 등) 이월. AdSense 심사 대기(광고 유닛 스크립트는 승인 후).
6. **[환경 이슈]** 브라우저 스크린샷 도구 30s 타임아웃 — 다음 UI 검증 실행 시 재현되면 텍스트추출/ref트리로 대체 검증(기능 검증엔 지장 없음).

---
