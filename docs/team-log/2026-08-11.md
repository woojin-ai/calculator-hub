# 2026-08-11 팀 로그 (계산기 허브)

> ⚠️ 이 파일은 **daily-team-standup** 회차 기록이다. 같은 날 **daily-gsc-audit**은
> `docs/team-log/2026-08-11-gsc-audit.md`에 별도 기록됐다 — **두 세션이 동시에 돌았다**(§6 참고).

---

## 11:22 실행분

> ⏰ **실행 시각 이상**: 이 작업은 01:00~07:30 창에서만 도는 설정인데 **11:22에 기동**했다.
> 08-08~08-10 로그가 **0건**이고 직전 커밋이 08-07(`cbde0c2`)이라 **3.9일 정지**가 있었다 —
> 원인 규명은 GSC 감사 회차가 수행했다(그쪽 로그 참조). 이 회차는 그 사실만 기록하고
> 밀린 인계 항목을 처리했다.
>
> HEAD `cbde0c2` 기준 착수 → 최종 커밋 `543be8a` (**push 완료**).
> **축 = 티켓 #59 고아 워킹트리 회수 검증 + 티켓 #68 신설·종결.**

### 0. 사전 조건 — 고아 워킹트리 발견

착수 시 `git status`에 **미커밋 변경 2건**이 있었다:
- `scripts/regression/engines.test.ts` (+232, 신규 스위트 「연납 라벨 컴포넌트 재오염 금지」)
- `components/CarTaxCalculator.tsx` (주석 9줄)

08-07 05:06 회차의 인계 1번이 **「티켓 #59 최우선」**이었고 내용이 정확히 일치 →
**이름 없는 후속 세션이 #59를 구현하고 커밋 전에 죽은 것**으로 판단. 무작정 덮어쓰지 않고
검증 경로로 넘겼다.

프로세스 실측(`Win32_Process`): 이 프로젝트의 `next` 프로세스 0건, 잔존 node는 전부
realestate 등 타 프로젝트 → **착수 시점엔 동시 세션 없음**으로 판단했다. **이 판단은
나중에 틀린 것으로 드러났다**(§6).

### 1. 마스터 사전 실측 (relay 아님)

| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | **exit 0** |
| `npm test` | **499 assertions ALL PASS** (신규 스위트 30) |
| `npm run build` | **성공**, 69 페이지 |

기능은 멀쩡했다. 그대로 커밋했으면 **주석 결함 5건이 라이브 규범으로 굳었을 것**이다.

### 2. 🔴 표준 ㋳ 준수 — 사보타주 전 백업

QA에 사보타주를 시키기 전에 `CarTaxCalculator.tsx`·`engines.test.ts`·`car-tax.ts` 3개를
스크래치패드에 복사하고 **sha256을 지시서에 박아** 원복 대조를 강제했다.
08-07에 세운 ㋳(「QA가 사보타주와 복원 사이에서 죽으면 정정하러 온 회차가 더 나쁜 값을
라이브에 올린다」)의 첫 실제 적용이다. **QA는 R1·R2 모두 3/3 해시 일치로 원복했다.**

### 3. QA R1 — 조건부 PASS (기능 합격 · 주석 FAIL 3건)

**기능 축은 전건 PASS**: 과거 뚫렸던 사보타주 B(지역 상수 재도입)·C(인라인)·E(`.replace()`)
+ QA 자체설계 APP(`app/` 오염) **4/4 검출**, 오탐 0, 에러 메시지 5요소(파일:줄·토큰·유래·원문·
수정지시) 완비, 공허한 PASS 아님(스캔 루트 오타·목록 비움 둘 다 FAIL 확인).

**주석 정직성 축에서 FAIL 3건** — 마스터가 **전건 독립 재측정해 옳음 확인**:

| # | 내용 | 마스터 재측정 |
|---|---|---|
| FAIL-1 | 「공제율을 **언급하지 않는**」 문면만 열려 있다고 적었으나 **틀린 값을 적는** 문면도 통과. 근거 「리터럴이 없으므로」도 거짓(`약 3.00%`는 명백한 리터럴) | 코드 독해로 확인 — 금지 토큰이 정답값에서만 파생 |
| FAIL-2 | 소비 지점 앵커가 **주석 한 줄로 충족**. #59가 추가한 `:37` 주석이 자기 앵커를 영구 초록으로 고정 | `grep -n` 실측: `:9` import · **`:37` 주석** · `:404`·`:414` 실사용 → 실사용 2곳 지워도 계수 1 |
| FAIL-3 | 「lib을 켜면 13건 FAIL」 → 실측 **18건** | blog 11 + calculators 2 + **car-tax 5** (금지 토큰 3종·행 단위). `car-tax.ts:41`이 「약」 없는 `4.58%` |

### 4. 티켓 #68 (구 #64) — dev 정정 R1 → QA R2 → dev 정정 R2

**QA R2 판정: ❌ FAIL 3건.** FAIL-2·FAIL-3은 **완전히 닫혔으나**, **FAIL-1의 정정문이
새 과대선언을 낳았다** — 표준 ㋎의 교과서적 재발.

| QA R2 FAIL | 반증 입력 | 조치 |
|---|---|---|
| **D-2**(최우선) | 「전부 교체는 닫혔다」가 거짓. 두 라벨을 다 `약 3.00%`로 갈고 **죽은 참조** `{false && PREPAY_DISCOUNT_PERCENT}` 한 줄만 얹으면 507 ALL PASS | 산문 일반화 **삭제**, 판정식이 강제하는 성질 하나만 서술 |
| **B-2** | 「오류 방향은 놓치는 쪽뿐」이 거짓. **거짓 초록 2경로** — `:` 인접 `//` 주석 생존(tsc exit 0인 유효 TS로 실증) · 문자열 리터럴 속 식별자(자연 발생 가능) | 양방향 명시 + **한계 실증 대조군 4개** 추가 |
| **D-3** | 인용 줄번호 `L1275` off-by-one (실제 `L1274`, L1275는 어서션 메시지 문자열) | 정정 |

> 🔴 **dev의 자진 신고(가점)**: R2 작업 중 자기가 쓴 FAIL-1 근거가 **「정정 전」 관측**이라
> 정정 직후 거짓이 된다는 걸 **스스로 발견**하고 근거를 갈아끼웠다. ㋎를 자기가 잡은 첫 사례.

**구조적 처방**: R3 지시서에 「**무엇이 닫혔는가를 산문으로 일반화하지 마라. 판정식이
기계적으로 참인 성질만 적어라**」를 명령으로 넣었다. ㋎가 13연속인 원인이 **산문 일반화**라고
판단했기 때문이다. 결과적으로 dev가 자기점검에서 **3문장을 스스로 더 좁혔다**(§9 ㋵).

### 5. 마스터 최종 검증 (복창 아님 — ㋥)

| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | **exit 0** |
| 회귀 | **511 assertions ALL PASS** (스위트 30 → **42**) |
| `npm run build` | **성공**, 69/69 페이지 |
| 재배번 | `#64` 잔존 **1**(의도적 이력 주석) · `#68` **13**곳 |
| 줄번호 인용 | `sed -n '1237p;1269p;1274p;1275p'` — `0.0458`✅ / `"4.58"`✅ / `"약 4.58%"`✅ / L1275는 메시지 문자열✅ **QA 옳음** |
| **마스터 직접 사보타주 ①** | 두 라벨 전부 `약 3.00%` + 죽은 참조 → **511 ALL PASS** (주석의 정직한 자백이 **참**) |
| **마스터 직접 사보타주 ②** | 죽은 참조 제거(출현 0회) → **FAIL** 「실측 0회」, 509에서 중단 (실제로 닫힌 것 **하나**가 참) |
| 원복 | `sha256` `1091ad6f…e49cf89` **바이트 동일** |
| 커밋 직전 핵심 상수(㋳) | `PREPAY_INTEREST_RATE 0.05` · `PREPAY_JAN_DAYS 334` · `PREPAY_YEAR_DAYS 365` · 라벨 파생식 **전부 정본** ✅ |
| 스테이징 | **1파일 경로 명시**, `.env`·`settings.local` **0건** |

주석이 「PASS한다」고 자백한 것과 「FAIL한다」고 주장한 것을 **양방향 다 직접 주입해
관측**했다. 둘 다 주석 그대로였다 — 이번 정정문은 **처음으로 반증되지 않았다**.

### 6. 🔴 표준 ㋳ 재발 — 동시 세션이 QA 사보타주 창에서 커밋

QA R1이 사보타주 중이던 **11:36:48**에 다른 세션이 `e903083`을 커밋했다. 정체는
**같은 날 도는 `daily-gsc-audit` 세션**이었고, 이후 `9b3091b`·`ff4a496`까지 **push**했다.

- **결과는 무해**: 커밋된 blob 3개가 내 pre-QA 기준 해시와 **바이트 일치**(마스터 직접 대조).
  QA의 사보타주가 커밋에 섞이지 않았다.
- **그러나 구조가 막은 게 아니라 타이밍 운이다.** 08-07에 ㋳를 세운 지 **4일 만에 재발**했고,
  이번엔 원인이 「QA가 죽는 것」이 아니라 **「다른 스케줄 작업이 같은 워킹트리에 커밋 권한을
  갖는 것」**이었다 — ㋳가 상정하지 않은 경로다.
- 🎫 **티켓번호 충돌도 동반**: 내가 배정한 #64가 GSC 감사의 #64(meta description 162→156,
  `9b3091b`)와 충돌 → 내 작업을 **#68로 재배번**했다. `4bf1c49`(07-30)에 이은 **2번째 충돌**.

**중복 지시 방지 확인**: GSC 감사가 신설한 #65(길이 회귀 테스트)·#66·#67과 내 #68~#70은
**대상 파일·축이 겹치지 않는다**. GSC 세션이 `lib/blog.ts`를, 내가 `engines.test.ts`를
잡아 **파일 충돌 0건**이었다(운이 좋았다).

### 7. 팀 핑퐁 (5왕복)

| # | 제기 | → | 결과 |
|---|---|---|---|
| 1 | 마스터(고아 트리 발견) | QA | ✅ 기능 4/4 PASS · 🔴 주석 FAIL 3건 |
| 2 | **QA R1 → dev** | 되돌림 | #68 정정 (507 assertions) |
| 3 | **dev → 자기 반증** | 자체 | 🟢 자기 정정문의 근거가 거짓임을 **스스로 발견**해 교체 |
| 4 | **QA R2 → dev** | 되돌림 | 🔴 **차단 FAIL 3** — 정정문이 새 과대선언 생성(D-2·B-2·D-3) |
| 5 | **dev R3 → 마스터** | 종결 | 511 assertions, 마스터 양방향 사보타주로 주석 진위 확인 |

### 8. 배포

- 커밋 `543be8a` 「티켓 #68 종결」 — **1파일**(`engines.test.ts`) 경로 명시 스테이징.
- `git push origin main` → `ff4a496..543be8a` **성공**. Vercel 자동 배포.
- ⚠️ **커밋 메시지 1차 실패**: Bash에서 PowerShell here-string(`-m @'…'@`)을 써서 첫 줄이
  `@`가 됐다 — **표준 ㋐를 마스터 본인이 위반**했다. push 전이라 `--amend`로 정정
  (`8dfa12a` → `543be8a`). 2차는 Write 도구로 메시지 파일을 만들어 `-F`로 넘겼다.

### 9. 운영 표준

- **[★신규 ㋵] 「무엇이 닫혔는가」를 산문으로 일반화하면 반증 입력 하나에 거짓이 된다.**
  ㋎가 13연속인 진짜 원인이 이것이다. 「전부 교체는 닫혔다」는 죽은 참조 한 줄에 무너졌다.
  → **판정식이 기계적으로 참인 성질만 적어라.** 이 지시를 R3에 명령으로 넣자 dev가
  자기점검에서 3문장을 스스로 좁혔고(「(b)가 강제하는 성질은 하나」→ 어서션 1개로 범위 명시 /
  「FAIL하는 경우는 이때뿐」→ 「이 어서션 하나가」 병기 / 「정규식으로는 불가능」→ 「미시도」로
  강등), **정정문이 처음으로 QA·마스터 양쪽에 반증되지 않았다.**
- **[★신규 ㋶] 탐지기의 「한계」도 대조군으로 못박아라.** dev가 `(3a-3) 한계 실증 대조군`
  4개를 넣어 「거짓 초록 2경로 + 죽은 분기 + 과소계수」를 **기계 검증 대상**으로 바꿨다.
  주석의 자백은 썩지만 어서션은 안 썩는다 — 검출기가 개선되면 그 4개가 먼저 FAIL해서
  주석 갱신을 강제한다.
- **[재확인 ㋳ — 4일 만에 재발, 새 경로]** 이번 위반자는 「죽는 QA」가 아니라 **커밋 권한을
  가진 다른 스케줄 작업**이었다. 백업+해시 강제가 실제로 방어했으므로 **모든 사보타주
  지시서에 사전 백업 + 해시 대조를 상시 포함**한다.
- **[재확인 ㋎ — 13연속]** dev R1 정정문이 새 과대선언 생성. 단 **처음으로 하류가 자기 것을
  스스로 1건 잡았고**(핑퐁 #3), R3는 반증되지 않았다.
- **[재확인 ㉹ — 19연속]** 하류가 상류를 반증. 이번엔 **3중**: QA R1이 마스터 승인 직전
  구현물 반증 / QA R2가 dev 정정문 반증 / dev가 자기 자신 반증.
- **[재확인 ㋐ — 마스터 본인 위반]** Bash에 PowerShell here-string. 메모리에 이미 적혀 있는
  표준을 어겼다. → **커밋 메시지는 Write 도구 + `-F` 경로로 고정.**
- **[재확인 ㋥]** QA·dev가 올린 수치(18건·줄번호 3개·해시)를 **전부 직접 재측정**한 뒤에만
  커밋 메시지에 실었다.

### 10. 🎫 신규 티켓 (#67까지 소진 확인 후 배정)

> 배정 전 실측: GSC 감사 로그가 **「#67까지 소진」** 명시 → 내 작업을 #68로, 신규를 #69·#70으로.

| # | 내용 | owner |
|---|---|---|
| **#68** | #59 스위트 주석 정직성 정정 | dev — **✅ 종결**(`543be8a`) |
| **#69** | 🔴 **컴포넌트가 「정답 아닌 값」을 인라인하는 축이 무방비.** 금지 토큰이 `PREPAY_EFFECTIVE_RATE`에서 파생되므로 **정답값만** 탐지 대상 → `약 3.00%`는 원리적으로 못 잡는다. `car-tax` 외부 공시값 앵커는 **lib 정의부만** 본다. **이게 티켓 #52의 실제 라이브 결함 유형 그 자체다.** 닫으려면 렌더 결과 검증(빌드 청크 스캔 — QA가 R1에서 실제로 해본 경로다) 또는 라벨 스냅샷 | dev |
| **#70** | 소비 지점 앵커가 **「출현」만 세고 「사용」을 검증 못함**. 죽은 참조·문자열 리터럴·타입 위치가 전부 1회로 계수. `stripComments`는 파서가 아니라 오류가 **양방향**. 근본 처방은 TS AST 수준 판정 — #68에서 **미시도**로 명시 | dev |

### 11. 미확인 (명시)

- 🔴 **QA R3 미실시.** dev R3 정정분(511 assertions)은 **마스터 검증만** 받았다. 마스터가
  양방향 사보타주를 직접 주입해 주석 진위를 확인했으나 **독립 재검증은 아니다.**
  08-07에도 「QA R4 미실시」가 같은 자리에 있었다 — **2회 연속 마지막 라운드가 무검증이다.**
- **화면 실물 확인 안 함.** 이번 변경이 테스트 파일 1개뿐이고 `components/`가 HEAD와
  바이트 동일이라 dev 서버 기동을 생략했다. 렌더 무변경은 **빌드 성공 + 바이트 동일**로만
  추론한 것이고 **눈으로 보지 않았다**(QA R1이 빌드 청크에서 `약 4.58%` 1건을 직독한 것이
  가장 가까운 근거지만 그건 #68 정정 **전** 관측이다).
- 3·6·9월 연납 미구현 · 지방교육세 공제 전/후 · **334 vs 332일**(위택스 실고지값 미취득) —
  이전 회차 승계.
- GSC 색인/노출 — 측정 수단 없음(GSC 감사 회차 소관).

### 12. 마케팅팀 / CS팀

- **마케팅 배정 없음.** 이번 회차의 변경은 **회귀 테스트 주석**뿐이라 사이트에 새로 생기거나
  바뀐 화면·콘텐츠가 **0건**이다. 고정 소재 반복 금지 원칙에 따라 카드뉴스 소재를 만들지
  않았다(게이트는 이미 개방 상태 유지 — 닫지 않았다).
- **CS 문의 채널 여전히 미연동** — 지어낼 문의 없음.

### 13. 사용자 확인 필요 블로커 — **오늘 신규 없음**

GSC 자격증명 · #12 커스텀 도메인 · #11 세션 동시 실행 · #6 평문 크론 시크릿 — 전부 발송 완료,
**재발송 금지**. §6의 ㋳ 재발은 #11(세션 동시 실행)과 **같은 축**이므로 신규 발송하지 않는다.
#68~#70은 팀/마스터 선에서 판정 가능 → 알림 대상 아님.

### 14. 다음 실행 인계

1. **[🔴 최우선 · QA R3]** `543be8a`의 주석 정정분은 **독립 재검증을 못 받았다.** 2회 연속
   마지막 라운드 무검증이므로 다음 회차가 **먼저** 처리할 것. 검증 포인트 = 새 주석이
   기계적으로 참인가 + `(3a-3)` 한계 실증 대조군 4개가 공허하지 않은가.
2. **[티켓 #69]** 정답 아닌 값 인라인 축 — #52 실결함 유형. **이번 회차가 남긴 최대 구멍.**
3. **[티켓 #70]** 출현 ≠ 사용. TS AST 필요, 고비용 — 착수 전 비용 대비 판단 필요.
4. **[GSC 감사 인계 승계]** #65(길이 회귀 테스트 — **여유 1자짜리 2건 대기, 시간 문제**) ·
   #67(기획 스펙 10건 재오염원) · #66→#62 병합 검토. **그쪽 로그 §11~12를 읽을 것.**
5. **[이월 유지]** #63(헤지 판단) · #60 · #61 · #47 · #46(iii) · **#27 나이 조견표 2027**
   (외부 고시 대기 불필요한 유일 항목) · `MINIMUM_WAGE` 교체(12-31) · #44 배정 금지(12-26 이전).
6. **[⏰ 시한]** `PREPAY_JAN_DAYS 334`/`PREPAY_YEAR_DAYS 365` 2028 윤년 335/366 — #53 병합.
7. **[⚠️ 운영]** **스케줄 세션 2개가 같은 워킹트리에 커밋·push 권한을 갖는다**(§6).
   착수 시 `git log --oneline -3`을 **작업 전후로** 찍어 다른 세션의 커밋을 감지할 것.
   티켓번호는 **배정 직전에** 전 로그 grep으로 재실측할 것(오늘 충돌 1건 발생).

### 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. **`git add -A` 미사용**(1파일 경로 명시 — 공개 저장소 +
`.claude/settings.local.json` 평문 시크릿). `.env*` 미열람·미커밋. Vercel CLI 직접 배포 안 함.
`cd`+`git` 연결 호출 0건(전부 `cd "…" && git`을 단일 호출 내 절대경로로 처리). Bash heredoc
미사용(Write 도구 사용). 이미지/영상 생성 도구 호출 0. 결제 행위 없음.
동시 세션 소유 파일(`lib/blog.ts`) **무접촉** — QA·dev 지시서에 금지 조항 명시.
사보타주 전건 원복(마스터 2회·QA 다수·dev 다수, 해시 대조 확인).
