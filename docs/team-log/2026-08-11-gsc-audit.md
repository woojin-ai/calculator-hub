# GSC 일일 점검 — 2026-08-11 (11:27 착수 / 정시 08:00 대비 3.5h 지연 스폰)

## 결론

GSC **API** 점검은 **자격증명 부재로 미진행**(누적 **27일째** — 알림은 07-26에 1회 발송 완료, **재발송 안 함**).

오늘의 실질 성과는 3건이고, **전부 지난 4일간의 스케줄러 정지가 만든 것**이다.

1. **🔴 스케줄러가 3.9일간 완전 정지했다** — 08-07 13:01 이후 **08-11 11:20까지 실행 0회·커밋 0건.** 원인은 08-01에 설치한 리퍼 자신의 구멍이다(§4).
2. **🔴 라이브 회귀 1건 적발·정정·배포 완료** — meta description 162자(상한 160 초과). 원인은 티켓 #52의 **옳은 정정**이 문자열을 5자 늘린 것. (§3, 티켓 #64)
3. **고아 작업 1건 회수** — 티켓 #59(직전 회차 최우선 인계)가 **완성·그린 상태로 4일간 미커밋 방치**돼 있었다. 독립 검증 후 커밋. (§2)

---

## 0. 사전 조건 (근거 명시)

- **저장소/배포 정상**: `git fetch` 후 착수 시 `HEAD == origin/main` (0 ahead / 0 behind), HEAD = `cbde0c2`.
- **GSC API 자격증명 없음 (27일째)**: `.env.local`의 키 6개는 전부 메일 발송·Instagram/Meta 계열 — **Google/GSC 키 0개**(값 미열람, **키 이름만** 확인). 서비스 계정 JSON **0건**. `app`·`lib`·`scripts`에 Search Console/googleapis 참조 **0건**.
  - ⚠️ 구분 유지: `app/layout.tsx`의 `naver-site-verification`은 **소유권 확인용**이지 Search Console **API 접근 권한이 아니다.**
- **정본 URL은 기억이 아니라 `lib/site.ts`에서 읽었다**(표준 ㉱): `SITE_URL = https://calculator-hub-delta.vercel.app`.
- ⚠️ 무인 안전수칙 준수: `cd` + `git` 연결 호출 **0건**, 전부 `git -C` 또는 분리 호출.
- **동시 세션 3개가 `isRunning: true`**(Daily team standup · Realestate team standup · Debate claude round). 이번 회차는 그 세션들의 파일을 **일절 건드리지 않았다**(§5 FAIL-A).

---

## 1. GSC API 점검 결과

| 항목 | 결과 |
| --- | --- |
| 색인 생성 현황(Coverage) | **미확인** — API 자격증명 부재(27일째) |
| 모바일 사용성(GSC 리포트) | **미확인** — 단 이 리포트는 **2023-12-01 구글이 폐지**했다(영구 불가) |
| 노출/클릭 | **미확인** |

**알림 재발송 안 함** — 07-26에 1회 발송 완료(이슈당 1회 원칙).

**크롤 입력 전수 확인 — 색인을 막는 요소 없음:**

| 항목 | 결과 |
| --- | --- |
| `robots.txt` | 200 · 85 B · `Allow: /` · **차단 규칙 0건** · sitemap 선언 정상 |
| `sitemap.xml` | 200 · 10,608 B · **62 URL** (08-06과 동일, 증감 0) |
| `ads.txt` | 200 · 59 B |
| `noindex` | **1건뿐** — `/links` (`noindex, follow`, 의도된 것) |

---

## 2. 🔴 고아 작업 회수 — 티켓 #59 (커밋 `e903083`)

착수 시 `git status`에 **라이브 코드 2파일이 미커밋 상태**로 있었다. mtime **08-07 06:06~06:07**, 4일 경과.

`docs/team-log/2026-08-07.md` §10과 대조한 결과 이것은 **직전 회차가 "이번 회차에서 유일하게 안 닫힌 축"이라며 최우선으로 인계한 티켓 #59**였다. 구현은 끝나 있었고, 커밋 직전에 세션이 죽었다(§4의 정지가 13:01 시작이므로 그 이후 어떤 회차도 이어받지 못했다).

### 2-1. 채택 판단 근거 (표준 — 「죽음의 적극적 증거」)

08-06 회차는 동시 세션의 미커밋 작업을 **"내 작업 아니라 커밋 안 함"**으로 처리했다. 이번엔 반대로 채택했고, 그 차이의 근거는 다음이다:

- 파일 mtime이 **4일 전에 고정**(진행 중 아님)
- 현재 실행 중인 3개 세션이 **그 2파일을 전혀 건드리지 않음**(status 관측)
- 원 저작 세션은 프로세스 부재로 사망 확정

### 2-2. 마스터 독립 검증 (보고 복창 아님 — ㋥)

| 검증 | 결과 |
| --- | --- |
| `npm test` | **499 assertions ALL PASS** (신규 스위트 30) |
| `npx tsc --noEmit` | **exit 0** |
| `lib/` 오염 여부 | `git status lib/` **공백** = 핵심 상수 무변경 (표준 ㋳) |
| 핵심 상수 실측 | `0.05` / `334` / `365` / 파생식 / 라벨 파생 **전부 정본** |
| 사보타주 C (리터럴 인라인) | 검출기 **(i)** FAIL 확인 |
| 사보타주 E2 (**리터럴 0개**, `.slice()` 순수 가공) | 검출기 **(ii)** 독립 FAIL 확인 |
| 원복 | md5 `c7e078d4…` **바이트 동일** |

### 2-3. 🔴 내 첫 사보타주 시험이 틀렸다 (자기 정정)

1차 시도에서 나는 `PREPAY_DISCOUNT_PERCENT.replace("4.58","3.00")`을 주입해 **"검출기 (ii) 확인"**이라고 결론지을 뻔했다. 그러나 그 주입문에는 **리터럴 `4.58`이 들어 있어** 실제로는 검출기 **(i)**가 잡은 것이었다. 리터럴을 0개로 만든 `.slice()` 재시험을 해서야 (ii)의 독립 발화가 증명됐다(메시지 문면도 다르다).

> **교훈(㋲ 계열 확장)**: **탐지기가 2개일 때, 두 축을 동시에 위반하는 사보타주는 아무것도 증명하지 못한다.** 각 검출기마다 **그 축만 단독으로 위반하는** 시험체가 필요하다. 하마터면 "두 축 다 닫혔다"를 한 축의 증거로 주장할 뻔했다.

---

## 3. 🔴 라이브 회귀 — meta description 162자 (티켓 #64, 커밋 `9b3091b`)

### 3-1. 발견

라이브 66 URL 전수 온페이지 감사(§6)에서 `/blog/car-tax-prepay-relief-2026`의 description이 **162자**로 상한 **160자**를 초과.

### 3-2. 원인 — 옳은 정정의 부작용

| 시점 | 문자열 | 길이 |
| --- | --- | --- |
| `1ac472c^` (#52 이전) | 「본세의 **3%**를」 | **157** |
| `1ac472c` (#52 이후) | 「본세의 **약 4.58%**를」 | **162** |

`3%`(2자) → `약 4.58%`(**7자**) = **+5자**. 티켓 #52의 정정은 법령 원문 대조를 거친 것이므로 **되돌리지 않는다.**

### 3-3. 팀 핑퐁

**① 마스터 → planning-team + marketing-team 동시 진단 호출.** 두 팀 모두에 "내 수치를 근거로 쓰지 말고 네 실측을 근거로 써라"를 명시(㋣/㋥).

**② 두 팀이 독립적으로 같은 결론 + 서로 다른 문안.** 둘 다 155자 안을 냈으나 수단이 갈렸다 — 기획 `깎아주는`(붙여쓰기), 마케팅 `돌려주는`(의미 변경). **마케팅은 기획의 안을 "붙여쓰기 꼼수"라고 명시적으로 배격**했다.

**③ 🔴 마스터가 두 팀의 수치 불일치를 직접 재측정해 판정.** 기획 「+5자(157→162)」 vs 마케팅 「+6자(156→162)」. 내 실측 결과 `약 4.58%` = **7자**이므로 **+5, 기획팀이 옳다.** 이 재측정이 결정적이었다 — **+5가 맞으면 순수 삭제 2건(`실제 `·`바로 ` = 6자)만으로 156자가 나오므로, 두 팀이 다투던 `깎아 주는` 문면을 아예 건드릴 필요가 없어진다.**

**④ 마스터 판정: 순수 삭제만. 두 팀 안 모두 미채택.**
「할인/공제」 문면 축은 **별건 티켓 #62 관할**이고, 길이 정정이 그 축을 선점하면 관할 침범이다(㋧). 마케팅의 `돌려주는` 논거(기획문서와의 드리프트 해소, 의미상 더 정확)는 **버리지 않고 티켓 #66으로 이관**했다.

**⑤ dev-team 구현 → 지시와 바이트 동일 확인.** dev는 편집 **전에** "현재 문자열 − 2건 삭제"의 파생 결과가 내 지시 문자열과 바이트 동일한지 프로그램으로 검증한 뒤 적용했다(지시문에 다른 축이 섞여 들어오는 것을 배제). 좋은 절차다.

**⑥ qa-team 독립 검증 → 내용 7/7 PASS, 그 바깥에서 FAIL 2건**(§5).

### 3-4. 적용 결과

```
「차종별 실제 금액으로」 → 「차종별 금액으로」   (실제 = 3자)
「내 차도 바로 확인해」   → 「내 차도 확인해」     (바로 = 3자)
```
**162 → 156자.** 보호 토큰 13종 전건 잔존.

---

## 4. 🔴 스케줄러 3.9일 전면 정지 — 리퍼 자신의 구멍

### 4-1. 실측

| 근거 | 값 |
| --- | --- |
| 마지막 스케줄 세션 활동 | **2026-08-07 13:01** (`reaper.log`) |
| 이후 로그 | `스케줄 세션 없음 — 확인할 대상 0개` **연속** → 08-11 11:00까지 |
| 복귀 | 오늘 **11:20경 3개 태스크 동시 catch-up 스폰** |
| 교차검증 | `git log --since="2026-08-07 13:00" --until="2026-08-11 11:00"` → **커밋 0건** |

### 4-2. 원인 — 08-01 대책의 판정 기준에 상한이 없다

리퍼의 종료 조건은 **나이 90분 초과 AND 트리 CPU 증가 < 0.5s/60s**다. 즉 **CPU를 계속 쓰는 세션은 나이와 무관하게 불사신**이다. 로그가 그 장면을 그대로 남겼다:

```
2026-08-07 12:31 PID 57140: 나이 1719분 | 트리CPU증가 1.19s — 작업 중이므로 유지.
2026-08-07 13:01 PID 57140: 나이 1749분 | 트리CPU증가 0.97s — 작업 중이므로 유지.
```

**나이 1749분 = 29시간짜리 세션을 30분마다 "작업 중"으로 판정해 살려두고 있었다.**

⚠️ **단, 트리 CPU 검사를 되돌리면 안 된다** — 그건 "npm/git 자식을 돌리는 정상 세션을 멈춘 것으로 오판"하는 문제 때문에 **의도적으로 넣은 것**이고, 샘플 창을 10초로 줄였을 때 실제 오탐이 났던 이력이 있다. **처방은 CPU 검사 제거가 아니라 최대 수명 백스톱 추가**(예: 6시간 초과 시 CPU 무관 강제 수거).

### 4-3. 비용

티켓 #59(직전 회차 **최우선** 인계)가 **완성·그린 상태로 4일간 방치**됐다(§2). 라이브 결함이 아니었던 게 다행이며, 만약 §3의 162자 회귀가 그 창에 발생했다면 **4일간 그대로 노출**됐을 것이다. 실제로 #52 커밋(08-07 04:06)이 만든 회귀가 **오늘까지 4일간 라이브에 있었다** — 감사 회차가 없었기 때문이다.

### 4-4. 알림

**🔔 알림 발송(11:5x)** — `PushNotification` 호출했으나 반환값 **"Mobile push not sent (Remote Control inactive)"**. 데스크톱 알림만 나갔고 **휴대폰 전달은 미확인**이다. 이 경로의 **5회 연속 실패**이므로 **이 팀로그와 최종 보고가 사실상 유일한 전달 채널**이다. "발송 완료"로 쓰지 않는다.

---

## 5. QA 독립 검증 결과 — 내용 7/7 PASS, 그 바깥 FAIL 2건

QA는 내 목록을 읽기 전에 `git diff`부터 직접 뜨고 자기 측정을 먼저 했다.

| 항목 | 판정 |
| --- | --- |
| 길이 156 | PASS |
| 회귀 여부 | PASS — **6,426행 전수 행단위 대조로 차이 행 = [1498] 하나뿐** |
| 보호 목록 13종 | PASS (13/13) |
| 범위 침범 | PASS (`publishedDate` 혼입 없음 — #29·#24 재발 없음) |
| 게이트 | PASS (**격리 스냅샷**에서 이 변경의 테스트 델타 = **0** 실증) |
| 가시 렌더 | **판단 PASS / 근거 정정** |
| 문장 성립성 | PASS |

### 5-1. QA가 내 근거를 정정했다 (판단은 유지)

나는 "`line-clamp-2`라 레이아웃 영향 없음"이라고 판단했고 결론은 맞았지만, **이유가 틀렸다.** QA 지적:

> 높이가 안 변하는 진짜 이유는 "안 보여서"가 아니라 **156자도 162자도 모든 컨테이너에서 2줄을 넘겨 채우므로 clamp 높이가 양쪽 다 정확히 2줄**이기 때문이다.

그리고 **`/blog` 데스크톱(1단 전폭 ≈688px, 2줄 용량 ≈98자)에서는 첫 분기점 cp index 95가 경계 안이라 2번째 줄 꼬리 글자가 실제로 바뀐다**(`…차종별 실제` → `…차종별 금액으`). 결함은 아니지만 **"화면에 아무 변화 없음"은 부정확한 서술**이다. 수용한다.

### 5-2. 🔴 FAIL-A (커밋 차단) — 동시 세션이 같은 워킹트리에서 사보타주 중

QA가 검증 중 `git status`를 시각순으로 관측해, 다른 세션이 `CarTaxCalculator.tsx`에서 **공제율 라벨을 통째로 제거한 상태(t2)**를 포착했다(t3에 원복됨). 그 시점에 `git add -A`/`commit -a`를 쳤다면 **라벨 없는 컴포넌트가 라이브로 나갔다.**

**마스터 대응** — QA 처방대로 실행하고 결과를 남긴다:

| 절차 | 실측 |
| --- | --- |
| 커밋 직전 `CarTaxCalculator.tsx` 상태 | **HEAD와 동일(clean)** — 원복 확인 |
| 라이브 라벨 존재 | `L404`·`L414`에서 `PREPAY_DISCOUNT_PERCENT` 참조 **정상** |
| 커밋 직전 L1498 재측정 | **156** · `약 4.58%` 존재 |
| 커밋 직전 핵심 상수 | `0.05` / `334` / `365` **정본** |
| 스테이징 | **`git add lib/blog.ts` 경로 명시 1건.** `--cached --stat` = `lib/blog.ts \| 2 +-` |
| 민감 파일 | `.env`·`settings.local.json`·`launch.json` 스테이징 **0건** |

⚠️ **`scripts/regression/engines.test.ts`의 미커밋 변경(+99/−11)은 그 세션 소유라 손대지 않았다.**

### 5-3. 🟡 FAIL-B (경미, 범위 미완) — 재오염원 생존 → 티켓 #67

`planning/car-tax-prepay-relief-blog-content.md:24`(이 글의 **정본 스펙**)가 **그 자체로 161자**다. 여기서 재생성하면 방금 고친 결함이 돌아온다. 계통 스캔 결과 **`planning/*.md`의 description 스펙 10건이 160자 초과**(최장 196자).

07-26의 「desc 8편 160 초과 정정」이 **코드에만 적용되고 스펙에는 미적용**이었던 것. → **티켓 #67** 신설.

---

## 6. 라이브 전수 온페이지 감사 (66 URL)

모집단은 정본대로 sitemap 62 ∪ `/links` ∪ `/blog?page=2,3,4` = **66**(중복 0 확인, `/links`는 sitemap 미등재 확인 — 08-06과 동일 정의).

### 6-1. 네거티브 컨트롤 먼저 (표준 ㉲)

16개 검사 항목을 **전부 위반하도록 조작한 가짜 레코드**를 통과시켜 **16/16 발화** 확인 후 실측에 들어갔다. HTML 엔티티 디코더도 적용(08-04 거짓 경보 재발 방지).

### 6-2. 실측 (정정 배포 **후**)

HTTP 200 **66/66** · canonical 정확히 1개 **66/66** · og:url↔canonical 불일치 **0** · `<title>` 고유 **66/66**(중복 0, >60자 0) · meta description 고유 **66/66** · `<h1>` 1개 **66/66** · viewport **66/66** · `lang="ko"` **66/66** · og:title↔title / og:desc↔desc 불일치 **0 / 0** · twitter:card **66/66** · JSON-LD 파싱 실패 **0**.

구조화 데이터: `WebSite` 66 · `Organization` 66 · `BreadcrumbList` 52 · `BlogPosting` 36 · `WebApplication` 16 · `FAQPage` 16 · `CollectionPage` 4 · `ItemList` 4.

| 지표 | 정정 전(오늘) | 정정 후(오늘) | 08-06 |
| --- | --- | --- | --- |
| title max / min | 59 / 12 | **59 / 12** | 59 |
| desc max / min | **162** / 53 | **159 / 53** | 159 |

**desc max가 기준선 159로 복귀했다.**

### 6-3. ⚠️ 「og 6종」 지표는 회귀가 아니라 **정의 불일치**였다 (자기 정정)

내 탐지기는 「og 6종」에 **`og:image`를 포함**시켜 **66건 전원 FAIL**을 냈다. 08-06 로그의 「og 6종 66/66」과 정면 충돌하는 수치라 회귀로 의심했으나, 원문 HTML을 직접 열어 확인한 결과:

- 실재하는 og 태그는 `og:title`·`og:description`·`og:url`·`og:site_name`·`og:locale`·`og:type` — **정확히 6종**
- **`og:image`는 66/66 전부 부재이며, 이는 의도된 것**이다 — `lib/og.ts:38`이 **"og:image는 1단계 범위 밖이다"**라고 명시

즉 **두 회차가 서로 다른 6종을 세고 있었다.** 회귀 아님, 신규 결함 아님. 기존에 인지된 이월 항목(마케팅 KPI 축)이다.

> **교훈(표준 ㋢/㋮ 실례 추가)**: **같은 이름의 지표가 같은 모집단을 세고 있다고 가정하면 안 된다.** 「6종」이라는 **개수가 일치**했기 때문에 정의 차이가 오히려 더 잘 숨었다. 지표명에 **구성원을 병기**해야 한다.

---

## 7. 모바일

| 항목 | 결과 |
| --- | --- |
| viewport 메타 | **66/66 존재**(실측) |
| `lang="ko"` | **66/66** |
| 오늘 변경의 레이아웃 영향 | **구조적으로 없음** — `BlogPostCard.tsx:34`의 `line-clamp-2`가 156자·162자 양쪽 다 2줄로 고정(§5-1) |
| **375px/320px 실측** | **🔴 미확인** |
| 픽셀 육안 검증 | **🔴 미확인 — 무인 실행 불가(28일째)** |

### 7-1. 🔴 브라우저 도구 3회차 연속 불능

`preview_start`가 **300초 타임아웃**(08-06에 2회, 오늘 1회 — **3회 연속**). `node_modules`에 puppeteer/playwright/jsdom **전부 없음**(실측). **모바일 실측 수단이 연속 상실 상태**다.

오늘 변경분에 한해서는 위 구조적 논거로 오버플로가 불가능하지만, **이것을 "모바일 검증 통과"라고 쓰지 않는다.** 08-06 인계 §9-7의 대체 수단 검토 건은 **여전히 미해결이며 우선순위를 올린다.**

---

## 8. 배포(push)

- **커밋 2건, 전부 push 완료** (`cbde0c2..9b3091b`)
  - `e903083` 티켓 #59 종결 (고아 워킹트리 회수) — 2파일 경로 명시
  - `9b3091b` 티켓 #64 종결 (desc 162→156) — **1파일 1행** 경로 명시
- **제외**: `.claude/settings.local.json`(🔴 **평문 크론 시크릿**) · `.claude/launch.json` · `.claude/settings.json` · `.codex/` · `ebook/` · `trading/` · `planning/blog-year-slug-policy.md`(QA FAIL 상태 유지) · `planning/simplified-tax-table-byl2-raw.txt` · **`scripts/regression/engines.test.ts`의 신규 미커밋 변경(동시 세션 소유)**
- **`git add -A` 미사용.**
- **라이브 반영 실측 확인**: 배포 후 폴링 → `/blog/car-tax-prepay-relief-2026`의 라이브 meta description **156자** 확인. 이어서 **66 URL 재감사에서 desc max 159 복귀** 확인.

---

## 9. 미확인 (명시)

- **GSC 색인/노출/클릭 — 27일째 측정 수단 없음.** 오늘의 온페이지 결과는 **"색인을 막는 기술적 요소가 없다"**는 뜻이지 **"실제로 색인·노출되고 있다"**는 뜻이 **아니다.** 애드센스 재신청 판단에 직결되는 구분이다.
- **375px/320px 실측 미수행** — 브라우저 도구 3회 연속 타임아웃(§7-1).
- **QA의 컨테이너 폭·2줄 용량 수치(≈688px/≈98자 등)는 CSS 산정치이지 렌더 실측이 아니다** — QA 본인이 한계를 명시했다. 인용 시 재측정할 것.
- **QA의 `planning/*.md` 10건 길이 수치는 relay다**(티켓 #67) — 착수 시 재측정 필요(㋥).
- **`og:image` 66/66 부재** — 의도된 이월(§6-3), 결함 아님.
- 3·6·9월 연납 미구현 · 지방교육세 공제 전/후 · 334 vs 332일 · 간이세액표 근사식 미감사 — 이전 회차 이월 유지.

---

## 10. 사용자 확인이 필요한 블로커

| # | 사안 | 알림 |
| --- | --- | --- |
| **신규** | **스케줄러 3.9일 정지 / 리퍼에 최대 수명 백스톱 필요**(§4) | **🔔 오늘 1회 발송 — 단 "Mobile push not sent", 전달 미확인. 재발송 금지** |
| GSC | API 자격증명 부재(**27일째**) | 07-26 발송 — **재발송 금지** |
| #12 | 커스텀 도메인 | 08-01·08-02 발송 — **재발송 금지** |
| #11 | 스케줄 세션 동시 실행 | 07-29 발송 — **재발송 금지** |
| #6 | 공개 저장소 평문 크론 시크릿 | 07-27 발송 — **재발송 금지** |

---

## 11. 🎫 신규 티켓 (#63까지 소진 확인 후 배정)

> ⚠️ 배정 전 `grep -rhoE "티켓 #[0-9]+" … | sort -n | tail -1` 실측함 → **#63**. 메모리에 적힌 「#54까지 소진」은 **스테일이었다**(정정 완료).

| # | 내용 | owner |
| --- | --- | --- |
| **#64** | desc 162→156 정정 | dev — **✅ 종결** |
| **#65** | **desc/title 길이 상한을 회귀 테스트로 강제.** `lib/blog.ts:60`이 "**lint 상한 160자**"라고 선언하지만 **구현체가 없다**(`scripts/` 2파일에 길이 검사 0건, eslint에도 0건). 이번 회귀가 **정확히 그 구멍으로 통과**했다. 여유가 **1자**뿐인 글이 대기 중(desc 159 `bmi-guide-2026`, title 50 `weekly-holiday-allowance-eligibility-2026`) | dev |
| **#66** | 기획문서↔라이브 문면 드리프트 `깎아 주는` vs `돌려주는` → **#62와 같은 축, 병합 검토** | 기획 |
| **#67** | `planning/*.md` description 스펙 **10건이 160자 초과**(최장 196). 07-26 정정이 코드에만 적용되고 스펙엔 미적용 → **재오염원** | 기획 |

---

## 12. 다음 실행 인계

1. **[🔴 최우선 — 인프라] 리퍼 최대 수명 백스톱.** §4의 구멍을 안 막으면 정지가 재발하고, 재발하면 **오늘 같은 라이브 회귀가 며칠씩 노출된다.** 사용자 결정 필요 사항이나 알림 전달이 미확인이므로 **다음 회차가 이 항목을 반드시 재확인할 것.**
2. **[🔴 티켓 #65]** 길이 회귀 테스트. 저비용(데이터 순회)·고효과. 오늘 회귀의 **재발 방지책 그 자체**다. 여유 1자짜리가 2건 대기 중이라 시간 문제다.
3. **[티켓 #67]** 기획 스펙 10건 — #64의 재오염원. 수치 재측정 필요.
4. **[티켓 #66 → #62 병합]** 문면 드리프트.
5. **[🔴 도구] 브라우저 도구 3회 연속 타임아웃** — 모바일 실측 수단 부재가 고착되고 있다. 대체 수단 도입 검토 우선순위 **상향**.
6. **[⚠️ 동시 세션]** `scripts/regression/engines.test.ts`에 타 세션의 미커밋 변경(+99/−11)이 있다. 그 세션이 티켓 #64/#59 앵커의 자기참조 결함을 사보타주로 실증 중인 것으로 보인다 — **결과를 확인하고 이어받을 것.**
7. **[티켓 #44 배정 금지 유지]** 2026-12-26 이전 main push 금지. 정본은 `planning/ticket-44-age-table-2027-workorder.md` §0.
8. **[이월 유지]** 08-07 §10의 #63(헤지 판단) · #60 · #61 · #47 · #46(iii) · #27 · `MINIMUM_WAGE` 교체(12-31) 전부 유효.

**티켓번호 #67까지 소진.**

---

## 13. 운영 표준 (이번 회차)

- **[★신규 ㋵] 탐지기가 N개면 사보타주도 N개여야 한다.** 두 축을 동시에 위반하는 시험체는 **어느 검출기가 잡았는지 구분하지 못하므로 아무것도 증명하지 못한다.** 내가 실제로 (i)이 잡은 것을 (ii)의 증거로 쓸 뻔했다(§2-3). **검출기별로 그 축만 단독 위반하는 시험체를 만들 것.**
- **[★신규 ㋶] 같은 이름의 지표가 같은 것을 세고 있다고 가정하지 마라.** 「og 6종」이 두 회차에서 **개수는 6으로 같은데 구성원이 달라서**, 정의 차이가 오히려 더 잘 숨었고 66건짜리 가짜 회귀를 만들 뻔했다(§6-3). **지표명에 구성원을 병기할 것.**
- **[★신규 ㋷] 두 팀의 수치가 엇갈리면, 중재하지 말고 직접 재측정하라 — 그 값이 해법의 모양을 바꾼다.** 기획 +5 vs 마케팅 +6에서 내 재측정(+5)이 **"순수 삭제만으로 충분"**을 열었고, 그 결과 두 팀이 다투던 `깎아 주는` 문면을 **건드릴 필요 자체가 사라졌다**(§3-3③). 수치 판정이 곧 범위 판정이었다.
- **[재확인 ㉹ — 19연속] 하류가 상류를 반증했다.** 이번엔 **3중**: QA가 마스터의 `line-clamp` **근거**를 정정(결론은 유지 — §5-1) / QA가 마스터·dev 누구도 안 본 **재오염원 10건** 적발(§5-3) / QA가 **동시 세션 사보타주**를 포착해 커밋을 차단(§5-2). 더해서 **마스터가 마케팅팀 수치를 반증**했다(§3-3③).
- **[재확인 ㋳] 사보타주와 커밋 권한의 워킹트리 공유가 또 발생했다.** 이번엔 **QA가 먼저 발견해 차단**했고, 마스터는 커밋 직전 재확인 절차(라벨·길이·상수·스테이징 4종)를 실행해 통과했다(§5-2). **04:42 사고 이후 도입한 절차가 실제로 작동한 첫 사례다.**
- **[재확인 ㋥] relay와 검증을 구분했다.** QA의 컨테이너 폭 산정치와 `planning/*.md` 10건 수치는 **내가 재측정하지 않았으므로 §9에 relay로 격리**했다. 반대로 내가 재측정한 것(길이·상수·라벨·행대조)은 근거와 함께 본문에 적었다.
- **[재확인 ㋢] 「6일 연속」은 틀린 단위였다.** 기획팀 지적대로 정확한 표현은 **「6개 감사 회차 연속」**이다(07-28·08-04 회차는 미확인). 캘린더 일수와 회차 수를 섞지 말 것.

---

## 제약 준수

확정 컨셉/스택/구조 임의 변경 없음. **`git add -A` 미사용**(경로 명시 3파일). `.env.local` **값 미열람**(키 이름만). Vercel CLI 직접 배포 안 함(git push만). `cd`+`git` 연결 호출 **0건**. 동시 세션 소유 파일(`engines.test.ts`) **무접촉**. 사보타주 전건 원복 후 **md5 바이트 동일 확인**. 이미지/영상 생성 도구 호출 0. 결제 행위 없음. 수치를 새로 만들지 않았다 — 모든 값은 실측이며, 검증하지 않은 전달값은 §9에 **격리 표시**했다.
